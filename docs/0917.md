# 如何用 Firebase 创建应用程序

> 原文：<https://www.freecodecamp.org/news/create-apps-with-firebase/>

Firebase 是 Google 开发的一个平台，用于创建移动和网络应用程序。

我们刚刚在 freeCodeCamp.org YouTube 频道上发布了一门课程，教你如何使用 Firebase。这是初学者的最佳课程。

学习完 Firebase 的基础知识后，您将学习如何将 Firebase 与不同的 JavaScript 框架集成。

Cybernatico 开发了这个课程。他有很多使用 Firebase 的经验。

以下是本课程涵盖的部分:

*   如何设置 Firebase 并连接到前端应用程序
*   不同方法的防火墙身份验证
*   燃烧基积垢操作
*   用于存储文件的 Firebase 存储
*   用于过滤数据的 firebase firestorm 查询
*   Firestore 数据库中的 Firebase 实时监听器
*   Firebase 托管
*   使用不同 JavaScript 框架的 Firebase

观看以下或 freeCodeCamp.org YouTube 频道的完整课程(4 小时观看)。

[https://www.youtube.com/embed/fgdpvwEWJ9M?feature=oembed](https://www.youtube.com/embed/fgdpvwEWJ9M?feature=oembed)

### 副本

(自动生成)

Firebase 是 Google 的一个平台，用于创建移动和网络应用程序。

在本课程中，您将从 cyber Nautico 学习如何使用 Firebase 特性，以及如何将 Firebase 与不同的 JavaScript 框架集成。

火焰基地。

因此，开发工具需要构建一个包含身份认证、数据库、文件存储、安全工具和分析的全栈应用程序，以构建一个包含易于使用的文档的全栈复制。

在本视频结束时，我们将能够了解 Firebase 中的身份认证，以及它们如何处理数据库、文件映像存储、上传、数据和数据库更新、Firebase 查询。

最后一天，我们学习了如何将应用托管到 Firebase。

我们还将学习如何将 Firebase 与 React、JavaScript、next 或 Angular 等模板一起使用。

所以现在就开始吧。

首先，让我在 Firebase 中创建一个项目。

让我进入火焰基地前端。

继续。

所以我从一开始就向你们展示如何做到所有这些。

这里我有一个代码沙箱，它有两个输入，电子邮件和密码。

让我们先来解决认证问题。

在这里，我们有很多选择，比如密码、电子邮件、认证、电子邮件链接、谷歌登录苹果、脸书、Twitter、GitHub、微软、雅虎等等。

所以让我先给你看看这个东西。

现在，我们的项目已经创建。

现在让我们创建一个应用程序。

所以点击这里的网络应用。

输入它的名字，也许会说 Firebase basics。

注册的应用程序。

是的，这里我有电子邮件，电子邮件输入和 bathra 输入。

和提交时按钮。

如果你在这里输入任何东西，这将被馈入到这个电子邮件的名称中存储的输入名称与值，分别。

因此，对于电子邮件，它将通过电子邮件发送此数据状态。

对于密码，它将是密码。

所以我们必须先安装 Firebase。

所以它会拖延 Firebase，到这里来，到这个文件，只是添加 Firebase 作为租户这样，它会被添加。

而对于 React app 来说。

对于 VS 代码，我的意思是，您必须使用 npm install Firebase 命令进行安装。

现在，您可以看到，我们在这里获得了所有这些数据，我们必须将这些数据导入到 app.js 中。

所以创建一个文件 fair，这里是 fire base config.js。

让我们把所有的东西都放在这里，复制这些东西，然后粘贴到这里。

让我删除评论。

为了让它更清楚。

我们必须导出这个常量应用程序。

这是一个配置数据。

我们有 API 密钥认证域 ID 和所有。

所以我们必须在这里导入它。

所以导入 app。

从上一步开始，文件名为 Firebase config。

所以现在我们可以在这个应用程序中使用 Firebase。

所以继续安慰。

好吧，我们首先针对身份验证。

因此，我们必须转到“身份验证”选项卡，单击此处开始。

它伴随着我在这里展示的许多类型的指示，密码电子邮件链接，谷歌，脸书等等。

所以我们必须首先启用它们。

因此，让我们首先启用电子邮件密码注册和登录。

也在这里添加您的域名。

让我添加一个域，域是这样的。

所以把这个抄下来，加在这里。

它是 sec 添加的。

所以它会起作用。

我想是的。

现在我们要做的是从 Firebase 身份认证中导入一些东西。

就像这里所说的，在这里输入电子邮件和密码。

所以 get it get auth 是一个检查我们是否被认证的函数。

这个功能创建一个有电子邮件和密码的用户。

所以让我来吧。

首先，让我定义 auth 实例。

所以让 auth 等于在这个函数中这样对待提交的员工，如果我们点击按钮，点击提交按钮，这个就会被发送进去。

如果我们点击提交按钮，这个，这个函数将运行，这是处理提交。

所以在这个用电子邮件和密码创建用户的函数中，我们必须传递这三样东西。

首先是认证。

看，它在这里被传递，而不是电子邮件和密码。

所以让我把邮件从 data stick 发到 data dot 邮箱。

或者我在和数据点密码，密码。

所以我们必须在控制台中记录响应。

我们将使用，然后它会向我们发送我们的响应。

将响应添加到参数中。

在回调函数中，我们可以只说控制台点日志响应点用户。

但是如果失败，登录失败，我们可以使用 catch error 来捕获它，我们将显示一个警报，这是错误提示消息。

去拯救它。

现在，让我们尝试登录。

我们这里没有电子邮件。

所以现在就来试试吧。

的确邮件还是先打开控制台，在这里播放所有这些东西。

从控制台，添加电子邮件和一些密码。

提交。

看，我们正在阅读这里的所有数据。

提供商付费程序。

好吧，我们不需要这个，我们只需要访问令牌，我们需要显示名称原始电子邮件，我们用来注册是经过验证的电话号码和所有。

所以这里不验证。

因此，如果我们刷新它，我们应该会在这里看到这封电子邮件。

看，它来了。

这里有一个很棒的事情，让我登录或者注册，我的意思是，用同一个电子邮件两次。

如果我们再次注册，或者再次创建用户，我们将在警报 submit 中看到一个错误。

看，我们正在阅读已经重复使用的电子邮件。

这就是我们如何在 Firebase fire 商店中创建用户。

所以不是火储存，我的意思是在授权中，我知道为什么我每次说火基地的时候都要重复储存。

好了，创建完成了。

现在让我们看看如何找到登录，我们有这个方法，与电子邮件和密码。

所以只需在这里添加它，替换创建用户。

这里也是，所有其他事情都是一样的。

保存。

现在让我们尝试用这封电子邮件登录，清除它控制台清除？在这里提交所有数据，这些数据实际上是从这里返回的。

但是如果我们输入了不在这里的错误电子邮件，我们应该会看到一个错误。

所以让我用一个错误的电子邮件，约翰 44084401800 gmail.com。

然后单击提交。

看，找不到用户，因为该用户不在 odd 中。

我们输入了错误的密码。

所以让我们输入错误的密码。

提交。

看，仍然登录 ID 或从我们这里得到的密码。

所以注册和签到就完成了。

我的意思是，创建帐户并登录。

所以现在让我把重点放在电子邮件，或谷歌注册认证。

所以你可以阅读文档，就像我对所有其他登录方法做的那样，因为我不能向你展示一切。

最后，让我先向你展示一下谷歌认证提供商。

所以我们需要吉他和谷歌认证提供商。

然后，我们必须为提供商 Google 创建 auth 实例。

因此，让谷歌提供者等于新的，我们必须使用新的谷歌认证提供者初始化提供者。

好吧，那我们还要导入一个东西，叫做用 pop，pop pop up 登录。

它在这里。

因此，我们有三件事:离开谷歌认证提供商，登录 Papa。

我们在这里设计了一个函数。

我们将通过地球，这个地球的供应商名字是谷歌。

这就是全部，但在这里，我们还必须提供谷歌的供应商，使这一点，然后保存使用您的电子邮件在电子邮件支持 ID 项目支持电子邮件，保存。

因此，它已经被创建，所以保存和填写提交按钮在这里。

这应该会在这里打开一个弹出窗口。

所以如果我们只是说当尝试时，好的，它会成功的。

看鱼的名字。

至于河道疏浚邮箱账号，这里提供的是邮箱。

如果它再次尝试，请单击提交。

好吧。

好吧。

所以我们会得到一个错误，因为我们必须在这里添加方法。

但这在这个自定义域中不起作用。

所以让我来解决这个问题。

所以，所以，所以如果我只是复制这个并添加到这里，或者只是做一个分享按钮。

好了，不是四个小时，好了，把网页链接复制到这里就行了。

从那里，只是张贴在这里的领域。

单击添加。

现在应该可以了。

所以，如果我们点击提交按钮，好吧，未经授权的域名。

等等，再提交一次。

好吧，这里有什么问题？我们永远不能触发这个错误。

所以提交或刷新它，然后点击提交。

好的，现在我们开始了。

让我给你看看弹出窗口。

我想你也能看出来。

那就点击确定。

它在哪里？单击电子邮件任意 URL。

所以看，我们在这里得到返回的所有数据，我们以前得到的密码注册，电子邮件。

因此，这封电子邮件是经过验证的，因为我们已经使用了电子邮件注册。

对不起，我们已经使用了谷歌注册我的意思是，供应商已经改为谷歌。

同样，我们也有 GitHub。

我们有脸书，苹果，推特，以及 GitHub 的一切。

我们必须导入 GitHub 提供程序。

然后我们必须加上这是什么？我就打开 GitHub 的赋值方法吧。

GitHub，我们必须添加客户端 ID 和密码，它将返回我们的授权处理程序、作者和学习，我们必须粘贴到 GitHub 网站，GitHub 应用程序，我的意思是，如果我们去 github.com，我们应该会看到这里的设置。

所以去设置吧。

然后到开发者设置这里。

AWS 应用程序在此处创建新应用程序。

让我打开这个文件。

所以我必须使用你从这里得到的认证 URL。

我们在这里我们在那里从这里开始。

等一下。

如果我们添加 GitHub，这一个，所以我们必须在这个授权授权回调 URL 中添加它，然后我们必须获得客户端 ID 和秘密固件。

所以你必须弄清楚。

如果你不是，如果不是，可以在同一个播放列表里查看我关于数据认证的视频。

就这么定了。

因此，我们已经完成了谷歌认证和密码，创建用户和创建用户和登录。

现在让我们转到数据库。

对于 FireStore，让我只去这个云 FireStore，然后，好吧，我们不需要这个。

如果你想的话，你可以阅读文件。

但是我将向您展示如何执行 Firebase FireStore 的所有四个操作。

因此，打开左侧的 fire store，选项卡，然后创建数据库，以测试模式启动，然后启用它。

所以现在我们可以使用云 FireStore。

在 Firebase 配置中，我们必须导入一个名为 so import 的东西，获取 fire store。

等等，让我先添加一下 Firebase slash fire 商店。

文件夹，我们必须导入，从 Firebase FireStore 获得 fire store，然后我们必须创建一个非常数的数据库，然后我们必须在 gate fire store 函数中传递这个应用程序。

所以我们结束了。

此外，我们必须在我们的应用程序 dot j s 中导入数据库，因为我们必须使用它来创建对集合的引用。

所以，我们必须在这里创建一个集合。

因此，我们可以在这里手动操作，也可以从 React 应用程序的应用程序中操作。

所以让我从 React 应用程序来做。

因此，我们必须删除所有这些我们现在不需要的东西。

让我把这个也去掉。

我们首先导入了两个东西，一个是 collection 关键字 collection 函数，用于向 Firebase 添加一个集合，然后是 add doc，用于向 Firebase FireStore 添加文档。

所以我们可以从我正在做的火垒和火店开始。

这个。

因此，让我们在这里创建一个集合。

所以只要输入 const。

Collection，ref 等于 collection 关键字，它会取这里的东西，其中首先是数据库，数据库，这个数据库。

第二，集合名称。

因此，我们可以将集合设为 users，名称 users 将出现在集合中，稍后会显示。

现在我们必须使用添加文档函数添加文档来发送数据，它接受两个参数。

首先是收藏参考。

其次是数据。

所以我再补充一个。

好吧，现场剪辑。

所以加邮箱就行了。

这将是数据点电子邮件，然后让我们添加密码数据点密码。

好吧，那我们必须记录我们的反应。

或者我们可以说，为了成功，警惕。

我们可以说警惕。

数据已添加。

但是如果它是一个错误，我们可以只抓住它，说错误。

我们可以说控制台或警报。

警惕。

一条是不是的消息。

省省吧。

现在让我们试着添加数据。

所以让我来处理邮件和一些密码。

像添加单据集合一样提交此功能。

这在所有框架中都很常见，比如 React Angular。

因此，如果我们必须从 Angular 添加数据，我们必须添加 doc，如果我们必须从 what it feel 或 JavaScript 或 react 或 Vue js 添加数据，我们必须使用该 Doc。

所以它对于所有其他框架都是通用的。

所以看，这个新的数据现在在这里，这个新的集合名为 Nishan s 440，速率为 bla bla bla，还有密码，让我再加一个。

所以我就把它当成一个结束，提交。

补充道。

就在这里。

如果刷新这个页面，我觉得应该是。

所以让我们检查一下。

你看，我们现在有两个系列了，妮莎。

有一个是最新的，这个是前一个。

现在让我们看看如何读取数据。

所以我们可以为此创建一个函数。

所以 const 获取数据，会是一个箭头函数。

因此，要获取数据，我们需要一个名为 Get docs 的函数。

这个函数从 Firebase 获取数据。

要获取数据，我们只需在这里传递集合引用。

所以集合 ref，我们必须在这里传递它。

它返回一些数据作为响应，就像任何 API 调用说响应一样。

在这里，我们可以只做控制台点日志响应点数据，我们必须匹配数据，因为这非常笨拙。

它有一个参数，叫做让我们看看项目，然后我们必须返回什么，我必须返回项目点数据或 doc，让我做这个数据，我们看看我们得到了什么。

所以我们必须调用一个函数，因为如果我们将它粘贴到提交按钮中，我们就不会调用它。

如果我们点击它，我们应该在这里看到数据。

因此无法读取地图的属性。

好吧，我想这不是数据，是文件。

我们可以在这里查看。

如果我们去读取数据。

获取一次数据好的，好的。

好吧好吧。

好的，去叫医生。

好的，这不是一天的数据。

好的，区域，让我们检查添加和管理数据。

我想瑞德已经来了。

那么它在哪里读呢？阅读文件好的，医生数据。

让我看看好的，别管它。

让我们试试这个提交，无法读取属性。

好吧，我想这是医生。

这一个是数据匹配，保存它提交，然后无法读取地图的属性。

所以我们传递集合引用并获取 Doc。

然后，我们控制对狗的响应，我认为这不是 OSI 提交，因为导航，我们在这里添加了两个对象，这是这些对象，这两个 id。

所以让我们也添加 ID。

所以用一个大括号把它括起来，让你对操作者感到紧张。

所以它存储数据和 ID。

所以 Id 将是 item.id。

省省吧。

现在，如果我们不需要它，我们可以尝试一下。

好的，这里我们不需要三个括号，因为它只是一个单独的字符串。

所以现在就试试吧。

提交，看，它说这两个对象有它们的 id，这个 ID 和这个 ID。

所以如果你有一个文档，我们现在试着更新数据，const update doc，我们需要一个函数来更新数据。

所以这些都是常见的，我再跟你说一遍。

因此，将该函数添加到提交按钮更新数据函数中。

所以在这里，我们必须导入两个东西。

所以我们可以补充说，如果我们打破这一点，这将是对我们有利的。

所以我们必须做一个文档和更新报告。

所以我们一次只能更新一个数据。

所以在更新文档中，我们要发送按钮，在哪里发送 ID。

所以你可以寄，我是说，因为我们这里不需要身份证。

让我从这里选择任意一个 ID。

我会证明给你看。

至于深度，你可以看我的另一个视频，我展示了如何使用 ID 参数更新数据。

这只是一个普通的反应，而不是来自 Firebase。

所以我们能做的是，我们必须使用 doc 函数。

所以首先，我们必须指定要更新哪个文档。

所以说 const。

为了更新等于 2，我们必须使用我们刚刚导入的 Doc 函数，它有三个参数。

其中两个在这里，让我用这个来核对一下收藏参考。

所以是数据库和用户。

要将它传递到这里，需要数据库、集合名称和 ID。

所以让我来选择这个的 ID。

这一次，复制这个 ID，我们将静态地更新这个字段。

所以把这个想法加在这里。

好了，现在 Doc 被选中了，哪个文档更新了。

所以如果我们选择提交按钮，点击这里的提交按钮，这个任务测试为 0，并且密码 123456 将被更新，那么我们必须调用 update doc 来更新它，我们将通过 doc 来更新。

我的意思是我们建议更新和新的数据。

让我们添加电子邮件，他们应该是，让我们说 ABC，并添加密码什么的 12312345 好的。

因此，如果它成功了，我们只需调用它们，就会显示一个警报，并说数据已更新。

但是如果它失败了，我们可以使用 catch error 来捕获它，只是一个警报，它会说 error dot message fine resident 我们正在传递数据库，集合名称和 ID，在 doc 中升级函数。

然后我们正在做所有这些事情。

省省吧。

好吧，我们也称之为 get 函数。

好吧，这不重要。

所以点击提交。

所以让我先检查一下，这封邮件可以吗？邮件是这样的。

它将被更改为这个 ABC。

密码更改为 12345 提交更新，如果我们用 Emily 的 ABC 检查此处，密码为 123456。

如果我们在这里添加这个 ID，让我给你看。

我们可以一次更新一个字段。

因此，如果我们不想要密码，我们可以删除它，或者让它成为您想要的任何东西，或者简单的密码。

所以保存，提交。

所以在这里，它们应该是 ABCD。

所以让我把它变成 ABCD。

这应该是 ABCD，电子邮件提交，更新了电子邮件现在 ABCD。

密码也是一样的。

这就是我们投标文件的方式。

所以第四个是删除文档。

让我给你展示一下。

因此，我们可以让它删除数据，并将此处的功能传递给按钮。

而且我们这里不需要传数据，只需要传 doc 来更新或者 dock 来删除。

让我再来一个函数，叫做 delete dock，或 delete，dock DRC。

好吧。

所以在这里加上函数就好了。

在好的，更新文件的地方。

所以我们必须使用数据删除警报。

所有的事情都很好。

所以拯救吧。

现在让我们检查一下。

如果我们点击提交。

第二，它将删除这一个与电子邮件 ABCD 和密码 126。

提交。

好吧，删掉。

请注意，它不在这里，我们只有一个字段，其中有 MEC 的电子邮件和密码。

这就是删除功能的工作原理。

如果我想删除这个 ID，我们必须复制 ID 并粘贴到这里。

所以信任第三个参数 ID。

所以你必须从参数中获取 ID。

对于这个删除函数，从这里传递 ID，它将到这里，它将到这里，然后简单地在这里传递 ID。

提交已删除。

请注意，我们这里没有任何字段，我们在 FireStore 中有空数据。

这就是 Firebase FireStore 中添加文档、获取文档、更新文档和删除文档的工作方式。

所有用于 CRUD 操作的操作。

现在，让我们看看 Firebase 存储的文件存储。

所以我们必须在这里导入等等。

从火基存储，存储来了。

它来了。

好吧。

因此，它应该是来自 Firebase 存储的良好存储，然后我们有导出常量。

所以导出常量存储等于获取存储。

类似地，通过应用程序，然后在这里导入存储。

所以我们不需要数据库。

因为我们要把这些东西移走，让它变干净。

删除这个，删除这个，好的，不要删除这个，删除处理提交，不是函数只是获取数据的内容，删除数据等等，这里，让我先创建一个，让它处理提交。

让我创建一个输入字段。

所以我们不需要这个。

所以移除它，在 JL 上，它需要一个事件，我们将调用句柄输入函数。

所以我们也可以这样设置。

因此，他们的数据应该是事件点目标点文件和 zeroeth 索引文件将存储在这里，在句柄输入中，然后如果它在存储中看到，让我在这里打开存储单击 web 单击单击上传文件。

所以我们在这里得到数据。

让我和他一起给你看这是控制台点日志。

让我在这里说文件。

所以文件会在这里，不是这个，我们不需要用文件，我做错了，我们需要用数据。

所以处理输入。

好的，那么我们也不需要这个，粘贴到这里。

如果我们选择一个文件或者保存它，但是我们必须添加类型。

因此类型将是文件。

我们不需要在名称中输入占位符，所以删除它们。

因此，如果我们选择一个文件，他们应该有我们在的标志，我已经决定出现在这里，如果我们说，如果我们按下提交按钮。

让我在这里选择一个文件，比如 REST API，单击 Submit。

看，文件现在在这里。

现在我们必须将文件发送到 Firebase 存储器。

所以去这里的储藏室。

让我们启用一个存储。

所以已经启用了，好吧。

因此，我们必须导入这个 get 存储，并在顶部引用。

所以我们不需要储物，因为我们已经有了这个伟大的传统。

我们必须在这里使用它，如果我们想使用存储，就要获取存储。

看，它在这里做这个，但是我们已经在 Firebase 配置文件中做了，然后我们必须使用 mountain reference reference 指定哪个文档保存哪个数据。

所以在这里创建，这应该是图像名称。

所以图像名应该称为数据点名，因为看，图像的名称就在这里。

所以我们可以说，数据点名称。

就这么定了。

现在我们必须将山的引用传递给一个函数。

这个函数是通过上传字节上传的。

但是如果我们到这里，我们应该能清楚地看到所有的功能。

因此，我们必须使用此 upload for upload by resumable 从 Firebase 存储中导入它。

这里，我们需要的另一件事是获取我们的下载 URL。如果我们的文件上传成功，我们将在控制台中获得一个 URL，我们可以将它发送到数据库。

比如说，我们提交了一个个人资料图片，这是一个触发销售，然后我们可以发送头像或个人资料图片的链接到数据库。

所以我们必须使用这个上传字节是一个移动功能。

因此，请等待上传任务，创建一个这样的实例，它采用存储引用，而不是山引用。

所以包存储数据找到文件的种类，所以它是数据而不是文件。

所以我们必须完成这个叫做上传的任务。

所以在这里，让我们开始吧。

因此上传 task.on 状态已更改。

所以它需要一个回调 URL。

所以我就这么说吧。

如您所见，它采用了一个名为 snapshot snap shot 的参数，因此在第一次回调时，我们必须计算照片上传的进度，然后使用该数据，我们可以创建一个进度条。

所以会在这里展示。

如果是错误，我们可以处理错误。

让我在这里补充一下。

所以让我来纠正这个错误。

所以控制台点日志，错误，而不是消息等待。

如果成功，如果文件上传完成，我们可以调用下一个函数获取下载 URL。

所以你可以看到它返回给我们一个 URL。

我来清除这个评论。

如果我们刷新这个，好，让我试试这个。

我们的存储中没有任何文件，所以我只选择一个文件。

因此，我们选择如果我们在这里提交，看到上传是 0%完成。

好的，发生了未知错误。

好的，让我刷新页面再试一次。

如果您在这里查看，我们今年会传递脚本，没问题。

所有的事情都很棒。

这里也是。

因此，让我们尝试使用 ref 来指定参考数据点团队。

好吧，让我把它传到外面。

保存。

现在让我们试着上传一个文件。

单击提交报告撤消好的。

这里的错误是什么？好的，让我看看。

有效载荷的非线性检查。

让我看看这个。

检查有效负载检查有效负载 Firebase 存储错误。

它变了。

好的，上传任务。

图像就绪点 jpg 数据点名称。

好的，剪报在这里。

我们这样开始，为什么要传递这个，我们把它传递到这里，存储区。

因此，如果我们看到 ref const 今天是您的存储，这很好。

我们在这里传递 ref 存储 ref，然后数据和上传字节假设我们正在使用这个文件，但这里我们使用的是数据线。

那么这里的问题是什么呢？所以，它没有被适当地污染，因为为什么 const 上传任务流任务在这里。

有限状态不改变。

好吧，最终是好的，快照进度错误。

但是为什么不能在这里上传一个文件呢？是的，因为规则不是在这里设立的，我想。

所以，我们也必须检查规则。

所以打开规则。

这里的规则是，我们只能在通过身份验证后上传文件。

就像我们一样，但我们在这里没有被认证。

所以让我再试一次，它会工作许可。

上传已经完成 100%。

我们可以在控制台中看到文件 URL。

如果我们转到这个链接，这是我们上传的图片。

现在，我们可以使用此链接将数据发送到 Firebase 数据库。

因此，如果我们在这里检查它，也刷新页面。

等等，应该放在这里。

在储藏室里的是什么？

等一下，看看这里的图片，上面有名字，所有你可以使用的东西，还有位置。

那么，如果我们想上传一个文件夹中的文件。

所以在这里的数据点名称中，我们可以在一个字符串变量中指定文件夹。

所以先把它作为一个模板文字文字，然后添加文件夹，假设它是图片，去掉图片名。

因此，我们将发送图像文件夹中的图像。

因此，如果我们再次尝试发送数据，那么尝试它，选择另一个文件，任何随机文件，然后提交。

它应该上传到图像文件夹。

让我在这里检查一下，刷新存储。

看，它在图像里。

这是我们刚刚上传的当前图像。

这就是 Firebase 存储在 React、Angular view、膨胀、JavaScript next 等所有框架中的工作方式。

太好了。

现在让我们看看如何使用 Firebase FireStore 中的实时更新。

所以让我告诉你我的意思。

我们从 gate 函数中的 Get Doc 函数获取数据。

而且是空的。

给你。

如果我们曾经命名 Nishan，Kumar 和 email，submit，它会被添加，但它不会在这里被刷新。

为了刷新页面，我们创建了手动获取页面这个，点击这个，只有这样我们才能在这里得到这个对象数组。

由于 Firebase FireStore 考虑不周的问题，我们不得不创建一个叫做快照的东西。

所以导入这个函数。

让我现在就进去找医生。

因此，这个 on snapshot 监听 Firebase FireStore 中的实时更新。

这里，我们必须传递集合引用和一个回调函数。

因此，每当 Firebase FireStore 内部发生变化时，这个回调函数就会运行。

如果我们删除，如果我们更新，如果我们添加数据，任何变化。

所以我们必须这么做。

它有一个参数，叫做让我们把它做成数据，然后把它记录下来，所以我已经把它记录在这里了，所以复制并粘贴到评论里。

让我在这里刷新或格式化代码。

让我们从头再来一次。

刷新页面。

我们有一个物体从这里出来。

如果我们再添加一个对象，比如说 Nishan Kumar，并通过电子邮件发送其他内容来提交这些新数据，我们将在这里使用以前没有的好文档功能。

因此，提交，见补充，我们正在得到实时听取。

如果再加一个 34403，这里就有三个物体，来自这里。

因此，当这些数据在这里发生变化时，前端应用程序也会发生变化。

如果我们删除这个集合或任何字段，让我们看看删除文档会发生什么。

开始删除。

看，这里我们也刷新了页面。

我们只有两件物品来自火场。

这家消防商店。

这就是它的工作原理。

如果它完成了整个集合，我们现在就会有一个空对象或空数组。

所以删掉这个。

看，这个广告不是空的，因为它被实时收听。

这就是所有快照从 Firebase FireStore 实时获取数据的方式。

现在让我们看看 Firebase FireStore 查询。

因此，查询用于根据一些字段(如姓名或年龄)过滤数据。

这里我添加了四个字段，包含姓名、年龄和电子邮件。

所以让我们根据它们来筛选数据。

所以我们需要 Firebase FireStore 的关键字 query。

还有一件事叫做我们在哪里关键字。

所以让我创建一个查询常量。

所以收藏币，我们先加年龄查询。

所以它接受查询参数。

所以我们会过滤掉年龄，小于 28 的。

所以我觉得应该拿 2521 和 20 年龄。

所以这个过滤器，这里把集合引用作为第一个参数，然后在第二个参数中，它接受 aware，这里我们定义条件。

所以我们必须根据年龄筛选，添加年龄，柠檬逗号，然后我们必须添加条件，我们是否希望它等于或小于或大于，然后是值，所以我们希望年龄小于 28。

所以这里把这个设为小于，所以如果年龄是 28，那么这个就会显示出来，然后我们要通过这个 h 查询来获取函数。

在我们的例子中，它在快照上。

因此，在收集地点，他们已经通过了 H 查询，您将看到，我们将在这里找到三个数据，其中年龄小于 2820，21 和 25。

如果我们使它大于 28 或等于 date，我们将在这里只看到一个 28 岁的年龄，所以让我向您展示刷新。

因此，如果它等于 28，我们将只能看到该数据。

看，这里我们只得到一个 h，因为这里只有一个数据的年龄是 28。

这就是 H 或非数字滤波器的工作原理。

现在让我们尝试过滤名称查询。

所以对于名称查询，我们将使用如果名称等于这个 Nishant Kumar four，那么它将打印这个的数据。

如果我们刷新页面，让我们看看这里有什么。

好吧。

有点不对劲。

如果 name 等于这个 Nishant Kumar for，好的，我们也必须在这里传递查询。

不是 H 查询，因为它不存在。

所以，我们只是得到了这个名字，因为只有一个名字，因为这里的名字是 Shan Kumar，我们是基于这个来过滤的。

我们还可以添加电子邮件或我们想要的任何其他字段，这就是 H 或任何其他查询的工作方式。

这里很好。

好的，用电子邮件发送。

然后我们可以在这里看到这个查询。

让它只是尼山进一步 gml.com。

它们应该在这里，因为这个记录有这个的名字 email 和这个的名字。

这就是 Firebase FireStore 中查询的工作方式。

所以让我们来学习如何托管一个 web 应用程序或网站来 Firebase 托管。

所以我在这里有一个 React 应用程序博客网站，我们将主办。

因此，我们首先必须创建或安装 Firebase CLI。

因此，我们将在这里安装 Firebase 工具。

让我安装这个。

还有一件事我必须做的是，我们必须在这里创建一个构建。

所以 NPM，运行构建。

在这两次之后，我们将继续前进。

所以我们必须等待构建的生成和 Firebase 工具的安装。

现在我们有了构建。

所以我们来部署一下。

首先，让我们使用 Firebase 登录配置项登录 Firebase，输入。

所以它会在 Chrome 中打开一个窗口，用这个登录，允许这个，我们就用 Firebase 登录了，然后我们要做的就是初始化这个项目。

让我放大这个。

所以我们可以在里面做燃烧基地。

我们想继续。

所以是的。

我们希望主机只需按下页面选择选项并输入。

但是下一步，我们希望建立公共目录，因为这是 inter 的构建位置。

所以，好吧，我们不需要这个自动构建。

如果有问题，让我再做一次。

让我先清理一下。

我们必须用这个来初始化它。

所以，是的，我们想要主机。

内部版本是公共文件夹。

我们想要单页应用程序。

是的，伊冯，你不介意自动生成不。

不想覆盖它吗？号码

就这些了，让我们使用名为 Firebase deploy 的命令来部署它。

所以步骤是首先生成一个构建，然后登录，然后初始化，然后部署。

因此，我们有不同的命令来生成 Angular for React 和任何其他框架的构建，比如 next。

这就是我们在这里的做法。

所以看到它阅读或给我们一个网站链接在这里。

所以让我们打开它，看看他是否在这里看到我们的网站。

这就是我们刚刚在这个链接中托管的网站。

这就是 Firebase 托管的工作方式。

现在，让我们来看看 Firebase 安全规则。

因此，如果您的 BEN Firebase 规则没有设置为 public，这意味着任何人都可以写入或读取数据。

因此，即使我们没有在 Firebase 中通过身份验证，使用登录名和密码，我们也可以干燥数据或读取数据。

让我们试试这个。

目前，我们还没有登录，因为我们得到了这个消息，没有登录，进来，从这个所有者状态改变。

这告诉我们，如果我们登录，我们将看到一个 LF 已经登录。

但如果没有，我们会看到电子没有登录。

在我之前的视频中，我已经展示了如何使用授权状态更改或未更改状态是。

所以我们也可以从控制台删除数据。

看，它现在是空的，因为这里的数据是空的。

现在让我在不登录的情况下添加一个数据。

我们没有登录，没有登录。

在电子邮件，添加姓名，电子邮件和一些年龄，点击这里添加。

看，我们看到了数据孤岛的增加。

如果我们检查这里，它将被添加到这里。

它将有一个文件的高清五电子邮件和我们刚刚在这里添加的名称。

我们也可以打开控制台阅读它们。

看，它正从控制台传过来。

现在，让我们将规则更改为 authenticated，这意味着只有在经过身份验证的情况下，我们才会写入数据。

所以让我在这里做吧。

所以现在就去掉这个状态。

好的，只要请求点 auth 不等于空就行。

所以我们可以让它在这里为空。

这意味着当我们登录时，当请求的作者不为空时，它将允许我们读取数据或 ID 计时。

如果不为空。

当我们注册或登录时，请求中应该有一些数据或我们从身份验证中获得的数据。

这里要写的第二件事是任何情况下的允许比率，所以 right 是认证证明，而不是这个 read。

所以发布这个规则，并打开数据选项卡。

就像现在我们说的数据没有登录，我们没有登录，姓名，电子邮件和年龄，点击添加。

看，我们得到这个消息，称为缺少或权限不足。

但是我们可以读取数据。

因此，打开控制台，我们在这里得到数据，看到年龄 25，以及诸如此类的消息或字段。

我们在这里得到它。

现在，让我们将 rake 的角色也设置为身份验证配置文件。

所以像这样在这里做。

您将会看到，我们仍然会遇到在正确的许可操作中出现的错误。

所以保存这个或者发表这个。

打开控制台，我们会看到权限不够的消息。

所以有一个页面没有登录。

请参见错误和快照侦听器 Firebase 错误缺少权限或权限不足。

这意味着我们没有权限读取数据，因为我们没有登录。

现在让我们尝试登录，并输入电子邮件和密码。

在这里登录。

我们会看到一个娜美登录。

如果我们刷新页面。

所以，我们应该在这里看到数据，因为它允许我们在认证后吃东西。

现在，让我们添加认证后的数据，电子邮件和年龄。

所以点击这里添加。

因此，请参见此处添加的数据。

这就是身份验证的工作原理。

我是说，这就是 Firebase FireStore 规则中的规则。

身份验证。

这里还有一件事是允许用户在任何情况下都不能读或写。

因此，这意味着即使我们已登录或处于“已验证”选项卡中。

它应该允许我们不写数据。

所以我们会在这里加上 if false。

这里也是。

好吧，只要得到许可就行。

这将允许我们在任何情况下写入数据，即使我们已经过身份验证。

所以刷新页面。

登录后，让我们添加数据。

年龄，点击添加。

看，我们没有权限看数据，我的意思是写数据。

这就是 Firebase FireStore 规则的工作原理。

现在让我们来处理 Firebase 实时身份验证更新。

所以我的意思是，当我们登录时，我们的应用程序应该向登录失败的 Firebase 发送数据。

如果我们注销，我们的应用程序应该发送数据到 Firebase 被注销。

因此，如果在这里，我有一个电子邮件和一个密码字段登录按钮在注销按钮，我已经添加了一个电子邮件在这里我的邮件。

让我创建一个开关状态，它可以在舞台上听我们实时改变。

所以就像这样。

创造一个你会影响我们的效果。

就这么简单。

在这里使它成为一个空字符串。

我是说，阵列。

所以只要在这里调用一个函数。

我的意思是，当我们在记录时，它需要一个 off 参数。

第二件事是一个回调函数。

回调函数返回一些数据给我们。

所以我们可以说数据，并在这里安慰它。

数据。

如果我在这里检查这个控制台，让我们看看我们最初有什么。

我们正在读取这里的数据，因为现在我们已经登录了。

所以让我从头开始注销。

好吧。

非常抱歉。

现在我们在这里注销。

所以让我输入电子邮件和密码。

电子邮件是这个，密码是什么。

好吧，好吧。

所以如果我用电子邮件密码点击登录，这个功能还是没有状态改变。

它正在向已登录的 Firebase 发送数据。

如果我们单击“注销”,这将向 Firebase 发送数据，表明我们已注销，并且它将持续存在。

即使我们刷新页面，它仍然会存在。

所以除非我们注销或登录，否则状态不会改变。

所以如果我登录，点击这里的按钮。

我们将在这里得到所有的数据。

看，即使我们刷新页面，仍然会得到数据，仍然会让他退休。

但是如果我们点击注销，这应该是空的。

这意味着我们现在已经注销了。

让我刷新页面，您仍会看到空值，因为我们尚未登录，我们已注销。

我再给你一件事。

所以创建检查数据是否存在，或者它是否不为空。

或者，如果我们已经登录，我们会在这里显示一个警报。

每次它运行时，我们已经登录，但如果或少于其他，好吧，只是让它其他，我们会看到一个警告，我们注销或未登录的证据。

现在让我们试试这个。

来看我们正在阅读这条名为已登录已登录的消息。

所以刷新页面。

还是一样，因为我们没有登录。

如果我们输入电子邮件和密码，我们将看到我们现在登录。

好的，如果我们刷新，我们就登录了，因为 Firebase 已经将我们的状态声明为已登录。

如果我们注销，状态将变为注销。

这是关于 Firebase 中的状态变化，它引导我们进行实时更新，以便在这里创建一个名为 index dot j s 的文件，以及初始 JCS 和 HTML 的时间，所以 index dot HTML。

所以他们跑来了。

所以让我们在这里添加嵌入代码。

然后把这个做成 Firebase js。

首先，在第一个视频中，我们将学习如何认证和使用 JavaScript 的 CRUD 操作。

通过 src 的 index.js 编写脚本。

让我把它格式化。

因此，我们将在正文中写入所有内容，并将脚本标记放在正文之后，因为它应该在此之后加载。

因此，让我们从创建一个 Firebase 应用程序开始。

所以请到这里的控制台。

添加项目。

让我们把它变成火力基地。

前端，继续。

在这里创建项目，它正在创建。

所以让我们在这里用 live server 运行这个文件。

所以它在这里运行，但是我们看不见，因为我们什么都没有。

让我安排一天，让我们进行四次 put，或者首先让我们进行两次身份验证输入。

因此，我们将输入电子邮件，电子邮件的占位符。

与电子邮件，使这一点，然后第二个应该有密码的乐趣。

密码保存，我们有电子邮件和密码。

让我们给他们一个造型。

所以也创建一个 CSS 文件。

所以这应该是 index dot css，简单。

在这里链接 css，我们可以只说链接，CSS，和索引点 CSS，保存。

所以给这个 div 一个类名，或者写了类名的类，比方说。

这里，在 CSS 中，我们将只表示显示 flex height 通过搜索和调整内容中心未读的内容一个行项目一个行项目中心，所以它们现在在中心位置。

所以我们需要让他们变得聪明。

所以我们也可以改变行的方向，或者我认为应该是列方向，是列方向而不是行方向。

所以我们现在有两个输入字段，电子邮件和密码。

让我们添加一些标签标签电子邮件，并把它们放在一个分区。

所以这个电子邮件和密码对不起，这个电子邮件标签和电子邮件输入字段现在是一个组合。

所以第二年再做一次，或者输入密码，如果现在我们有了这两个密码，他们应该是密码。

或者我们能做的只是重新命名两个主体。

好，现在好了。

因此，我们将在密码，让我们给他们的水平一些造型，或离开它，这很好。

我们不关注风格。

所以我们需要一个按钮。

所以做一个按钮，做一个测试提交层，给按钮一个类名称提交按钮，或 BTN。

在 CSS 中，我们在这里添加类名。

它应该是绝对的位置，然后左 50%，顶部 50%和枢轴，然后我们还必须在顶部增加一点点保证金。

所以页边距顶 30 像素，或 4050。

好吧。

我们现在有电子邮件和密码提交按钮。

现在让我们从这些输入中获取数据。

因此，使这成为 ID 添加 Id 值，并使其成为电子邮件。

它们应该是密码 id 密码。

同样，让我们添加输入字段的名称。

它们应该是电子邮件。

它们应该是密码。

所以名字就是密码。

现在让我们正确地格式化它们。

给他们一个小缺口或什么的，然后，然后只是等待。

好吧，好吧，没关系。

现在我们有了电子邮件和密码字段。

现在让我们从他们那里获取数据。

所以要获取数据，只需打开 index js 文件并创建一个函数。

所以 const 寄存器，这将是一个箭头函数。

这里，让我们从这些输入字段中获取数据。

第一个是男性。

让我用输入的类型。

所以这里应该是男性。

这里应该有密码，密码的类型。

那么让我们在这里添加输入。

所以 const const，email 等于我们应该称之为 document dot 通过 id 得到元素，email id 和 dot 值得到它的值，然后我们只需要用 password 等于 document dot 通过 ID 得到元素，用 password 得到它的值。

如果我们能够安装这些值，我们应该能够看到它们。

但是首先，让我们把这个函数绑定到这个按钮上。

点击注册上的提交按钮。

现在，如果我们试图安慰他们控制台点日志，电子邮件和密码。

让我们看看我们得到了什么。

所以打开控制台检查元件。

并输入电子邮件我结束一些密码。

提交。

现在，我们在控制台中获得了电子邮件和密码。

我们删除这个控制台日志。

让我们创建 Firebase 应用程序，点击此处的 web，并将其命名为 fire base。

运行和，这里我有一个关于如何将 Firebase 添加到 JavaScript 库的指南。

所以在这里使用一个脚本标签。

所以我们现在不需要这些。

所以只要打开这个文档，我会在评论或描述中留下一个链接，复制这三个 CDN 标签，我的意思是 CDN 链接，然后基于它。

不好意思，贴在 index.js 之前了。

所以我们有这三个。

什么是三个链接或者脚本标签？首先是为了所有人，这是为了 Firebase FireStore。

第三个是应用程序。

所以我们必须使用这个叫做 Firebase 的东西，在我们的 index.js 中初始化它，保存在这里，打开 index . js，然后我们可以在顶部初始化它。

然后我们必须在这里添加我们的配置。

所有这些数据，这些数据复制这些东西。

我 API 的关键或域和所有，并在这里添加它。

好，然后我们必须初始化我们的数据库 FireStore 数据库和作者。

所以让我们开始吧。

在这里做。

现在我们可以利用这个机会报名了。

所以我们可以只说 auth。

点签，我们必须使用创建用户与电子邮件，和密码。

所以小拼写，这是一个骆驼字母用户是用大写字母 V w，大写字母 H，大写字母 l。

我们必须将这个电子邮件密码作为 params 电子邮件密码传递进去，这是一个承诺。

所以我们可以有一个 then 块，它表示对我们的响应。

我们可以在控制台中说响应点用户，RS 点用户。

如果捕捉失败，我们也添加一个 catch 块。

所以这应该是为什么我们应该叫它，这里应该有一个错误，错误。

我们可以说控制台点日志错误点消息。

还有一个是为了代码。

所以我们来补充一下代码 air 红点代码。

现在让我们试试这个。

但首先要做的是继续在这里控制台，打开侧菜单中的认证选项卡，认证步骤，点击这里开始。

然后激活电子邮件和密码登录方法。

省省吧。

我们到此为止。

所以点击用户。

你看我们这里没有任何用户。

所以我们在邮件里加一条。

而是先打开控制台输入邮箱和一些密码。

单击提交。

看，我们在这里得到所有的数据，所有的显示名称，电子邮件和其他一切。

因此，我们可以使用显示名称，它的空电子邮件是我的名字，这里的其他一切。

如果我们在 Firebase 控制台身份验证中刷新此数据，我们将在这里看到我们的电子邮件。您可以看到，我们的电子邮件已注册。

现在。

如果我们试图再创建一个数据，如果你试图用相同的电子邮件创建帐户，我们会失败，在这里提交。

你看，我们得到的这个消息叫认证或电子邮件已经是。

所以我们可以在这里添加一个警报。

警告，显示错误信息。

所以只需在这里添加，并保存电子邮件和密码。

请注意，该电子邮件已被另一个帐户使用。

所以这是注册创建一个登录 const 的函数。

我的意思是，我们将在这里使用相同的数据，相同的输入字段等等。

所以在登录时，我们还需要一个按钮。

所以在这里创建一个按钮。

这将指向登录。

我们可以把它定得和我们签约时一样低，但是它们是重叠的。

所以我们可以做的是，我们在这里提交按钮，使说登录按钮和空气只是添加一个新的登录按钮的样式。

在这里做同样的风格。

但会是一样的。

所以左转到 60%。

这是 30%的罚款，不介意的设计。

我们只是在使用那里的功能。

所以在 index.js 中，我们使用这个注册器作为登录方法的登录。

还有这里。

让我们像这样在这里添加电子邮件密码。

我们可以说 auth dot 使用电子邮件和密码登录。

我认为拼写是正确的。

它还需要两个参数，电子邮件和密码。

所以他们做的所有事情都是一样的，所以只需复制这个，然后在 catch 块中粘贴它。

让我们在这里添加一个警告。

好吧，警报没用。

现在让我们尝试登录。

因此，输入电子邮件和密码，并点击登录，但首先打开控制台。

要检查它是否工作，请不要登录。

你看，我们正在阅读所有的数据，这里说作者显示姓名，电子邮件和电子邮件验证和所有的数据，如果我们输入了错误的电子邮件，还没有注册，我们将得到一个错误的警告。

所以签到吧。

看到这里得到这个没有用户根据标识符，用户可能没有使用或可能已被删除等等。

所以起作用了。

如果我们发送了正确的电子邮件，但输入了错误的密码，我们会得到密码错误登录。

看，密码无效，或者用户不知道没有密码。

好吧。

无效签名者运行良好。

现在让我们看看如何使用相同的数据来 Firebase FireStore。

所以把这个 FireStore 数据库钉在这里。

哦，天啊，巧克力。

因此，在这里创建数据库并以测试模式开始，因为我们正在测试它。

所以所有的事情他们都很好。

因此，它正在初始化和供应云 FireStore。

同时，我们可以在这里创建一个函数。

所以它会说 const 保存它的数据是为了销售功能。

我们将使用这种方法来保存数据。

因此，让我们在这里注释所有这些代码，因为我们已经看到它是如何在所有这些输入中工作的。

不，不要表彰，我们会重用。

所以在这里再创建一个按钮。

所以它应该被称为健康数据。

我们可以在这里修改这些按钮。

因为我们现在不再需要它们来保存数据，所以我们将只在 FireStore 数据库中保存电子邮件密码。

那会很好的。

所以我们在这里保存了数据，并在这个按钮上添加了这个功能。

所以点击点击这个保存数据的功能。

所以我们给用户读一封邮件，我们读邮件密码，我的意思是，在这里添加它，它会工作。

如果我们单击这里的一个按钮，应该会在我们的函数中获得电子邮件密码。

所以要添加数据，我们可以做这个简单的事情。

如果我们检查这里的文档，它被列在这里，我将在描述中留下链接。

所以我们必须首先添加集合名称。

所以 DB 点集合。

所以 collection 应该是用户的集合，那么我们会使用一个 add 关键字在集合中添加数据。

所以首先是我们的电子邮件，添加电子邮件作为电子邮件，然后我们有一个密码。

所以加密码为密码。

如果成功了，我们就可以使用我们的 Then 块。

所以它需要我们的文档引用。

我们可以只在控制台上记录这些数据，但它会使用这些数据，拷贝这些数据，并在这里替换控制台日志。

如果失败，我们将使用 catch 块。

好，现在让我们试试这个，我们的数据库是空的，你看这里，让我们添加一个电子邮件和一些密码。

因此，如果您打开控制台并保存数据，我们将得到名为“用 ID 这个 ID 编写的文档”的消息。

如果我们检查 Firebase FireStore 并刷新这些，我们应该会在这里获得与 will Sawyer 相同的 ID 的数据，无效密码在集合中列出。

例如，这是一个数组，这是一个对象，在这个对象中，我们有无效的密码字段。

我们再加一个，修改一些数据，保存。

看，另一个在这里。

也应该在这里。

看，我们的广告文档功能正常。

号码

现在让我们看看如何从 FireStore 读取数据。

因此，为了读取数据，我们也在这里添加一个函数。

所以 const 读取数据，并在索引中添加函数。

所以在这里添加按钮。

但是结束了。

应该读出来，等一下。

读取数据。

就说点击事件上的点击。

他们应该在这里读取数据。

他们当我们有一个按钮称为读取数据，将对应于这个功能称为苏拉地形数据。

这非常非常简单。

所以我们只能说 DB，点集合，在集合中，让我们解析集合名，它叫做 users。

这里的集合名称是 users。

你会看到，这里实际上使用了一个 get 关键字来获取数据。

所以我们可以使用 get，它会给我们发回一些数据。

因此，我们可以只说，点，然后，在控制台中看到，即将到来的数据，我们可以把它作为文件，或者让我们把它简单的数据，然后我们只说，控制台点日志数据。

因此，如果您现在打开控制台并单击 save data 按钮，则 get 未被定义。

等一下，让我看看有什么问题？未定义门。

好吧好吧。

好的，如果我们复制这个，让我替换所有这些东西。

等等等等。

所以 gate 没有被定义，因为我们必须在这里用一个点。

在此之前，获取文件，保存并再次检查，读取数据。

我们在这里得到这些数据。

但我们不清楚这是什么？让我把它过滤掉。

所以数据到文档，我们能做的，我们必须把文档放进去。

所以我们将再次使用 Docs 来读取数据。

所以我们在这里读取这个数组。

在这个数组中，我们必须找到我们的数据。

所以我们要映射这个数组。

所以我们会说 map，它会给我们发送一个项目作为后备，然后我们会说 console，或者我们会说 return item。

所以我们要归还这个物品，不是数据，要这个物品有一个数据作为报废。

因此，如果我们保存并再次检查，我们应该得到这两个对象的两个字段的数组对象。

但问题是，我们拿不到身份证。

所以我们应该通过添加 ID 来获得 ID，我们应该是 item.id。

因此，创建，再次单击重新创建，我们应该有一个 ID 在这里，看到这两个文档的唯一 ID。

这就是我们如何留住 Firebase FireStore 的。

现在在第三件事情中，lift 看看如何更新数据。

所以就说 const 吧。

另外一个叫做更新数据的功能相当于 Mr 功能。

我们将在这里命名集合。

因此，我们将添加集合名称，然后我们必须指定集合 ID。

所以我们有两个身份证，这个和这个。

所以我们可以只说 doc 关键字，要传递思想。

所以就把身份证递给这一个。

然后我们必须更新它。

所以我们可以说更新。

我们必须首先添加更新日期数据进行更新。

所以，让我们先把电子邮件变成别的东西，让它看起来更好。

男孩 123，我读了读 gmail.com。

所以密码应该相同。

因此，如果我们将密码设置为“ok ”,我们就可以将 URL 设置为 123456。

因此，我们将更新第一个字段，即 501 的尼山测试。

email.com 无效。

所以我们需要把这个函数传递给一个按钮。

所以我们可以在这里创建一个按钮。

这只是为了显示你更新一个字段，只是通过这里的一个按钮，点击这个比率，这个函数更新数据。

大卫，所以我们必须得到一个基于承诺的回应。

因此，只需使用点，然后我们就可以说警报数据已更新。

保存它，让我们现在检查它。

如果我们单击“更新一个字段”,这个 Rashaan as 4401 应该是电子邮件中在此联合的电子邮件。

单击更新一个字段好的，它还没有定义。

所以我想我们必须在这样的字符串中使用它。

现在，让我们再次尝试更新一个字段，请参见此处的更新。

如果我们检查这里，看到这封电子邮件了吗？

所以我们可以再改成 12321234。

如果我们保存了它，再次更新它。

这里也是 123 现在是 1234。

就这么定了。

现在让我们删除这个字段，使之表示删除数据。

这里还有一个按钮，用于删除该字段，使其成为这个名为“删除数据”的函数，粘贴它并在此处保存文件。

这里也是，但让我们删除这个字段，称为 gmail.com 比率的密歇根人 440。

和密码 126。

所以我们只说删除三个，我们在这里设置 doc，没问题。

所以我们只用这个 delete。

所以删，删。

我们会拯救它。

现在让我们试试这个。

因此，如果我们单击此处，它会将名称更改为 derryl one 字段，单击此处，它会被更新。

但是这个应该被删除，是的，但是它现在已经不在这里了。

好吧，很少走错地方。

好吧，没关系。

所以我们已经从这个 ID 中删除了这个字段。

它不在这里了。

让我再给你们看一遍，再看一遍。

所以 make this 表示应该删除数据的位置。

让我在这里添加一个字段。

所以添加一个电子邮件和一些密码。

有数据保存在这里，它应该在这里。

所以我们有两个领域。

这个和这个。

我们可以阅读它们。

如果我们点击这个速率数据。

我们在这里得到两个字段，我们可以更新一个字段。

所以我们用这个 ID 来更新。

所以让我用这个 ID。

所以如果我再读一遍，把 ID 复制到这里。

因此，让我得到这个迪克西 ca 无论什么 ID，我们将更新这一领域的尼山至于道路愤怒 gmail.com。

所以保存索引点 J 的文件。

它们应该被更新。

如果没有，它们应该被更新。

如果我们点击更新，这个名字应该是阿什是一个好孩子 123 由地址 gmail.com。

让我们试着更新一个字段。

您将在警报中看到名为“数据更新”的消息。

我们应该会看到 Ashish 123。

所以她是个好孩子。

如果我们删除这里的字段，那么这个字段也将被删除。

但问题是，我们没有这个字段的 ID。

所以它不在这里，如果我们点击删除，我们应该得到一个错误。

所以我们可以说点接球。

所以它需要一个叫做误差的参数。

所以我们可以说控制台点日志错误。

如果我们现在在这里删除这个字段，我们应该去掉那个被删除的等待。

好吧，这个 ID 不存在。

但是你怎么能删除我觉得这里有点不对劲。

没关系。

所以让我们用这个 SB 代替 ID 作为 WB in whatever。

读取数据，复制 ID，放在这里。

一件外套，不是两件外套。

他们说，我们将删除这个名为“第二个”的字段。

密歇根人 440 雷吉·米勒通信密码 126 身份是 WBQ·TMG。

所以点击删除一个字段删除。

他们不应该在这里。

你看，我们只有一个领域。

如果我们也删除这个，我们也必须像这样传递相关领域的想法。

如果我们读取数据，这里会有空对象，空数组意味着这里什么也没有。

所以我们的四个操作现在知道如何工作了，它们是创建、读取、更新和删除以及注册和登录方法。

所以我们做了很多。

我的意思是，我们已经做了数据库 FireStore 数据库。

接下来是角 2。

首先，让我们创建一个角度项目。

确保您已经使用此命令安装了 Angular CLI。

为了创建一个新的应用程序，我们将使用名为 mg new I app 的命令命名我的应用程序。

所以粘贴到这里，让我们在应用程序名称了。

但是我们先在这里把目录改成 Angular，然后我们会用这个命令，这个应该是 Angular，去学火基，保存它。

它将在该文件夹中创建一个项目。

这需要一些时间。

所以即使是路由，是的，我们在 CSS。

所以它在这里安装了所有这些东西。

所以让我们等待它安装。

Angular 应用程序现在已经创建好了。

所以现在我们来换个文件夹。

就做 T D 吧。

为什么它有角火基角和角？连字符，火？节拍指数。

现在我们在这个文件夹里。

所以这里有很多文件，我就不跟你解释了。

这些是什么？但是我们必须在这里安装一个名为 Angular phi 的包。

这是 Angular 的官方套装。

所以你看，它在问我们这个问题。

你可以说是或不是都没关系。

所以它会在这里做一些有趣的事情。

所以是的，我们想继续。

它正在安装软件包。

看到它的正前方是 Firebase 的官方角库。

他们在这里，我会告诉你如何使用这整个事情。

所以它正在安装软件包。

我们必须等待它。

现在它问了几个问题。

因此，我们希望在这里进行身份验证。

认证，所以选择。

我们不需要部署，好吧，这只是选择认证。

我们暂时不需要部署离开它。

我们需要 FireStore。

好的，我们需要在 FireStore 中进行身份验证，然后输入，它会自动为该身份验证和 Firebase FireStore 配置项目。

假人。

所以输入你的邮箱账号。

是的，这是我的电子邮件帐户。

这是一个类似的项目名称。

所以所有来自 Firebase 的项目都列在这里。

因此，让我在这里选择 Firebase 前端项目。

这个项目来自火店云。

所以是的，我们需要这个应用。

你想给你的新应用起什么名字？所以让它等一会儿？我们做了错事。

我们必须重新开始。

因为我们不需要新的应用程序。

我们这里已经有一个应用程序，我们可以使用我在很长一段时间内从远处犯的 sir 错误。

时间长了，我的意思是，所以我们不需要部署，这个认证和 FireStore，进入。

对，邮件就是这个。

先做项目名 Firebase 前端。

所以我们需要这个。

所以只要用箭头键导航，我们不需要创建一个新的应用程序。

如果您没有任何 app 或在 fire store 中，您可以创建它。

所以它正在下载配置数据。

一切都结束了。

如果我们检查我们的环境是什么？环境？在 SRC 环境中，文件夹在哪里？环境，OTs，我们将在这种恐惧中看到我们的存储环境，项目 ID，所有这些都来自这个项目概述。

让我展示给你看。

如果我们向下滚动到这里，你会看到，所有这些东西现在都自动出现在这里。

因此，这对于生产和开发 TypeScript 环境文件来说都是一种解脱。

那么现在，当我们重新启动我们的应用程序，所以我们可以只说 Engie 服务器，输入。

因此，让我转到 FireStore 数据库设置，让我们先转到身份验证，因为首先，我们将处理身份验证。

开始了，让我们等它开始。

这需要一些时间，可能一天或两天。

然后，我将返回到我们的项目现在已经运行，让我们在 localhost 4200 上打开它。

好的，在这里打开。

本地主机 4200。

让我们把它放在前面。

所以关闭 app 模块 Ts。

让我们检查腹部 STM AB 点组件点 HTML。

所以我们也不需要这个终端。

因此，关闭面板移除，以便我们可以移除或保留它，让我们移除此处的所有 HTML 数据，在 TS 中，您将看到我们正在获取输入，所有内容都将自动导入，如环境、获取 FireStore hit 以及我们之前选择的所有内容。

让我格式化它，然后打开 ab.component.ts。

这是这个 HTML 的组件，所以我们写 HTML 层。

这里我们写逻辑。

逻辑上，是的。

所以先拿数据还是好的。

但是让我们先处理身份验证。

所以我们需要两个输入字段。

因此，让我们把它作为占位符输入的输入，他们应该是电子邮件，并使其自动关闭。

我们在这里有一个电子邮件字段，然后我们有一个用于密码输入占位符密码的字段，他们有，我们有用于电子邮件和密码字段的输入和密码。

我们能做的就是从这一页复制之前的输入。

所以只需复制这个 div，然后像这样放置 HTML 文件。

因此，删除按钮，命令按钮，让我们只是从索引点 CSS 添加样式，以复制所有这些样式，并放置到这个应用程序点组件点 CSS。

保存和保存这个。

现在他们在这里，但他们不是没有中心。

所以我们能做的是，我们不，好吧，我们不，我们不能用身体。

所以复制这些东西，或者剪切并粘贴到好的，等等，让我们开始吧。

所以只需复制主类名并替换主体。

它会像这样在中间。

所以让我们给他们一点空间。

所以在这个输入中我们能做的，fat 是类名，我们这里没有类名。

因此，给它们一个输入字段的类名，复制输入类名字段并将其添加到这里。

所以让我们给他们之间留一点余地。

所以空白 10 个像素。

好吧，我们之间有一些差距。

好的，我们能做的是，我们不需要输入标签或者移除它们。

没有他们也很好。

现在让我给这个输入一个类名。

因为你可能很难看到这里的输入字段。

但如果不是，那也很好。

所以类输入字段。

所以不要混淆这个领域和这个领域。

这是一个字段，这是单复数字段。

在谷歌 rela 和复数中，我指的是单数和复数。

复制输入字段，在这里我们可以做一点设计，所以应该是 49，对不起，应该是 100。

让我们把它定为 100。

应该是 40 px 好的。

为什么这不起作用？好的，我们还需要一个点。

所以如果我们刷新好吧，太难看了。

所以把这个做成 20 个高度，300 个输入。

这些现在看起来不错。

我想现在我们需要一个按钮。

所以我们可以在这里做一个按钮。

按钮。

所以他们应该等一下，这是谁的动作，这是按钮吗，应该是让我们签名，再来一个。

报名吧。

看，我们有两个按钮。

也给他们上一堂课。

或者我们可以在这里做。

提交按钮，我们已经有，所以我们不需要使用它。

您可以移除这些按钮。

它将只是按钮和等待 30 px 30 将是小的，我认为不是 300 万卢比，使其成为 100 未读和更高，它应该是 2020 下午。

好吧。

此外，保证金已经位 10 像素，我们的注册按钮在登录按钮现在创建。

把高度设为 40 可以，这样看起来也不错。

还有一件事要做的是光标是这里的指针。

因此，我们有登录按钮和注册按钮。

现在我们需要从这两个字段获取数据。

让我们开始吧。

所以我已经将全部代码包装在一个表单标签中。

我使用这个登录表单作为已知函数 handler register 的 mg，只处理登录表单的值。

这里我们有两个领域的任何模型。

当我们点击注册或登录时，我们可以收集我们的值。

所以现在我们要做的是我们要做的是进口一些东西。

所以首先，导入，我们必须从 Firebase 导入什么，而不是 Firebase，我们必须使用 AngularFire，然后 auth。

从这里，让我们导入一些东西。

首先是 auth。

其次是创建用户的电子邮件和密码。

第三个是登录用电子邮件和密码登录，这三样东西。

让我来分解一下。

现在进口，像这样。

我们必须在这里创建一个构造函数，没有构造函数，我们必须在这里创建一个 auth 类型，auth 将是这样的 auth 类型。

所以远程控制台上的值在这里。

就在这里。

所以，我们要做的是，我们必须做这件事，这是所谓的创建用户与电子邮件和密码，然后我们必须通过这个奇数然后电子邮件。

所以电子邮件是我们的价值点。

第三件事是，值点密码密码，当您点密码时它将正确工作，这就是为什么我不能正确键入，但我们必须使用他们的这不是 Jared 教的，它不存在于应用程序组件中，使它成为公共的，现在它在这里是好的。

我们可以保密，但公开也可以。

现在我们可以签名了，它会返回日期，它会返回一些数据。

所以我们会抓住它。

所以我们只说回应。

这里的类型可以是任意的。

我们称之为控制台点日志或警报。

回应。

好吧好吧。

因此，我们必须在这里对其进行控制台操作，而不仅仅是在控制台点日志响应点用户上显示警报。

但是如果它失败了，我们会抓住它。

所以捕捉错误，我们会在控制台中显示。

总之，在警报中。

如此警报，它们应该是错误点消息。

那就省省吧。

让我们现在就报名吧。

在 Firebase 中，我们有一个电子邮件，这是尼山作为零日 gmail.com。

所以我们可以注册或登录。

让我们先注册，因为这里有创建用户，输入密码，在这里注册。

看到所有的电子邮件已经在使用中，我们在这里得到这个错误。

如果我们登录，我们将同样，因为两者是相同的。

因此，请删除登录按钮。

但是如果你用不同的邮箱注册，就可以了。

让我想想。

让我告诉你打开控制台，清除它，改变一点点的电子邮件和密码注册。

我们正在获取你看到的所有数据。

在这个版本中，它将在 FireStore Firebase 身份验证中更新(请参阅)。

现在让我们添加登录名。

复制这个表单或者把表单留在这里，注释掉这个句柄注册函数，再创建一个名为句柄日志的函数，就像这样。

所以它会接受这个值，我会把这个函数传递给未处理的寄存器。

所以编译失败，因为我们必须保存这个 VS 代码，我们将只使用第二个属性，称为用无效密码登录，所以它会采取同样的事情。

首先是认证，只需复制电子邮件和密码的认证值，我们就完成了。

让我们也复制这个然后捕捉块。

把它放在这个之前，在这个研究之后不是之前，允许这个。

现在让我们尝试登录。

错误是什么？好吧，我们做错了。

所以我想我们多用了一个 s。

花括号现在工作正常。

所以输入邮箱和密码。

所以艾米丽的尼山，测试一些密码。

报名吧。

好吧，让我们把这个按钮变成一个登录。

现在。

所以点击登录进入即将到来的密码。

如果我们打开控制台，它会工作，因为我们有正确的电子邮件密码。

看，它返回给我们这些数据，我们从 Firebase 获得的所有数据，如果我们输入了错误的电子邮件，它不会出现在身份验证中，它会在警报中得到一个错误，请参见 auth 或 user not found。

但是，如果我们输入了错误的密码，但正确的电子邮件，我们仍然会得到一个错误。

或者密码错误。

现在，我们的身份验证已经完成。

现在让我们看看在 FireStore 数据库中添加文档。

所以我们需要一些变化，使这成为添加数据。

添加数据，比如说函数名，这应该是添加数据并从 air 中删除所有这些东西，还有之前的登录方法。

所以我们不需要这个 public 或 author 实例，也不需要 fire off 的这个 import 的名称。

然后我们必须将这些数据添加到这个函数中。

因此，如果我们输入一些名称和密码，他们应该在控制台这里。

如果我们检查一下。

现在我们在这里导入一个东西。

所以进口。

它将来自角火和古拉之火。

重要的是让他们先记录下来。

首先要导入的是我们导入什么添加文档？其次是 Firebase。

好的，我们还必须使用角火斜线火基火基火库而不是火基，然后添加狗然后我们必须导入火基火来初始化我们的火库，然后集合关键字来指定集合。

因此，首先在这个跟踪中，然后在构造函数中，我们必须添加 fire store 的实例。

因此，我们可以说公共消防商店和 fire store 就像这样保存，所以你看，我们没有我们这里只有一个帐户一个文档，所以我们再添加一个。

所以补充一下，我们只是先使用集合，所以 cones DB 实例来选择我们的数据库，这样我们就可以在这里有多个集合。

因此，让我们选择要在其中添加数据的集合。

所以我们可以像这样添加集合，然后传递这个 fire store 实例。

因此，这个点 fire store FireStore 的第二件事是一个集合名称，所以它的用户和我们在这里完成，然后我们必须在 add doc 函数中传递这个表时态和我们必须添加的数据。

所以通过表实例和这个值，我们得到了奇异值。

如果成功，我们可以使用 then block，如果成功，我们会说很多数据，但如果失败，我们会说 dot catch block 一些错误，它作为参数，我们可以抛出一个警告，它会说错误 dot 消息，现在让我们试试这个，哦，天哪，这是 Barry，当阅读这个时，千万不要翻译这个网站。

因此，让我们添加一个电子邮件和一些密码，发送 123456 添加数据数据集，让我们在这里检查。

我们这里有另一个数据，显示了电子邮件和密码。

我们可以添加更多的字段，所以让我们添加更多的字段。

因此，将自我命名的输入名称占位符命名为 name，ID，as name。

我们这里不需要，但是没关系。

类型将是文本。

这很好。

我们现在有三块田地。

因此，如果我们发送它，我们将在文档中的第三个字段放在这里。

让我们试试这个。

第二，不，我们没有遗漏任何东西。

他让我们添加姓名、电子邮件、发送数据时的密码，这将是第三个新的输入字段，名为 C，我们在这里有所有这三样东西。

这就是我们如何使用 Angular 在 FireStore 中添加数据，现在让我们从 FireStore 中读取文档。

所以导入，得到 Doc 的功能。

让我们在构造函数中创建一个集合。

我们能做的是在这里再创建一个函数。

所以我们会像这样调用 get doc 或 get data，我们可以在这里创建一个集合，然后将集合传递给 get Doc 的函数。

所以集合是 DB 实例，要传递这个头发，它会返回一些数据。

所以我们称之为 block。

我们会将响应发送给我们，我们将只做控制台点日志响应点数据，而不是数据点文档点不文档，做 s 做 CS 文档的点映射，它将再次获取一个项目。

然后我们可以像这样返回带有商品 ID 的商品。

所以把这些想法变成 item.id 这里的问题是什么？它是不可调用的。

嗯，我们做错了什么。

好的，我们必须告诉你我们提高了返程罚款。

所以我们能做的是，我们只需在 J S 中打开 JavaScript 文件，然后复制这个数据块并替换它，它将是相同的。

但是我们在这里得到的数据是什么，我们可以只说 item dot data point。

如果我们打开控制台，但我们必须调用这里的函数，在构造函数中，我们将使用这个关键字进行引用。

如果我们说好，让我们检查控制台。

刷新，我们正在获取您在这里看到的三个区域的密码，密码，电子邮件和名称，如果它有一个名称。

所以现在我们可以把它存储在一个变量里。

所以我们在这里做一个变量。

所以 public 公共数据，它会是一个数组。

所以让系列中的任何一点都像这波一样实现。

如果我们只是把这些数据存储在这里，它就会工作。

因此，我们可以只做数据，或者这个点数据等于 spread 运算符中的这个响应，将它扩展到这个数据中。

它被存储在数据变量中。

所以我们必须公开或复制公共数据。

在这里，让我们在表单后添加，我们可以在这里输入所有的东西，然后在盟友列表标记中做一个邪恶的标记，让我们循环通过数组，这样我们就可以在 g4 和 g4 中做，然后它需要一个项目选择，比如让项目或这个数据数组，然后我们可以只做项目预订，做花括号， 我认为我们可以只做项目点名称保存我们正在获取名称，因为我们在这里只有一个名称，所以如果我们将它作为电子邮件，或者让我们像这样添加，那么首先将是电子邮件，第三将是密码保存，我们将获得我们的电子邮件密码，如果我们有一个名称，我们也将获得它。

我们说的是所有这些。让我放大它，您还会看到电子邮件、密码和各个列表的名称。

所以读文档也完成了。

现在让我们更新数据。

所以我们需要一个按钮。

首先添加一个按钮。

这将被称为更新，保存它。

让我们在这里创建或导入两个东西，它们将被称为 dock 和 update dock，dock 将用于指定要更新哪个文档。

但是更新文档将用于更新我们使用该文档选择的文档。

所以创建一个名为 update data 的函数。

好了，不是 const 文件，就是这样，升级数据，只是转储数据，它会采取我们的想法字符串，类型字符串，好，然后我们必须添加按钮或功能到这个按钮上点击不是点击，只是点击，只是使更新数据功能和 ID 将被传递到这里。

所以 item.id 我们必须在这里接收它，我们保存它来保存文件。

如果我们控制了 ID，我们应该了解我们正在处理的领域，打开控制台。

让我们尝试对第一个字段进行点击更新。

它来了，这个和这个。

这是我们的身份证。

我们不会使用动态导入，我的意思是，我们不会单击按钮，也不会在四个按钮中填充相关内容，我们只会使用我们需要的静态数据静态更新它。

因此，我们在这里可以做的是，我们可以指定要更新哪个文档。

所以使 const 数据更新等于 2，我们将在这里使用 doc 关键字，它将带三个参数。

他们两个被称为这个火灾存储实例。

第三个是 ID，选择要更新的文档，然后我们将使用 update dot 函数。

我们将在这里传递两个东西要更新的数据和我们想要更新的数据。

所以我们把名字加在这里。

所以还是加上 Ashish 这个名字吧。

好吧，省省吧。

如果成功了，我们就把那块扔在这里。

我们可以更新警报数据。

如果它失败了，我们将捕获它，并说错误警报不能或只是在这里抛出错误点消息。

好吧，保存。

现在让我们更新任何字段。

所以这里我们没有 Ashish 这个名字。

因此，如果我们单击此处的更新，它将被添加。

我们可以做的另一件事是，我们可以在更新后调用这个获取数据函数，因为它会自动调整页面并获取所有数据，所以 make this 表示这些数据是调用此函数的好数据。

所以现在让我们先清理数据。

在这里，点击它更新，看到艺术家的名字在这里弹出。

如果我们将其更改为其他内容，比如说在那里，然后单击另一个，比如说最后一个，它们应该在这里，第三个字段的名称已更新，它在这里。

如果我们单击上一个，它已经有一个名称 Nishant，它将在这里，这是我们的名字，而不是 Nishan，所以他们可以像这样改变它。

好的，第四个我们会删除。

因此，我们还需要一个按钮，让我们添加它，这将是简单的，以及删除函数名称将删除数据。

保存文件，在这里创建一个函数，删除数据。

然后，我们必须添加在文档更新之后开始的集合。

所以把这个当做北约删除的文件简单点。

我们必须导入删除码头，所以删除码头，所以刚刚在这里添加。

所以我们可以删除 dock 并传递这个简单的。

其余的将在课堂上完成。

平常的事情。

让我们也了解一下。

字符串的概念，因为我们也需要 ID 并将其添加到参数中。

该事件将在这里看到一个删除功能按钮。

如果我单击此处的删除，首先将其作为数据删除。

如果我们在这里删除，他们应该被删除，删除，它会消失。

如果我们点击删除这一个，好的，让我只是改变了一些事情，使这次更新在这里。

但首先，让我把它改成尼山。

我们也可以改变其他领域。

因此，如果我们在这里更改电子邮件，在更新，它将被更新。

所以让我们说尼山 123 在 gmail.com 的比率。

所以在这个视频中，我们建立了两个领域。

因此，如果我们改变第一个，名称和电子邮件，它会在这里改变，点击更新，更新，电子邮件现在是德肖恩，123 在利率 gmail.com。

和密码是一样的密码，但是名字被改变了。

所以让我们删除这个 Deshawn 字段。

现在也不是了。

我们只有一个领域。

而且是动态更新的。

现在，所有四个操作都完成了，这就是所谓的 CRUD 操作，以及 create as far 和 sign up，并使用电子邮件帐户和密码登录。

这就是 Angular 的很多内容，以及如何在 Angular 中处理 Firebase 认证和清除操作。

现在让我们在 React 中处理 Firebase 认证和 CRUD 操作。

因此，在创建应用程序的 React 文件夹中，我们只需键入 MPX。

等等，让我放大终端 MPX 创建反应应用程序和应用程序名称。

所以让它燃烧起来。

让我看看有棱角的，嗯，有棱角的火基。

所以让我们让它起反应。

名字都是小写字母。

这一点你应该记住。

所以我们等它结束吧。

它正在 react 的文件夹中创建一个新的应用程序，我们的 React 应用程序现在已经创建好了，它从这里开始。

所以我们还需要做一件事。

因此，只需打开一个终端，只是 npm 安装 Firebase，我们必须安装这个软件包，所以安装它，并等待它完成。

然后，我们必须在这里创建输入字段，让我们创建它们。

因此，从那里删除 sluggers 小部件，以创建两个输入字段，所以我们只说输入和占位符，让我把这作为一个电子邮件和电子邮件的名称以及电子邮件的类型。对于这种类型的电子邮件，电子邮件重复一次密码。

经理是密码名称作为密码，这个也是密码类型大卫，我们可以看到两个输入名称和通过电子邮件和密码。

因此，删除类名 app，并将其替换为 app header save，让我们删除不必要的类名，例如此 app 徽标媒体查询和 app 链接，以及底部的关键帧删除此处的背景颜色好的，现在给此输入两个类名或一个姓氏，我将它们都包含在内。

让我把它格式化。

所以这应该是我们应该称之为输入字段的输入类名称，所以我们应该给输入字段 10 个像素的空白。

所以我们有一些余量。

让我们在底部的按钮注册按钮提交表单，使之成为光滑的每次注册，因为我们将首先使用注册方法。

所以在 Ambrogio 中我们需要两个函数。

因此，为它们创建 const 和 input 输入，这样我们就可以同时处理这两个输入，再提交一个函数来处理这个函数的 submit equals。

所以句柄输入，我们将把它放在这个输入字段上。

所以在改变的时候，需要一个参数。

我们将使用 params 中的参数和 props 将事件发送到这个函数，而不是这里的 props 和 same。

因此，在“处理提交”按钮中，我们将它映射到单击“处理提交”的按钮中。

因此，让我们在这里创建一个状态首先从 react 导入使用状态从 react 导入使用状态，我们将只创建一个状态让我们创建一个 const data 状态，data 等于 US 状态它将是一个对象，它包含两个属性。

因此，首先将是电子邮件，它最初将是空的，然后密码最初也是实体，然后我们将不得不在这里得到输入。

比如说 event，我们将把它保存在这里，然后创建一个新的 lead 变量。

让输入等于 2。

因此，我们将这样做，在输入中，我们将只做事件点目标点名称。

如果名称匹配，我们将在名称中存储值。

因此，如果是来自这个电子邮件名称的电子邮件，如果是来自这个名称的密码，如果它们匹配，我们就将它们存储在各自的状态对象状态中。

所以电子邮件电子邮件密码密码。

所以这里我们只说事件点目标点值。

在这个输入输出中，我们设置它的输入，然后我们只需存储这些数据并设置数据。

设置数据。

我们可以用三个点来表示输入。

我们还必须存储以前的数据。

但让我告诉你我的意思，如果我们安慰这个。

所以控制台点日志，这个数据。

如果我们在这里输入电子邮件和密码，它将不会完美地工作。

让我演示给你看打开控制台。

内部邮箱和密码，注册。

看，我们只得到密码，而不是电子邮件。

这就是为什么，因为我们只设置了输入字段。

假设第一个冬天是什么时候首先是电子邮件，然后是密码。

所以这个密码覆盖了邮件。

这就是为什么我们只说密码。

因此，要解决这个问题，我们必须传递数据以及这个数据，这样它将包含当前数据和以前的数据。

所以保存，现在让我们检查，刷新和刚刚输入的电子邮件和密码。

报名吧。

好吧，我们认为这是空的。

我想我必须在输入之前通过它。

所以首先告诉你数据，然后输入。

现在让我们检查一下。

电子邮件和密码。

是的，现在我们看到我们正在阅读电子邮件和密码。

现在我们可以在 FireStore 认证中使用它。

因此，让我们添加要导入的凭据身份验证身份验证。

让我们导入这两样东西，哦，是的，我们需要应用程序和数据库。

首先，让我们创建它们。

因此，在源文件夹中，创建一个名为 Firebase can fake . js 的文件，在该文件中，我们只需导入该项目的 FireStore 配置。

所以把这些东西复制到这里，添加到这里。

所以我们必须导出联系人。

我们还需要 FireStore 数据库里的一样东西。

净火商店，这是所谓的，所以从火基地斜线火商店获得火商店。

所以我认为进口名称是正确的。

获得火店好吧，火剑是单线。

我是说一个单词。

所以我们也必须初始化买家线程。

所以导出，导出常量让我们把它做好，只是数据库等于被解雇存储在这通过应用程序凭证在这里，这个应用程序，现在它会工作。

现在让我们导入这些工具，我在 Ember js 主文件中打开数据库，导入应用程序，并从 one step back Firebase 配置文件中导入数据库。

好吧。

所以我们到此为止。

但是，我们必须获得真实的 IHS 信息，才能打开认证步骤。

首先，我们必须发送电子邮件，让我们从 React 应用程序中再添加一个。

因此，让我们把它作为导入 MPD，让我们把第一个 Firebase 和第二个 auth 都加入进来。

这个和这个花括号里的，我们必须输入通行证。

第二个是使用电子邮件和密码登录，并使用电子邮件密码创建用户以创建帐户，在此创建帐户。

所以我们需要这三个，我们需要为 auth 创建一个实例。

所以 const 还是等于下车。

在提交函数 handle submit 中，我们只需调用这个名为 Create user with email and password 的函数，我们将传递这个 author，而不是 author 实例，然后是电子邮件密码。

因此，我们可以只传递状态名称数据点电子邮件，逗号数据，点密码，所以它需要三个参数，你已经从我以前的视频中知道了，对于其他框架，它需要数据电子邮件和数据密码，Emily million 密码，然后我们必须执行 then 块才能成功。

消息如此回应。

我们将只说控制台点日志响应，如果它是对数据或用户数据的正常响应。

如果失败了，我们可以说是捕捉错误。

它们应该是警告错误点消息。

因此，如果我们保存，现在让我们尝试添加一些输入。

如果我们注册，我们会得到一个错误，因为我们已经在认证中存在的符号，我们不能创建一个重复的李，注册。

你看，我们已经在使用这封邮件了。

但是如果我们用错了或者不同的邮箱，比如说 4403。

然后点击注册。

我们在这里读取所有这些数据将是成功的。

还有这里。

如果我们刷新“authentications”选项卡，它将在用户中显示。

就这么定了。

现在，让我们添加“登录”按钮，以添加此声明或使注册成为登录。

在这里，只需将使用电子邮件密码创建用户的功能替换为使用电子邮件密码登录。

所以补充到这里。

如果我们不这样做，它应该可以工作，我们会得到相同的数据。

我们之所以获得这些数据，是因为我们现在没有创建我们要登录的帐户。

如果我们使用了不在这里的用户标签中的错误的电子邮件，我们将得到错误。

所以试试看地球用户没找到。

但是，如果我们使用了错误的密码，我们将得到密码错误。

因此，请使用商店中不存在的其他内容。此身份验证登录密码错误。

这就是我们如何处理注册或登录方法。

在此之前，要向您展示的一件事是，我们可以将这个令牌存储在我们的社交存储中。

我们可以用它来认证。

有杀螨剂也有其他方法，你也可以使用它们。

所以这些都做好了。

现在让我们看看如何向 Firebase FireStore 发送数据。

所以我们这里也需要一些东西。

所以首先将 collection 创建为一个集合或者获取这个集合并添加 dock。

首先，让我从 Firebase slash fire store 导入这个，我们导入集合，将文档添加到集合将用于指定集合，添加锁将在集合中添加数据。

让我删除它，因为我们将从这里发送数据。

所以我们可以把这个按钮关掉。

使用添加数据登录，我们需要在这里添加一个名为名称的字段。

所以我在这里补充一下。

要将占位符作为名称，名称作为名称，键入作为文本。

好吧，我们结束了。

这里还有一个名为 name 的对象属性。

就这样了。

现在我们也可以在这里写上我们的名字。

所以我们能做的就是在句柄提交函数中，我们必须使用。

首先，让我们创建一个集合。

因此，如果您打开 FireStore 数据库，我们将在这里看到我们的集合名称。

所以叫用户。

所以我们需要添加集合名称。

所以我们可以这样做 const 比方说数据库，编辑用户添加哪个数据库？然后我们必须通过这个数据库是什么？这个数据库在哪里？好吧，等等，我们是数据库然后是用户。

现在我们有了它们，它需要两个参数，我们将使用 ok。

让我们创建一个 DB 实例。

实例，因为我们现在必须等待。

所以我们将使用和 Doc，然后我们将传递数据库实例和数据。

所以我们的数据就是这个数据。

所以干脆这样传。

一切都结束了。

所以如果成功了，我们就调用这个块。

这里我们将只显示一个警告，说明数据已发送，否则如果失败，我们将捕获 read any catch 块，这将向我们显示一个错误。

所以提醒，错误点信息。

保存，现在让我们尝试输入名称，电子邮件和一些密码。

添加数据数据发送，如果我们在这里检查，它应该在这里，如果我们刷新它没问题。

好吧，等一下。

在 Firebase 配置中等待一秒钟？是的，但是我们在这里没有得到数据。

不过。

所以我们正在发送它。

它在哪里？它在哪里？我们向哪个数据库实例发送数据？等等，广告狗？是添加文档吗？是的，它是一只狗。

现在让我们再试一次。

让我们使它成为 Kumar 的任务，然后添加数据，是的，它现在保存在这里。

结束相同的电子邮件密码是 Kumar 的使命。

好了，我们现在可以知道如何添加服装了。

接下来是从 fire store 获取数据。

因此，我们将创建和使用有效的第一个函数，但获取数据的缺点等于这个误差函数。

在这里，它将照亮我们获取数据的逻辑。

所以我们还需要一个叫做养狗的东西。

我们将使用这个 get dogs 并在这里传递实例。

因此，该实例称为 DB 实例，它将指定要传递的文档、文档名称和数据库，然后只需创建一个使用效果挂钩，或者您也可以这样做，只需在按钮上暂停该函数，如果我们单击“添加数据”按钮，或者让我们说“获取数据”，该函数将运行，它将获取我们的数据，但我们必须使它成为一个异步函数。

理论上，我们必须等待，我们将在一个变量中存储数据，所以假设 const data 等于 await got await get Doc 的 DB 实例。

我们可以像这样控制这些数据，但是我们必须把它映射出来，我的意思是，我会告诉你为什么。

打开控制台，单击获取数据。

我们正在获取所有我们无法读取的数据。

让我把它过滤掉。

所以在顶部我们有码头，我们将绘制它。

它将接受一个项目参数，然后他们将简单地返回点点点项目点数据，然后项目的 ID，所以它将是项目点好了，现在让我们检查，获取数据。

所以我们从这里得到三个数组，或者三个三个的对象，一个数组中的三个对象。

这就是我们现在回归的 Firebase FireStore。

所以接下来是更新数据。

让我也用一下这个函数。

或者，或者，或者好吧，好吧。

好了，现在让我们创建一个新函数，日期数据的缺点。

让我算算，就停一下。

好的，让我想想，让我先做这个。

让我们回到 add 函数，在这里添加，而不是获取数据。

看我在做什么，我要更新的数据是一个箭头函数。

所以我们有提交句柄，它添加数据，所以让它添加数据。

我现在不运行它，但是它很好。

为了更新数据，我们将使用一个名为 doc 的函数，让我们打印这个函数。

这是一个函数。

因此，我们将指定要更新哪个文档。

所以让我们说，我们将使用一个新的变量收集数据来更新。

所以它需要 doc，这里我们要传递三个东西。

三件事。

所以数据库，集合名称。

对于文档，它是什么？起始数据、数据库、集合名称和 ID。

因此，我们必须指定要编辑哪个文档，是这个文档，还是第二个或第三个文档。

所以我们将从这个字段中获取 ID。

让我在底部创建一个新按钮。

但是，但是问题是，好吧，我们能做的是，让我们先绘制这些数据。

这也很简单。

所以我们必须创建一个状态，比如说，数组，然后设置数组。

使用状态和数组，空数组，然后在这里设置设置纪元就可以了。

关于 Nury 数据，我们不需要去安慰它，然后我们将只是映射编译的问题，它没有被很好地定义，我们将稍后解决它。

所以数组在哪里，我们要把它映射出来。

让我们把它画在这里。

要数组点图，它将采取和项目参数任何你想写的东西，你可以写在这里，你会返回一些东西。

所以让我们先还是昏昏欲睡，还是黯然为父，然后三 PII 税。

所以我将只说项目点名称，电子邮件密码，所以它会看起来通过数组，就像我们在角版本。

所以现在它将是一封电子邮件。

这里我们将输入密码。

所以在 angular 中，我们已经使用过了，但是在这里我们使用这个数组映射来循环遍历数组。

所以在按钮里，我们也需要一个按钮。

因此，制作一个按钮，将数据发送到该函数。

所以点击按钮，如果你点击按钮，它就会发送数据。

因此，我们可以说，函数名称是什么，更新数据，更新数据，我们只需设置 id，因此更新字段中的 item.id 更新函数在这里接收 ID，它将正常工作。

所以它现在在这里。

因此，我们添加了要更新的视频。

让我们在这里添加我们的名字，如果我们刷新，按钮也会更新，好的，我们在这里没有得到这个。

为什么？红点图？它在这里。

好的，让我们删除这些输入字段，看看或者只是评论一下，好的，我们可以有好的数据，但不是这个更新？Y.

我们正在映射数组，并将它返回到这里。

但是我们看不到这个。

这就是为什么好吧，好吧，好吧，应用程序标题。

如果我们选择应用程序标头，如果我们删除所有这些内容或仅删除应用程序数据类，我们看不到阵列映射的原因是什么？所以埃里克地图项目，它返回一些深度和一个按钮在里面。

好吗？复制这个或者剪切这个，然后把所有的东西都去掉。

但是更深入的，我们需要关闭它，然后好的，如果我们只是打开控制台，让我们看看我们得到了什么。

刷新。

嗯。

错误不在这里。

我们把它放在这里。

好吧，问题是我们不能坐在这里。

好吧，这是一个搅拌机的错误，我把所有的东西都还原了。

因此，让我们创建一个使用效果挂钩或使用效果。

因此，我们正在设置数据，但当应用程序加载时，我们不会加载数据。

因此，我们将在这里使用 use effect 挂钩，我们将传递一个空数组，使其成为一个空的依赖数组，我们将在这里获取数据，调用内部的 get data 函数，它现在将工作，您可以看到我们在这里获取了所有这三个字段，即电子邮件和密码。

但是，但是如果我们刷新，你就看不到我们的字段了。

如果是的话，我们就返回这个项目数组的点阵图数据。

好吧好吧。

好吧。

为什么我们看不到我们的领域，我不能说出这个类的好框架。

好的，现在我们看到了好的，我猜是因为字体的颜色是白色的，去掉颜色。

现在我们可以看到这个，我们可以看到我们的名字，我们的电子邮件和密码。

所以让我先把这些东西拿掉。

因为我们现在不需要他们。

因此，如果我们单击此字段上的“update ”,此 ID 将被传递给此处的此函数，它将进行更新，因此，我们来创建或导入一些内容。

所以我们需要从 FireStore 导入更新 dock。

所以我们还需要一个来删除文档。

所以删除 dock 应该在这里。

所以放下死码头，我们将只做更新数据更新，非数据更新码头。

而且我们会传递数据来更新和里面的数据。

所以我们就这样更新吧。

因此，我们将更改名称，因为名字不应该是 chan Kumar，而应该是 Nishant。让我们将电子邮件添加为其他名称。

好的，如果我们复制并修改第 123 行的一些小细节。

好吧，我们只更新这个。

如果成功，我们将显示一条名为“警报数据已更新”的消息。

但是如果它失败了，它会抓住它，说它会出错。

然后我们还会说控制台点日志错误或警报。

因此，如果我们现在单击这里的“更新”字段，那么这个 ID 将传递给这个函数。

这是什么性质的？

等等，还有最后一件事，给定水平线的图表区域。

所以它现在和这条线分开了。

这是一个领域。

这是一个字段，也是最后一个字段，单击此处更新。

该 ID 将被传递给研磨数据功能。

并将它传递给这个函数 late 变量称为 late data 来更新。

它首先使用 ID 参数指定哪个文档目标。

它只是用这个前导变量和数据来更新数据。

我想你知道我的意思吧？如果成功，我们只需调用 get data 来获取更新的数据。

如果我们单击第一个字段，它们应该是 Nishan 而不是 Nishant Kumar，九将更改为这个字段。

所以我们在这里升级一下。

更新后看到现在是尼山，图像也被改变了。

如果我们点击或者如果我们把这个作为我们的 sheesh 和 email 是 ash in，等等，等等。

所以如果你点击这个更新，他们应该会变成 Ashish。

而且镜像应该是这个更新改的，还可以获取密码。

因此，如果我将密码设置为 abc、def 和 update 的字符串，则该名称将与电子邮件的名称相同，并且密码只会单独更改 update，而密码没有任何更改。

所以我们也可以通过那里。

如果我们更新，这三个字段都将改变。

看到他们正在被改变。

这就是我们上传数据到 FireStore 的方式。

第四个叫做删除数据。

所以它只会说 const，delete data。

而在这里，虽然它会把 ID 和删除数据更新或使其数据删除。

所以它需要三个参数，就像更新文档一样。

所以这里非常重要的删除文档，我们将不得不绕过这个弯路，在删除文档函数中删除，没有别的。

然后是这个 then in catch 块，以防成功的领导者把它的数据删除。

我们必须把这个函数传入一个 ID，按钮必须在这里做一个按钮。

这将被删除。

点击按钮。

我们只说删除数据并传递 item.id。

保存我们的它没有定义。

因此，让我们将它保存在删除数据函数 ID 中。

所以我们现在有一个删除按钮来删除。

他们应该被删除，删除。

发现它不在这里。

如果我们检查一下火场。

它会被实时删除。

如果我们删除第二个，Aashish 删除了，它不在这里，也不在这里。

所以我们添加了 CRUD 操作的所有四个方法，添加、删除、获取数据和更新数据创建、读取创建、读取、更新和删除。

这就是我们的操作方式，我们在 React 中执行 CRUD 操作，并使用 SAML 电子邮件、电子邮件和密码注册进行认证。

你也可以增加一种叫做 Google 认证或 GitHub 认证的注册方法。

这很容易。

好吧。

现在让我注释掉这些代码中的命令，然后保存它。

我们可以在这里添加数据，它会显示在底部。

因此，如果我们输入姓名，陈库马尔，一些电子邮件和一些密码，如果我们好的，添加数据。

让我在这里制作一个添加数据的按钮。

它在哪里，函数名是添加数据。

如果我们现在单击“添加数据”，就会出现在这里。

资料组

好的，我们必须首先在 ad 锁中使用这里的读取数据。

这是获取数据的函数，如果我们再添加一些 Nishant 123，所有的事情都会一样。

然后就到了这里。

那么让我从这八个开始给你看，删除，添加一些数据添加数据，然后。

就在这里。

让我们把它做得像鱼一样。

这个应该是 Ashish，或者是 roid rage gmail.com，或者只是让它变成 4 到 0 个更短的数据，然后它就在这里了。

这就是我们如何添加数据，我们可以更新它们。

它正在更新到我们在这里添加的数据。

您可以通过向该按钮和函数传递数据来使其动态化。

对不起，按钮，对不起，传递数据到这个输入字段。

所以我不会那么做。

这就是我们在 Firebase 中所做的一切，并为 Firebase 做出反应。

让我们学习如何使用 Firebase 认证我们的下一个应用程序。

我们将使用谷歌和 GitHub 认证加 mln 注册，我们也将看到如何发送数据或保存数据。

在下一步中，它也被称为 CRUD 操作。

那我们开始吧。

首先，我们必须创建下一个应用程序。

所以我们可以使用这个命令和 bX 创建下一个应用程序的最新版本。

这只是将命令粘贴到这里，让我们添加应用程序的名称。

所以应该是下一个，克鲁德。

接下来，肮脏和古怪。

好的，这个看起来不错，然后加上。

所以它从我们的手机里下载了图书馆里所有的软件包。

然后我们要做的就是在 Firebase 里创建一个 app。

所以让我们来创造它。

让我们说下一个 auth crud 行，继续。

我们不需要分析，只是在这里创建一个项目。

所以我们的项目正在创建中。

这里也在 React 应用程序中等待一秒或等待一分钟，10 继续。

我们的 Firebase 项目就是在这里创建的。

现在我们要做的是创建一个应用程序。

因此，要创建应用程序，只需在这里键入 grad，OT，只需键入 driver dot find。

和近青娜文件夹。

所以我们进入那个项目文件夹，叫 Next，OT 或者 net next 哭着没找到。

这里我们有页面，我们有主页的索引，我们有包含所有依赖项的节点模块。

我们有 package dot JSON，它包含了所有的包列表，yarn log，我不知道这是什么好忽略忽略节点模块和所有的文件。

现在我们要做的就是在这里安装 Firebase。

所以打开终端，只需输入 npm I Firebase。

所以我们需要这个命令来让 Firebase 工作。

同时，我们也在这里创建一个文件。

所以只需创建 Firebase 配置。

点 j s。

这里将包含所有这些配置数据。

所以把这些东西复制粘贴到这里就行了。

还记得评论吗？好吧，我们把评论减到最少。

所以我们已经在这里导入了应用程序。

所以我们需要从文件夹中导出这个，比如说导出 const app。

现在我们可以在我们的项目中使用这个应用程序。

好的，现在我们要做的是在这个主页上，它在哪里。

但在此之前，让我们创建一个文件夹，这就是所谓的布局。

布局，布局里面创建一个名为 index dot j s 的文件。

所以这将包含我们所有页面的布局。

所以我们将创建两个页面。

所以一个是注册用的。

一个是主页。

所以我们现在不需要 next。

那么我们能做什么呢，让我们创建两个页面。

所以我们已经有了索引，它将作为主页。

所以还是先改改吧。

所以我们不需要这些代码。

所以在这个头上去掉它们，尽管我们需要这个头，去掉主标签里面的所有东西。

所以所以把它拿掉。

然后保存。

让我们在这里改变标题。

这应该是下一个。

读取所有代表创建、读取、更新和删除的 crud。

打印到控制台。

现在让我们定义我们的认证方法。

单击此处开始。

我们将使用电子邮件密码。

所以启用这个。

我们必须使用网络好吧，我们不需要这个密码。

我们现在不需要这个。

所以省省吧。

另外，让我们再添加两个提供者。

所以谷歌使能。

保存。

那我们也加上 GitHub 吧。

我们将使用 GitHub 和谷歌的签名，所以下一个应该是 GitHub。

这一个在哪里？

但是为此，我们需要一个客户端 ID 和客户端密码。

所以去 github.com 吧。

然后转到设置。

在这里，我们将有选项开发者设置，打开认证应用程序。

让我们创建一个新的登录或 LDAP。

所以让我们把它变成毕业证书。

主页 URL，让我们把它设为 localhost 或这里的这个 URL。

所以首先，让我们启动应用程序。

所以我们必须使用 npm 来启动应用程序。

运行 Dev。

第一次学习 localhost 3000。

所以让我们复制这个网址，并把它放在主页的网址里。

然后我们需要回调，我们应该从 Firebase 获得回调。

所以复制这个认证 URL，并粘贴它，然后注册应用程序。

所以它作为客户端 ID 返回，我们需要在这里使用它。

和客户秘密。

所以我们需要重新生成一个新的客户秘密。

让我输入密码。

好吧，这是不对的。

我总是忘记我的密码。

好吧，还是不对。

让我们试试这个。

好吧，这不好。

所以我们需要这个客户的秘密。

因此，只需使用复制符号或图标复制并粘贴它，然后保存它。

所以我们启用了电子邮件和密码，谷歌和 GitHub。

所以所以我们现在能做什么。

所以我们现在在本地主机 3000 中打开。

因此，让我们创建布局页面。

在此之前，我们先给这个家起个标题 h1，叫 home，它会出现在这里看到它出现在这里。

现在，让我们做我们现在应该做的，创建多一页，它应该是注册点 j s。

让我们把它变小。

好吧。

还有一个，我们需要登录点 j。

所以我们有注册组件和登录组件。

在索引中，我们在这里很好。

所以我要做的是，在这里为路线创建一个布局。

首先要做的是导入 import，比方说从下一个路由器导入路由器。

所以我们不必使用 react 路由器，因为它不是 React，而是 next。

它是路由器自带的。

之前预定义的路由器，我是说现在我们必须在这里创建一个函数来调用 export const，比如说 layout。

所以我们要导出这个常量布局，我们可以使用它，它会接受一些提示。

然后在一个 div 中，我们可以只说路由器。

好吧，我们不是路由器，我们运行路由器，我们需要链接抱歉，链接。

等待下一个链接的第二个导入链接。

然后，我们必须包装我们的路线和链接。

所以链接网格要等一下，让我检查一下链接的命令部分。

所以当新部署下一个链接时，点击这个。

好的，我们必须使用 h ref。

下一个链接参考

让我们先输入主页的链接。

如果你检查我们的应用程序，或者如果我们去注册，它应该只是默认添加访问者注册的斜线，所以让我们在这里添加一个组件。

导出当前导出非常量默认函数寄存器，然后我们返回一个东西，先在里面返回一个 div，然后添加一个 h1，应该是保存它，我们现在转到注册页面，不是这个，好的，我们现在转到注册页面。

我们可以看到，我以前在这里发短信，现在它在反射。

让我们为登录做同样的事情。

只是让这个说登录，这个士兵也登录。

让他们从名为 Home Node js 的索引中提取样式。

我做的这个索引首先导入样式并添加到这里。

我们会把它放在中间，这是所有的文本，然后我们能做的只是在这里添加主要内容。

所以在这个 div 中，只需将这个 Dave 复制到页脚并粘贴到这里。

把这个登记一下。

再复制一次，粘贴到这里。

等一下。

这里没有定义。

所以我想我们也要进口人头。

注册页面中的页眉和图像。

不，它工作得很好。

他们在登录时做同样的事情，只是把文本改为 login。

它在哪里？这个和默认的函数名。

现在，如果我们转到登录，我们必须更改添加登录的输入。

但是我们必须添加登录链接。

让它登录。

让我们在这里添加登录。

这里有登录文本，在这里输入文本。

首页再加一个。

让我们说回家吧。

如果我们回到家，那将是家的文本和中心。

好吧，这个没找到，我猜是为什么？如果我们开门回家，等一下。

这不是家，这是索引。

所以我们把它改成 home.js 我们必须改变输入。

好吧，这是自动改变是。

我们现在有了这个布局。

我们传递这个，所以我们也需要返回数据。

所以等一下。

好的，一切正常。

好了，现在我们运行，我们在主页上有这个索引，或者这个 Firebase 配置文件。

所以还是进口吧，就说进口吧。

所以应该说是从烽火国来的。

因此，我们必须从 Firebase config 导入应用程序，即船应用程序。

在主页中，运行良好，然后我们可以在这里看到我们的页脚，由虚拟驱动。

现在让我们先创建我们的地区页面，然后我们将移动到主页。

所以关闭所有这些东西。

因此，在这个名为 register.js 的页面中，让我们添加样式和数据。

所以让我们删除页脚，我们不需要它。

保存，我们去起始页，注册。

保存，就在这里了。

因此，我们现在要做的是，我们不需要在这里导入图像。

因此，让我们导入 Firebase 配置文件。

因此，从 Firebase 配置文件导入应用程序，自动导入，然后我们必须创建文本输入到文本输入只是创建它们。

所以我们再说一遍输入和输出。

所以我们要做的是给这个输入一个类输入一个占位符，它们应该是 email。

第二个应该是密码。

传递密码。

他们有，我们有邮箱和密码。

所以让我们给它们设计一点风格。

所以把这个作为 input，input，input box，input box 的类名就好了。

这里也是第二个输入，分解一下。

像这样。

然后保存大卫的知识，让它有点风格。

所以在这个名为 styles dot home dot model dot css 的文件中，我们只需要在这里添加。

输入框，我们可以说，宽度是 200 像素。

但是我们运行 work 是因为使用样式表示我们，状态样式点输入，它是什么类输入框？保存好的，问题是我们必须在这个骆驼案例中使用它。

那就行了。

所以就说输入框吧。

现在它变宽了。

重复重要的第二次输入密码，保存，它们现在都很宽。

现在让我们添加一些高度，使其为 30 像素。

好吧。

让我们也给他们一点空间。

所以就说，边距，五个像素。

现在我们有了电子邮件和密码。

好的，这个看起来更好，我们可以输入电子邮件和密码。

所以现在我们要做的是创建一个状态。

所以让我们创建两个状态。

首先是常量，比如说电子邮件。

和密码。

设置将使用状态空字符串，然后 const。

密码，密码，设置密码，然后我们必须在这里创建一个新的状态钩子。

空字符串。

现在让我们来读一读。

因此，在电子邮件中，我们必须使用“关于变更的变更事件”。

事件需要事件。

我们可以设置电子邮件事件点目标点值。

蝎子把同样的东西放在这里。

但这次在这里，他们应该设置了密码。

现在，如果我们在这里输入密码，我们甚至可以让设置值。

所以价值是电子邮件的权衡。

所以电子邮件，这一个应该设置密码。

等等，等等，等等。

所以值，我们应该是密码。

完成后保存。

现在我们可以正确输入我们的电子邮件密码了。

现在让我们在这里创建一个函数。

应该注册的常量，然后我们必须从 Firebase 导入功能。

所以应该先从防火墙导入，然后再从防火墙导入。

所以我们将不得不导入两个东西，即调用或获取 auth。

与第二个合作或签约。

注册。

用邮箱注册的方法是什么？这是它最快的注册。

爸爸，让我看看用电子邮件和密码登录的方法。

这不是收银机。

这不是报名。

说让某些 Google 注册 Firebase 注册方法或者 Firebase Firebase 认证很奇怪。

让我们继续，我们必须使用 web 好的，用电子邮件和密码创建用户，我现在明白了，我们将使用这两个，你看到了，在这里 auth，用电子邮件和密码创建用户，然后让我们创建一个 auth 实例。

所以只需说成本或等于下车保存它，只是复制获得创建用户的电子邮件和密码，然后你只需通过认证，然后电子邮件，电子邮件，然后密码。

所以现在，如果我们已经登录，如果我们注册成功，我们可以直接转到主页。

因此，我们需要创建一个路由器实例，因此只需从下一个路由器导入使用路由器使用路由器，然后创建一个路由器实例，以说明 const router 它可以很好地获取路由器路由器，但在其中使用路由器使用路由器，然后我们只需说明路由器点推至主页。

因此，如果我们在布局页面的“它在哪里”中看到，路由器表单是斜线 home。

所以它会把我们推回到原路。

所以拯救吧。

现在我们需要在这里创建一个按钮，它会把我们带到那个页面。

所以只要导入一个，不好意思，只要创建一个按钮，说，注册。

我们这里有按钮。

让我们试试一个小按钮。

让我把它作为一个类名称的瓷砖，点按钮，或按钮标志。

所以在按钮，类名中，我们只说，按钮。

让我们使重量与此相同或 100 像素将是好的，我想让我们尝试 100 罚款与 20 像素到 2200。

好吧。

让我们补充第三个是我在做什么，应该是高，而不是太多。

所以我想这是对的。

这是对的。

所以我们这里有个按钮。

使其 35 也成为光标指针。

如果你悬停在这里，我们会看到一个指针光标。

现在我们可以做的是使用注册按钮，只需在单击事件时添加按钮，单击按钮的功能名称，就在这里注册。

现在让我们在这里添加我们的电子邮件和密码来注册。

所以尼山是为了更远的 mail.com。

和一些密码。

但在此之前，使密码的类型是密码，输入类型。

密码，这是电子邮件类型的电子邮件。

因此，如果我们在这里键入密码，它们应该是点格式的。

我们看不到。

所以让我得到一些密码 123456126 在这里注册。

因此，如果我们看到，我们现在在主页上，我们已经注册成功。

现在打开用户身份验证，我们可以看到我们的电子邮件，提供商是电子邮件和密码。

所以我们需要回到这里再创建两个。

再来两杯。

那是什么？选项，一个给 Google，一个给 GitHub 多创建两个按钮。

按钮，你应该是谷歌或只是说注册 30 注册与谷歌。

同样，GitHub 多了一个按钮。

GitHub，让我们在其中添加一些红色水平线。

因为我在这些按钮之间也做了标记。

好吧。

但是，但是我们没有正确地思考，因为我们使用了 flex box。

所以这是盖茨，它被同步到我们这里的点。

好吧，没关系。

所以现在我们必须使用谷歌和 YouTube 注册按钮。

所以只要在这里重复按钮的样式就可以了。

就像这样格式化。

现在我们已经做好了，但问题是我们必须使用让它成为谷歌或按钮替代。

我们将不得不在这里使用更多的信息和通信技术。

所以好吧，好吧，好吧，看起来很好，没关系。

我们只是使用字体，我们只是展示功能，而不是平铺应用程序。

所以我们要做的是再创建一个或两个函数。

首先是与谷歌签约。

第二个函数将是 GitHub，应该是 GitHub。

所以我们可以用一个，我们可以用一个函数来做这两件事。

但是会很复杂。

所以我不想把事情搞复杂。

所以我们必须导入 Google 注册和 GitHub 注册的方法。

所以还是导入吧。

首先是 Google Auth provider，我们必须导入的提供者，然后是 GitHub Auth provider。

好吧。

这是我们这里需要的两样东西。

所以我们必须为 Google 指定提供商。

因此，让我们说谷歌提供商等于这个新的谷歌认证提供商。

同理，为 GitHub 做。

复制并粘贴。

我的谷歌有 GitHub，get hub，这个是 GitHub OAuth 提供者。

所以在谷歌的这个功能中，我们可以说，我们可以使用这个名为。

好的，这里我们还需要一个东西，叫做注册，弹出，这个登录会弹出，然后我们可以使用这个弹出方法。

我们必须通过这个选项，这是奇数，和提供者。

所以第一个按钮是谷歌。

因此，我们将添加谷歌提供商，这一个，我们将记录的线索记录，是什么？回应回应。

我们也可以在这里做同样的事情，我们可以记录响应。

所以还是先控制一下吧。

响应点用户，我们必须从这里获取用户 ID。

让我们在这里安慰它。

现在，这个按钮注册谷歌这个功能，我们必须把它映射到这个按钮这里。

所以我们可以在这个谷歌按钮上做的，只是说，点击这个按钮，然后在这里格式化代码，把按钮放在底部中心。

为 GitHub 做同样的事情。

这一个在底部在底部很好。

所以现在如果我们点击谷歌的这个按钮，它会打开一个你看不到的窗口。

但我只能看到，因为我没有记录那个窗口。

所以先打开控制台，看看我们在控制台上得到什么。

如果我们点击这里的按钮。

因此，打开控制台，在这个提供商中看到，我们不在沙漠中，我们没有电子邮件，我们只有我的电子邮件。

所以我们再和谷歌签约吧。

点击，我们看到一个弹出窗口。

我们必须使用我们的电子邮件来注册。

点击您的电子邮件。

它将向我们显示我们在控制台中获得的用户内部的所有数据，您可以在控制台中看到令牌或显示名称、电子邮件。

验证邮件是真是假，是不是匿名的。

我们有照片的网址，和所有的东西。

如果我们看到 Firebase Auth，就让我们刷新它。

我们应该看到我们的新图像层和供应商谷歌广告，你看，它现在工作正常。

所以接下来我们需要添加 GitHub，那就添加吧。

所以当好的，在这个数据提供者中，他们把同样的方法放在这里。

但是这个提供商，谷歌，应该是数据提供商，GitHub。

保存，我们上去复制这个方法 const function，并将其添加到 GitHub。

点击这个 GitHub，GitHub 的标志在这里。

因此，如果我们再次登录 GitHub，让我们右键单击这个按钮。

好吧，你看，我们还在拍这个？现在 GitHub 的弹出窗口是什么，而不是 Google。

您可以看到，我们看到红色身份验证帐户以不同的凭据存在。

那是因为我们已经在使用我的电子邮件，呃，你知道。

所以我们要先把这个删了，才能起来工作。

因为我们不能用两个不同的账户来保存电子邮件。

所以如果我们现在刷新，再试一次，GitHub，点击你的邮箱，或者点击授权。

你看，它现在工作了。

如果我们打开控制台，也许我们会在这里看到 GitHub 的提供者。

你看，显示名称是相同的，如果我刷新这个，我用来添加的电子邮件，或 33 个字段，作者用户，提供商现在是 GitHub。

我的电子邮件现在列在这里。

我想，我们到此结束了，作者也结束了。

最后要做的是，在这里把它推送到主页，所以就推送到主页。

删除控制台点日志或保留文件。

所以如果我们现在注册，我们应该在主页上。

好激动又谷歌了。

注册谷歌。

他们会成功的，因为我们已经有了 Emily 这个符号，而且我们在主页上。

让我们在这里再做一件事，我们将要进行的 CRUD 操作是从 react 导入使用效果。

然后在函数的底部创建一个使用效果挂钩。

使用效果趣味间接挂钩。

如果我们有两个选项，两个参数是参数 is，传递我们要运行的函数中的数据，我们没有依赖数组。

因此，如果它是空的，这意味着它将只运行一次，当我们加载我们的页面。

但是如果我们有一些东西在里面，比如说一个状态，那么这个使用，函数会在状态改变的时候运行。

所以我们必须在这里做一个检查。

所以在这里，让我们看看，让我们只是存储我们的令牌。

内部会话存储。

这是一个很好的认证实践。

所以我们可以只说展示存储。

点集项，我们要用 token，E，然后数据，Rita 叫 response.user.ox。

令牌。

我使用此会话总数，因为当我们关闭此选项卡时，它会被销毁。

因此，如果我们关闭选项卡，我们应该能够自动注销。

我们可以看到这样的页面。

我也是。

还有这里。

好，我们现在在会话中设置令牌。

所以回去重新登录，用 Google 或者 GitHub 或者你的邮箱密码，这三个里面的任何东西？如果您打开“Applications”选项卡，我们应该会看到我们的“where is it this next art ”(存储在哪里)好的，我们不会看到第二组项目令牌。

有什么问题？什么是好的，我们说它是本地主机 3000。

这里，这一个，我们在这里看见代币。

所以我们必须在这里创建一张支票。

首先，让我们在加载应用程序时从会话存储中获取令牌。

因此，假设令牌等于存储，我们必须获得两个连接器套件项目，即密钥名，称为令牌。

让我们在这里检查一下，如果我们有令牌，是否要将它连接到。

所以如果我们没有令牌，它要接龙，这个就空了。

如果不是空的。

如果我们这里有一个令牌，我们可以使用路由器点推送返回到主页。

好吧，省省吧。

所以如果我们回去，这里有我们的令牌。

如果我们试图返回，会被扔回到这个主页，你看，我们不能回到历史，因为我们有 tukar 在这里。

让我们对主页做同样的操作，在这里打开主页。

所以打开主页，像以前一样创建一份遗嘱。

如此重要，你将从反应和我们做的影响。

路由器未定义。

因此，让我们创建一个 auth 它不是路由器，首先从下一个路由器导入您要路由的路由器，然后创建一个 const 或让路由器等于让我们说，使用路由器这一重要的，我们现在导入，然后就可以了。

所以现在我们必须做一些事情，比如如果我们没有令牌，我们就把我们推回注册页。

保存。

所以我们不能回去。

但现在让我们试着毁掉那个令牌。

所以在 session 应用中删除，就删除这个。

所以如果我们现在刷新页面，我们没有，我们没有令牌。

所以我们不能返回或前进到主页。

因为它会检查我们是否有令牌。

这里，作者现在完成了。

现在，让我们通过使用下一个路由器，转到使用 React 路由器的 CRUD 操作。

接下来。

所以再次和谷歌签约吧。

因此，我们将在我们的主页中做这些事情，打开主页，让我们开始，虽然让我们在主页之间创建两个表单权重，我们不需要 h1，或者直接离开，这没关系。

创建两个输入。

所以先输入，这个输入，在这里重复第二次。

因此，这应该是关闭的，让我们说占位符的让我们说只是名字，这一个应该是占位符关闭让我们说年龄。

所以我们有两个字段名字和年龄。

让我们给他们一些风格。

所以复制叫基本姓的样式，那个样式就对了。

只要使用 styles.my digit，这个输入框，输入框。

他在这里又重复了一遍。

好吧，让我们做页脚，因为它检查我们的应用程序中的最后一个空间。

我们还可以清除这个控制台，关闭控制台。

在我们的应用程序中拥有更多空间。

让我们也添加一个按钮。

按钮，使其增加。

所以现在我们可以使用这个按钮添加数据。

因此，我们可以在这里为我们创建的按钮使用类名。

因此，只需在点按钮中添加类名，然后断开这个按钮。

好吧，保存。

现在我们有了一个按钮，可以用来向 Firebase 添加数据。

但在这之前，我们将在 FireStore 数据库中存储数据，只是为了在这里存储数据，但我们也有实时数据。

因此，在这里创建数据库，好的，创建数据库，使其处于测试模式，因为我们不在生产中，我们只是在测试功能。

所以服务器完成了。

启用它。

因此，与此同时，从 Firebase fire store 调用导入函数。

所以在 Buda 函数中，我们只是调用了 get fire store。

等等，进口是错的。

Firebase 火店完全没问题。

所以导入，炒掉存储这个函数。

让我们在这里再创建一个用于导出的导出。

让我们创建数据库数据库。

所以我们将使用 get fire 存储。

它会在里面添加这个应用程序来初始化它。

我们有 Firebase 应用程序 Firebase，它是什么？这里创建的数据库。

我们可以添加一个标题。

所以我们可以导入这个，主页里面的数据库是什么。

所以我们在这里进口。

因此，我们有导入整个应用程序第三，也导入数据库绑定。

所以现在我们必须创建数据库的实例，我在集合中。

所以我们可以说 const。

集合等于不集合，比方说创建成本，数据库，ref 等于二。

所以让我们在这里导入集合。

因此，要从 Firebase FireStore 创建一个集合来导入集合，我们只需创建包含两个内容的集合，第一，数据库，我们刚刚导入的这个数据库，第二，我们要为集合指定的集合名称。

所以让我们把它变成非常好的数据。

所以我们到此为止。

我们在这里创建了一个数据库引用。

我们还需要一个函数。

所以 const，添加数据到数据库 ref，我们不需要这个。

所以我们在这里导入几个东西。

除了称为添加文档的集合。

所以我们需要使用添加文档来添加数据。

不同的方法有不同的函数。

所以 add Doc 是用来添加文档的，我们已经读取了文档的 Doc，这一点我们将在后面的部分中展示。

在稍后的视频中。

因此，我们将只使用添加文档，我们只是说，在这里传递这个数据库引用。

然后在此输入电子邮件和年龄状态的电子邮件和密码状态。

所以我们处于两种状态。

让我们创建一个 const，比如说 name，set name。

该名称使用状态。

使它成为一个字符串，空字符串来初始化它。

年龄也是如此，所以我们从名字和年龄开始。

年龄啊，所以我们可以在这里输入姓名。

所以我们可以说在改变时，它会带一个参数，叫做 Event event。

我们将事件点目标点值存储在这个名称值中。

然后再复制粘贴一次，把这个设为设定年龄。

如果我们输入退休，就说名字，好吗？你看它没有定义。

所以在这里输入它，我们发现它使用它，现在它会工作。

现在我们可以在这里输入一些东西。

好吧，就这么定了。

所以，在这个输入的年代，让我们把它做成一个数字的数字输入类型。

所以只需添加数字类型。

因为我们不能在这里添加字母字符，我们只能添加年龄。

Find，尽管把它作为文本输入，因为它是更简单的文本元素。

而且让我们也加上他们应该是州名的值，这叫名字和他们应该在哪里。

价值应该是。

应该是数字或者不是数字应该是年龄。

保存。

现在，让我们试着把这个按钮添加到这个函数中。

不好意思，把功能加到按钮这里。

我在刷东西，所以点击交换添加数据。

现在我们必须在这个数据库中传递数据。

所以 name 应该是状态名。

而且应该是下一个应该是年龄状态的年龄。

因此，如果成功，我们将使用它们来表示数据已经发送。

所以我们可以在这里发出警报。

所以我们可以说数据已发送。

但是如果失败了，我们可以使用一个类 catch 块来抛出错误。

所以我们可以只显示错误。

所以控制台点日志，我们来安慰一下。

因此，控制台日志错误，我们可以只说错误。

保存。

现在我们来试试。

所以我们在这里要做的是破坏东西，只是创造价值，在火店里加点东西。

所以我们可以将姓名设置为空，也可以将年龄设置为空。

好的，不是空的，这应该是空的，因为它是一个数字，所以这里的边也是空的。

所以现在尝试输入我的名字尼尚特，25 岁，然后在这里添加它。

好了，数据发送完毕，点击确定。

他们正在被清除，但不是这封邮件。

不是这个年龄，因为我不知道这种输入类型的数字有什么问题。

文字可以，数字不行。

因此，如果我们检查 fire base fire store，我们应该在这里获得我们的数据，我们的姓名和年龄，以及收集文档 ID 的收集 ID。

所以这个 ID 叫唯一。

这是唯一的 ID。

所以再加一个，就说尼山到公元 27 岁发好吧。

应该在这里。

如果我们刷新这个，这个应该在这里。

你可以看到第二张收款凭证，名字是密歇根 2 号，年龄 27 岁。

但问题是，我们在这里添加一个数字，但我们在这里提供字符串。

所以我们要把这个名字转换成一个字符串抱歉，把这个数转换成一个你知道的数。

所以用一个数字把它包起来。

所以现在如果我们再试一次，让我们加上肖恩三或三岁 27。

让我们做 1000 点击添加然后标题。

这应该是新文档。

好吧。

你看年龄现在是数字了。

它不是字符串，我们的添加文档已完成。

现在让我们开始阅读文档。

因此，对于文档，我们还需要在这里输入一个函数，它被称为“使用非单个文档获取文档集”,并在这里创建一个函数。

const 获取数据并使其成为一个箭头函数。

然后我们必须使用这个 get doc 函数，我们必须传递这个数据库引用。

而这一切都是为了获取数据。

所以我们要先调用这个函数。

那么如何调用它，我们必须在页面加载时调用它。

所以在这个使用效果挂钩上，就过了。

是啊。

因此，如果我们有一个令牌，我们必须像这样保存数据，我们也可以创建一个检查，如果我们有一个令牌，只有这样，我们才会调用这个获取数据函数，这总是我们可以，我们可以，我们正在调用这个函数。

所以我们必须使这个函数同步。

并利用它，因为我们在这里使用一个承诺。

所以我们可以说点，然后记录我们得到的响应，我们可以说响应。

且让我们计算控制台点日志加分，现在保存打开控制台。

我们得到了这些数据。

所以如果我们刷新它，让我们刷新。

所以我们得到了所有这些数据。

我们在这里，我们在这里。

但是里面有很多数据我们无法正确读取。

所以我们需要得到这种格式的数据。

所以这应该是一个数组，这应该是一个对象。

而在对象内部，我们会有不同的名字和年龄属性。

好吧。

所以我们能做的就是绘制地图来获取数据。

所以我们可以不回应狗。

所以我们必须把这个盒子映射到这里。

它需要一个参数，或者一个值，比如说，数据。

安全把数据，还有数据里面的价值，也就是所谓的数据。

所以很奇特。

所以如果我们现在保存，我们在这里得到这三个对象，在一个对象数组中。

我们有一个数组，就是这个数组，原始数据。

在里面我们有两个对象 101，和两个，在对象里面，我们在所有这三个对象中有代理名称的两个属性。

让我们在这里添加 ID，这个文档 ID 来区分这三者。

所以我们必须用花括号把它括起来。

我们必须将它与 ID，Mr object 合并，所以我们只说 ID，我们只说 data.id。

现在我们可以看到我们的想法了。

嗯，在这里，你看，我们有这种想法的总部一天，无论第一个，第二个应该是下午，如果那个，这是下午观察，第三个也在这里。

好吧，你看这里。

那么现在在数组中把它存储在哪里，以创建一个附加的状态，比如火灾数据，或者数据，好名字，火灾数据。

我们状态下，应该有一个数组先空数组，然后你要在这里设置设置它。

所以把这个关好。

现在，让我们映射我们的这个文件应该审计，以查看我们的用户界面中的数据。

所以这个 BAFTA 按钮将首先在这里创建一个 div，然后我们只需说 fire data map 来获取这个 map 中的数据。

所以我们可以只说数据迭代日期，我们必须在这里返回一些数据。

所以我们把它放在一个 div 里。

同样，我们必须首先使用 h1 作为名称。

因此，假设我们必须以数据对象点名称为目标。

然后第二件事是，板是 p 标签，它应该是数据点年龄保存。

现在我们可以分别看到我们的名字和年龄，就像这样。

所以让我们把它做成两个，因为它是三个，太大了。

现在是三点。

现在我们可以分别看到我们的名字和年龄。

让我们把它们排成单行。

所以我们可以使用 styles 点 flex，然后在 home 点模块化 CSS 中，我们可以添加这个 flex。

我们可以只说显示 flex。

还要对齐内容，对齐内容，均匀树。

好吧，但在这里它甚至不是最好的，使这之间的内容页。

好吧，那第三个呢？为什么它没有得到适当的反映。

所以让我们检查一下网格。

因此，主要的高度是好的，让我们在这里删除所有这些预定义的样式。

只留下输入框和它后面的样式。

好吗？好吧好吧。

好吧。

我们再补充一句，好吧，好吧，等等，这该死的精炼不重要。

让我在这里加上页边空白。

或者只是说，gap 将伸缩项之间的间隙添加到像素之间的间隙 53。

好了，现在我们有一些空白，我们不需要 JSON 内容。

好吧，好吧。

我们只知道名字和年龄。

让我们也使用对齐项目来垂直对齐它。

好吧，没关系。

所以我们也可以在这里添加名称。

所以名字，文字，年龄文字。

名字叫做 27 号能源任务。

就像 FireStore 一样。

所以，raid 现在也完成了这个 raid 操作。

好吧，我们有什么可以达到这么高的罚款。

做得好，名字和年龄使用 RAID 操作。

我们在这里得到所有这些数据。

现在让我们看看如何更新这些字段。

因此，要更新这些字段，我们需要一个与此姓名和年龄字段相邻的按钮。

因此，添加一个按钮，并给它一个样式的预测姓氏的按钮样式，我们有在家庭点模型点 css，他们应该是按钮。

所以我们可以只点一个大小按钮。

好吧。

所以我们什么都没有。

所以就在这里说更新吧。

到现在为止，我们已经点击了这个，这个应用程序发送它到函数到函数。

要先在这里创建一个函数，我们应该先构造一个日期字段。

它应该在这里接受 ID 参数。

为了保存它，我们将从这个按钮发送 ID。

点击时。

然后他们会输入 ID，所以假设它是一个 data.id。

现在，让我们在这里控制台点日志 ID。

现在打开控制台，如果我们单击此处的按钮，更新第一个字段，您可以继续阅读获取 ID，因为我们必须像这样使用箭头功能。

现在它可以工作了。

所以点击 Nishant 的更新，在这个函数的位域中获取 ID，这个和这个。

现在让我们更新字段。

所以我们首先需要一个函数，应该是 doc。

我们必须更新文档。

所以 doc me add doc 会告诉我们在 FireStore 中编辑哪个有趣的字段。

而且这个更新文档会编辑。

因此，我们必须首先添加文档，让我们创建一个日期的数据或字段，以使该文档成为需要一些参数的选定文档。

首先是数据库。

第二个是集合名称，称为 current data 集合名称。

所以我们可以从这个得到。

这个这个。

我们需要的第三样东西是 ID，所以我们现在不能编辑它。

所以第二件要做的事情是，我们必须使用 update doc 函数，并将这个失败传递给它的第一个参数。

我们必须导入的第二件事是导入名称，我的意思是导入名称，然后我们必须有更新后的名称。

然后再加上年龄和数据年龄。

没错。

因此，如果它成功了，我们就可以抛出它们，我们就可以说控制台点日志。

它们应该在控制台中。

让我们在这里添加一个警告。

警惕。

数据更新。

但是，如果它抛出一个错误，我们可以只说捕捉错误，我们可以在这里添加一个警报或控制台日志无关紧要。

或者如果你正在构建一个应用程序，这是否重要。

所以我们在这里做了这些事情。

现在我们来尝试更新。

因此，如果我们单击此处的“更新”,它会被更新，但我们做错了。

好的，等一下更新字段。

所以我们这样做是错误的，因为我们必须添加这个函数作为 get ID。

我们不需要这种头发，我们必须在添加按钮。

所以这个获取 ID，应该在这个获取 ID 更新的按钮里。

所以如果我们点击这个，我们会找到我们的 Id。

所以我们这里出了点问题。

所以名字现在准备好了。

我们没有柠檬水。

所以删掉这第一条。

因为它看起来不太好。

因此，如果我们刷新它，现在就可以了。

所以现在我们要做的是得到这个 Nishan，然后在这个输入框中得到一个 25。

所以我们可以发送我们的名字和年龄。

好吧，我们该怎么办？好的，我们还要发送姓名和年龄。

所以只说数据点名称和数据点年龄，然后没有，然后在这个函数中，我们必须保存它的 get ID。

所以先说名字，然后再加上年龄。

现在让我们在这个输入中设置姓名和年龄。

各州。

所以 set name 应该是这个名字。

不过，好吧，就把那个名字加进去。

年龄越大越好。

所以现在让我们尝试更新点击更新。

等待你看到的名字和年龄。

但是我们还要做一件事。

因此，如果我们单击此处的更新，此添加按钮应更改为更新。

所以我们开始吧。

所以创建一个名为 is bit 的房地产布尔值，并设置 is update US state 它应该为 false。

然后，当我们单击此处的更新按钮时，我们会将主题设置为 true。

那么在这个按钮上的什么地方，我们调用这个函数。

所以只要把这个设为真。

如果这是真的，那么我们将在这里显示一个删除更新按钮来读取他的添加按钮。

因此，我们可以说，如果它是真的，它将显示更新按钮或添加按钮，以在第二个值中添加按钮，第二次检查。

并在第一个只是使添加更新。

更新。

所以让我们再创建一个函数来更新数据。

所以这应该是 const update。

更新等同于我们函数的字段。

然后我们必须添加这个字段进行编辑。

让我们再为 ID 创建一个检查。

所以复制这个并添加 ID ID 和 set ID 的常量，它应该是 null，因为 ID 是 number。

所以 set ID 应该是这个 ID。

要设置 ID，我们只需点击 set ID s ID。

我们在这里设置 ID、姓名和年龄。

因此，在这个更新字段中，只需复制这个先前的功能更新文档，我们必须将它添加到这里。

取消注释就可以了。

所以这份更新文件没有被编辑，正在更新。

因此，他们应该为国家大写添加位字段到这个按钮的更新。

所以现在让我们试试那些尼山和 is 24 的第一次点击更新。

您会看到按钮现在更改为“更新”。请注意，让我转而说“单击更新，已更新”。

如果你检查这里，好，现在，尼山是名字。

所以你看到的是 26 岁。

现在。

所以这里还得在年龄前加上数字属性才能把它转换成数字。

如果我们再试一次。

如果我们把它设为 27，点击这里更新，更新。

这个数字现在是年龄或数字的数字。

所以它工作得很好。

所以我们现在要做的就是把所有的数据设置为空值，如果我们有日期的话。

好吧。

还有一件事要做，我们在这里看不到我们的添加按钮。

因此，要看到添加按钮，我们必须在加载应用程序或单击添加数据时将该设置设为 true。

好吧，如果我们刷新就不会了。

好的，在这里。

所以我们能做的就是好吧，没关系，别管了。

现在我们可以更新。

并且我们可以在升级成功后将其设置为 false。

所以我们可以说设置更新为假。

所以我们现在试试，我们更新到 25 H，点击更新就可以更新了。

按钮现在添加了。

但是这个号码不会从这里删除，因为我不知道输入号码的电子邮件有什么问题。

所以让我们更新第二个东西，第二个值。

要想在第七和第八天发布这个问题，只需更新这个 Nishanth 更新。

它反映在这里，你看到的是第 27 首 NMS 圣歌。

现在，让我们添加删除功能，只是添加一个按钮，就像这个更新删除这一点。

所以它应该在这里，只需向上添加一个按钮，删除删除，保存，它会做与构建相同的事情，只是传递 ID。

我们不需要姓名和年龄，我们只需要使用 get ID 函数在这里读取 ID。

然后我们只需要再导入一个叫做 delete doc 的东西来删除文档。

因此，只需创建一个函数来调用 delete doc 文档文件，然后我们只需使用这个 delete doc 函数，并传递它，好的，我们必须使用 will，首先创建它，然后传递失败编辑。

我们可以用它们来记录这些数据。

我们将在这里抛出一个警报，提示数据已删除。

好吧，那就在它出现的时候捕捉它，任何错误。

所以只说“报警”不能删除该字段。

所以让我们试着删除尼山的名字，这是一个 25 点击这里删除。

好吧。

好的，我们还必须在这里添加功能，在这个删除按钮中。

稍等一下。

好的，问题是我们使用 get ID 来删除和更新。

但是要删除，我们不必使用 get ID，我们刚刚在这里使用了 get document 和 pass ID。

传递 Id 和接收 ID，就像 ID 现在做的那样。

再刷新一下。

让我们检查一下。

删除尼山的第一个字段，然后是 25。

所以此处删除。

你看我们没有付清尼山和 H 25。

在文件里。

但是我们可以在这里看到。

这意味着我们已经完成了数据。

当我们单击 Delete 时，我们可以调用 Get Data 函数再次单击它。

所以现在我们在这里看不到任何字段。

因此，我们还必须在更新字段中为它提供 Get Data 函数。

这里只是把它粘贴在这个警告之后。

它在添加字段中的位置。

如果我们单击或者如果我们将其添加为“让我们将数据添加为默认值”Nishan，单击“添加已发送”，它将在此处偏转。

再加一个名字，比如说 Ashish e sh ISHH 25 AD，AD sent。

让我们加上第三个叫 Raju 的，还有 H 27。

广告在这里。

所以我们有三个领域。

现在再加一个最后一个，应该在那里。

让我们添加一个茶点击此处添加，发送。

现在我们有四个字段。

在这个 fire store 中，我们有四个具有名称和年龄属性的对象字段。

现在让我们尝试更新这个罗杰字段。

所以这里的 25 岁，让 25 点击更新。

现在它看起来是实时的。

它正在得到适当的更新。

让我们稍微有点 Ashish 使 SOB 2500 点击更新。

没有运行。

我真的很好奇。

当然晚了，让我在这里点击状态这个删除按钮，删除。

我们在这里没有看到德肖恩的名字，在火焰基地也没有。

因此，我们现在已经完成了所有四个操作，它们被称为 react create，用于创建读取数据的数据速率 update，用于更新数据 delete，用于删除数据，也称为 crud。

所以我们这里也加了认证，除非注销，否则回不去。

所以让我们再创建一个按钮。

这里是注销的最后一个按钮。

因为为什么不。

所以我们可以在这里创建一个 div，然后把它变成这个按钮里面的一个按钮。

按钮上可能写着注销。

注销，保存到此处的注销按钮。

让我们在这里创建一个函数。

const log out 相当于一个 arrow 函数，那么我们只需在会话中销毁令牌。

然后，我们只需导航到我们的主页 ATO 注册页面。

因此，我们可以说，我们可以说会话存储点删除项，让我们添加名为令牌的密钥名，然后我们将返回到主页，这样路由器点将会推送至注册页寄存器。

现在，我们只需在按钮中添加这个注销按钮，在单击注销时降低此按钮的功能，以单击注销。

现在，我们要说起始页。

现在我们可以回到主页了。

因为我们在下一个 js 中有预测路线。

下一个 js。

如果我们注册，只有，然后我们可以去那个主页。

用我的电子邮件帐户注册。

你看，我们现在看到的是主页，上面有年龄名称，或者是姓名和年龄以及所有的数据。

所以睡觉前还有一件事，我必须再创建一个登录页面。

让我们创建一个小的登录点。

它已经存在于这个位置。

好了，我们已经打到这里了。

登录，就可以了。

让我们进入登录页面。

我们可以在这里看到日志索引。

因此，我们必须创建相同的东西，我们没有注册复制所有数据，并将其粘贴在登录。

改一些文字或者标题就行了。

所以让它登录。

保存。

好的，重新加载，登录。

好的，好的，登录。

所以在这里，我们不需要 cookie，我们需要 Google GitHub fine。

没关系。

但是在这里，我们不必使用这个网站，创建用户和密码，创建 im 用户的电子邮件密码，因为这是一个登录页面。

我们必须在这里登录。

但让我告诉你我的意思。

因此，如果我们回到注册页面，让我们添加一个随机的电子邮件。

所以让我们以 gmail.com 的速度加上尼山 123。

假设这是一封用户的邮件。

让我们添加一个密码 123456 点击注册。

所以应该在这里通过 fire 认证，Firebase 认证。

因此，我们在红色 gmail.com 有这个 Deshawn 123 和密码，如果我们现在尝试创建具有相同电子邮件帐户的用户，我们无法创建它。

让我们试试它会抛出一个错误。

所以尼山 123 以 gmail.com 的速度。

这里添加的密码就像打开控制台一样。

好吧，让我们也在这里抓住它。

所以只说捕捉错误，我们可以只说控制台点日志错误点代码。

要打开控制台，单击注册，您会看到电子邮件。

并且已经在使用，因为我们已经有了这个 fires 中的电子邮件，所以在这个 users 中的这个身份验证步骤中，您可以看到我们也在这里得到这个错误。

所以我们必须做的是登录，而不是重新启动，使用相同的电子邮件。

所以我们可以使用 alert 抛出一个错误。

所以就说电子邮件已经存在了。

因此，要登录，我们必须创建一个登录函数。

在这里登录谷歌，并通过一个登录，所以它应该用电子邮件和密码登录。

只需用这个名为“用无效密码签名”的功能替换密码无效的创意。

现在它可以工作了。

所以如果我们刷新这个，让我们再试一次。

让我们添加这个电子邮件尼山 123 在 gmail.com 和密码，使注册文本登录这里。

所以他们应该可以在这里不点击登录。

好的，我们需要知道登录页面在做什么，不是登录，而是注册。

所以没有赚到电子邮件，最近的最近的 123 和红克 gmail.com。

密码登录您可以看到我们现在已经登录了。

但是，如果我们使用错误的电子邮件登录，即我们没有正确注册，我们会得到一个错误。

所以我们可以使用任何没有注册的电子邮件。

所以，让我们试试这个尼山 1234 的汇率 gmail.com 密码什么的。

登录，你会看到我们收到的这封电子邮件名为电子邮件已经存在。

好的，等等，我们在这里添加了错误的信息。

所以你应该收到一封不存在的邮件。

在寄存器中，我们必须添加先前的错误消息和已经存在的消息。

因此，只需在这里放入 catch 块，然后再次登录。

你会发现电子邮件并不存在。

因为我们这里没有尼山 1234。

因此，让我们在登录页面中创建它的注册页面意味着电子邮件和密码。

我们正在使用这个尼山 1234 在利率 gmail.com 和密码一从六注册。

我们看到了所有的数据。

现在，如果我们使用错误的密码再次登录，该怎么办？

如果我们刷新它，我们应该能够在身份认证中看到 1234 帐户，看到我们得到了这个。

所以我们用这个邮箱登录，但是密码应该是错的。

让我们试试这个 123 我们仍然会得到这个错误，称为电子邮件不存在，因为我们没有使用自定义的错误信息。

或者我们可以说无法登录。

如果我们得到这个错误是因为我们在电子邮件或密码中有错误，请登录。

所以你可以看到无法登录，因为我认为密码是错误的。

所以 123456 签到。

现在我们可以在这里签到了。

因此，我们已经在我们的应用程序中完成了从身份验证到 CRUD 操作的所有功能。

到目前为止，我正在注销并准备睡觉