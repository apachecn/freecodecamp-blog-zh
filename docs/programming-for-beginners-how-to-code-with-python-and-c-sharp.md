# 面向初学者的编程——如何用 Python 和 C#编程

> 原文：<https://www.freecodecamp.org/news/programming-for-beginners-how-to-code-with-python-and-c-sharp/>

当谈到学习如何编写计算机程序时，可能很难知道从哪里开始。

好吧，别再找了。

我们刚刚发布了一个完整的课程，教授 Python 和 C#的基本编程技能。这门课程是为绝对初学者开设的。

这门课最棒的一点是它是由 Jabrils 教授的。他是 YouTubers 上最有趣的科技人之一。他将自己强大的技术能力和有趣的风格带到了这个初学者编程课程中。

![one-jabrils--1-](img/3d19388390468709bbc3110740c3018f.png)

以下是您将在本课程中学到的主题:

*   什么是 IDE？
*   安装您的第一个 IDE ‼ (Windows)
*   C#和 Python 的区别
*   编写您的第一个程序！
*   什么是数据类型？
*   什么是书
*   什么是字符串？
*   什么是 Char？
*   什么是浮动？
*   什么是数组？
*   什么是变量？
*   什么是 If 语句？
*   什么是 Else 语句？
*   什么是 Else If 语句？
*   什么是 For 循环？
*   什么是 While 循环？
*   什么是方法？
*   什么是函数？
*   如何评论你的代码！
*   你可以编写任何你能想到的代码！

观看以下全部课程或在 freeCodeCamp.org YouTube 频道观看[(5 小时观看)。](https://youtu.be/__izua1kKeI)

[https://www.youtube.com/embed/__izua1kKeI?feature=oembed](https://www.youtube.com/embed/__izua1kKeI?feature=oembed)

## 副本

(自动生成)

如果你熟悉贾布里勒，你就会知道他是最有趣的科技创造者之一。

YouTube 公司要求他创建一个完整的编程课程。

我们很自豪能够在 freeCodeCamp 频道上发布整个课程的视频。

伙计们，我一生都被告知编程很难。

这只是为了书呆子。

它需要太多的数学，你的成绩不够好。

但是我已经编程 13 年了，我可以确定所有这些假设都是错误的。

这就是为什么当 YouTube 最近向我提供创建入门级编程课程的机会时，我知道这是我尝试和纠正一些误解的机会，我相信这些误解仍然阻止像您这样的普通人进入编程的神奇世界。

但首先，我们必须停止这个高工资的医疗场。

所以你想学编码。

我的意思是，我不怪你把保险计算机科学作为第二个最值得学习的专业。

当然，现在你可以去你的学校学习如何编程，或者你可以向像我这样没有传统教育背景的笨蛋学习如何编程。

玩吧。

等等，在你点击关闭这个视频并在你当地的大学注册一门课程之前。

至少让我试着用一个简短的故事来赢得你们的心。

这一切都是从我九岁的时候开始的，我的朋友邀请我去玩一种我从未听说过的新游戏。

是啊，伙计。

太疯狂了。

你可以玩大金刚，如果你想和林克马里奥皮卡丘战斗。

果然，他说的是任天堂 64 上的原版。

就像，我一直玩电子游戏。

但是从那天起，这个游戏就让我迷上了电子游戏。

我记得 2000 年我们家从弗吉尼亚搬到加利福尼亚时，整个穿越美国的旅程中，我都在用记事本设计我自己的 Smash Bros 关卡，当圣诞节来临时，我向我妈妈要了一台带有 Super Smash Brothers 的任天堂 64，我玩了无数个小时，完全被它的概念迷住了，我把所有内部吉祥物聚集在一起，在三维视频游戏中一决雌雄。

这样的事情怎么可能发生？这整个经历让我有兴趣更多地了解视频游戏是如何工作的，不，不是学习如何编程，因为我还在那个年龄，还在学习读写做基础数学。

他又一次让我对学习游戏如何运作产生了兴趣。

记住这一点，因为这在以后会变得很重要。

不久之后，我偶然发现了一个叫做 newgrounds.com 的小网站。

像我这样的人制作简单的低成本游戏和互动电影的地方。

我相信这真的是一次在我脑海中播下种子的经历，那就是制作我喜欢玩的视频游戏对像我这样的人来说是可能的。

所以后来我遇到了一个叫做 Game Maker 的东西，它标榜自己可以从今天开始制作游戏，不需要任何代码。

经过几周的恳求，我妈妈终于给我买了。

伙计，那是我一生中最快乐的时光。

我完全不知道自己在做什么。

但是，只要尽我所能创造出能让计算机做我想做的事情的逻辑语句，就足以让我着迷。

第一次感受到真正疲惫的感觉。

因为我彻夜未眠试图理解如何创建一个库存系统是我永远不会忘记的事情。

从那天起。

我继续尽我所能地学习，向所有愿意帮助我学习越来越多不是关于编程，而是关于游戏如何工作的人寻求帮助。

因为你知道，理解游戏是如何工作的是最初吸引我开始编程的原因。

甚至都不知道。

在我的编码冒险经历中，当遇到困难时，我总是会依靠它。

这个故事的寓意是，我知道有一种极端的冲动想学这些东西，但又不知道从哪里开始是什么感觉。

所以我很清楚你错过了什么，以及如何让你站在围栏的这一边。

谢天谢地，我已经学会了如何不用传统的学校教育来做这些事情，这意味着你和我，我们说着同样的语言。

如果这个故事没有萦绕在你的心头，你可能还没有。

不，我只是在开玩笑。

我只是在开玩笑。

但我相信这个会。

这是我的朋友们，glar zargar 是一个非常聪明的程序员，他不会开玩笑，只用一个 ti 80 计算器就能完成整个 star 系统。

当我告诉 zargar 我要做你的编程课程时，他一直说要教我。

现在扎尔加比我聪明千万倍。

但你为什么不想要佐格莱尔？来教这门课？看看这个 zargar 什么是数组？嗯，你看，我们需要一点背景故事，因为传统上内存只能以 32 位的速率存储信息，这意味着递归深度称为摩尔定律上拉在 1996 年获得了牵引力，我们不仅获得了更多的权力和内存，好吧，对不起，你已经证明了我的观点。

这些观众一点也不明白那些博士课程让我慢了下来，是时候走了。

下一次，记住，深呼吸。

哦，伙计。

欢迎来到我的课程。

在这个系列中，您将带着开始编程所需的一切离开。

是的，你看过标题，对吧？这不是点击诱饵。

事实上，在本课程结束时，你将利用我们所学的知识编写你的第一个应用程序。

但是如果你想从这门课中学到什么，请记住这个。

问问你自己，你为什么在这里？是什么驱使你想要学习开始编程所需的一切？对我来说，这是为了理解游戏是如何工作的，不管你的理由是什么，我鼓励你现在就在评论区写下你的理由。

如果在这门课的任何一点上，或者在你的编程冒险中的任何一个地方，事情变得困难了，我希望你回到那个评论上，并被提醒你开始的原因。

相信我，你能行的，我保证，这比你想象的要容易得多。

我会尽我所能在沟通方面做得更好，让这门课对你们来说更容易。

下一部分见。

是的，看你点开了这个视频，对学习一些编程原理很感兴趣，真是太紧了。

好吧，我告诉你一件事，如果你想成为一名程序员，没有它你不会走得很远。

这叫做身份证明。

有很多很多不同类型的 id，有些来自你肯定听说过的大型组织，比如苹果的微软 Xcode 的 Visual Studio 和谷歌的 Android Studio。

但是也有来自各种其他组织的 id。

JetBrains 的饼状图 Eclipse 来自 Eclipse sublime 来自几个家伙，GitHub 的 Adam，仅举几个例子。

你可能已经猜到了，今天的课是关于 ID 的。

嘿，这是什么意思？咄，当然，我怎么能忘记涵盖 Id E 代表集成开发环境。

顾名思义，这是一个用于开发游戏、软件、硬件的数字环境，实际上几乎是任何有代码的东西，它提供了对开发的许多方面的集成或控制，从调试到编译，这意味着添加、删除或更改阻止您的程序按预期运行的代码，这大致意味着获取您的代码并将其转换为计算机可以理解的东西。

有些 ID 只支持单一语言，例如，IDL，这是一个下载时与 Python 语言捆绑在一起的 ID，它只支持 Python，但像苹果公司的 Xcode 这样的 ID 支持一堆不同的语言，如 C，c++，Java，Python，不胜枚举。

但正如你所看到的，我在这里的观点是，在选择你的 ID 时有许多不同的选择。

老实说，这可能有点吓人。

但是，如果你想在以后开始编程，当你获得更多的经验时，你可以用微软的 Word 来写你的程序，如果你真的想整个，我不建议这样做，无论你获得多少经验，你都会浪费大量的时间。

这就是数字环境，它是编程的各个核心部分。

你想在哪里编码，一些游戏、软件、硬件，你说得出，它就是你最好的朋友，帮助确保你的成功。

现在是时候安装你的第一个 ID 了。

你准备好了吗？让我们开始吧。

抬起头来。

外面有很多不同的 id。

但是，与其带你浏览每一个 ID，这将是一个非常无聊的讲座，你反而会得到我的偏见，我将向你展示如何安装我最喜欢的两个 IDs Microsoft Visual Studio，如果你想进入 Windows 开发或想使用一个流行的引擎 unity 制作游戏，你应该会感兴趣。

我们还将安装 Microsoft Visual Studio 代码，如果您对更一般的编程或数据科学以及大量其他任务感兴趣，您应该会感兴趣。

是的，他们的名字中都有 Microsoft Visual Studio。

是的，他们是两个不同的身份。

现在，让我们开始吧。

好的，那么你要做的第一件事就是打开你的浏览器。

我打算用谷歌浏览器。

到目前为止，你可以选择你想要的任何东西，这并不重要，但你想在搜索栏中搜索 Visual Studio，然后单击下载，你会看到 Visual studio.microsoft.com 单击下载，它会直接带你到你想去的页面。

如你所见，我们这里有一个 Visual Studio 2019 社区版，让我们点击那里的免费下载。

然后你还有你的 Visual Studio 代码，它会等一会儿，你会看到它在这里弹出，让你知道它正在下载。

然后我将返回，然后我将单击 Visual Studio 代码，免费下载，这是我们需要的第二个 ID。

它会在这里弹出，让你知道它正在下载。

就等着下载吧。

好的，一旦下载完成，你可以在谷歌浏览器中点击它们，如果你正在使用的话，他们会开始安装。

但如果你没有谷歌浏览器，那么你会想找到你的默认下载文件夹，在大多数情况下，它应该是 Windows CE 下载中的下载。

但是如果没有，您将需要找到它，以便安装 IDs。

所以首先，让我们安装 Visual Studio 代码。

我要双击这个。

您会看到这里有一个窗口，显示欢迎使用 windows studio、代码向导、安装向导。

这部分真的很简单。

就像你做过的任何其他安装一样，你只需点击下一步并同意，如果你想读就读。

点击下一个。

然后在这里，你想把它放在默认位置，就像在某个地方看到程序文件一样。

但是因为我已经在我的系统上安装了这个，所以我将它安装在一个非常特殊的位置，这样它就不会干扰我们的预设。

所以，好吧，就这样，然后点击下一个。

在这里，我建议您在这两者上检查 open with code 和。

还要确保选中了添加路径。

这里要做的是让你有一些 Python 脚本，而不是必须打开第一个 Visual Studio 代码，然后点击文件，然后打开并搜索它，你可以只去喜欢，假设其中一个是 Python 脚本，你只需右键单击它。

然后你可以在这里找到它，它是用代码打开的。

所以它只要直接在 Visual Studio 代码中打开那个文件就很方便了。

如果你有一个 a 文件，对不起，你有一个包含很多 Python 脚本的文件夹，你也可以对目录做同样的事情，你只需右击文件夹中的某个地方，点击打开代码。

非常非常有用，我会，我肯定会建议检查这两个，然后点击下一步。

现在，我将返回并删除这些，因为我不想弄乱我的预设。

但是这是你的人生应该有的样子，当然，这是 C C 程序文件的下一个目的地。

然后之后，只需点击安装，安装，然后你可以看到它将要安装。

所以我要花几秒钟，我要花很长时间，然后嘭的一声，你就知道了，Visual Studio 代码已经正式安装了。

我们将点击“完成”并启动 vinje Visual Studio 代码。

我要把这个哦，错误的窗口。

将 Visual Studio 代码带到这里。

这是它看起来的样子。

现在，在我们开始之前，有几件事你想做，你肯定想在这里添加 Python。

所以点击安装。

事实上，我将向您展示如何按照传统方式搜索 Python，然后安装 Python。

这允许您将 Python 与 Visual Studio 代码结合使用。

有几种方法可以做到这一点。

但是这个是直接来自微软的。

所以只需要安装最上面的那个叫做 Python 的就可以了。

我们需要的另一个扩展名为 code runner，刚刚搜索了 code runner，打开扩展并下载第一个扩展将允许我们在 Visual Studio 代码中运行 Python 脚本，而不必离开 ID。

非常好看。

好了，现在你知道了，Visual Studio 代码已经安装好了。

如果你想做新文件，只需进入文件，顶新文件和八达繁荣。

如果你想保存一个扩展名，只需将文件保存在任何地方，比如我们只做文档，就像我的，我的文件我不知道，pi 会给你一个 Python 脚本，点击保存。

现在它被认为是一个 Python 脚本。

如果您在这里看到这个弹出窗口，告诉我们 Python 已安装。

Visual Studio 代码通常很好地让你知道你需要什么，你不需要什么，但真的让我知道你需要什么。

我们在弹出窗口中点击“下载”,我们想要下载 python 3.7。

然后，我将在这里点击“保存打开的文件夹”。

我将双击它来运行它。

接下来你要做的就是点击添加 Python 3.7 路径，然后点击立即安装。

现在就要安装了。

给一点时间，Python 应该可以安装了。

就像那个装置成功了一样。

回到我们的下载文件夹。

我将双击 Visual Studio 安装程序。

我不确定你现在看到的是什么，但它要求我输入密码。

所以我打算这么做。

让我看看，坚持住。

不，这是一个旧的别针，抱歉。

好吧，不好意思，这里有一个黑人。

然后我们有 Visual Studio 安装程序，只需点击继续。

它将开始下载和安装一些东西，不需要太长时间，然后你应该会看到这样的东西弹出。

他们会给你一些选择，你可以忽略其中的大部分，也许等你有了更多的经验后再来找他。

但你真正需要的是这个。

dotnet 桌面开发，因为我们要找的是使用 C sharp 的控制台应用程序，所以只需点击那个。

然后你只需点击安装，并看到安装在系统驱动器建议。

同样，我已经安装了这个。

因此，我将把它放在一个非常特殊的位置，但肯定会把它安装在您的系统驱动器上的推荐位置。

所以我要点击 OK。

您会看到弹出的这个信息，感谢您安装，请立即进行调查。

这将开始安装，可能需要一点时间。

但一旦完成，你就没什么好担心的了。

好的，一旦达到 100%，再等一会儿。

这是一个自动的开始，因为我们已经检查过了。

bada boom，现在你可以连接你所有的开发者服务，开发者服务。

只是现在不打也许以后打，反正到了某个时候要逼你做个交代。

所以现在，我们先不要启动 Microsoft Visual Studio，准备第一次使用。

我们在这里。

现在，如果你想创建一个新项目，你只需点击创建一个新项目。

然后我们会问你想做什么类型的项目。

我们只是要做一个简单的 C 控制台项目和 C 夏普在这里，然后点击下一步。

你可以说出它的名字，让我们都说出它的名字，不管它是怎样的。

并创建控制台应用抱歉。

我不知道如何阅读。

给它一秒钟，然后砰的一声，它就启动了，现在你正式加入了。

这几乎就是你为升 C 所要做的一切。

因此，如果你在用 c#或 Python 编写程序，理解它们的语法是很重要的。

但是首先什么是语法？关于编程的语法，它是一组必须遵循的规则，为了运行，你的代码必须遵守一个过程，如果你想让你的程序运行的话。

所以今天，我们要看看两种不同语言的语法，看看它们之间的区别，因为我认为这对于你了解它们之间的对比会很有帮助。

另外，你几乎可以同时学习两种语言。

左边是使用 IE Visual Studio 的 C 语言 sharp，如果你不做任何 windows 开发或者想使用一个叫做 unity 的流行游戏引擎制作游戏，你应该会感兴趣。

右边是使用 IEEE Visual Studio 代码的语言 Python，如果你想进入普通编程或数据科学领域，你应该会感兴趣。

好了，这里我有两个全新的控制台项目。

所以我们今天要看的第一件事是这两种语言如何利用数据类型。

现在数据类型本身是没有用的，你必须给一个变量分配一个数据类型。

现在从左边的 C 调开始，我要去掉这两行，因为我不再需要它们了，每个新项目都会用到它们。

然后我们今天要用一个布尔值作为例子。

布尔型数据只能是两种类型中的一种。

这要么是真的，要么是假的。

让我们用 C 调初始化一个布尔值。

我们就叫它做吧。

然后我们给它赋值 true。

所以这样做等于真的。

这一切都很棒，这是一个布尔值，我们把它赋值为真。

但是，C sharp 不会将它识别为布尔值，除非您强制转换它的数据类型。

在 C sharp 中，你只需在变量之前转换数据类型，同样，变量必须在转换数据类型之前转换数据类型。

这是布尔的缩写，它等于真，所以你的错误消失了，一切都没问题了。

这个语法适用于每个变量。

例如，如果你想初始化一个字符串变量，你必须转换它。

所以我要说这是一个字符串。

我要把 str 简称为 string，然后我要给它打个招呼。

例如，你必须对每个变量都这样做。

然而，有一个小陷阱。

所以我要去掉这条线，因为我不再需要了。

你只需要在第一次使用时初始化你的变量。

所以，如果我想改变 duvet 布尔变量，我只需要到这里来。

例如，我需要做的等于假，这是因为我们在这条线上。

程序知道 do it 变量的数据类型，他知道它是一个布尔值。

这样我就不用再做了。

事实上，如果我再次尝试，你会看到我得到一个错误，这个错误告诉我，我不能两次创建同一个变量。

所以请记住，您只需在第一次使用时进行初始化。

这种约定适用于每一种数据类型。

所以它是布尔字符串，是浮点数形式的整数，每一个都是。

现在在 Python 中，事情有了很大的不同。

假设我们想做同样的例子，总是想初始化一个布尔变量？在 Python 中，你所要做的就是写下变量的名字，然后给它赋值。

所以在这种情况下，我们要给它赋值 true。

好了，我们开始初始化一个布尔变量。

没错。

在 Python 中，你不必强制转换你的变量，你不必强制转换你希望你的变量成为的数据类型，就像你在 C sharp 中所做的那样。

这个好处来自于 Python 是所谓的解释型语言这一事实。

这意味着，你要做的就是创建一个变量，然后给它赋值。

根据你赋予变量的值，Python 将解释变量应该具有的数据类型。

有了这种语法，Python 语言就有了很大的灵活性。

这也是为什么这么多人喜欢 Python 的原因之一，因为就计算机语言而言，它就像一股清新的空气。

这也是当我使用 Python 时，许多开发人员对数据科学感兴趣的原因之一，因为正如你所看到的，你可以节省大量时间，而不必键入，你知道数据类型一遍又一遍地为你的变量。

然而，灵活性并不止于此。

我将简单地打印出 do it equals，比如，保存它，点击播放按钮，你会看到 do it equals true，显然，这是你所期望的。

但是看看这个。

如果我们想把它重新赋值为假。

例如，我们下来说 do it 等于 false，保存，点击播放按钮，我们有一个 false 值。

但这才是真正酷的地方。

所以目前，正如你所理解的，do it 是一个布尔变量。

但是我们可以做它等于 13037，例如。

所以首先我们赋值为真，然后我们把思维符号改为假，然后我们再次改变思维，把它赋值为 1337。

当我点击播放，没有错误，它只是打印出来，这是 1337。

最后我们赋给它的东西，在 C 调中是非常灵活的，如果我们想把 do it 变成一个整数，我们就不能这么做，例如，我们只会得到一个错误，说，你知道，你不能把整数转换成 bool。

变量一旦有了自己的数据类型，就不能再更改了。

这可能是 C sharp 和 Python 之间最大的区别之一，因为它们是如何转换数据类型的。

接下来，我想深入了解命令终止以及 C sharp 和 Python 之间的区别。

首先，什么是命令？举个例子，这段代码是一个命令。

它说的是我们的 dude 变量，给它赋一个假值。

这里也一样，取我们的 dude 变量，给它赋一个真值，并把它转换成一个布尔值。

这些是命令的示例。

所以在一个非常低的层面上，计算机的工作方式是它需要知道这些命令何时开始，因此它知道它需要处理什么。

这就是命令终止发挥作用的地方。

我们需要某种东西来区分所有这些不同的命令。

在 C 调中，命令的结尾是一个分号。

这些分号，就是你要加到每一个命令末尾的，以便告诉计算机，嘿，这个命令结束了，你可以在处理下一个命令之前处理这个。

我对它进行了粗略的简化，但这就是它在更高层次上的工作方式。

所以在 C 调中，只要你有一个分号来分隔你的前一个命令和下一个命令，你就可以把你的命令放在任何你想放的地方。

例如，我可以把这个，这个命令移到前面的命令之后。

没有错误，没有问题，我可以把它放回下一行，然后按 Tab 键很多次，没有问题，没有错误。

使用分号作为命令终止符有很大的灵活性。

现在在 Python 中，它们也有很大的不同。

所以你可以看看 C 调，好的，分号，作为 Python 的命令终止是有意义的。

他们是怎么做到的？嗯，答案很简单，实际上，他们不用分号，而是用换行符。

因此，如果你想分开两个命令，你只需把它放在一个新的行。

事实上，这很容易证明，我们不能像在 C 调中那样做。

如果我们把它们放在一起，然后保存它，你会在问题选项卡中看到，它说我们有无效的语法，因为你不能，它不知道这里发生了什么。

为什么会有一个值和一个变量下午，它只是不明白 Python 是如何考虑所有停止的，你可以简单地添加一个分号和 Wallah，它现在将作为命令的终止。

事实上，为了证明这一点，它确实如我所说的那样，我将把它们都放在同一条线上来命令终止。

然后，我将按 play，向您展示它会运行并打印出 1337。

所以，是的，在这方面，这使得 Python 非常非常灵活。

因为如果你愿意，你可以使用分号。

然而，您会发现大多数开发人员并不这样做，因为使用新的行命令终止符而不是分号通常会快得多。

请允许我演示一下。

因此，我将把两个脚本都恢复到原来的状态，这是一个新行。

这是一个新的系列。

做这个。

好了，现在他们的背恢复了。

所以你可以节省很多时间的原因是，如果你看两个不同的脚本，它们都使用一个新的行终止符，即使你看更复杂的 C sharp 脚本，你经常会发现 90%以上的 C sharp 脚本都会使用一个新的行作为命令终止符。

所以你实际上节省了很多时间，你知道，不用输入分号，分号，分号，你会看到它在这种情况下变红，因为它让你知道它几乎没用。

就像我们知道你想通过转到新行来终止这个命令。

然后，如果我们要按退格键来变白，因为就像，好吧，现在它在使用中，我们明白你在这里试图做什么。

但是请知道，这是 Visual Studio Code ID 附带的一个特性。

因此，您可以随意使用这些信息，但是要知道，如果大多数 Python 开发人员在您的代码中看到任何分号，他们可能会侧目而视。

所以，是的，这里有一些语法差异。

接下来，我想看看它们在代码阻塞方面有什么不同。

所以第一个问题，什么是代码阻塞？我会用升 C 调来给你们一个简单的例子。

首先，也是最重要的，你指示代码块的方式是使用这些大括号。

在这些大括号里面的任何东西都是代码块。

所以一般的语法，你不必遵循这个，但是大多数开发人员会放一个代码块，中间的一行是在这个代码块中运行的所有代码，然后在最后放一个大括号。

在这里，我们可以把 do it equals false 移到这里。

当然，去掉这个，因为我们不需要它。

就像这样，我们有一个 C 调的代码块，然而，像这样的代码块是没有用的，因为它会被运行。

然而，屏蔽代码允许你做的是，你可以添加一些东西，比如，一个 if 语句。

所以我们可以说如果这样做，那么 set duals 等于 false。

这段代码只有在 do it 等于 true 时才会运行。

我想向你证明这一点。

所以我要来这里，我要写控制台，点写线，这将打印任何我们想要的控制台。

我要说检查。

然后我会下来再做一次。

我要说控制台点写，或者等等，那是什么？控制台，点写线，好了。

嗯，好吧，设为假。

没错。

最后，我们需要一个控制台点读键。

这只是确保当我们点击开始按钮时，终端不会关闭，这是在这里，我将按 boom，terminals off screen on，打开它，我们开始。

你看到它去检查，然后它设置为假。

但是你不相信，因为我们屏蔽了这段代码，它仍然在运行。

所以我要改变 do a T 等于 false，并观察当我点击开始按钮并将终端移到这里时会发生什么。

它只是说检查，因为，同样，do it 等于 false，我们说 if do it，这是另一种说法，如果 do it 等于 true，那么运行这段代码。

事实上，做等于假，这只山羊从未跑过。

这就是代码阻塞，你会发现代码阻塞非常重要，对所有编程都非常有用。

但这是升 C 调的演奏方法。

现在在 Python 中，又一次有了很大的不同，我要去掉这两行，这样就不会那么混乱了。

因此，条件和缩进下的所有内容都将被视为该代码块。

好的，我将运行它，你会看到我们将 checking 设置为 false。

然后我们在这里还有一个额外的打印，我们下一次的移动，但是也打印出了 false。

再说一次，你不会被说服，因为这段代码无论如何都会运行。

所以我们要把这个值设为 false。

然后，我还将删除它，这样在打印时就不会混淆，然后保存，点击播放并仅检查。

这就是你在 Python 中实现代码阻塞的方法。

如你所见，代码阻塞是另一种语法，因为 C sharp 和 Python 有很多不同之处。

但是知道如何用这两种语言进行代码遍历是非常重要的，因为你会在 for 循环中经常用到它，在方法、函数、所有类型的东西的 if 语句中。

今天我想讲的最后一件事就是所谓的变量作用域。

那么什么是变量范围？对代码阻塞的介绍，就是这里的代码块。

记住，这也是一段代码，这里，这是一段代码。

但是有了这些代码块的介绍，现在你不得不担心你的变量的范围。

让我给你举个简单的例子。

假设在这段代码中，我们想要初始化一个新的变量。

我们来初始化一个整数，举个例子，我们称它为 a。

然后我们给它赋值 0，对吗？因为我们在这段代码中初始化了这个整型变量，所以它不能在这段代码之外使用。

为了演示，我会到这里来，我会在代码块之外，控制台点写线，哦，亮线。

举个例子。

你可以看到发生了什么，它说这个变量不存在。

什么叫不存在，所以我们在这里初始化？就像我说的，这个变量，因为我们在这个代码块中初始化它，所以不能离开这个代码块。

如果我们想留下这段代码，我们必须这样做，我们到这里，哦，我们到这里，我们初始化 int a 等于，比如说，5。

然后在这个代码块里面，我们只是改变，我们只是把它重新赋值回零。

因为它是在这段代码中初始化的，这是我们的主要函数，所以我们可以在这段代码之外使用它。

这就是变量范围。

简而言之，在 c#中，这种语法没有什么不同。

如果我们进入这个代码块，初始化一个 A，给它赋一个零，然后走出这个代码块，试着打印一个变量，你会看到当我们保存并点击播放按钮时，我们会得到一个语法错误，说这个变量没有定义。

同样，如果我们想绕过这个问题，我们必须改变变量的范围。

所以来看看常规缩进，比如 a 等于 5。

然后，当我们进入此范围时，我们只需重新分配它，将其更改为零，然后我们将能够打印它并批准我点击“保存”，然后我点击“播放”。

给你，检查五个。

再说一次，我只是想确保我涵盖了所有的基础。

之所以打印出 checking，是因为我们说过 do it 等于 false。

而如果 do it 为真，那么这个字节码就会运行，但是因为 do it 为假，所以两个代码都不会运行。

所以它跳过了整个过程。

它只打印出 5，这是我们给 a 赋值的。

差不多就是这样。

这就是你需要注意的关于语法的所有东西。

总之，尽管 C sharp 和 Python 在语法上有差异，正如你所看到的，差异很小，足以记住重要的东西，你必须在 C sharp Python 中转换数据类型，而不是在 C sharp 中，你必须使用分号来终止命令。

Python 使用新的换行符，C sharp 使用花括号进行 co 分块，而 Python 使用缩进。

当然，它们的语法有更多的不同。

但是这些主要的区别经常让开发人员感到困惑。

好吧。

好吧。

好吧。

是时候编写你第一个程序了。

你准备好了吗准备好，我们走。

所以我们要做一个简单的 MATLAB 应用程序。

在控制台中，因为它很简单，不需要你安装成千上万的东西，你可以向你的朋友和家人炫耀。

现在，在我们开始之前，首先，这个项目的代码在描述中。

另外，我认为让你知道这一课有点像分班考试是很重要的。

如果你刚开始编程，你可能很难理解代码中发生了什么。

如果那是你，没关系。

因为我觉得这节课有点难以理解。

一旦你能够自己编写这样的项目代码，那么，我不认为这个课程能教给你多少东西。

但是我鼓励你看完整个过程并跟着做。

总之。

同样，代码在描述中。

所以你可以参考一下，跟我来。

因为如果你做到了以下几点，你会得到成为程序员最大的启发。

在你挣扎之后，我鼓励你浏览我的课程 LinkedIn 描述的剩余部分，或者只是查看那些不容易跟上的课程。

我有超过 20 个视频教你如何编码，这些视频深入讨论了它们各自的主题。

然后，当你感觉舒服的时候，回到这一课，看看你将如何做第二次或第三次编码你的第一个项目。

相信我，当你下次尝试编写你的第一个项目时，你会发现你有多少不明白的地方，你不会想停止编程 15 年，我可以告诉你，这种感觉很好地概括了编程，它会变得很容易上瘾，但会以最好的方式上瘾。

然而，如果这是你第二次、第三次或更多次编写你的第一个项目，我鼓励你不要逐字复制代码。

但也许创造你自己的故事或在 if 语句和函数这个疯狂的实验室游戏谁知道你的实验可以开始一个全新的编程趋势。

最后，如果你想和我分享你的项目，请发微博给我，我很乐意回复你。

但是现在，所有的事情都解决了，让我们开始编写你的第一个程序。

我们将用两种不同的语言制作他的应用程序，因为我认为这对于你了解它们之间的对比非常有帮助。

另外，你几乎可以同时学习两种语言。

左边是使用 ID Visual Studio 的 C sharp 语言，如果你想做 windows 开发，或者如果你想使用最流行的游戏引擎之一 unity 制作游戏，你应该会感兴趣。

右边是使用 ID Visual Studio 代码的 Python 语言，如果你想学习更一般的编程或数据科学，你应该会感兴趣。

好了，让我们开始写疯狂库程序吧。

现在，在我写任何项目或程序或原型之前，我通常喜欢开始我喜欢称之为骨架的东西，骨架是什么，它就像一个循序渐进的过程，在这个过程中，你可以预期为了完成实际的项目必须要做的事情。

因为这是一个更小、更简单的程序，我们可以简单地通过写一些注释来完成。

所以首先，我将注释掉初始化变量，它只是代表初始化是右边的短木塞，然后简单地从用户那里得到输入。

所以我们要初始化变量。

然后我们将要求用户填写这些变量的数据。

然后在那之后，我们需要初始化呜呜，编织故事。

在我们初始化我们的故事之后，我们只需要打印我们的故事。

对于零件，我们的程序将完成。

简单，容易，切中要害。

现在让我们用 Python 做同样的事情，做一个注释，然后一个网络变量，然后从用户那里得到输入。

在那之后，我们将结束我们的故事。

然后在那之后，我们需要印刷这个故事。

就这么简单。

接下来我要做的就是美化我们的剧本。

我将在所有的评论之间添加空格，这有助于我阅读和理解需要发生的不同过程。

完美。

好吧，继续。

在我们做任何事情之前，C 调在左边，我们需要我们的故事，因为一切都取决于我们的故事，真的。

所以我要贴上一段《黑客帝国》的引言。

我将快速突出显示所有这些内容，并按几次 Tab 键来美化它们，以便于我阅读。

但是欢迎你自己编故事。

欢迎你从网上抓取任何东西。

无论你想用什么作为你的疯狂图书馆的故事是完全没问题的。

如果你也想用的话，我甚至把这个故事放在了描述中。

但是现在我们有了。

这是他们想用在 C 调上的故事。

现在我要对 Python 做同样的事情。

只需粘贴故事字符串，同样的东西只是在一个 Python 格式。

接下来我要做的就是把这个故事打印出来。

在 C 调中，你会想要写点写线。

然后即兴故事，疯狂的故事，嘣，分号，完成。

在 Python 中，我们需要等待，我将把它改为 Madlib story，这样它就是一个完美的母亲故事。

在 Python 中，这真的很简单。

你只需要打印，然后 Madlib story，boom。

所以接下来我们要做的是从用户那里获取输入。

但是我们不能真正从用户那里得到任何输入，因为我们没有变量传递给用户来收集数据。

所以我们的下一个实际步骤是，我们必须初始化我们的变量，这样我们就可以把它传递给用户。

初始化我们的变量。

嗯，我们需要把我们的疯狂图书馆的故事变成一堆变量。

我们可以很简单地做到这一点，如果你熟悉这个游戏，疯狂的 Libs 之类的东西，你知道，这个游戏的全部乐趣在于你有一些故事或一些文本或类似的东西。

你把所有的动词，名词，形容词等等，替换成上下文相似的词，希望是，但不是完全相似。

这就产生了非常有趣的故事。

所以我们要做的是滚动我们的文本，用变量替换动词，名词，形容词，我要做的第一件事是矩阵，我要用花括号把它括起来，哦，是的，我需要把它变成一个插值字符串。

我要对所有这些弦都这样做。

这需要一个变量，这是一个词，矩阵繁荣。

所以我要来一个新的变量，我要初始化字符串数据类型，然后把矩阵放进去。

现在矩阵是一个变量，我们可以向用户索要，他们可以随意改变。

我提前做了这件事，我浏览了课文，挑选了很多我认为有兴趣修改的动词，名词，形容词。

我就这么做，例如，system 是另一个，我要把 system 改成变量，可以把它添加到这个列表中。

我会对整篇文章都这样做。

不过我打算跳过它，因为看起来没什么意思。

好了，我浏览了一下我的故事，用变量替换了所有有趣的动词、名词、形容词等等，这样我们就可以把它传递给用户，他们可以把他们想要的任何单词放在那里。

但是现在我想向你展示你的第一个优化技巧。

如果你看这里的这条线，句子作为商人，教师，律师，木匠，这些都是职业，这是他们的共同点。

事实上，他们背对背，我们能做的真的很酷。

我们将初始化一个字符串数组，我们称之为职业。

然后重新初始化等于新字符串的数组有多少个 1234，一个 4。

现在我们可以做一个很酷的魔术，把它变成一个插值字符串。

重载的例子，只是为了更好的练习，商人，哦，对不起，这不是我想做的，我想做专业的，零，因为我们要做一个 for 循环，一会儿我会讲到。

我要抄这个，因为我懒。

我将把它粘贴到这里，并增加它，然后我们将它粘贴到这里。

然后我会把它增加到 2。

然后最后一个职业，把这个加到三，已经从零开始了，简单点。

现在我们可以做的是，当我们从用户那里得到输入，因为有四个职业，它们都是背对背的，我们可以简单地写一个简单的 for 循环，然后一个接一个地向用户询问四个不同的职业。

我一会儿会告诉你怎么做。

但是这里还有两个加法，也是这样做的，所以我要初始化一个新的字符串数组，把这个加法叫做 AJ，DJ 夏普加法等于两个新的字符串数组，这里只有两个。

所以在这里，把它变成一个插值流，会是一个 D j，0。

我要复制这个，因为我很懒，这是第二个形容词。

我就要递增了。

好了，我们现在有了两个字符串数组。

最后注意，如果你看下面，你会看到一堆关于未赋值局部变量的错误。

这只是因为我们还没有给这些变量赋值。

我相信他们现在没有，这是你的程序不喜欢的，但是当我们得到用户的输入时，我们会分配变量。

在右边的 Python 中，也是一样的。

首先，让我们把所有的字符串变成插值字符串。

它就像 C 升，除了它是 F 而不是$1 符号。

所以我要给所有的字符串加一个 F。

完美。

让我们在这里弹出这个变量，矩阵，或者说，让我们把它变成一个叫做矩阵的变量。

然后我们在这里初始化它，做矩阵等于空字符串。

好，然后我们做下一个内插字符串系统。

然后像这样的系统就等于空字符串。

我会对这篇课文中所有有趣的动词，形容词和名词都这样做。

好的，所以我把所有有趣的单词拿出来，把它们变成变量。

现在我将向你们展示如何用 Python 来做这个优化技巧。

同样，我们有这四个职业，我们想把它变成一个字符串数组。

在 Python 中要做到这一点，职业等于括号，因此，房间是空字符串，空字符串，空字符串，空字符串替换空字符串。

这里我们有一个，一个有四个字符串的数组，我们可以传递给用户，然后他们可以改变啤酒或者任何他们想要的东西。

和 C sharp 不使用插值字符串是一样的，professions zero，我懒，我是复制这个然后粘贴到这里，增量，粘贴到这里，增量粘贴到这里，增量，哎呀，对，是三。

然后我们还想要我们的形容词数组，只有两个。

所以我要做括号，空字符串，然后另一个空字符串到空字符串。

然后是这里，这是一个边，零，我要复制它，因为我很懒，然后把它粘贴到 dependent 上，然后递增，然后 bada boom，现在脚本处于完全相同的状态。

好了，最后一段，让我们从用户那里得到输入。

所以首先有两个功能需要她自己熟悉。

一个我们已经讲过了，那就是控制台点写线。

这是我们将用来在控制台上打印内容的地方。

第二个是控制台点读取线。

这是我们要用的地方，没有输入参数。

但这将返回给我们一个字符串，我们可以设置字符串变量。

无论用户在控制台中键入什么，只要按下回车键，就会出现这种情况。

这就是控制台点读行将返回的内容。

例如，我们可以做矩阵等于控制台点读取线。

就这么简单。

所以让我们开始吧。

我提前编写了一个有趣的小故事，用户可以在终端上看到，以确保我不会在这一部分出错。

但是让我们从控制台点写线开始。

我们就像，欢迎一样。

用户，欢迎用户。

然后我们可以到另一个，控制台点写线。

然后让我们说让我们玩，让我们玩一个疯狂的游戏。

然后在那之后，我们可以得到他们的名字。

所以我们可以说，让我们做另一个控制台点写线。

然后请告诉我你的名字。

然后这里是终端将要停止的地方，允许他们输入一些内容。

这里我们可以做 Neo，因为这是文本中字符的名字，等于，我们可以做控制台点读行。

无论他们在这里返回什么，都是变量 Neo 将要被设置的值。

万一你不相信我，让我们实际运行这个程序，看它被打印在屏幕上。

我们需要做的第一件事是，由于所有这些错误，它不允许我们实际运行程序。

让我们把这个注释掉，哦，等一下，我需要把这个注释掉，然后把那个注释掉。

所以这个不会被编译。

现在让我们用尼尔来代替它。

所以不管我们给终端取什么名字，它都会给我们一个回应。

这将向你们证明，我们能够这样设置变量。

所以我要来到顶部，点击开始。

我的窗户实际上超出了框架。

我会把它放在屏幕上。

因此，当我们把你好，欢迎用户，让我们玩我疯狂的 libs，与我分享你的名字，我将把我的名字带到这里。

当然，它会退出，因为我们也没有让它暂停。

我认为我们需要取消等待，让我们做另一个读键概念，读键将等待按键，然后它将退出终端。

所以再运行一次。

把它放回屏幕上。

欢迎用户。

让我们玩一个疯狂的 Libs 游戏，请告诉我们你的名字，我将把 bros 和他们的指纹放回我们放入的同一个变量，这让你知道它实际上在工作。

所以现在我要把它恢复到初始状态。

我们可以做 z Ctrl，z Ctrl，z Ctrl，Z Ctrl。

所以很多次，我们回到了半结肠，我们回到了最初的状态。

如果你没有控制 Z，我认为在苹果上，它是苹果 Z，我相信。

但是，如果你没有这两个按钮，你可以上来编辑，然后点击这里的撤消。

这和我在键盘上用快捷键做的事情是一样的。

同样很快，让我们在流程的最后添加控制台，控制台点读取键。

这将确保它在退出前暂停应用程序。

好了，现在在 Python 中，让我们开始从用户那里获得输入。

所以我们在这里要做的不是写控制台点写线，同样，它就像打印一样简单。

我们可以打印，欢迎，用户，下来到另一个打印。

让我们玩一个疯狂的 Libs 游戏，然后另一个打印并说，请，请与我分享你的名字。

然后我们可以简单地做 Neo 等于输入。

从技术上来说，从技术上来说，我们可以把这个字符串加入到输入中，这是完全一样的。

我们可以这样做，它会打印出来，然后不管下一行是什么，它都会得到输入。

然而，我们确实需要在这里添加一个换行符。

否则，它会，它会允许你在问号后面输入，我们想换一行，只是为了让它更整洁一点。

现在最酷的事情是，我们已经在变量 Neo 中设置了我们的实际名称，我们可以在打印函数中使用它。

例如，如果我们在控制台上点写一行。

我们来插值字符串，做 Hello。

然后我们可以在这里做，尼欧。

然后感叹号。

然后我们可以说，你准备好了吗？你准备好了吗？你想知道更多关于空间耗尽的事情？所以我会继续下一行。

他学会了如何拼写更多关于。

然后下一行，当他们回答他们想知道更多的信息时，我们可以把它放进矩阵里。

所以我忘了我的分号。

所以我们可以做矩阵等于控制台点读线。

就这么简单。

在 Python 中，这几乎是完全一样的。

我们会做印刷。

用 F 插值字符串是什么？你好。

然后这将是尼尔。

哎呀，尼尔。

然后你准备好了吗？你准备好了吗？然后，实际上，我要分手了。

哎呀，我要把这个分解成另一个印刷品。

实际上，我想把这个作为一个输入。

输入。

这就是你想知道的，哦，想知道更多的东西。

当然，我们需要做矩阵变量等于他们想知道更多的东西。

所以，为了确保你仍然和我在一起，请记住，我们目前唯一的工作是从用户那里获取信息。

这就是我们没有关注的部分。

在这个部分中，我们只使用了两个函数。

第一个调用 read 行，它只是简单地向用户输出一些字符串。

第二个是控制台点读行，它将返回用户在控制台中输入任何内容，然后我们可以给它赋一个变量。

就是这样。

没有比这更复杂的了。

所以继续我们的小故事，我们可以有很多乐趣。

然后做控制台点写线，然后插值字符串，做一些像，哦，然后我们可以把你想知道更多的，然后矩阵哈。

你知道，只是玩得开心。

然后我们可以做另一个线控制台，点写线，插值字符串。

让我们看看。

下一步我们要做的是得到变量系统。

所以我们需要给他们一些背景知识。

所以我们可以这样做，首先，让我们先看看。

好吧，请原谅我。

好吧，首先，告诉我你已经知道的关于母体的事。

然后让我们做最后一个，给他们一点指导，因为这是 madlibs。

毕竟，我们要做的是，让我们看看你会把矩阵归类为什么，然后我们可以在这里加上一个小分号。

最后，最后，我们可以做系统等于控制台点读取线。

是的，非常简单。

再说一次，当我们从用户那里获得数据、信息和输入时，我只是想从这些疯狂的库中获得乐趣。

所以，你知道，你也想给出，你知道，现在这都是些疯狂的 libs 线索。

但是你也想给用户一些提示，告诉他们要替换什么单词。

所以我只是简单地问你会把矩阵归类为什么名词，他只是想在 Python 中玩玩，没有太多真正的变化，我们只是要打印出完全相同的字符串。

所以所以哦，哦，你想知道更多吗？我的意思是，你把矩阵，然后打印另一行。

这将会说，好的，首先，告诉我，哦，告诉我你已经知道的关于矩阵的另一行。

我们在 df 然后现在怎么办？你会喜欢老虎戈尔吗？然后矩阵。

其实我忘了这是 Python。

所以我们可以把它作为输入，然后 do 系统等于这个输入。

只是为了向你证明，我们只是想在这里编造故事，没有比这更复杂的了。

我将把所有这些剩余的变量初始化为空字符串，这样错误就会消失。

因为空字符串等于空字符串，等于空字符串等于空字符串。

现在我们可以把这个故事打印出来。

即使大部分的单词，大部分的角色，将会是呜呜，等于空字符串将会是空字符串。

我们可以把这个打印出来。

我的控制台在左边。

那么我在布里尔家的名字是什么？然后，正如我们所写的，你好吉姆布鲁斯给了我们她的名字。

你准备好了吗？你想更多地了解什么？那里有一个印刷错误。

我们可以解决。

例如，我想更多地了解比萨饼。

哦，你想知道更多关于比萨饼的事吗？好吧。

好吧，首先，告诉我你对披萨的了解。

什么名词？你会把比萨饼归类为我把比萨饼归类为馅饼吗？

这就是我们得到的代码。

所以现在它做了 Mad Libs，它替换了实际 Madlib 故事中的变量。

他们给了我们这个披萨是馅饼。

那个派是我们的，然后就都是空的了。

但正如你所看到的，这是这是我们目前所做的，这是终端脚本的一部分，我们只是在构思这个故事。

这就是全部了。

希望在这一点上，我已经说服你，我们没有做任何复杂的事情。

在这个阶段，我们只是将一个故事打印到控制台，并请求变量。

仅此而已。

所以让我们继续完成我们的故事吧。

接下来我想做的是，当然，我们要做控制台点写线。

我们要插入一个字符串。

接下来我想放的是，让我们看看。

给我一个反对的例子，现在反对，反对系统，对，因为下一个词是敌人，我们将再次尝试替换敌人，我们只是尝试给他们一些提示，一个好的名词来替换。

所以这应该行得通。

所以我们要做敌人等于控制台。

点读线。

好了，希望我打印到控制台的这个字符串，足以给他们一个提示。

用一句好听的话来说，用继续前进代替敌人，我们要做另一个控制台点写线。

接下来我们会得到什么？接下来，我们要把这个词放进去。

让我们来看看。

我能给内心什么暗示，我不知道放松。

现在，让我放松一下。

现在，当然，这需要用现在时，现在时。

然后我们可以在等号控制台里面做点读一遍行。

我们只是继续下去，我们不会赶上 Python 的速度。

所以最后离开的是系统。

所以我们需要打印让我们看看，插入一个字符串。

让我们做什么，给我一个反对的系统。

我总是忘记这是 Python，所以我们不必打印，然后做一个读取行。

我们可以只做输入。

让我们看看敌人有什么变数，对吗？是的，敌人等于输入。

酷毙了。

然后我们可以用内部等于输入做同样的事情。

然后我会对字符串进行插值。

然后让我们现在就做。

现在给我放松一下。

现在，现在时态。

好吧，哦，不，分号，不是 Python。

酷毙了。

我们都赶上了。

接下来是我们在视频中稍早设置的有趣部分。

现在我们需要对字符串数组使用 for 循环。

所以我们可以做的很简单，为 int i 创建一个 for 循环。

小于职业，点长度，分号，然后是括号。

因此，这个 for 循环将遍历文本提示中的下一个职业，我们可以简单地询问。

让我们看看。

我想在此之前，我们需要提醒用户说控制台点写行，并说好的。

现在我需要专业的，专业的，专业的，关于什么是这个中继系统，并在这里插入字符串，嘣。

所以我们让它提醒用户将要发生什么，我需要专业的建筑系统。

然后在这里我们可以做控制台点写线。

我们要在这里做一个插值字符串的过程。

让我们做专业。

如有错别字，我道歉，职业，复数。

因为，再说一次，我们在做商人，我不记得他们最初，商人麦克兰，类似的东西。

但他们是农村的雇员。

然后我们可以简单地让他们知道他们在哪里，通过做 I 加 1，因为要从零开始。

所以我们可以加 1，从 1 开始。

然后简单的专业点长度。

好的，这是做什么的，让我给你们演示一下，有点模糊。

简单来说，当我们遍历 for 循环时，有四个不同的职业，它会向控制台写入，给我一个职业，复数，它会打印出他们当前填写的职业号码。

仅此而已。

然后除以长度就是我们需要多少职业。

稍后我会给你们看这个例子。

但是一旦我们这样做了，那么在每一次之后，我们把它打印到控制台，我们想要读取行并把 AI 的职业分配给它，这将几乎填充我们的整个职业字符串数组。

好的，要了解 Python，这并不太难，当然，我们需要写出我们的打印结果。

我们现在提醒玩家，for 循环在很大程度上将会发生。

好了，现在我需要四个与系统相关的亲鱼。

没错。

然后我们要做 for 循环。

对于 Python 中的循环，它们有点不同。

所以我们要做的是在职业范围和长度上。

然后我们会做冒号，下一行会做人工智能等于输入的职业。

然后在这里，我们放什么我们放专业，这绝对是 C 调专业的错别字。

然后是复数。

然后在这里，我们要做 I 加 1，希望这不是插值字符串，加一个 F 做一个插值字符串，除以长度，哎呀，加油，加油。

这就是职业的长度。

现在我们有了。

他们现在已经达到了速度。

现在这一点上，我很肯定你可以自己完成这个项目，因为我们本质上只是要做我们一直在做的同样的事情。

一旦你填写了剩余的，大概是三个或四个变量，应用程序就完成了。

然而，我想花一秒钟暂停一下，因为我经常不会这么长时间不留下评论。

以后给自己留评论总是好的。

或者如果你要把这个发给朋友，或者如果你要把它上传到 GitHub，你知道，其他开发人员正试图把它拆开，并从中学习它总是好的。

让我们开始吧。

只是为了解释这里发生了什么。

我只想说我们从用户那里得到矩阵变量 a 变量，会有很多评论从用户那里得到系统变量变量忘记了从用户那里得到敌人变量，我甚至没有添加系统，从用户那里得到系统变量抱歉，得到这个内部变量是什么？来自用户的变量。

然后这是手臂，说开始循环。

不行，开始吧。

现在我要说我要说获得所有职业。

来自用户。

此外，我知道我这里有一个错别字。

所以我要修复那个职业。

是的。

酷毙了。

实际上我要把这些结合起来。

是的，这对我个人真的很有帮助。

通常，当我阅读代码时，我会经常寻找绿色，或者注释所在的地方，无论注释是什么颜色，在语言中，我经常会寻找那些类似的标记。

所以我可以填写代码，知道在哪里发生了什么。

所以在 Python 中留下注释并做完全相同的事情总是好的做法。

并在此留下注释，获取矩阵变量。

来自用户。

我很抱歉，如果我有错别字，我不是很担心从用户那里得到系统变量，然后这将是从用户那里得到敌人的变量。

这是从用户那里得到内部变量。

这就是专业。

来自用户的变量将要改变他的职业。

我想这并不重要。

酷，有点多余，但它总是有帮助的。

好的，所以我帮了你一个忙，把剩下的变量的逻辑写了出来，因为如果我把它记录在屏幕上，可能会有点太单调了，但是我们还是可以从头到尾看一遍。

所以在这个职业的 for 循环之后，我们基本上得到了相同的变量。

我们通过向控制台写，给我一个现在时态的英雄相关动词。

同样，这只是给用户的提示。

所以他们大概知道我们要取代什么。

然后我们将把他们返回给我们的任何字符串保存到保存变量中。

然后我们得到未插入的变量，我们通过写入控制台来实现。

现在给我一个动词，让你想到过去时态的 relief。

再次，只是另一个提示，你可以看到它可能是多么平凡。

然后不管他们用辅导员 reline 返回给我们什么字符串，我们都会把它保存到 unplugged 变量中。

在那之后，我们需要两个形容词。

所以我们首先警告用户说，最后，我需要反乌托邦的形容词。

然后我们做一个 for 循环。

简单地说，我们会告诉他们，他们目前正在填写一个形容词。

我们会让他们知道他们在总数中的第几个。

然后我们会把它保存到它们各自的形容词变量中，不管它们返回给我们的是什么字符串。

最后，我们只需要得到战斗变量。

我们只是说，嘿，我们需要一个动词在控制台上。

然后不管他们返回给我们什么字符串，我们都会把它保存到战斗变量中。

这差不多就是 Python 这边的东西，只是在 Python 语法上完全一样，你知道，得到同一个变量，得到拔掉插头的变量，得到 for 循环的两个形容词，然后得到 fight 变量。

我们结束了。

伟大的工作家伙是 madlibs 的应用程序基本上完成了。

我是说，没什么可做的了。

除了我想在 Python 上做的一件事。

所以输入函数与控制台的点读行有点不同，常量读行函数总是会转到新的一行，输入不会。

因此，让我们把它格式化一下。

我要处理所有的输入，分号，然后是一个空格。

所以无论我们问他们什么问题，他们都可以在我们的问题之后输入。

所以我要再做一次。

实际上，我要去掉问号，因为它可能会与语法有点混淆。

所以这里有一些额外的空间。

然后在这里输入冒号。

这里我将做结肠空间。

我会在这里做结肠空间。

这是输入空间，还有输入空间。

这里，空间输入这里，空间，然后嘣。

好的，当我们在终端玩游戏的时候，看起来会干净很多。

所以剩下的就是演奏它，让我们，让我们真正地演奏它。

我要先升 C 调。

我要把这个拿过来，上面写着欢迎用户。

让我们玩一个纸牌游戏。

请告诉我你的名字。

我叫贾布里勒你好，贾布里勒。

你准备好了吗？你想更多地了解什么？我想知道更多关于比萨饼的知识。

哦，你想知道更多关于比萨饼的事吗？哈。

好吧，首先，告诉我你对披萨的了解。

现在怎么办？你会把披萨归类为我会说披萨是派。

给我一个令人难忘的名词。

嗯，也许是蛋糕。

现在给我们一个轻松的名词。

现在时态。

我想睡觉是一个放松的名词和现在时。

好的，现在我需要四种与馅饼相关的职业。

哦，我不知道。

贝克。

做饭。

主厨。

我不知道。

我不知道。

最后一个是什么？我只想说屠夫。

我不知道。

给我一个英雄。

现在时态的相关动词。

以下是现在时态的相关动词。

让我们看看。

保存。

我能想到的就这些了。

我知道那是真正的词，但我只能想到这个。

现在，给我一个让你想到解脱的动词。

按摩。

按摩用过去式按摩。

是啊。

那是动词吗？有事做吗？我想我不知道。

最后，我需要反乌托邦形容词来描述词语。

我不知道鸟，我想。

另一个是什么？我很脏。

动词是你做的事情，比如说，嗯，你做的事情，踢。

让我们看看这里有什么。

比萨饼是两排馅饼。

那个馅饼是我们的蛋糕。

但是当你睡觉的时候，你环顾四周。

你看到了什么？面包师、厨师、主厨、屠夫，哎呀，我没有听从社会复数的指示，正是我们试图拯救的人的想法。

但在此之前，这些人仍然是馅饼的一部分。

这使得它们成为我们的蛋糕。

你要明白，这些人中的大多数还没准备好接受按摩。

他们中的许多人被烤焦了，无助地弄脏了馅饼，他们会踢来保护它。

艺术，绝对的艺术。

现在让我们用 Python 来玩。

很快的。

不知道我怎么会错过这个。

但是我们需要把输入放在底部，这样控制台就会保持打开。

但是实际用 Python 玩就有点不一样了。

在你的项目文件名的顶部点击右键，然后显示一个浏览器。

然后，无论你的文件错在哪里，只要双击它，就会弹出一个终端。

它应该看起来几乎完全一样，只是我们做了一点 Python 语法上的区别。

请告诉我你的名字。我的名字是我能说什么？我叫法吉塔。

这次我很酷。

你好，维吉托，你准备好了吗？你想更多地了解什么？我想知道更多关于嗯，让我想想。

不是披萨。

哦，不要再说彩虹糖了。

彩虹糖是吧。好吧，首先告诉我你对技能了解多少？现在怎么办？一旦你把彩虹糖归类，我会说是糖果。

给我一个糖果的名词。

哦，蔬菜。

现在给我任何一个现在时态的放松名词，放松名词。

人、地方或事物？这实际上可能是不正确的，但我明白它在问什么。

现在放松。

我不知道。

放松自己。

好吧，现在我需要和糖果有关的专业人士。

让我们看看。

贝克。

下来妈妈。

职员。

我不知道。

应该是复数。

我总是忘记复数店员。

贝克。

我不知道你把它叫做什么，糖果制造商的糖果。

制造这里的巧克力的巧克力工厂主。

让我想想，给我一个现在时态的英雄相关动词。

英雄拯救了什么？

他们打击犯罪。

动词战斗，打击犯罪。

打击犯罪。

我不知道。

现在，给我一个让你想到解脱的动词。

编码。

这就是用过去时态代码所做的。

最后，我需要反乌托邦。

让我们做反乌托邦，让我们做，空，我猜。

让我们用黑色和动词，动词。

滑行。

让我们看看这里有什么。

彩虹糖是糖果法士达？糖果是我们的蔬菜。

但是当你放松的时候，你环顾四周，当你看到面包师、职员、糖果制造商、巧克力工厂主，人们的思想，我们正在努力打击犯罪。

但在此之前，这些人仍然是糖果的一部分。

这让它们变得更加蔬菜化。

这太深奥了。

你要明白，这些人中的大多数还没准备好被编码。

而且很多都是那么的空虚。

对糖果绝望的黑暗，他们会滑动来保护它。

哇哦。

哇哦。

这就是诗歌。

好了，伙计们，祝贺你们创建了第一个程序。

你做到了。

恭喜各位。

信不信由你，你刚刚写了一个实际可用的程序，你现在就可以和你的朋友和爱人一起玩，用你的新技能给他们留下深刻印象。

但是如果你还是迷路了，你应该感到非常幸运。

因为这个视频是编程课程的一部分，我把它放在一起是为了教你开始编程所需要知道的一切。

我保证到最后，即使没有我的帮助，你也会很容易理解这个应用程序。

我羡慕你能学到的所有知识。

所以，也许有些事情你理解了，有些事情让你困惑了。

嗯，我设计了这门课，你只需要上你想上的课，不需要事先准备课程。

你还在等什么？现在，如果你想成为一名优秀的程序员，对数据类型有一个坚实的理解是非常重要的。

因此，让我们谈一谈它们，让你熟悉它们的基本知识。

在最基本的层面上，假设你有一些数据存储在一个变量中。

这个数据有某种与之相关的类型。

有许多不同的数据类型，它们都有不同的指令和规则。

这种计算机知道它能做什么，不能做什么。

现在有六种主要的数据类型被大多数语言所共享。

bool，代表布尔，以 19 世纪数学家乔治·布尔的名字命名，他的工作使它普及了 char，它代表字符串，它代表字符串 int，它代表整数 float，它代表浮点值，以及 array，它实际上不是一种数据类型。

这是一种数据结构。

但是根据定义，它是一个有序的序列或相似类型数据的排列。

不，还有更多的数据类型存在。

但是如果你想了解更多关于这些的知识，我在每一个方面都有一课，查看描述中的链接，因为这一课，我们只是简单地复习一下。

现在，每一个都有自己独特的规则和说明。

例如，让我们看看整数数据类型，这是一种用于整数的数据类型。

假设你有一个等于 11 的整数。

如果你想从中减去 1，你可以把这个操作写下来，发送给计算机执行。

猜猜你的计算机会怎么做，看看这个操作，然后我们会看看它是否有做这个操作的指令，然后我们会去，是的，我知道如何从一个整数中减去一个整数，然后计算机会从 11 中减去一，然后返回给你一个 10。

一切都好。

一切都没问题。

但是现在让我们来看一个字符串数据类型，它也是一个字符串。

如果你有字符串 11，你想从字符串 11 中减去整数 1。

好吧，你把这个操作发送到你的电脑，你的电脑会看一下它的操作，然后检查它是否有如何操作的指令，然后它会意识到你发送的完全没有意义。

它没有说明如何从字符串中减去整数。

这和告诉你的电脑从 hello 这个词里减一没什么区别。

你是怎么做的呢？你的计算机会返回一个错误，让我知道你试图做的操作没有意义。

在这种情况下特别是类型错误。

现在，让我们做同样的操作，但是把它们都变成字符串。

你认为会发生什么？嗯，我们得到了 attending in，但是作为一个字符串，也许我们会得到另一个错误。

好的，如果你又有了字符串 11，你把这个操作发送到你的计算机，从字符串 11 中减去字符串 1，你的计算机会看它的操作，然后再一次，意识到它没有关于如何做的指令，因此，在空中返回给你。

但是你可能会发现下一部分很有趣。

如果你告诉你的计算机取字符串 11，然后加上字符串 1，它会看这个操作，然后检查它是否有任何关于如何做的指令。

然后说，是的，实际上，我可以帮你做，然后它会返回给你一个 111 的字符串，因为字符串 1 和字符串 11 都是字符串，它所要做的就是在第 11 个字符串上多加一个，举个例子。

这就是数据类型。

这就是为什么理解数据类型是非常重要的，如果你不知道一个数作为 int，float，或者 string 的区别，你将很难写程序。

查看 LinkedIn 的课程描述，了解有关每种数据类型的更多信息。

但总之，请记住数据类型是具有不同规则和指令的不同类型的数据。

根据变量的数据类型，有些操作是可以做的，有些是不可以做的，当你开始使用它们时，掌握和理解这些操作将变得非常重要。

那么我们来谈谈公牛公牛是布尔的简称。

如果你听到 bull 或 Boolean，它们的意思完全一样。

1847 年，乔治·布尔在其著作《逻辑的数学分析》中引入了这一概念。

但是从编程的角度来看，什么是布尔型呢？Boolean 是一种数据类型，它只能被赋予两个不同的值，真值或假值。

就是这样，老实说，没有比这更复杂的了。

可能会让你感到惊讶的是，金条是编程最基本的组成部分，你的大部分代码只是简单地检查某件事是真是假。

并且通过组合一堆体积增加了复杂性。

但是我们一会儿就会谈到这一点。

但是现在，让我们深入了解如何使用卷的本质。

我们将通过使用两种不同的语言来做到这一点。

因为我认为学习他们如何比较和对比对你很有帮助。

另外，你几乎可以同时学习两种语言。

左边是使用 it Visual Studio 的 C 语言夏普，如果你想做 windows 开发，或者如果你想使用一个流行的游戏引擎 unity 制作游戏，你应该会感兴趣。

右边是使用 ID Visual Studio 代码的语言 Python，如果你想进入普通编程或数据科学领域，你应该会感兴趣。

好了，这里我有两个全新的控制台项目。

如果你不知道如何用 C sharp 或 Python 或两者来创建一个新的控制台项目，一定要看看描述中的视频。

叫做如何安装广告。

在视频的最后，我会告诉你怎么做。

好了，今天我想讲的第一件事，就是一些基本的布尔语法。

所以我要做的第一件事就是删除这两行不需要它们，它们是每个新 C 调项目的标准。

然后我会转换一个布尔数据类型，bowl，然后我会创建一个名为 pizza time 的变量，然后将它赋值为 true。

现在，你知道的关于布尔的第一件事是，它们只有两种状态，要么为真，要么为假。

这大概是欺凌者在分配它时唯一的灵活性。

但是在 Python 中，金条有更多的灵活性。

所以我要创建一个新变量，我们称它为披萨时间，当然，我要给它赋一个真值。

现在它知道这是一个布尔值，但我也可以给它赋一个假值。

或者我也可以说，给它赋值一，等于真。

最后，我可以给它赋一个零值，这就等于假。

为了证明这一点，我将向你们展示所有不同的案例。

所以我们在这里做 true。

然后为了检查它，我们需要一个 if 语句。

所以我要说，如果比萨饼时间，那么我们需要打印比萨饼，或字符串比萨饼。

就像这样，我要点击播放按钮，你会看到它打印出比萨饼。

接下来，我将更改 false 值，它应该不会打印出任何内容。

如果我们按下播放键。

你看，什么都没有印出来，正如我们所料。

现在，我要将它更改为一个值，并点击“play”按钮。

你会再次看到我们的披萨很好吃。

最后，我要说比萨时间等于零，保存它，点击播放按钮，你会看到我们什么也没打印出来。

这是怎么回事，因为很明显披萨时间变量是一个整数。

我们说如果是零，那么打印比萨饼的时间。

那没有任何意义，对吧。

这是 Python 作为解释型语言的好处之一，它允许你在条件语句中传递整数。

如果它是零，那么它会把它转换成一个假值。

但如果是 1，它会把它转换成真值。

事实上，任何不为零的数值，并且它被用作一个条件语句，将返回一个真值。

所以皮特的时间等于 9，保存点击播放，你会看到它打印出比萨饼，比萨饼时间等于负三，点击保存，点击播放，仍然打印出比萨饼在这不仅限于整数，你也可以使用浮点数。

因此，如果我们说比萨时间等于点 0000001，点击保存，您会看到比萨时间现在是一个浮点数，我们点击播放，它仍然会打印出比萨。

这是 Python 独有的，我们甚至不能想到用 C #来做这件事，如果我们没有尝试过，我们会得到各种各样的错误。

所以这只是你使用语言、Python、字体和折痕的一个额外的灵活性。

所以现在我想进入金条拥有的许多、许多、许多操作者，这正好证明了这些东西是多么强大。

但是让我们从等式运算符开始。

现在，让我们说，你想创造一些像用户登录系统，对不对。

所以我要在这里创建几个字符串。

我要说字符串，密码类型有些 su p T 代表密码类型，我要说等于 ABC 123。

然后我们再做一串分号，然后再做一串。

这将是密码的 P A。

我们将再说一遍，等于 ABC 123。

好吧，假设这是用户的实际密码 PA，ABC 123。

他们输入的是 ABC 123。

那么我们如何检查它们是否匹配呢？我们必须使用一种叫做等式的东西。

等号等号运算符是这样的。

pt 等于 PA。

仅此而已。

我知道你在想什么，这看起来很混乱。

我已经编程 13 年了。

我同意你的观点。

所以我通常喜欢对我的金条做的一件事是，首先，我喜欢在我要检查的布尔值周围放上括号，这样在我扫描代码行的时候，读起来就容易多了。

但是为了向你们解释这里发生了什么，两个等于背对背，我们称之为不等等于运算符，不要和一个等于混淆，一个等于仅仅是赋值运算符，它意味着你想给一个变量赋值，等于，也就是检查两个东西是否相等。

所以如果我们要读这一整行，我们只是简单地初始化一个布尔变量，叫做 pizza time。

我们用布尔运算给它赋值真或假。

这个布尔运算只是检查 p t 是否等于 PA。

如果是，那么比萨饼时间将等于真。

但如果不是，它将返回一个假值，时间等于假值。

我是说，很简单，对吧？我们只是问两个事物是否相等。

如果是，那么给我们一个真，如果它不给我们一个假。

这很简单，如果你以前登录过任何东西，这几乎就是他们所做的一切，除此之外可能还会有一些额外的检查。

好吧，我想我已经赢了那匹马。

但是我一直在强调这一点，因为理解这一步真的很重要。

如果你明白这里发生了什么，那么我认为很多编程对你来说会很容易理解，因为这就像编程的一个基本要素。

继续前进。

为了向您证明这是可行的，正如我所说的，我们将在这里运行一个控制台点写入行，将它打印到控制台，披萨时间。

然后我们还需要一个控制台点读键。

这只是确保控制台不会在我们运行它时立即关闭，点击这里的开始按钮。

然后我会将控制台显示在屏幕上。

如你所见，它返回一个真值。

但是如果我们把其中的一个稍微关闭，如果我把它变成 1 到 4，然后我们点击开始按钮，你可以看到无论什么返回给我们的都是假值。

所以现在不是吃披萨的时间，因为它们不匹配，不平等。

现在在 Python 中，我将快速重现同样的场景。

时间等于，我们需要一些，我们需要 p t 等于 A b，c 123 p p A 等于一个字符串，ABC 123。

然后披萨时间等于 P t 等于 P 哦，p a，像这样继续加上王子，让我们在这里打印披萨时间。

然后，我会点击播放按钮，并返回到真正的价值完美。

当然，我可以让其中一个稍微关闭，然后点击播放按钮，返回一个假值。

现在，我真的希望所有这些都有意义，因为如果它从这一点开始做所有的事情，那就很容易理解了。

但是接下来，让我们继续我们的下一个操作符，它是相等而不是相等的操作符。

相等不相等运算符的工作原理是，假设你想检查一些不好的单词，对吗？所以我们要把 pa 改成不好的词，对吧。

所以不是检查它们是否相同，而是检查它们是否不同，不会用感叹号来表示。

这就是说，如果它们不相等，我希望你返回一个真值。

然而，如果它们彼此相等，那么我希望你返回一个假值，它几乎只是翻转布尔运算，这也是我们在这个场景中想要的，因为我们试图过滤掉坏词，如果你键入的是坏词，那么就不是吃披萨的时候了。

为了向您证明这将返回 true，因为它们彼此不相等，我将点击这里的 play 按钮，然后将控制台带到屏幕上，您可以看到它返回一个 true 值。

这就是你需要知道的关于等式运算符的所有内容。

接下来，让我们进入关系操作符，从大于操作符开始。

假设你有一个登录系统，你想限制一个用户可以登录的次数，你可以使用一个关系运算符，让我们从一个大于号开始。

首先，让我们暂时忽略这两个变量，创建新的变量，我们要做一个整数，我把它叫做 log 尝试，等于，假设它是 3。

然后让我们做另一个整数，比如说 log max 等于 5，对，所以我们能做的是归结为我们的披萨时间布尔，我们可以用 log Max 大于 log attempts 来代替它。

这实际上是检查 log Max 是否大于 log attempt。

鉴于 log Max 在 log 10 中是 5 加 3，这将为我们的披萨时间布尔变量返回一个真值。

为了向您证明这是正确的，我将点击开始按钮，打开控制台，您可以看到我们有一个真实的值。

这是大于运算符，我们也可以使用小于运算符，它将检查记录的最大值是否小于日志尝试，如果为真，则返回真值，如果为假，则返回假值。

但是还有一个大于等于运算符，它只检查是否大于或等于日志尝试次数。

最后，当然，还有小于等于运算符，它将检查 log Max 是否小于或等于 log attempt。

在 Python 中，这些操作符一点也不变，我只是加快速度，我要做的是，log 尝试等于 3，log 最大值等于 5，然后到我们的 pizza 时间，进去做 log 最大值大于 log 尝试。

然后我将运行它，只是为了证明这也是一样的。

瞧啊。

我想讲的最后一个运算符是结束运算符和 or 运算符。

所以在这里你真的可以看到金条的力量被展示出来。

好吧，让我们回到以前的场景。

因此，我们将把它改回 ABC 1234，过去实际，然后我们过去输入的也是 ABC 123。

让我们把这个布尔运算替换成 P，t，等于，也就是我们的等式运算等于 P，a。

那么，如果您想检查密码是否匹配，并且他们处于一定的登录尝试次数之下，该怎么办呢？我们可以使用 and 运算符，在 C 调中是这样的，你只需输入一个 and 符号，然后再输入一个百分号。

这就是终端操作符。

然后你可以做，你可以写出另一个布尔运算。

所以我们要写日志尝试小于日志最大值。

所以这个完整的布尔函数问的是，它几乎是在问，我们输入的密码实际上匹配吗，以及两个&符号。

我们的日志尝试是否少于我们的日志最大值。

如果为真，如果这两个都为真，那么返回一个比萨饼时间的真值。

但是如果它们中的任何一个是假的，不管是哪一个，那么返回一个错误的值作为我们的比萨饼时间。

为了向您证明此操作是如何完成的，我将按下顶部的“start”按钮，打开控制台，您可以看到这里有一个真正的值。

只需要，只需要其中一个是假的。

我将把它改为通过，即 ABC 124。

然后我们点击“保存”,然后我将点击“开始”按钮，将常量窗口带到这里。

你可以看到我们有一个假的，即使有一个是真的，我们要求 n。

这意味着它们都必须为真，披萨时间才会为真。

顺便提一下，我通常喜欢尽可能多地使用括号。

当谈到像这样的布尔运算时，我喜欢段，因为它让我更容易阅读，当我浏览代码行时，像我这样的括号知道，好的，这个布尔运算，我看到它，我知道还有另一个，它让我更容易理解，甚至有时我会在整个布尔运算上加一个括号。

这让我读起来容易多了。

我不知道对你有没有帮助。

但这是一个你可以使用的提示。

在 Python 中，语法略有不同。

所以，我要加快速度，把它改回 pass 123，或者不要 pass，就像 ABC 123 一样。

抱歉，ABC 123。

然后我们把它改成 P t 等于 p a，或者你没有告诉我，我们在 Python 中的方法是，你只需要输入单词 And，log attempt 小于 log Max，伙计，这已经变成一个很小的窗口了，也许我应该发出一点。

酷毙了。

现在我要通过点击播放按钮来证明这一点。

你可以看到我们得到了一个真实值。

但同样，它所需要的只是这些布尔运算中的一个为假。

整件事都变得虚假。

我要证明的是，我们将把它改为 4，点击播放按钮，我们得到了一个返回给我们的假值。

但是，如果你想让他成为一个仁慈的社交媒体开发者，对吧，在那里你不在乎他们是否犯了这样或那样的错误，只要你犯了其中一个错误。

首先，永远不要这么做。

但是如果你想让它这么做，你只需要把 AND 操作符改成 or 操作符，然后 C 调，这是通过替换两个&符号来完成的。

有两条垂直线。

我不确定这些角色叫什么。

但是你用两条竖线来代替它，它在反斜杠键所在的键上，刚刚查了一下，我猜它叫做竖线字符不知道 been，无论如何，这将检查其中一条是否为真，我们知道其中一条为假，因为 p t 不等于 PA。

但是第二个是正确的，因为日志尝试次数小于最大日志次数。

为了证明这将返回真，因为其中一个为真，我将点击开始按钮，然后将窗口移过来，你可以看到我们返回了一个真值。

在 Python 中，or 操作符有一点不同，就像你在 Python 中为 an 操作符写出 And 一样，你必须为 Python 中的 or 操作符写出 OR。

现在在 Python 中，or 运算符有点不同，就像你必须为 an 运算符写出一个，你必须为 Python 中的 or 运算符写出 OR。

当然，为了证明这是可行的，我要点击这里的播放按钮。

你会看到我们得到了一个真值。

所以我希望你能明白是什么让布尔在编程领域如此强大。

仅仅从这个简单的例子来看，我们已经有了一个布尔值来检查日志尝试，你知道，就是实际输入的密码。

然后我们可以把这个布尔值嵌入到另一个布尔值中，继续增加复杂性。

数十亿是非常强大的，理解它们非常重要。

是的，你们知道了，伙计们。

这几乎是你所需要的一切。

开始数十亿人的编程。

所以字符串是几乎所有程序中最重要的组成部分之一。

为什么？嗯，很可能在部署的某个时候，甚至在开发过程中，有些人必须与您的应用程序进行交互。

因此，你的程序需要以某种方式与它们交流。

很有可能你会使用字符串，最基本的字符串是一系列的字符串，因此被称为字符串，你可以用任何语言写任何单词，你可以写任何数字，甚至任何密码，你能想到的，让字符串变得非常强大。

这差不多就是所有的弦，它们真的很容易让你理解。

在使用它们的时候，有几件事是非常值得了解的。

所以我们来看几个例子。

现在我们要用两种不同的语言来做这些例子，因为我认为这对于你来说非常有帮助，看看它们是如何比较和对比的。

另外，你几乎可以同时学习两种语言。

左边是使用 it Visual Studio 的 C sharp 语言，如果你打算做 windows 开发，或者如果你想使用一个叫做 unity 的流行游戏引擎来制作游戏，你应该会感兴趣。

右边是使用 ID Visual Studio 代码的语言 Python，如果你想进入更一般的编程或数据科学，你应该会感兴趣。

好了，这里我有两个全新的控制台项目。

如果你不知道如何制作一个新的控制台项目，也不知道是 C sharp 还是 Python，或者两者都不知道，一定要看看描述中的视频。

叫做如何安装广告。

在视频的最后，我会告诉你怎么做。

好的，首先我想了解一些基本的字符串语法。

首先，我要删除这几行，它们是你全新的 C 调作品的素材。

然后我要初始化一个字符串，举个例子，我们称它为 a。

如果你想给一个字符串赋值，你必须用双引号。

然后你可以输入任何你想要的字符串，然后在末尾加上分号。

就 C 语言的尖锐字符串语法而言，你只需要担心双引号，确保它们是双引号。

现在在 Python 中，我们有了更多一点的自由。

所以我要在这里初始化一个字符串。

然后我要给它分配 hello。

正如你所看到的，我使用了双引号，就像我们在 C sharp 中做的一样，但是你也可以初始化一个字符串并赋值 Hello，同样使用单引号，在 Python 中这两个都被算作一个字符串。

为了证明我会先打印 a 再打印 b。

开始了。

就这样，你好，你好，这两个都是字符串。

事实上，今天，你会发现，这种自由，单引号中的双引号，可以赋给一个字符串，非常好，Yossi。

接下来我要讲的是，字符串是由字符组成的数组。

为了证明这一点，在左边的 C 调中，我们简单地做一个控制台点写线。

然后我要做 a，我要用括号，在它们之间放一个零。

这是数组中的一个元素。

同样，字符串是一个字符数组。

所以我只是要求从一个字符串数组中得到第 0 个元素，这是 0，这是 1，这是 234。

在几乎所有的编程语言中，数组总是从零开始。

现在，为了证明这一点，我将点击这里的开始按钮，在非洲运行它，我总是忘记做控制台点读取键，这只是确保终端保持打开，直到我们按下一个键并点击开始。

然后把这个拿过来，你会看到 A 数组中的第 zeroeth 元素也是一个字符串，也就是 h。

它非常灵活，我们可以把任何我们想要的放在这里作为练习。

所以如果我们想得到零，我们仍然可以数这是零，然后他是 1234。

所以如果我们在这里输入四个字符，那么当我们点击开始按钮时，你会看到它打印出来哦，事实上，这种技术是很多游戏开发者在试图创建一些聊天系统时使用的，每次弹出一个字符，他们会在这里写出整个对话，比如你好，欢迎。

欢迎来到我的商店。

然后他们会写一些函数来接收一个字符串，然后我们会抓取每一个字符，然后我们会打印出一个字符，中间有一个停顿，可能比这个停顿少很多，比如 1/10 秒的停顿，然后打印下一个 1/10 秒的停顿，打印下一个，以此类推。

在这里做一点补充，在 Python 中，我要去掉 B，我们只需要保持双引号的约定来匹配 C 调，这里给定 B，然后我实际上只需要打印。

我们先从零开始，按播放键，你会看到我们得到一个 h。

再一次，我们要做一个 4。

所以我们可以按下播放键，哦，我忘记保存了，然后按下播放键。

然后我们有哦，就在那里，在两种语言中几乎是一样的。

同样，只要记住所有的字符串只是一个字符数组。

接下来我要讲的是对字符串使用加法运算符。

为了证明这一点，在左边，当 C 调要去掉这个 Bechtel 时，我们有了它，从右边去掉它，我就在这里做一个加号，等于，哦，加号等于，这是加法运算符，这两个在这里，当你看到加号等于，那是加法运算符，加号等于，然后在它上面加一个感叹号。

然后我会把它打印出来，你会看到它会是 Hello，然后我会在结尾加上一个小感叹号。

启动它，把它拿过来，瞧。

这个过程有一个很酷的地方，因为字符串 a 只是一个字符数组，使用加法运算符就相当于说，取这个字符串，把这个字符加到它的末尾。

然而，酷的是，你不仅仅局限于添加字符。

同样，单引号表示一个字符，双引号表示一个字符串。

例如，我们可以加上世界末日。

现在，当然，我们要改变这些单引号或双引号，让 C 夏普知道，这现在是一个字符串。

接下来要做的是，它会把这个字符数组中的每个字符都加进去，或者这个字符串会把每个字符都加进去。

为了证明这一点，我要点击开始，然后我们把这个拿过来，瞧，你好，世界。

好了，现在在 Python 中，我要把它恢复到初始状态，然后做一个加号等于感叹号。

现在，当然，这只是将 Hello 字符串，A 字符串，添加一个感叹号，然后用 play 按钮打印出来，确认下来。

这正是我们所得到的。

现在，我想再次重申，Python 不在乎你对字符或字符串使用双引号还是单引号。

不像 C 调，我们可以把 world 放在两个单引号中间，然后我们点击 play，它会打印出来，不会给我们任何错误或任何问题。

这只是使用解释语言的好处之一。

如果你想用单引号或双引号，那完全取决于你自己的判断。

但是为了保持一致，在这节课中，我们也要对字符串使用双引号，在我们继续之前，你可能会想，如果我们可以做加法运算符，那么如果我们使用乘法运算符或减法运算符，除法运算符呢？不幸的是，加法运算符是唯一可以在字符串上使用的算术运算符。

所以不要太高兴。

现在，转义字符是真实的字符，所以在这节课中我不会深入讲解它们。

但是它们对 string sing 非常重要，因为 string 是一个字符数组。

但是让我们快速地看一下其中的几个。

所以我就在这里补充几个。

我要用反斜杠。

然后我要做反斜杠 T，我要做反斜杠，单引号，反斜杠双引号，然后反斜杠，反斜杠。

这五个是最流行的转义字符。

第一个，这是新的一行，这是一个标签。

这是单引号，这是双引号，这是反斜杠。

我要把这个打印出来，让你们看看他们是怎么做的，点击开始按钮，然后把这个带过来。

所以你有了新的一行，这是反斜杠，N，然后是制表符，这是反斜杠，t。

然后是单引号，是反斜杠，单引号，双引号，是反斜杠，双引号，然后是反斜杠，是反斜杠。

这些坏男孩很方便，因为他们帮助你做一些事情，当你第一次开始编码时，可能不是很直观，例如，当你只有字符串时，你如何按 Enter 键，或者你如何使用字符串的双引号，需要双引号来初始化，例如，我要把这些都去掉。

给你举个简单的例子。

我将使用转义字符，在末尾加上双引号，这样看起来不错。

杀字开头双引号。

这将看起来像引号中的 hello，我将点击星号按钮，然后将它带过来。

这就对了，你已经通过使用转义符从字符串的语法中转义了。

在这节课中，我又简单地讲了一遍。

但是如果你想了解更多，可以看看我上的关于角色的课，我会更深入地了解他们。

因此，在我继续学习 Python 之前，我将把它恢复到原来的样子。

现在在 Python 中，转义字符的语法是完全一样的，我要去掉这一行。

然后把它们加起来，就像 C 调，反斜杠 t，反斜杠，单引号，双引号，反斜杠。

一定要把双引号加回来。

是的，你知道了。

一件很酷的事情是，它实际上是一个 Visual Studio 代码，它将改变字符串中转义字符的颜色。

所以这很方便。

我把它打印出来，我们会得到相似的结果。

现在你知道了。

好了，接下来我要讲的是插值字符串。

现在，没有理由让一个人如此热爱一个语法，但我绝对热爱插值字符串。

在这里，你也会喜欢的。

事情是这样的。

好吧，假设我们想传递一些信息，对吗？我要摆脱这一切，做喜欢你好，名字。

你好吗没错。

现在，假设你不想在这里有真实的名字。

你想有一些用户输入的名字，对不对？所以让我们为这个名字创建一个新的字符串。

初始化字符串，使其名称等于 job Brill 的。

没错。

你通常会这样做，你会把这个字符串分段，你会这样做，你会在这里删除它，你会加上，加上名字，然后再加上字符串，对吧，所以你有三个不同的字符串，但是你必须把它分开，把字符串变量放在这里，然后你通过加上字符串的后半部分把它缝合在一起。

我是说，听着，这行得通，能完成任务。

但是当你不得不写很多这样的字符串时，随着时间的推移，它真的会变得很累，很容易感到沮丧。

但这正是插值字符串发挥作用的地方。

首先，为了向您证明这是可行的，我将点击开始按钮。

然后我们把屏幕放在这里，砰的一声，它就工作了。

但是有了插值字符串，现在我们不再需要分割字符串了，我们可以把它组合成一个字符串，然后简单地在字符串的开头加上美元符号。

这个美元符号表示字符串现在是插值的。

你可以在字符串中间添加括号，在括号里面，你可以添加变量。

所以当我把这个打印出来的时候，看看这个。

这太令人兴奋了。

现在你有了同样的东西，只是你不需要打断你的弦，我喜欢插值的弦。

所以你可以想象一下，如果你有一个很长的文本，或者类似的东西，你有很多变量需要注入到文本中，你可以简单地把一个字符串变成一个插值字符串，你知道，到处都加上括号和变量。

而且节省了很多时间。

它真的很美。

我喜欢它。

不，运行 Python，这差不多是一回事。

我要设置一下，然后打个招呼。

你好吗 Python 中唯一的区别是，你将使用 F 来代替$1 符号，来表示插值字符串。

现在我们有了创建者初始化器名变量。

这将是贾布里勒·罗斯。

所以现在我们可以做同样的事情使用括号和 bada boom，点击 play 来证明给你看，你好，布里尔的《你好，如此美丽，我爱它》。

最后，我想介绍的是字符串数据类型附带的一些方便的函数。

为了演示这些方便的功能，我要去掉这一行，它不再需要了。

我要展示的第一个是小写函数。

所以我们可以做名字等于名字点，来降低 C 调，来降低方法，用调用来加分号。

然后我们来到这里，把名字打印出来。

接下来会发生的是，它会接受名字，然后它会把名字中的每个字符都小写。

它开始证明，把窗口拿过来，你会看到它已经把这个变成了所有的小写字母。

当你可能正在创建一个用户数据库或类似的东西，而你不想要重复的时候，这可能是非常方便的，那么不管他们传入你的数据库的名字是什么，你每次都要把它小写，然后只做一个匹配。

这是该功能最常见的用例之一。

但知道总是好的，以防万一你需要它。

接下来，我想向您展示它的反面，即大写字母函数。

我不是完全从头开始，而是将添加到我们的控制台日志记录中。

在第一行，我将重置名称，使其等于 Brill 的名称。

然后我们要做名字等于名字，to 到 upper 是 C 调的函数。

然后我要复制这个 Ctrl C Ctrl V。

现在，它将首先打印到控制台，它和所有较低的，然后所有较高的，并批准我点击开始按钮，并把它带过来。

瞧啊。

接下来，string 数据类型附带的一个非常非常有用的函数是 split 函数。

所以我要重置 name，我要说 name 等于，不过这次我做了一点不同。

中间的逗号是 jabril，因为我们要用它作为我们的字符，我们要用它来分开字符串。

因为我们把字符串一分为二，所以我们需要一个数组。

我会说，string，然后加上括号，加上一个注释，让 C 调知道这是一个数组，我们把它命名为 equals，然后我们把它命名为 dot split，然后我们传入字符，我们想用 split 作为逗号。

然后我们可以简单地到这里来，在控制台上点写一行，并命名为。

然后我们要做数组的第一个元素，也就是零。

这就是复制和粘贴。

然后，我们将打印该数组中的第二个数组元素，也就是一个，并批准它将拆分这个，我将在这里点击 start。

然后，我将打开窗口，您会看到它根据逗号、戳、实、漂亮来拆分名称。

我想展示的最后一个方便的函数是 contains 函数。

所以我要做名字等于，我喜欢宇宙飞船。

我不知道，我喜欢宇宙飞船。

然后我们需要一个布尔值，因为 contained 返回一个布尔值。

所以 Boolean，我们就说 does，它等于 name，点 contains，然后我们需要在这里传入一个字符串。

然后我们会说，这里面包含单词“the”吗，然后我们简单地做控制台点写行。

然后为了证明它确实返回了一个真值，如果它返回了，如果它在字符串中有单词 the，我会点击 start，然后把风带过来。

你可以看到最后一个返回了 true，是的，contains 是字符串数据类型的另一个非常强大的函数。

这是非常好的使用案例，知道这些总是好的。

在 Python 中，这与他们在 C 调中的做法略有不同。

所以我们在这里命名，我们要做名字等于，哦，等于名字，点。

低一点。

哼。

我们要在这里打印名字。

我们要把它打印出来，按下播放按钮，显示小写的名字，正如我们所期望的。

然后下一个，我要再做一次 name 等于 Brill 的，name 等于 name 点 upper 函数，然后再打印 name。

我要把它打印出来。

你会看到我们都是小写，然后都是大写。

现在，下一个字符串函数将真正展示 Python 如何在这方面比 C sharp 略胜一筹。

所以我们要做名称等于相同的刺拳，然后铁路。

然后在这里，我们要做的就是名称等于名称，点分裂，我们只是重新分配名称，它会立即把它变成一个数组。

很简单，重新分配它，然后我们所要做的就是打印名字，证明他们，然后点击播放按钮。

这就是它的刺拳轨道。

是的，就我个人而言，这是我最喜欢使用 Python 的原因之一，因为做事情很容易。

就像这样，而在 c#中，我们必须有我们的字符串名称，我们必须初始化一个新的字符串数组来保存我们将要创建的新的字符串数组，然后一次打印一个。

这就像三行代码。

容易，简单。

我喜欢使用 Python 的一个优点，也是最后一个优点是 contains 函数。

所以名字不等于我喜欢宇宙飞船。

然后到这里来，然后 do 初始化一个布尔值。

然后我们要做名字点。

包含，我会解释为什么这看起来很奇怪，然后，然后我们可以打印，好吧，只是为了证明作品可以按下播放，它不工作。

当然，那是因为我忘了保存它。

点击“保存”,然后点击“播放”,下来，这就是它的真实值，我真的想在这里暂停一下，强调一下 Python 真的不在乎你用双引号还是单引号。

如您所见，我们在这里对名称使用双引号，对它包含的字符串使用单引号。

它仍然返回 true，它只是解释。

现在，这一部分看起来如此丑陋的原因是因为这就是所谓的包装方法，或者更确切地说是方法包装器，在你编程的早期，理解这个术语并不重要，但本质上这是什么，Python 不希望你这样使用。

他们更喜欢你用 if，然后用 in。

所以如果字符串是一个名字，我们就打印。

Python 真的很自豪，你知道，作为一种语言，有点接近我们实际上，你知道，像人类一样使用语言。

所以这是我更喜欢你们使用的方式，这个，这个，这个方法。

为了证明我们要点击 play，我们将确保它被保存并点击 play，我们应该会看到两个 trues，我们这样做了，但没有，伙计们，这几乎是你开始用字符串编程所需要的一切。

char 数据类型很容易掌握。

char 是 character 的缩写。

如果你知道如何读和写任何语言，那么你知道什么是字符。

第一个字母，你的名字，字符字母，你的数学课字符，你在社交媒体 care 上过度使用的标签符号，reg dir，任何语言的任何字母，任何一位数，任何符号或符号，甚至一些字形，如阴阳符号或单个字符。

但这已经超出了本课程的范围，请记住这一点，当你想进一步了解它们的时候。

但是现在，只需要知道字符在今年的编程世界中非常重要，大量的数据类型与字符串重叠，因为字符串是由字符串组成的。

事实上，早在编程语言的早期，C 字符串甚至没有作为一种数据类型存在，相反，你必须创建一个字符数组，但字符串，如桌面下载 C 冒号反斜杠程序文件，你所有的应用程序都安装在这些文件中，都是计算机每天使用的一系列字符，使你的计算机体验尽可能流畅。

字符对于计算机能够向我们传达信息起着至关重要的作用。

差不多就是这些了。

现在从这里开始，没有太多的东西要看了。

但是让我们快速地看一下我们如何在两种不同的语言中使用字符。

因为我认为看他们如何比较和对比会很有帮助。

另外，你几乎可以同时学习两种语言。

所以在左边，它是一种使用 Visual Studio 的 C 语言，如果你想做 windows 开发或者想使用一个叫做 unity 的流行引擎制作游戏，你应该会感兴趣。

右边是使用 ID Visual Studio 代码的语言 Python，如果你想进入更一般的编程或数据科学，你应该会感兴趣。

好了，这里我有两个全新的控制台项目。

如果你不知道如何制作一个新的控制台项目，无论是 C sharp 还是 Python 或者两者都不知道，请务必查看描述中的视频。

叫做如何安装广告。

在视频的最后，我会告诉你怎么做。

首先，让我们看看一些基本的 char 语法。

所以在左边这里，C 调将移除空白控制台项目中的标准。

然后我要写 char 来转换我们的数据类型。

然后我们把变量叫做 a，我要给它赋一个 z 字符。

现在，这是你写一个字符的方法，C 调的字符，必须在两个单引号中间。

非常简单，我们可以在这里放任何我们想要的字符，我们可以放 a，我们可以放 j，我们可以放 8，我们可以放美元，你可以在这两个单引号之间指定任何你想要的字符，这就是 char。

现在在 Python 中给字符赋值有一点点不同。

所以我们可以像在升 C 调中一样，做一个相等的单引号 z。

然而，我们也可以做一个等号双引号，和 z。

Python 会设置其中任何一个，它会计算出这两个都是 char。

但是你会看到，如果我们回到 C 调，然后把它们从单引号改成双引号，你会看到我们会得到一个类型错误。

在 C 调中，它专门对字符串使用双引号，对字符使用单引号。

但是，在 Python 中，这是非常聪明的，我们正在计算，如果它是一个字符，或者一个字符串，这是解释型语言的好处之一。

为了证明这一点，我要把它变成变量 b。

然后我们要打印 A，然后我们要打印 b。

我们将点击右上角的“play”按钮，您会在输出中看到下面的内容，它会打印这两个内容。

河马小厨师，没问题。

我会很快把它变回单引号。

对吗？好了，现在基本的 char 语法结束了，我们来谈谈转义字符。

现在，什么是转义字符，毫无疑问会有一天，你可能会写出一些故事，或者创建一个名字列表之类的东西。

毫无疑问，你会遇到这样的问题，我如何按 Enter 键？或者按 tab 键或插入引号？我是怎么做这些事情的？这就是转义字符发挥作用的地方。

好了，现在让我们初始化一个转义字符，我要把一个新变量转换成一个字符。

然后在我的字符内部，我要打反斜杠。

现在从技术上讲，反斜杠是转义字符，就像转义字符的语法一样。

但我喜欢把整个角色称为逃避角色。

这是在，这是值得讨论的。

但是我要打反斜杠，其中是一个最常见的转义字符，叫做新行。

现在，我真的想让你明白转义字符有多重要。

如果我把反斜杠换成正斜杠，你看，他们会在这里得到一个错误，因为有太多的字符和字符文字。

单引号内的所有内容都被称为字符字面量。

而且每个字符只能放一个字符，这些数是 2。

但是当你输入反斜杠时，它被认为是一个字符，这又是一个换行符。

为了证明它确实如我所说的那样，我们要做控制台点写线，这只是为我们打印终端，我要打印 A，这是一个有 z 的变量，然后我们要对 A，B 做另一个女人，然后我只是复制并粘贴 Ctrl C Ctrl，V，然后我们要做另一个 A， 所以我们应该看到的是打印出来的 z，然后是新的一行，然后是另一个 z，我会运行它。

而且是在屏幕之外。

哎呀，我忘了我还必须做控制台点读取键，否则，控制台将不会保持打开。

单击“start ”,它在屏幕外，我将把它显示在屏幕上。

你会发现我们得到了我们想要的。

我知道有些人可能会等一下贾布里勒。

那不是我所期望的。

Z 的中间有两个空格。

让我们在这里停一下，真正检查一下我们程序的逻辑。

所以我们在 B 和 A 之间得到两条线的原因是，当你可能期望它们之间没有线的时候，因为函数本身是右线，它为我们做了一个新的线。

所以它打印出 z 等于 z 会打印出 z，他们会去新的一行，然后我会打印出新的一行，然后去新的一行，然后打印出 z，然后去新的一行。

我要再运行一次。

确认一下，打印出 a 到新行，打印出新行到新行，然后打印出 z 到新行，单词到此结束。

所以它完全按照我们的要求做了。

在这种情况下看起来有点奇怪，因为右行函数在没有告诉我们的情况下增加了一行。

好了，现在我们来看看左边 Python 中的转义字符，我要把 B 变量改成单引号。

我会用单引号，因为这两者之间更容易约定，我们要做一个新行。

我把它反过来做反斜杠。

你可以在 Visual Studio 代码中看到，它实际上会将转义字符稍微染成黄色，以帮助你提供更多的帮助，这很好。

Visual Studio regular 不想这么做，总之，new line，然后我们要打印出 a 然后我们要打印出 B，然后我要再复制粘贴一个 a。

然后让我们点击播放按钮来运行它。

你会看到，我们得到了和升 C 调完全一样的结果，你已经知道为什么会有两条线了。

接下来，让我们看一下连接字符连接，它只是简单地将一些不同的东西放在一起。

例如，如果你有三个不同的字符，你可以把它们连接起来，变成一个单词或者一个短语。

这就是连接的全部意思，所以要连接字符，你会觉得很简单，一个新字符 c 等于 a 加 a。

但事情没那么简单。

在 C sharp 中，当您将两个字符相加时，字符数据类型使用加法运算符将字符转换为 intz。

事实上，每个字符实际上都有一个整数值。

我们来看看这个图表，这是 Unicode 的 ASCII 图表部分，你可以看到每个字符都有一个十进制值。

所以我们可以以字符 2 为例，做二加二。

这将得到 100，这是一个小写的 D 字符。

但是要知道没有太多的用例，对于这样的操作，只要知道这是它如何在引擎盖下工作，这是很有趣的事情。

但是让我们继续。

所以你要做的是，把你的字符变成一个流，就像那样，然后你会得到一个错误。

因为同样，当你在字符上使用加法运算符时，它们会被转换，你试图把一个整数赋给一个字符串。

所以覆盖它的方法就是用双引号，然后加上，这就像一个快捷方式，可以快速的把赋值转换成字符串。

赋值也是变量右边的所有内容。

现在我们可以添加所有的字符，字符串 c。

所以我要做 a 加 b 加 a，也就是 z 加新行加 c。

然后我可以删除右边的两行。

然后我会添加 c 而不是 a，然后我会点击 play。

别把这个拿过来。

你可以看到现在有点不同了。

这是为什么呢？好吧，让我们来看看。

再次，右行，每次你调用这个函数，它都会返回一个新的行，然而，我们用的是一个字符串，只有一个写行。

所以除了我们涂过的那个，没有新的裂缝。

所以它确实打印 z，转到新的一行，然后打印另一个 z，正如你在这里看到的，打印 z 转到新的一行，然后打印 z。

这就是那里正在发生的事情。

编程的一切，总是你必须遵循的基本逻辑。

是的，现在在 Python 中，因为 Python 是一种解释型语言，它使得做这样的事情变得非常容易。

我们要做的就是做 c 等于，然后我们可以做 a 加 b 加 a，就这么简单，归结到我们的打印，我们不需要两个。

所以我就删掉两个。

取而代之的是用 C 替换 a，然后点击角落里的播放按钮保存。

你可以看到我们得到了完全相同的结果。

它打印出一个 A 到新的一行，对不起，它打印出一个 z 到新的一行，然后打印到另一个 z，就像你在这里看到的。

简单，容易。

上帝保佑蟒蛇。

这就是连接字符的全部内容。

现在让我们最后来看一看其余最重要的转义字符。

所以你知道反斜杠或者换行符。

还有一个非常重要的问题你也应该知道。

那就是反斜杠 r。

这是返回字符。

所以返回字符所做的，是返回到行首。

现在，回到过去，这对我们真的很重要，因为你会去一个新的行，但它不会回到开始。

就像你在上一句中停下来的地方，它会在相同的点上移到新的一行。

所以你必须调用返回转义字符来返回到行首。

那么，如果我们在控制台中运行它，会发生什么呢？让我们点击开始播放按钮。

我把它拿过来，你会看到我们只有一个角色。

还有那个 z。

即使我们有返回，发生的事情是，它打印出，它回到行的开始，再次打印。

为了证明这一点，我们可以把这个改成为什么。

然后只是为了额外的证明，我们可以改变，我们可以添加一个加号 u，并点击开始播放按钮。

你会看到发生了什么，它打印出 z，但是然后返回字符返回到 x，进入一行，然后它打印出 y。

然后它打印出一个你，基本上覆盖了我们的第一个字符，也就是 z。

现在在新的现代，你不会真的太多地使用返回字符。

不过，我提到它是因为知道可能会有一些情况，您可能会使用旧的终端或旧的引擎或类似的东西。

你正在失去你的意识，你在使用换行符，但是它没有返回到开始的句子。

在这里，这是返回字符使用，它将你所有的问题都会消失。

此外，在大多数新的现代终端程序中，下一个 y&z 的新行实际上是这样的，看起来也像新行，然后返回。

有趣的是，我们可以证明，如果我们使用 Python，用回车转义字符替换新的线形字符，点击“保存”,然后播放，您会看到我们得到的并不是我们所期望的，它实际上是一个新行，而不是回车。

这是因为我们用来解释代码的东西叫做代码运行器。

而 code runner 解释一个回车转义符的方式很可能是这样的，我无法验证。

但是这可能是它如何解释一个普通的回车符，因此，当我们有一个新的行和一个回车符时。

此外，如果您真的想了解 code runner 和 Microsoft Windows 附带的终端 PowerShell 之间的区别，您可以在资源管理器中右键单击 Python 脚本显示的名称，然后双击它来运行 Python 脚本。

哦，等等，对不起，我们必须在这里添加输入。

输入，我们将保持术语打开，双击 Python 脚本。

这就是我们想要的。

这就是两个不同的终端解释 interp breading 的区别对不起，代码。

好吧，我希望这不会太混乱。

再说一次，你不会真的担心，你知道，以这种方式使用字符。

通常，大多数人甚至不会与这么低级的字符进行交互，但是当你一直在处理字符串时，知道这一点是很好的。

是的，让我们继续。

下一个转义字符是反斜杠 t，它是一个制表符。

我要把它变回简单的字符串。

然后，我将点击“开始”按钮，将控制台带过来，您可以看到这是 z，tab z。

就像 Python 一样简单，都一样，只需要做一个反斜杠 t

我们按下播放键，你看到那个 z 了吗，tab z？简单。

所以接下来我们要看的转义字符是，引用，转义字符。

所以在某些时候，你可能想加上字符单引号，或者类似的东西。

你会说，好吧，很酷，单引号。

等等，我该怎么做？嗯，当然，简单来说，就是加转义符，反斜杠，然后单引号。

就在这里，这两段，反斜杠，单引号是单引号，转义字符。

当然，你可以用那四根弦做同样的事情。

比如说你想写一些代码什么的，你可以写反斜杠，然后是双引号。

这就是双重转义字符。

现在，如果我们运行这个程序，点击开始按钮，你可以看到，我们得到一些非常奇怪的带双引号的字符串。

单引号 z 只是一个演示，向您展示如果您真的需要，如何使用引号。

现在在 Python 中，实际上有点不同。

所以我们还是可以做同样的事情。

用反斜杠，哇，我要替换那个反斜杠，然后用单引号，得到我们的转义字符单引号，比如，你可以按 play。

哦，对了，说声抱歉，跑了。

哦，当然，我必须停止代码，右击输出，然后停止。

如果您有输入，代码就会运行，这只是一个快速警告，因为您再也不能点击播放按钮了。

但我会按播放键。

你会看到我们有一个叫做转义符的原罪，我用双引号来代替，同样的交易，对，按剧本，你看到了吗？哦，等等，我得保存它，抱歉。

然后点击播放。

你会看到他们有一个双引号，没什么大不了的，对吧？现在，Python 有一点特别，一点不同，那就是我们可以用单引号或者双引号来表示字符。

所以我们可以，你知道，如果我们想用双引号，我们就在外面用单引号。

然后我会保存并点击播放。

这是同样的事情。

但是如果是相同的，我可以把外面的引号改成双引号，就像这样。

在中间加上一个单引号，然后保存那个热门剧本。

你可以看到我们在两个中间有一个单引号。

这是解释语言的另一个好处。

这可能会节省你一点时间。

但只是是的，就是这样。

最后一个重要的转义字符实际上是反斜杠本身。

现在，你已经知道我要说什么了。

但是如果你想写一个反斜杠转义字符，你得做反斜杠转义出来，然后再做一个反斜杠。

所以两个反斜杠会给你一个。

为了证明这一点，我要把它从这里移走，然后我要点击开始按钮，把它拿过来，瞧，z，反斜杠，z。

在 Python 中，这是完全一样的，你只需要做反斜杠来转义，然后再做一个反斜杠。

我只想把它留着玩。

来吧，下来。

就这样，z，反斜杠，z。

就这样，伙计们。

这差不多是你开始用字符编程所需要知道的一切。

让我们来谈谈浮点数，为浮点值发送的浮点数，或者浮点数，两者都差不多，你可以认为是介于整数之间的数字或值。

因此，如果 1 和 2 都是整数，并且 2 紧跟着 1，那么浮点数就构成了 1 和 2 之间的所有数字 1.1 1.2 1.3 1.4 1.5 一直到 1.9。

但是不要搞错了，浮点并不仅限于一个小数位的值。

例如，圆周率是一个浮点数，通常表示为 3.14。

但是我们不知道圆周率实际上有多少个小数点，研究人员已经计算出圆周率的 2000 亿位数，并且还在研究如何计算更多的位数。

所以简而言之，两个整数之间的浮点值是无限的。

这是基本的两个浮点数，他们真的可以花一点时间去理解它们的整体。

但是现在，你只需要知道它们是介于整数之间的值。

现在让我们看几个例子，看看如何在代码中使用浮点数。

我们将通过使用两种不同的语言来做到这一点，因为我认为看到它们如何比较和对比会非常有帮助。

另外，你几乎可以同时学习两种语言。

所以左边是使用 ID Visual Studio 的语言 C sharp，如果你想做 windows 开发，或者如果你想使用一个流行的游戏引擎 unity 制作游戏，你应该会感兴趣。

右边是使用 Add Visual Studio 代码的语言 Python，如果你想进入更一般的编程或数据科学，你应该会感兴趣。

好了，这里我有两个全新的控制台项目。

如果你不知道如何制作一个新的控制台项目，无论是 C sharp 还是 Python，或者两者都不知道，请务必查看描述中的视频。

叫做如何安装 ID。

在视频的最后，我会告诉你怎么做。

好的，那么我想进入 floats 的第一件事是不同的操作符。

如果你对整数使用的操作符非常熟悉，那么你也会对浮点使用的操作符非常熟悉。

为什么？因为它们是完全相同的运算符。

所以直接跳到左边的 C 调，我要去掉这两行，不再需要它们了。

它们是每一个新的控制台项目的标准配置。

我要初始化一个浮点数。

所以我要打出 float。

然后我把它叫做 a，只是举个例子。

我给它打零分。

实际上，这一点也不好玩，因为零是整数。

所以我们来赋值，我们来赋值 7.77。

听起来很有趣。

现在，实际上，在我们开始讨论操作符之前，让我们稍微了解一下 Python 和 C sharp 之间的语法。

浮动的语法之间并没有太大的区别。

然而，在 C sharp 中，你必须通过在数值末尾加一个 F 来说明你的数值是一个浮点数。

这只是进一步的指令，告诉编译器应该如何处理这个数值。

简单地说，当你的代码被转换成指令，让计算机执行，当它到达这一行时，它读取流程。

它清楚地知道这是一个浮点值。

然而，有一个警告。

即使这个变量 A 是一个浮点变量，你也不需要总是把浮点加到你的值的末尾。

例如，如果你只说浮点数 a 是一个整数，七个浮点数会自动地把它们自己从整数转换成浮点数，因此，你不需要加上 f。

现在在 C 调中，你必须在浮点值的末尾加上 F。

在 Python 中，你不必这样做，你所要做的只是键入你想做的任何值。

Python 会为您处理剩下的事情。

Python 能够做到这一点，因为它是一种被称为解释型语言的语言，你所要做的就是给变量赋值。

它会解释变量应该是什么数据类型，这在很多情况下是很好的，因为你不必担心浮点值和整数之间的差异，你可以只做加法和乘法除法，而不必担心任何东西。

但是现在让我们从增量操作符开始研究一些操作符。

所以一个增量操作符做它所描述的增量，语法是这样的，一个加号，就是这样。

如果我们到这里来写一个控制台点写行，它会打印到控制台上。

然后我们还需要控制台 C 调的点读键，这样控制台就不会对我们关闭。

我将点击“start”按钮，打开控制台窗口，您可以看到，它只是将 1 加 1 递增到 7.77，得到 8.77。

这就是增量运算符。

根据我的经验，它不像是一个非常好的、方便的工具。

但是，如果你只是想在某样东西上加一个，而不是像这样全力以赴，它可以节省你很多时间。

其他运算符需要做你知道变量加号加号就大功告成了。

在 Python 中做增量操作符，你实际上不能在 Python 中做增量操作符，如果我们输入一个加号，你可以看到我们的 ID 显示为红色，让我们知道这不是 Python 中的东西，每种语言都不同。

而且 Python 就是不关心增量运算符，我估计。

现在假设你想在你的 float 中添加多个。你将如何着手做那件事？这就是我们下一个操作符，加法操作符发挥作用的地方，加法操作符的语法是这样的。

所以你想做一个或你的变量，你的浮点加等于，然后无论什么值，我们可以做加等于 1，做另一种方式，做一个增量运算符。

或者我们可以传入一个流，例如 3.33，F，我在这个值的末尾加了一个 F，因为它是一个浮点值。

如果我们运行这个，点击开始按钮，打开窗口，你可以看到我们得到了 11.1。

现在只是一个小音符，为了确保我们涵盖了所有的碱基，你可以看到它不发音为 11.1 F。

原因是因为 F 是只有计算机需要的东西。

一旦计算机看到代码读为 F，并看到它是一个浮点值，就没有必要把它返回给我们，因为我们是人，我们很聪明。

我们知道浮点数和浮点数的区别。

看着它在 Python 中使用加法运算符，语法上完全没有不同，在我们的例子中，a+等于 3.33。

同样，我们不需要添加 F，因为 Python 中的 Python 不关心 F 的远近，到这里来写 print a，然后保存它，点击 play 按钮，你可以在这里看到，我们将得到 11.1。

就加法而言，最后一种增加浮点值的方法是，做一些我称之为修改赋值的事情。

这不是一个技术术语。

我不确定它有没有。

但我喜欢把这个和常规作业区分开来。

所以我们要做的就是把这些都去掉，以消除困惑。

然后我们可以做 a 等于 a 加 3.33 F。

这就是修改后的作业。

现在哇，这里发生的事情看起来有点令人困惑。

别担心，让我带你走一遍。

首先你需要知道这是赋值操作符。

赋值操作符右边的所有内容都是首先计算的。

所以不管 A 的状态是什么，在这个例子中是 7.77，都要加到 3.33 上。

一旦结果被计算出来，它会把结果扔到赋值的左边，这又是一次桑拿，因为我们把 a 赋值为等于它当前的值，加上一些额外的值，我们只是更新它，实际上只是用一些不同的方法来做同样的事情。

但是作为开发人员，您可以选择最适合您的编程风格的方式。

为了向你们证明，这是完全相同的结果，点击开始按钮，打开窗口，你们可以再次看到，我们有 11.1。

和在 Python 中做修改赋值是完全相同的语法。

所以我要去掉这条线，以消除困惑，重新开始，我们可以做一个等于自身，加上 3.33。

为了证明这是完全相同的结果，我按了播放键，你可以看到嘣，又一个 11.1。

接下来，我们有几个减法运算符。

第一个是减量。

接线员。

你已经知道了增量运算符的作用。

所以猜测减量操作符做什么应该是很容易的。

是的，你猜它只是从你的浮点数中减去一。

就像增量运算符是加号，减量运算符是减号，这就是全部内容。

为了向你们证明它确实如我所说的那样，我将按下开始键，把风吹过来。

你可以看到，我们有 6.77，因为 7.77 减 1 当然是 6.77。

同样，根据我的经验，它并不是最有用的操作符，它可以帮助你节省一些写代码的时间。

但事实就是如此。

同样，在 Python 中，他们没有递增运算符，也没有递减运算符，我可以打出一个减号，你可以看到它变红了，这在这种语言中是不存在的。

所以如果你想从浮点数中减去多个，你可以用减法运算符。

语法可能和你想的一样，减去等于，然后是你的浮点值，或者你的整数值，不管是什么数值，只是为了证明省略开始按钮，打开窗口。

如你所见，7.77 减去 3.33 等于 4.44。

在 Python 中，减法运算符是完全相同的语法，我们将从负等号开始，然后是你的值。

为了证明你碰到了播放按钮，你可以看到我们有 4.43999999。

现在你们中的一些人可能会问，这个差异是怎么回事，C 调 4.44。

但是 Python 说 4.43999 bla bla bla bla，好吧，我就给它直接浮点值是魔鬼做的东西，重要的是要记住浮点是整数之间的值。

任何两个整数之间的空间都是无限的。

但长话短说，如果你刚刚开始，你现在不必关心这样的事情，在某个时间点，他可能会钻研一些需要非常精确的浮点精度的项目。

在那个时候，你可以拔掉你的头发。

但是现在，你只需要知道你看到这个的原因，因为在 C #中，夏普浮点到最近的数字，有更精确的数据类型。

但这超出了本课程的范围。

对于几乎所有应用，2 到 3 点的浮点精度通常就足够了，您无需担心这种精度。

这是我们计算圆周率的一部分原因，我想是千万亿位，但是我们经常把它四舍五入到 3.14。

因为通常，这是一个足够精确的估计。

继续，最后一种减法，当然是通过修改赋值。

做一个修改后的减法赋值，就是去掉这个，我们做 a 等于它本身减去 3.33 F。

为了证明这是可行的，正如我所说的，我按下播放按钮，把它拿过来，你可以看到我们有 4.44。

在 Python 中，语法和 C 语言没什么不同，我们可以做 a 等于自身减去 3.33。

我将保存点击播放按钮，您可以看到我们得到了 4.43999。

接下来是乘法运算符，每当你想对你的浮点数做乘法运算时，你只需要使用乘法运算符。

它的语法是这样的，比如说，A 乘以 2，当你运行它的时候，它只是把你的浮点数 A 乘以 2，然后证明给你看。

我点击“开始”按钮，打开窗口，你可以看到 7.77 乘以 2 等于 15.54，当然你乘以你的浮点数不一定是整数，也可以是浮点数。

当然，它必须是某个数值。

在 Python 中，语法和 C 语言中的完全一样，A 乘等于证明你点击播放按钮，你会看到我们有 15 点 54 分。

当然，我们可以做乘法的另一种方法是，如果我们做一个修改的赋值和语法，当然，你已经知道它等于 a 乘以，来证明给你看，我按下开始键，把风带过来。

我们还有 15 点 54 分。

在 Python 中，语法也没有什么不同。

我们可以做 a 等于 a 乘以 2，然后按播放键，再给你们看一遍，我们有 15 点 54 分。

现在有一件事你没看到我做，就像 a 乘以，原因是不像减法或加法，没有递增乘法。

这到底是什么意思？

继续，最后一个运算符是除法运算符，它的语法是这样的 a 除以等于 Alice 产生一个有趣的，比如说，2.15。

当然，这要做的是，取我们的 a float，然后除以 2.15。

为了向你们证明这一点，我将按下开始按钮，把窗口移到 a 上，我们看到我们有 3.613953，这将是一个有趣的反复说。

在 Python 中，除法运算符是完全相同的语法。

所以我们可以做一个除以等于 2.15。

为了证明给你看，我要按下播放键。

而且你可以看到 Python 差很多，我们有 3.613953。

这种情况一直持续下去。

我知道我有点妖魔化了 Python 中的长小数点，但实际上这是一件好事，他们给了你这种精度。

最后一种方法是使用修改后的赋值，它的语法是这样的，我要重新开始，让它不那么混乱。

等于 A 除以 2.15，f。

为了向您证明这一点，我点击开始按钮，打开窗口，您可以再次看到，我们有 3.613953。

是的，你知道了。

浮点在编程领域非常有用。

它们被用于许多不同的应用。

不要让我用浮点精度来吓唬你。

这是非常非常罕见的，你将不得不在这个世界上浮点。

通常，你不会超过 2 或 3 点的精度，在这个精度下，一切都很容易理解。

但是这几乎是你开始编程所需要知道的关于浮点的一切。

是时候说说数组了。

现在，数组最常与循环一起使用，但它们几乎可以在任何上下文中使用。

那么什么是数组呢？你可以把数组想象成一个容器，里面有固定数量的数据类型相同的变量。

事实上，它们只是保存变量的容器，你可以独立地使用这些变量。

在任何情况下。

假设您想用几行代码更新一个充满姓名的数组，或者轻松地将一个包含 100 封电子邮件的数组保存到一个文件中，而不必到处复制、粘贴和更改变量。

嗯，数组使得做这样的事情变得非常容易。

但是我们不要不停地举例说明数组是自切片面包以来最酷的东西，让我们直接看几个例子和用例，来学习如何在代码中使用数组。

我们将使用两种不同的语言来做这件事，因为我认为这对于你了解它们之间的比较和对比非常有帮助。

另外，你几乎可以同时学习两种语言。

左边是使用 it Visual Studio 的 C 语言夏普，如果你想进入 Windows 开发，或者使用一个流行的游戏引擎 unity 制作游戏，你应该会感兴趣。

右边是使用 it Visual Studio 代码的语言 Python，这应该会让你对更一般的编程或数据科学感兴趣。

好了，这里我有两个全新的控制台项目。

如果你不知道如何制作一个新的控制台项目，既不是 C sharp 也不是 Python 或者两者都是，一定要看看描述中的视频。

叫做如何安装 ID。

在视频的最后，我会告诉你怎么做。

好了，让我们来看看数组。

所以数组仅仅是一种数据结构，意味着它是一种你可以构造不同数据类型的方式。

所以我不会过多讨论如何使用数组。

否则，这个视频返回的只是一长串例子。

但是让我们开始吧。

所以我首先要讲的是 C sharp 和 Python 中数组的不同语法。

但是在我们开始之前，我想让你想象你正在做一个四人游戏，假设每个人都有相同的 HP。

所以让我们先在 C 调课程中创建这些变量，去掉这两行。

因为我不需要它们成为每个控制台项目的标准，我将初始化一个整数，我将它命名为 HP one，我将它赋值为 100。

然后我会再做一次，指定另一个，HP 等于 100。

然后 int，HP 三等于 100。

最后，蚂蚁惠普四等于 100。

太好了。

所以四个不同的玩家有四个不同的整数。

但是有了提升，就不需要四个不同的变量了，我们可以简单地在这里初始化一个新的整数数组。

其语法如下。

首先，我们要输入要放入数组的数据类型，当然，在我们的例子中，是一个整数。

如果你想表示这是一个数组，你只需要添加一个开始括号。

在括号中，这就是你如何表示一个整数数组数据结构。

在那之后，我们只需要输入我们的变量名，我把它叫做 hp。

然后我们必须给它分配一个新的整型数组。

然后在这里的括号内，你必须指定你的数组的大小，我们有四个玩家，所以大小是四个在分号处，你就完成了。

现在我们有了大小为 4 的整数数组，我们可以给数组的元素赋值了。

为此，我将来到这里，我将说“HP ”,然后在“back the starting bracket ”,然后键入元素编号。

再说一遍，是四的大小。

所以我们要从零开始。

好吧，所以你可能对我为什么从零开始而不是从一开始有点困惑。

在大多数编程语言中，数组总是从零开始。

为什么这么问？嗯，零是第二个数字，在计算中，零实际上是第一个数字，不是最大的数字。

但是一个非常简单的事实是，记住它是很重要的，因为如果你开始一个，你可能会得到很多错误，可能会想拔掉你的头发。

所以只要记住数组，零总是在第一位，我们在元素零处有一个 HP 整数数组。

我们要做的是给它赋值 100。

现在我们只想对所有四个元素都这样做，我将对元素一的 HP 赋值，再给它赋值 100，元素二的 HP，如果我会打字的话，然后给它赋值 100。

最后是 HP 元素 3，赋值 100。

敬它。

再次，快速注意，你可以看到我们在元素 3 上结束，尽管我们初始化了一个大小为 4 的整数数组。

但是如果你数一下，你会发现这里有四个元素，012 和 3。

当然，这种偏移的原因是因为在计算中，我们总是从零开始，这可能会在开始时让你犯很多错误，但最终你会习惯的。

我知道，如果这是您第一次学习数组，可能很难从概念上理解这里发生的事情。

所以我将带你们快速浏览一遍。

所以我们这里有一个长度为 901234567 和 8 的数组，这下面一行是索引或者元素。

它们都可以互换。

但是在这门课中，我会把它叫做元素，第一行是这个元素的值。

如果这是我们的 HP 整数数组，它将在这里结束，就在第三个元素。

在我们初始化整数数组后，它的值点被全零填充。

所以我们在代码中做的就是遍历数组，给数组中的每个元素赋值 100。

从概念上讲，这就是这场比赛的进展。

这不仅适用于整数，也适用于所有类型的数组，如果你有一个字符串数组，一个浮点数组，一个字符数组，等等。

现在我们不再需要这些变量了，我们可以删除它们。

然后，为了向您证明这些工作，正如我所说的那样，我将编写一个控制台点写入行。

因此，我们可以将它打印到控制台，我将使用 HP zero，然后使用分号，我只需点击几次 Ctrl C Ctrl V copy paste，然后简单地将元素 0 更改为 1、2 和 3，我们还需要一个控制台点读取键。

这是为了当我们运行终端时它不会关闭。

现在，我将来到这里开始，点击开始按钮，然后打开控制台窗口，向您显示我们有四个已初始化 100 Hp 的玩家。

最后我想说的是，如果你试图给一个数组范围之外的元素赋值，你会得到一个错误，如果我在这里对第四个元素做 HP 并给它赋值 100，点击保存按钮，然后播放，你会看到它给出一个异常，告诉我们没有这个大小的数组，所以你不能给第四个元素赋值。

所以要注意你的比赛规模。

现在在 Python 中，数组的语法有点不同。

Python 实际上甚至不使用数组，他们实际上使用一种叫做列表的数据结构。

你可以用两种不同的方式在 Python 中初始化一个列表。

但是第一个的语法如下。

首先，我们要写出变量的名字，当然是 HP，然后我们要做等号，然后开始括号。

然后你只需要在这里输入你想要的值。

同样，我们有一个四人游戏，有四个不同的玩家，每个人都有 100 hp。

所以我只需要写 104 次，用逗号隔开。

就这样，完成了。

然后为了证明给你看，我只需要写 print 然后传入整个数组，HP，点击 play 按钮，你可以看到它打印出一个有四个元素的数组。

正如您已经看到的，Python 在做这类事情时有很大的优势。

在 Python 中，我们真的想用 C 调做两行代码，我们必须做八个 910，大约九行不同的代码。

差别很大。

你可能会问，这是因为语言的设计哲学。

Python 被设计得更容易使用，并能处理大量数据。

升 C 有点像是从 C 语言中抄袭来的。

所以这确实延续了 C 已经在做的很多东西。

但是，是的，你可以选择，两者都有利弊。

这是在 Python 中初始化列表的第一种方法。

这是第二种方法，我将在这里做 HP 等于空数组。

然后它就像 HP 点追加一样简单，然后你的新括号，因为它是一个函数。

然后你想添加任何你想要的东西。

因此，我将为第一个玩家固定 200，然后再次追加，为第二个玩家再追加 100，HP 点 a 固定第三个玩家，最后 HP 点追加到最后一个玩家。

为了证明它确实如我所说的那样，我要打印，HP，回到主题,“播放”按钮，你可以看到我们打印了 100 ^ 100 ^ 100 ^ 102 次。这个函数是使用列表而不是数组的好处之一，你可以初始化空列表，然后添加到列表中。

C 调也有一个，但是有点超出了这个视频的范围。

好了，现在让我们来谈谈让一场比赛如此强大的真正原因。

这就是数组迭代。

好吧，这很酷。

现在我们在一个变量中包含了所有玩家的 HP。

但是如果不是四个玩家有 hp，而是有 100 个玩家有 hp，这就太快了。

这就是四个循环发挥作用的地方。

现在，数组和 for 循环就像汉堡和薯条，它们是完美的组合。

现在让我们引入一个 for 循环，这样我们就可以很容易地给数组中的元素赋值。

所以我会在变量上面。

我会写出 for 循环的语法。

所以 for，然后 int i 等于零，然后分号，然后如果 I 小于，我们有四个元素，所以四个，然后我们要做 i plus plus 如果这是真的，然后我们需要在这里添加我们的代码块。

然后我们只需要写变量。

这里有两个，所以我不会过多讨论 for 循环是如何工作的，因为我有完整的一集。

如果你感到困惑，可以看看这个，但本质上，在一个非常外行的层面上，for 循环，它只是简单地把 0 赋给我们的整数，然后如果它小于 4，那么我们就在每次更新的时候把它加进去。

然后我们可以用眼睛作为 for 循环中的一个位置。

因此，如果这个四循环小于四，那么它们将递增，这意味着它将在三停止，这正是我们所需要的，因为我们的元素也在三停止。

这意味着现在我们可以改变这个零元素和两个 I 元素，我们不再需要它们了，因为它们会被 for 循环设置。

除此之外，因为我们也使用数组元素进行打印。

我们甚至可以到这里来做控制台点写线，然后是 I 元素的 HP，这意味着我们也不再需要这些线。

现在证明这是可行的。

我点击开始按钮，然后我将把窗口移过来，你可以看到我们有四个 100。

现在回到我设置的场景，假设你想要 100 个玩家，而不是 4 个玩家，那么，我们只需要这里的数组大小。

我们将把数组大小改为 100。

我们还需要改变 for 循环的迭代，在 100 处停止。

为了证明这一点，我可以按下播放键，然后把风吹过来，结果是 100 100。

有点不好说。

但是在 Python 中有 100 个，几百个。

同样，这里的语法有点不同。

所以我要去掉这些线，因为让我们重新开始。

但是，我们有两种方法可以做到这一点。

第一种方法非常非常简单。

你要做的就是做 HP，然后分配一个 100 的数组，但是乘以 100。

为了向您证明这一点，我将打印 HP，然后按播放按钮。

你可以看到我们有 100 个一百的排列。

简单，容易入点神爱 Python。

第二种方法，有点复杂，嗯，到这里来初始化一个空数组。

然后我们要做一个 for 循环。

Python 中的语法有点不同，因为 I 在 100 的范围内，然后哎呀，然后我会到这里来。

因为它是空的，我说的是空数组，但它实际上是空列表，我们不会得到任何元素，我们只是简单地做点追加，和 100。

所以在这里，你要做的就是打印惠普。

我们应该得到两个 100 的数组。

数百个，这可能会有点混乱，但无论如何，让我们这样做，点击播放。

是的，还有第二个。

还有第一个完全一样的。

有两种方法，作为开发者，你可以选择使用哪一种。

是的，这是最基本的，所有你需要知道的，去使用这个数据结构。

从现在开始，任何事情都只是我在听一百万个不同的例子，你知道，不同的用例。

但是从这一点来看，作为一名开发人员，您需要发挥创造力，以不同的方式应用这种数据结构。

希望你开始思考一些不同的创造性的方法，关于如何使用数组或者数组和 for 循环。

但这是你开始关于比赛编程所需要知道的一切。

好吧，我们来谈谈大坏变量，甚至不谈编程。

信不信由你，它在编程中也有同样的意思。

变量是用来存储数据的东西，你可以把它想象成一个容器，但是你放在这个容器里的东西是可以改变和更新的，给它起个名字叫变量。

现在有一件非常重要的事情，在谈论变量时需要理解。

那就是数据类型。

如果你能理解变量，我想你会省去一大堆烦人的错误，尤其是在开始的时候，当你创建变量的时候。

所有的语言都需要知道你的变量的数据类型，同样，把变量看作一个容器。

假设我们有两种数据类型想要存储到一个容器中。

假设一种数据类型是爬行动物，另一种是鱼，你不能把这两种数据类型存储在同一个容器里鱼需要被水包围才能呼吸。

爬行动物需要炎热干燥的土地来保持舒适和存活。

那么，如果你把一只爬行动物放进一个养鱼的容器里，会发生什么呢？没错，你会得到一个类型错误，就好像你创建了一个意味着十亿的变量，并试图给它赋一个字符串。

那么我们实际上如何使用不同数据类型的变量呢？嗯，实际上很简单，让我们来看一些使用两种不同语言的例子，因为我认为这对于你来说很有帮助，你可以看到它们是如何比较和对比的。

另外，你几乎可以同时学习两种语言。

左边是使用 Visual Studio 的 C 语言夏普，如果你想做 windows 开发或者想使用一个流行的游戏引擎 unity 制作游戏，你应该会感兴趣。

右边是使用 Add Visual Studio 代码的语言 Python，如果你想进入更一般的编程或数据科学，你会感兴趣。

好了，这里我有两个全新的控制台项目。

如果你不知道如何做一个新的控制台项目，或者 C sharp 或者 Python 或者两者都做。

请务必查看描述中的视频。

叫做如何安装 ID。

在视频的最后，我会告诉你怎么做。

好的，让我们从初始化一些变量和 C sharp 和 Python 之间的语法开始。

所以在这里左边和 C 调，我要从去掉这两条线开始，因为我不需要它们。

它们是标准配置。

新的控制台项目。

现在让我们来看看五种主要的数据类型。

首先，我们有金条。

你可以初始化一个布尔值，就像这样，只要输入单词 bool。

然后不管变量的名字是什么？

在这种情况下，我们只做一个。

现在让我们初始化下一个数据类型，它是一个 char。

所以你可以通过输入数据的类型，同样是 char，和变量的名字来实现。

让我们按字母顺序继续，我把它命名为 b。

现在 char 已经初始化了，让我们初始化下一个数据类型，它是一个字符串。

我们这样做是通过输入数据的类型，它是一个字符串，然后保持字母顺序，我们就把它命名为 C，我想你可以在这一点上得到模式，我们要初始化下一个数据类型，这是一个 int，只是数据类型的类型，然后命名，变量，我们会保持，我们会叫它 D 保持顺序惯例。

现在，最后一个数据类型是 float，我们称之为 e。

现在你知道了，用 C 调初始化变量是非常简单的。

您只需键入您想要初始化的任何类型的数据，然后命名该变量。

这是针对所有数据类型的。

然而，这不是初始化变量的唯一方法，你也可以在初始化变量的时候给它们赋值。

为了给变量赋值，你必须使用赋值操作符，不要害怕，因为这很简单，只要输入等于，这里的等于，这个符号就是所谓的赋值操作符，它允许你给变量赋值。

对于这个布尔值，我们把它设为真。

让我们仔细检查一下，给所有的变量赋值。

所以对于我们的 char 变量，让我们把字母 J 赋给它。

然后对于我们的字符串，让我们把串 candy 赋给它我们的整数，让我们把数字 13 赋给它。

然后对于我们的流程，让我们指定一个 7.77。

f，那是，那是个好数字。

现在你知道了，这是另一种初始化变量的方法。

但是注意，最好的做法是用一个像 true j 这样的赋值来初始化你的变量，例如，这些是赋值。

原因是，就像前面的例子一样，如果你在没有赋值的情况下初始化它们，有时它们会返回 No。

如果你使用这个变量，你可能会遇到一些奇怪的情况，你会得到错误。

所以最好的做法是在初始化变量的时候给它们赋值。

现在在 Python 中初始化变量，这要简单得多，但有一点需要注意。

所以 Python 摒弃了强制转换变量的想法。

就像这里，在 C sharp 中，我们必须把 a 变量转换成 bool 数据类型，把 B 变量转换成 char 数据类型，在它旁边输入数据类型，这就是所谓的数据转换。

然后 Python，你不需要这么做，我们要做的就是简单地输入变量的名字，然后给它赋值。

所以如果我们想得到一个布尔值，我们只需要给它赋一个真值。

Python 语言是一种被归类为解释型语言的语言。

这意味着 Python 根据你赋予变量的内容来解释变量的数据类型。

事实上，我们给 a 变量赋了一个真值，当你运行 Python 脚本时，Python 会知道 a 变量有一个布尔数据类型，因为我们给它赋了一个布尔值。

这适用于所有变量。

例如，如果我们只是复制我们在 C sharp 中所做的，b 等于 j，C 等于 candy，然后 d 等于 13，然后等于 7.77，Python 会将所有这些变量解释为它们适当的数据类型。

这是有好处的。

为了检验这一点，让我们回到升 C 调。

现在，事实上你必须在 C sharp 中显式地转换你的数据类型，当你初始化那个变量的时候，它就被固定在那个数据类型上了。

举个例子，如果我想下来，比方说，我不想让 a 变成 char，实际上我想变成公牛。

我做不到布尔 b 等于真。

例如，它会告诉我们这个变量已经被定义了。

所以我们不能重铸变量的数据类型。

我们能不能给标签重新分配不同类型的数据？答案是否定的，它会告诉你，你不能把它从原来的数据类型转换成新的数据类型，相当于说这个变量已经有数据类型了。

然而，在 Python 中，因为它是一种解释型语言，我们可以对变量做任何我们想做的事情。

举个例子，我们可以这样说，我想让 B 等于 true，它不会给你任何误差。

无论如何。

为了证明这一点，我在这里写一个打印函数，打印 A，哎呦，A，B，C，D，E，你会看到我们会得到 A 和 B 的两个 trues。

让我们保存并点击播放按钮。

还有瓦拉。

现在，虽然这种数据类型自由语法看起来很酷，但是不要误解我的意思，它真的很酷。

这是很大的灵活性，尤其是如果你知道如何使用它，对不对。

但是如果你不小心的话，它也会引起一些问题。

在学习 Python 和这种数据类型自由语法的早期，我发现自己遇到的许多常见问题是，我从来没有真正确定一些变量是什么数据类型。

我试着在某些情况下使用它们，但这真的让我很困惑。

另一个问题是，没有什么可以阻止你轻松地重新分配一个变量，例如，当你不想结束 C 调，他们会告诉你，如果你的脚本中已经有一个同名的变量。

它还会告诉你尝试改变变量的数据类型，如果你愿意，这两种方式都可以作为一种安全措施，但两种方式都有自己的优缺点，这取决于你决定哪种方式最适合你，我认为非常重要的一点是在初始化后给变量赋值。

所以在 C sharp 中，你只需要转换一次变量的数据类型。

事实上，你只能对你的变量的数据类型进行一次强制转换来证明这一点，我要去掉这一行，因为我不需要它。

让我们试着把 a 变量重新初始化为一个有假值的碗。

所以我要转换我们的数据类型，我要做 bool，然后 a 等于 false。

你可以看到，它告诉我们这个变量已经被初始化了。

本质上，就是这么说的。

当然，赋给变量的值必须是该数据类型可以接受的值。

例如，除了 true 或 false 之外，只有 Boolean，例如，当然，在 Python 中，因为它是一种解释型语言，这没有问题，我们可以简单地做一个等于 false。

如果我们想让 C 等于假，我们也可以让 b 等于假。

为什么停在这里，我们也可以做 d = false。

我们甚至可以做 c 等于假。

因为这一切都将在我们运行脚本时被解释，现在我在顶部制作了播放按钮，您会看到我们得到了所有 false 和一个 777。

因为我没有在这里做 II 对不起，等于假保存，点击播放按钮。

你看，所有的错误都是在你运行脚本的时候被解释的。

这就是变量的要点，它们真的很容易让你理解，只是你必须记住，这些都是容器，里面有某种特定数据类型的数据。

最后，我会给你们一个简单的例子。

这样你就能更好地理解变量是如何应用的，以及它们是如何有用的。

所以想象你正在做一个关于 boss 战的游戏。

而这个 boss 只能承受伤害，要么是伤害，要么是闪现，要么是眩晕。

它必须处于某种状态。

所以我要创造一个场景。

去掉所有这些线，不需要它们重新开始。

首先初始化一个 int，我把它叫做敌人 hp。

例如，这等于 50。

然后在那里得到布尔回忆这种伤害。

只能说它已经受伤了。

所以伤害等于真实。

得到另一个布尔值，我们称之为闪烁，闪烁等于真。

然后让我们打球。

晕，就说晕吧。

而且姑且说不晕。

其实并不重要。

我将用 if 语句来创建这些状态。

如果受到伤害，那么我们就要对敌人进行比如说 HP 减等于 4 的攻击。

如果我离开屏幕，如果我在那还有什么在闪烁？哎呀。

如果它在闪，那么敌人的生命值减 4。

最后，不好意思，看看有没有什么头晕的。

那么敌人的血量减也等于 4。

好吧，我来解释一下这里发生了什么。

就像我说的，我们有，让我们说你正在做一些与老板战斗的游戏。

唯一能让 boss 承受伤害的方法就是把伤害当做受伤、闪光或眩晕。

现在，这些对我想要创建的例子都不重要。

但它至少给了你一个场景的概念。

现在想象一下，你对你的游戏做了一些测试，你发现 for 的伤害不足以让游戏变得有趣。

你需要更高的伤害。

好吧，你必须回到你的脚本，改变敌人的负血量等于你知道，让我们假设 7 个敌人的负血量等于 7，在任何负血量等于 7 的情况下。

假设你在所有的脚本中都引用了伤害，你需要做多少次就做多少次。

这是一个变量非常有用的用例。

因此，你不必这样做，只要你在脚本中多次引用，你就可以直接在这里找到你的变量，初始化一个新的整数，把它叫做 damage，并把它设为 7。

然后你所要做的就是遍历你的脚本，给你刚刚初始化的整数添加一个引用。

所以我的续集统计，哎呦，损害，损害和损害。

现在让我们假设你重新测试了你的游戏，你会说，哦，等等，仔细想想，7 有点太高了，我需要把它降低一点。

好吧，不用再重复一遍，浏览你所有的脚本，修改你提到 HP 减伤害的地方，你所要做的就是把一个变量改成你知道的，一个更小的数字，比如说 6。

现在所有这些引用都已更新。

现在你的伤害值是一个变量，你现在有了更多的力量，你可以降低你的眩晕状态，比如说，如果他眩晕，那么我想做的是伤害等于 3，比如说，如果他眩晕，我想让它变弱很多，这将在它闪烁时更新，你做这条线，或者你做这条线。

当然，我会用 Python 快速地做这个例子，它会去掉所有东西，我不需要它。

我要做敌人，HP 敌人 HP 等于 50。

然后赫兹等于真闪烁等于假然后晕。

等于假。

我倒过来说了，这是真的。

然后我们要做的是，如果赫兹，那么敌人，HP 负等于伤害，我想我们需要伤害他们，直接看实际例子，负等于 6。

然后如果，如果它在闪什么，那么敌人的 HP 减等于伤害。

而如果晕，呜呜，晕，那么伤害等于三。

为什么不仅仅是为了好玩，我们可以运行这个，我会在它结束的时候运行所有的 print 和 PHP，看看我们会得到什么。

我没有预先计算这个，我们得到 38。

非常好。

如果我们用 C 调打印这个，我们会得到相同的结果。

我要去控制台点写线。

然后这个敌人 hp 是什么。

然后哎呀，类似的图标，然后我们还需要一个控制台点读取行。

所以当我们运行它时，控制台不会对我们关闭。

我在这里的开始按钮，然后把控制台拿过来，你可以再次看到，我们有 38 个非常好的。

现在你知道了。

同样，变量在概念上很容易理解，但是当你开始使用不同的数据结构，比如数组等等时，它们会变得更加强大和复杂。

但是这几乎是你开始编程和考虑变量所需要知道的一切。

好的，if 语句是程序设计中的一种条件语句，意思是它检查是否满足某个条件，if 语句最简单的说法是 if this than that，事实上，程序设计中的 if 语句实际上是 if This Then That 语句的简写，事实上，大多数人甚至没有意识到 if 语句的一个最重要的教训是，如果某事为真，它们就激活 If 语句，如果某事为假，则不激活 If 语句。

现在请记住，因为理解这一事实可以让你在调试 if 语句时省去很多麻烦，所以你只需看看 if 语句，问问它返回的是真值还是假值？老实说，没有比这更复杂的了。

如果你曾经问过 If 问题，你会理解 if 语句，现在让我们看看如何用代码编写 IF 语句。

我们将通过使用两种不同的语言来做到这一点，因为我认为这对于你来说非常有帮助，你可以看到它们是如何进行比较和对比的。

另外，你几乎可以同时学习两种语言。

左边是使用 it Visual Studio 的 C sharp 语言，如果你想做 windows 开发或者想用一个叫做 unity 的流行游戏引擎制作游戏，你应该会感兴趣。

右边是使用 IDA Visual Studio 代码的 Python 语言，如果你想深入了解一般编程或数据科学，你应该会感兴趣。

好的，这里我有两个全新的控制台项目，如果你不需要制作一个新的控制台项目，无论是 C sharp 还是 Python 或者两者都不需要，请务必查看描述中的视频。

叫做如何安装 ID。

在视频的最后，我会告诉你怎么做。

好的，今天的实验将会非常简单，因为一旦我向你们展示了 if 语句的布尔秘密，你们马上就能明白，并且 if 语句不会有任何问题。

正如我提到的，大多数人甚至没有意识到 if 语句的秘密是，如果它们的值为真，它们实际上只是在欺负运行一些代码块的人。

但是在我们深入研究之前，让我们先来看看 C sharp 和 Python 之间的一些语法差异。

好的，从左边的 C 调开始，我要去掉这两行，因为我不需要它们，它们是任何新控制台项目的标准。

我要写出一个 if 语句。

在 C 调中要做到这一点，你需要做的第一件事就是写出单词 if，然后你要加上括号，然后在这些括号里面是你要放布尔值的地方，所以我要在 C 调中放 true，真值是小写的。

然后在它下面，我会写一个大括号。

在大括号中，这是 if 语句为真时运行的代码块。

现在你知道了。

这就是你如何用 C 调写 if 语句，你可以在这里写任何你想要的命令。

所以你可以这样做，我不知道控制台点写线，如果你想的话。

然后我们可以放 Hi，比如你可以随便放。

只要返回真，括号中 if 语句的布尔值为真，这段代码就会运行。

现在，在我们继续学习 Python 之前，我只想提一件非常重要的事情。

C 调的 If 语句必须有圆括号，布尔值必须在圆括号内。

如果你要删除它，你可以看到我们会得到一个语法错误，因为 C sharp 中的 If 语句又必须将布尔值括在括号中。

现在在 Python 中，if 语句的工作方式是完全一样的，但是语法有点不同。

要用 Python 编写 if 语句，我们首先要写出单词 if，然后只需输入 true。

而 Python 中的真值是大写的。

所以不要忘记这一点。

最后，我们必须用分号结束，我想在这里暂停一下，做一下区分。

是的，Python 不使用括号，而是使用冒号。

这个冒号告诉 Python，if 语句的布尔值，已经完成了。

因此，继续用 Python 来编写 if 语句的代码块，这与 C sharp 的做法有点不同，我们在 C sharp 中表示代码块的方式是，我们使用从开始的两个大括号，并将所有命令放在这两个大括号中间，所有命令都在 if 语句下。

但是在 Python 中，我们必须转到新的一行。

我们用缩进来代替括号。

所以我们举个例子。

这里我们可以像印刷一样。

然后嗨。

现在你知道了。

所以下一个自然要问的问题是，我如何在 if 语句中添加更多的命令？答案很简单。

实际上，if 语句下缩进的任何内容都将计入 if 语句代码块中。

举个例子，我要敲 Ctrl C Ctrl V，只是给你们举个例子。

这里有四个不同的打印函数，都在 if 语句代码块中，因为它们都缩进了，都在 if 语句下。

所以下一个自然要问的问题可能是，我如何退出这个代码块？我的意思是，我不希望我的整个程序，都依赖于这个 if 语句。

答案是在 Python 中，缩进支配一切。

举个例子，如果你想退出这个代码块，你所要做的就是下来，然后回到常规缩进，把它们打印出来。

比如说。

为了证明这是可行的，正如我所说的，我将按下播放按钮。

你会看到我们得到很高很高的数量。

然后我会把它设置为 false，让你看到我们把它设置为 false，这个代码块不会运行，你不会被打印出来，我点击播放按钮。

就在那里我们只有我出局了。

你可能会问的另一个问题是，if 语句中的 if 语句呢，比如嵌套 if 语句，我想在 C 调中，你很容易做到，你只需要在 F 中写另一个 F，然后再写一个代码块，如果你想的话，你可以再写一个。

这种情况会一直持续下去，直到你厌倦为止。

但是在 Python 中，这可能不是很明显，所以我会展示给你们看，你们就在代码块中，再次缩进一个缩进，你们只需要做另一个 if 语句。

例如，冒号，然后只是另一个缩进。

只要你需要，你就保持这种形式。

这里我们要做另一个 if 语句。

嗯。

打印另一个 high。

这就是全部了。

这是我留给你们的最后一样东西。

所以在升 C 调中，我会把这些都去掉，这样就不会那么混乱了。

在 C 调中，如果你有一个代码块，你可以把它留空，如果你愿意，你就不会有错误，不会有问题。

升 c 调不会抱怨，你会没事的。

但是在 Python 中，你不能这么做。

所以如果我去掉所有这些行，只有 if 语句，然后我们会在语句后面的符号中返回。

你可以看到，如果我们在这里处理我们的问题，你会看到我们得到一个错误。

这是不允许的，你至少要放点东西进去。

所以你知道，如果你只是想写一些 if 语句，只是为了以后填写。

例如，如果你想的话，你可以只放一个，它什么也不做。

是的，你们知道了，伙计们，这是你们的 if 声明。

我只是想让你们记住我们的小秘密，条件是 if 语句检查它们只是一个布尔值，同样，如果布尔值返回真，代码块就会运行，如果布尔值返回假，代码块就不会运行。

因此，你可以创建一些非常复杂的金条，然后把布尔型放在 if 条件中，从最简单的程序到最复杂的程序。

这几乎是每个程序在幕后工作的方式。

如果你想学习如何更好地使用 If 语句，我强烈建议你去看看我上过的布尔运算课，我会详细讲解如何在现实世界中使用金条，一旦你创建了布尔运算，你就可以像这样把它嵌入到 if 语句中。

是的，这几乎是你开始编程时需要知道的关于 if 语句的一切。

事实上，你正在看一个关于 elf 语句的视频，我会假设你知道 if 语句是什么，或者换句话说，If This Then That 语句是伪装的，但如果语句本身非常有限，例如，假设你造了一个机器人为你准备午餐，你告诉它如果他们有辣驴，Delia's 给我一个你的机器人去吃午餐，他们没有任何辣 jack case 给我们。

所以它不会给你任何东西。

现在你不能对你的机器人发火，因为它完全按照你的指示去做了。

计算机是非常字面上的，但这就是 if 语句参与犯罪的地方，L 语句就派上了用场。

先说什么是精灵语句？用最通俗的定义来说，L 语句是一个条件语句，如果初始条件不满足，它就会运行，它们对编程非常重要。

回到我们的例子，一个 L 语句将相当于告诉你的机器人，如果他们是辣杰克的情况下，迪莉娅的愚蠢的一个给我任何东西，你的机器人再次去吃午饭，他们没有任何辣杰克的情况下，达利斯，初始条件没有得到满足。

所以你可以得到任何东西。

老实说，elf 语句不会比这更复杂了。

它们是让你理解的非常简单的概念。

现在让我们看看如何在代码中正确使用 L 语句。

我们将通过使用两种不同的语言来做到这一点，因为我认为看到它们如何比较和对比会非常有帮助。

另外，你几乎可以同时学习两种语言。

所以左边是使用 ID Visual Studio 的 C sharp 语言，如果你想做 windows 开发，或者想用一个流行的游戏引擎 unity 制作游戏，你应该会感兴趣。

右边是使用 ID Visual Studio 代码的 Python 语言，如果你想学习更一般的编程或数据科学，你应该会感兴趣。

好了，这里我有两个全新的控制台项目。

如果你不知道如何创建一个新的控制台项目，既不使用 C sharp 也不使用 Python，或者两者都不使用。

请务必查看描述中的视频。

叫做如何安装 ID。

在视频的最后，我会告诉你怎么做。

好的，就像 if 语句实验一样，这个 else 实验会很快很简单，因为没什么大不了的。

那么我们如何使用 L 语句呢？首先要知道的是，如果没有 if 语句，l 语句是毫无用处的。

它们 100%依赖于 if 语句。

为了在左边和 C 调上演示这一点，我要去掉这两条线，因为我不需要它们。

它们是每个新主机项目的标准配置。

然后我会写出 else 然后是我们的代码块。

但是你可以看到我们有一个错误，但是就语法而言，我们做的一切都是正确的。

那么我们做错了什么？同样，l 语句本身是完全无用的。

他们需要一个 F 语句来依赖。

所以要解决这个问题，我要在 else 语句上面写 if，然后让我们真的做 false，然后我们的代码块。

然后你就把它修好了。

如您所见，我们没有任何错误，没有任何问题，一切都很好。

让我来解释一下我们之间的关系。

这里有 if 语句，这个代码块只有在圆括号内的布尔值为真时才会运行，但实际上我们把它设置为假，它会运行并执行这个 elf 语句。

换句话说，如果这个语句为真，这意味着括号内的内容，如果返回真，那么运行这段代码，否则，运行这段代码。

就这么简单。

为了演示这一点，我将写入控制台。

所以我想用控制台点写线。

然后在这里，我会放一个 A 计划，然后我会在 L 操作系统的这里，然后写控制台点写线。

我要执行 b 计划。

然后，当然，我需要添加一个控制台点读键，这样终端就不会在我们按下一个键之前关闭。

嗯。

出于演示目的，我将把它更改为一个真值，然后单击这里的“start”按钮，将窗口带到这里。

你可以看到它打印出了 A 计划，非常直观。

原因是，因为我们说如果为真，如果括号内的东西为真，我们说为真，那么它会打印出来，事实上它为真，它忽略后面的一切。

现在，假设我们的 if 语句，Boolean 返回一个 false 值，记住逻辑，这是检查括号中的内容是否返回 true，然后运行这段代码，否则。

如果没有，那么运行下面的代码。

你可以看到，Visual Studio 的 ID 给了我们一点提示，这是不可及的，因为它有点降低了 alpha，比代码的其余部分更暗，这是一个很好的提示，只是证明了这是可行的。

让我点击开始按钮，把控制台窗口带过来。

你可以看到它打印出了 B 计划，正如我们所料。

现在在 Python 中，L 语句的工作方式完全相同，当然，语法有点不同。

就像 C 调的 L 语句一样，我们也不能在 Python 中单独写一个 L 语句。

举个例子，我会放一个 0，然后点击播放按钮。

您可以看到它返回了一个语法错误。

同样，这样做的原因是 l 语句需要依赖于 if 语句。

所以我要在 O 语句上面写一个 if 语句，我要写 if false。

然后我会打印出来，我只做一个。

然后你可以看到，我用了错误的 false，在 Python 中是 C 调的 false，在 Python 中是 true，两者都是大写的，这总是让我出错。

但是我们在那里修正了我们的 l 语句错误。

为了让这个看起来像我们在 C 调中做的程序，我要在这里写计划 A。

然后我会在那里写 B 计划。

同样，Python 可以使用单引号或双引号，这并不重要。

但是为了让它看起来更清楚，我将对两者都使用双引号。

当然，如果我把它改成真值，那么它会给我们一个计划 A，当我点击 play，哦，我当然从 print 得到 do。

很多。

我的错误，让我只是改变这个真正的快速打印，计划 A，然后点击播放。

你可以看到我们有 A 计划，没有 b 计划。

当然，如果我们将它改回 false，然后点击“保存”,再点击“播放”按钮，您会看到我们得到的是 B 计划。

因为它再次检验这个陈述是否为真。

执行这段代码。

否则执行这段代码。

就这么简单。

最后我想告诉你们的是，每个 if 语句只能有一个 l 语句。

例如，如果你用 C 调，执行 if 语句，从这段代码中运行这段代码，然后我们链接到另一段代码。

你可以立刻看到我们得到了一个语法错误。

因为即使你考虑逻辑，这也完全没有意义。

你是说如果这是真的，那么运行这是真的。

或者运行这个，然后或者运行这个，这在逻辑上没有任何意义。

所以这就是你做不到的原因。

当然，在 Python 中，我们不能连续执行两个 l 语句，也不能输入 l 然后打印计划。

天哪，该死的，C 计划保存点击运行，看到我们在这里得到一个语法，因为它完全没有意义。

是的，你知道了。

当与 if 语句配对时，l 语句是非常方便的，当然，你的 if 语句只是普通的 if 语句，在 Jag bullions 中，可以进行一些非常强大的逻辑运算。

关于 L 语句，你需要知道开始编程的一切。

好的，你有 if 语句和 Elif 语句，但是你也有 else if 语句。

从第一个显而易见的问题开始，什么是 else if 语句，所以你制造了一个机器人为你准备午餐，你告诉它如果他们有辣 jack case，dalias 给我另一个给我任何东西，你的机器人去给你准备午餐，他们没有任何辣 jack case，Delia's 和某种回报，给你一个芝士汉堡，但你意识到餐馆不提供薯条，现在你希望你有不同的东西，因为没有薯条和苏打水的芝士汉堡是什么？纠正这样的东西。

你可以引入 if 语句，其他犯罪伙伴，else if 语句。

那么它是如何工作的呢？回到我们的例子，使用 elsif 语句，你可以告诉你的机器人，如果他们是辣驴，给我一个，如果他们提供薯条，给我一个芝士汉堡，否则给我任何东西。

有了这个程序，你可以把你的机器人送到任何一家餐馆，如果条件合适，它肯定会给你想要的东西，因为 if 语句几乎只是金条，它们本质上就像备用 if 语句。

这就是 else if 语句的全部内容。

现在让我们看看如何在代码中正确使用 elsif 语句。

我们将通过使用两种不同的语言来做到这一点。

因为我认为看到它们之间的对比会对你很有帮助。

另外，你几乎可以同时学习两种语言。

左边是使用 Visual Studio 的 C 语言 sharp，如果你从事 windows 开发，或者想使用一个叫做 unity 的流行游戏引擎制作游戏，你应该会感兴趣。

右边是使用 ID Visual Studio 代码的语言 Python，如果你想进入更一般的编程或数据科学，你会感兴趣。

好了，这里我有两个全新的控制台项目。

如果你不知道如何制作一个新的控制台项目，也不知道是 C sharp 还是 Python，或者两者都是，一定要看看描述中的视频。

叫做如何安装 ID。

在视频的最后，我会告诉你怎么做。

好的，就像 L 板一样，elsif 实验室将会非常快速和简单。

因为再一次，这没有什么太多的。

那么我们如何使用 else if 语句呢？就像一个 L 语句一样，如果 elsif 语句完全依赖于一个 if 语句，而 elsif 语句中没有 if 语句，就完全没有用。

为了在 C 调的左边演示这一点，我将删除这两行，因为我不需要它们，它们是任何新控制台项目的标准。

我只想写点别的。

然后是 if 然后是 true，然后是我的代码块。

如你所见，我们得到一个错误，因为这是无效的语法，同样，没有 if 语句，elsif 语句是完全没有用的。

在 Python 中，除了 elsif 语句的语法，这是完全相同的事情，只是有一点不同。

所以不用显式地输入 else，你只需输入 e，L，I，f，else，如果是 shorten，你可以在这里输入 true，然后是冒号，然后是逻辑。

但即使我在这里加入一些逻辑，假设我要置零，你可以看到我们得到了一个错误，我要点击 play。

它告诉我们这是无效的语法。

同样，elsif 语句本身是完全无用的。

为了解决这个问题，我要在 elsif 语句的正上方，添加一个 if 语句。

所以如果为真，那么我们接着做控制台点写线。

然后我们将说计划 A，然后我将下来这里做同样的控制台点写行，然后计划拼写计划 B，分号，谁将在 Python 中做同样的事情，以便他们在同一点。

让我们看看真实，然后我们要做印刷规划。

记住，缩进非常重要。

所以我们要回到这里。

如果这是真的，他们想实施 b 计划。

那么这一切都是一样的吗？这是说如果这个陈述为真，那么运行这段代码。

否则。

如果这个语句不为真，但是这个语句为真，那么运行这个代码块。

在 Python 中，工作方式完全相同。

如果这个陈述为真，那么在这里运行这段代码。

Else 如果这个语句不为真，但是如果这个语句为真，那么运行这段代码，对于 elsif 语句来说，在一个点上输出一个差异，这是最小的差异。

但是 Python 确实以在各方面都进行了优化而自豪。

这包括你要为 elsif 输入多少字符。

升 C 调只有四节，你要打七节，包括空格。

还是那句话，微乎其微，但差异还是差异。

现在，elsif 语句的真正力量开始发挥作用了。

elsif 语句如此有用的原因是，您可以添加任意多的其他 if 语句，没有任何限制。

如果我来到这里，抓住这个，然后点击复制，然后粘贴，然后粘贴，然后粘贴，你可以看到我们没有错误，没有问题，没有问题，你可以做任何次数。

在 Python 中，没有区别，比如你可以抓取这个复制粘贴。

哎呀，你想做多少次就做多少次。

他们会注意你的缩进，确保你的缩进总是正确的。

因此，我认为触及操作的顺序以及它的重要性是很重要的。

所以对于任何 if 语句树，它总是首先检查第一个，如果第一个不为真，那么它会转到下一个。

如果下一个不是真的，那么它会去下一个，它会继续下去，直到它得到一个是真的。

比方说，这个为真，然后它会运行这段代码，然后退出，它甚至不会碰到 elsif 语句。

我可以给你演示一下。

我把它改成 C 计划，D 计划，e 计划。

假设这里的布尔校验返回一个 false。

假设这个也返回一个 false，这里的这个也是？我们应该期待它会检查这个，得到一个假的 elsif，检查那个，得到一个假的 l。

检查那个，得到一个假的，Elsa，检查这个，得到一个真的，然后我们运行这段代码，应该会打印出 d 计划。

为了证明我会按下开始键。

然后我会带来哎呀，我们忘记添加控制台。

控制台点读键，以便终端不会关闭，直到我们按下一个按钮。

然后我将开始，然后带来交叉和 Wallah，正如我们所料，我们已经制定了 D 计划。

当然，这在 Python 中也是可行的，我可以证明给你们看，只要把它改成 E，我把 B，C，D 搞砸了吗，E 就是 m，a，E，f，然后我们可以把这个设为 false。

然后我们可以把它设为假。

然后我们可以把它设为假。

我们应该会再次打印出 D 计划，按下播放键，我们会看到 D 计划。

我认为对你有用的最后一点信息是如何合并实际的 elf 语句。

假设你有一些变量，对吗？你有一个钱的变量。

你有这样的第一个语句，如果我有，我不知道 1000 美元，然后执行这段代码。

否则。

如果我有，比如说 750 美元，然后运行这个代码块。

如果我有超过 500 个，运行这个，如果我有超过 250 个，运行这个。

如果我有超过 100 个，那就运行这个。

假设你必须在控制台上打印一些东西，对吗？好吧，如果你做了所有这些检查，你有 0 美元，你永远不会打印任何东西，但这就是 else 语句发挥作用的地方。

在 if 语句树的最后，你要做的只是简单地添加 else。

我想做一个控制台点写线。

计划什么是 F 计划，这就是你要做的。

现在注意你的 elf 语句必须在 if 语句树的末尾。

不可能在别的地方。

但是在最后。

我的意思是，即使你从逻辑上考虑，这也是唯一有意义的事情。

这就是所有其他的陈述。

最后要注意的是，else if 语句和像这样的 if 语句没有什么不同，这是完全一样的。

唯一的区别是 else if 语句依赖于 if 语句。

记住这个简单的规则，你就可以轻松使用 elsif 语句，这就是你开始编程 else if 语句所需要的一切。

对于循环，是时候学习所有关于循环的知识了。

对于任何程序员来说，这些都是非常重要的知识。

那么什么是 for 循环呢？简单地说，for 循环是运行一段代码一定次数的捷径。

比如说，你有四个电话号码，你想给每个号码加一个区号。

不用硬编码四次电话号码一加等于区号，电话号码二加等于区号，电话号码三加等于区号，等等，你可以用 for 循环写一次逻辑，用更少的代码行完成同样的工作。

在这方面，for 循环非常强大，因为它们的伸缩性非常好。

因此，对于电话号码来说，这并不是一件坏事，你可以很快写出电话号码 1234 加区号。

但是现在想象一下，不是四个电话号码显示在区号二，想象一下你有 4000 个电话号码，你可以想象必须写出电话号码 1234567，一直到 4000+等于区号 4000 次，这是多么多余。

所以让我们来看看如何使用 for 循环来做这样令人惊奇的事情。

我们将通过使用两种不同的语言来做到这一点，因为我认为看到它们之间的比较和对比对你们来说非常有帮助。

另外，你几乎可以同时学习两种语言。

左边是使用 ID Visual Studio 的 C sharp 语言，如果你对 windows 开发感兴趣，或者想使用一个叫做 unity 的流行游戏引擎制作游戏，你应该会感兴趣。

右边是使用 IDE 的语言 Python，这是一个 Visual Studio 代码，如果你想进入更一般的编程或数据科学领域，你应该会感兴趣。

好了，这里我有两个全新的控制台项目。

如果你不知道如何制作一个新的控制台，project new，C sharp 或 Python 或这两者，请务必查看描述中的视频。

叫做如何安装 ID。

在视频的最后，我会告诉你怎么做。

好了，for 循环，for 循环是编程中非常有用的工具。

所以让我们从一些语法开始。

现在，c#中 for 循环的语法可能有点难记。

但这并不太难，你只需要记住这三件事。

但是首先，我要删除这两行，因为我不需要它们成为 C sharp 中每个新控制台项目的标准，所以我们可以删除它们。

这是你必须记住的，我会把它写在评论里，这样更容易记住。

所以有几个正斜杠。

所以首先你要写出单词 for，然后是括号。

现在在这些括号中，这个 for 循环分三步进行。

所以让我们通过添加两个分号来分段。

最后，首先要记住的是初始化变量变量。

我会把它缩短到初始化，因为我们很快就会用完空间。

然后你要记得做的第二件事是设定你的条件。

你要做的最后一件事就是发出命令。

只要你记住了，这三件循环的事情对你来说就很容易记住。

现在我们来看一个例子，如何真正初始化一个 for 循环，我会到下一行，我会写出 for 这个词。

然后我要做的第一件事是初始化一个变量。

现在，大多数人在这一步初始化的最常见的变量实际上是一个叫做 I 的整型变量。

你通常会给它赋值 0。

但是你可以给它分配任何你想要的。

实际上，你可以在这里初始化你想要的任何数据类型的任何变量，但是我们一会儿再回来。

接下来我们要添加的是条件，它只是问，我们希望这个 for 循环运行多长时间。

这个空间中的条件实际上只是一个布尔值。

让我们在这里写一个布尔值。

假设只要小于 10，我们就想跑。

对我们来说听起来不错。

然后我们需要添加的最后一件事是命令和什么命令，我们只是简单地指示我们的计算机做一些事情。

所以如果我们初始化一个名为 I 的整数，并给它赋值 0，我们说只要小于 10，我们就希望这个 for 循环运行。

那么在这里，我们可以说，我们希望 I 在每次循环结束时，简单地加 1，使用增量操作符，它只是在我们添加了增量操作符的变量上加 1。

最后，我们来到这里，为 for 循环添加代码块。

现在，这个循环每次运行，它都会运行代码块中的所有内容，也就是我们在这两个括号中添加的所有内容，然后在循环结束时，它会返回到顶部，当它返回到顶部时，它会简单地检查我们的条件，它会问我是否小于 10？如果为真，那么它将从这里退出。

它不会再运行这个循环，然后转到后面的行，但是如果为 false，它会执行我们的命令。

然后它会再次运行这段代码，继续运行，直到它检查到这里，这是假的，然后再次退出，运行 for 循环之后的任何一行。

所以现在用 Python 来做 for 循环，实际上有很大的不同。

Python 没有这种语法，你先初始化一个变量，然后设置一个条件，然后给出一个命令。

实际上，事实上，可能要简单得多。

但是它的语法如下。

首先你要键入单词 for，然后你要键入 I，for i in，然后你需要定义一个范围。

假设是 10，我们也要用同样的 10。

然后你在这里写冒号开始你的代码块。

然后在这里，你可以把任何你想要的命令放进去。

这就是在 C sharp 和 Python 中初始化 for 循环的方法。

然而，我想很快回到 C 调的初始化，指出一些真正有趣的事情。

现在，我想在大多数情况下，对于这种语法的 for 循环，你会发现大多数开发者会先初始化一个叫做 I 的 int，然后把它赋为零，然后就像这样，如果 I 小于某个数，那么我们就对它使用 increment 运算符。

但是我想在开发的早期放弃你的想法，因为这是 for 循环的实际规则。

但是为了理解我将要去的地方，我想我需要首先向你们展示一个传统的 for 循环是怎么回事。

所以我要在这里写一行。

我要打印出 I 的值。

当然，在这里，我需要写一个控制台点读键，这样终端就不会关闭。

我们一运行它，我就会来到这里，按下开始键。

然后，我会打开控制台窗口，查看我们正在进行的操作。

如你所见，I 被初始化并赋值为零，然后我们一路递增 increment，直到 I 不再小于 10。

然后我们退出 for 循环。

很简单。

但是这里又说了初始化。

所以我们可以随意初始化。

实际上，这不一定是结束，如果你想再次运行它，我们可以初始化一个 float。

你可以看到我们得到了完全相同的结果。

但是为什么停在这里，而不是使用增量运算符，我们可以做+等于点 5，我们想从一开始就展示这个结果，你可以看到我们是 0，然后我们增加+5，然后再增加+5，一直到 9.5。

当然，这还不是全部，如果我们想的话，我们可以初始化一个字符串，并把它赋值为等于空字符串。

然后这里我们的条件是只要我，然后我们可以通过点击点号来抓取长度小于 10 的字符串。

这么说吧，然后我们想做 I 加等于，比如说 z。

然后我们可以在这里运行这个 for 循环。

开始按钮，打开窗口。

我们可以看到，它创造了这个有趣的 for 循环，只要你在这里遵循这个约定，天空就是极限。

因为 Python 没有语法，你仍然可以实现它，但是你必须用其他创造性的方法来实现它。

还有，我也没有向你证明这在 Python 中行得通。

所以我会说得很快。

我要打印，然后我会运行这个，当播放按钮时，你会看到它打印出 10 范围内的所有数字。

这就是你使用 for 循环的基本方法。

至于你如何申请循环，那是你自己创造性的解决问题的努力，我会让你自己解决。

但是，我将留给您一个用例。

所以我继续为我们的场景写下了程序，因为否则，看着我打出来的将会是一段很长的沉默。

但我会带你走一遍。

但是首先，想象你已经收集了 700 个文件，上面有一堆糖果数据。

现在您有了这些数据，您需要将它们都放在一个地方。

现在你可以打开一个文本文件，手动将其添加到另一个文件中，然后打开另一个文本文件，手动添加下一个文件，然后添加下一个文件，添加下一个文件，但这对于我来说已经很累了，只是口头说说而已，你可以写一个带有 for 循环的程序来代替。

首先我们初始化一些变量。

第一个是一个包含所有文件名的字符串数组。

接下来我们有主数据字符串，我们要用 Python 编译所有的数据，我们做同样的事情。

我们初始化一个名为 filename 的字符串数组，里面有 700 个文件，然后我们的主数据字符串将所有的数据编译到这个数组中，然后我们进入 for 循环，在这个循环中我们初始化一个名为 I 的整数，并给它赋值 0。

这里有一个条件，差不多就是说只要 I，也就是 for 循环中的位置，只要它小于文件名字符串数组中的文件数。

然后我们想要继续循环。

然后，当然，我们的命令，我们只是想增加整数 I，然后在代码块中。

所有这些都是假的，我只是为这个场景编的。

但是这里有一个很大的假设，我假设你是数据收集者，你很擅长命名你的数据文件。

你拥有的每个数据文件都被命名为 candy，space，data space，然后是任何数量的数据文件，都是. txt，这很重要，因为如果你的数据文件被命名为更混乱的名字，那么我们就失去了使用 for 循环的能力，所以我们只能说你很擅长这样命名你的文件。

哦，澄清一下，这个美元符号意味着这个字符串是插值的。

这是一个插值字符串。

如果这让你困惑，看看我的字符串课，我会深入研究它，我们在这里做的是在 AI 元素处取文件名数组，这是我们在循环中的位置。

然后我们把这个文件名赋给那个位置，或者文件名数组中的那个元素。

然后在下一行，我在这里做了一个假函数，它接受文件名的名字，它应该读取数据。

但是这里什么也不做，只是返回。

就像一些带有实际数据名称的字符串，但这是一个真实的函数。

它存在于许多图书馆中。

但这超出了这个视频的范围。

所以只要知道这个，这个，这个就在这里，代表着真实发生的事情。

所以我们将文件名传递给 read data 函数，read data 函数将打开 rewear 所在的文本文件。

然后我们将它添加到主数据字符串中。

然后在 Python 中，我们做同样的事情。

我们在这里有一个 for 循环，在文件名的长度范围内，只是为了快速地解释一下这意味着什么。

所以 length 函数返回一个容器中的多个项目。

所以这个文件名是一个包含 700 个条目的列表。

所以当我们运行文件名的长度时，它将返回 700。

所以对于 700 范围内的 I，将是我们的 for 循环。

然后我们进入代码块。

这里，同样，我们有我们的文件命名约定，我们只是假设你擅长标记数据。

每个文件都被命名为 Kenny data，然后不管数字是多少，但它不是从零开始，而是从一开始，所以我们必须在 I 上加一，因为 I 确实是从 dot txt 开始的。

然后我们把它赋给 AI 元素的文件名列表，它的值从 0 一直到 700。

这是匹配的，因为他们都用眼睛看这里和这里。

最后，我们做了一个名为 read data 的伪函数，它除了从返回 Kenny 数据之外什么也不做，不管文件名是什么，但我们会运行这个函数，传递文件名。

假设这是一个真实世界场景，那么这将打开 txt 文件从其中获取数据。

然后我们将它添加到主数据字符串中。

最后，我们来到这里，打印主数据。

为了证明这些工作正常，我将来到这里，点击开始按钮，升 C 调，打开窗口，您可以看到我们拥有所有这些糖果数据。

看看这些糖果数据。

哇哦。

然后我用 Python 来到这里，点击播放按钮，然后出错。

哦，对了，对了。

所以 Visual Studio 代码有一个非常奇怪的地方，嗯，这并不奇怪，它很有帮助。

但是如果你选择了某样东西，然后按下播放按钮，它就会运行。

就像什么都没发生一样，然后按下播放键。

给你。

你可以看到 a 生成了所有的数据。

哇哦。

是的，你知道了。

这是一个创造性用例，其中 for 循环非常有用。

但是有大量不同的 for 循环用例。

但是我要花一整天坐在这里列出一堆不同的循环，但是你现在知道如何写你自己的四个循环了。

所以现在你可以想出真正有趣的方法来申请循环。

这就是开始 for 循环编程所需要知道的一切。

我们来说说 while 循环。

while 循环显然是循环的一种，它们与 for 循环有很多共同之处，除了它不是多次运行一个代码块，而是在某个条件为真时运行一个代码块，因此得名 while 循环。你会发现 while 循环在 for 循环中不太常见，但是偶尔你会发现 while 循环的完美用法。

所以对于 for 循环。

例如，当我做任何种类的机器人项目时，我发现 while 循环使用起来非常方便。

让我们看看如何在代码中使用 while 循环。

我们将使用两种不同的语言来做这件事，因为我认为这对于你来说非常有帮助，看看他们是如何比较和对比的，另外，你几乎可以同时学习两种不同的语言。

左边是使用 ID e Visual Studio 的 C sharp 语言，如果你想进行 windows 开发或想使用一个叫做 unity 的流行游戏引擎制作游戏，你应该会感兴趣；右边是使用 ID Visual Studio 代码的英语 Python three，如果你想深入了解一般编程或数据科学，你应该会感兴趣。

好了，这里我有两个全新的控制台项目。

如果你不知道如何制作一个新的控制台项目，无论是 C sharp 还是 Python 或者两者都不知道，请务必查看描述中的视频。

叫做如何安装 ID。

在视频的最后，我会告诉你怎么做。

好了，while 循环，今天的实验可能很短，因为 while 循环并不多。

但是让我们先来看一下 C 语言中 while 循环的语法，在左边，我要去掉这两行，因为我不需要它们。

它们是每个新的控制台项目的标准配置。

我会简单地写出 while，然后用括号括起来。

然后我们的代码块。

这是 C sharp 中 while 循环的语法，你只需要写 while，然后在这里传入一个布尔值。

然后你可以编写代码块，你可以把你所有的需求放在那里，这些需求会一遍又一遍地运行。

而这个布尔为真。

在 Python 中，while 循环看起来像 while and then true。

然后我会把 pass 放在我们的代码块中，past 的意思是什么都不要做。

这是 Python 中 while 循环的语法。

很简单。

关于 while 循环，你需要知道的一件非常重要的事情是，当 while 循环运行时，不管 while 循环之后发生了什么。

如果我来到这里，例如，进入一个控制台，控制台点写线，例如。

然后到达，例如，在 while 循环完成之前，while 循环之后的任何内容都不会到达。

同样，这样做的原因是因为当你运行 while 循环时，它只是一遍又一遍地运行代码块中的代码，不管有多少行，它都是一行接一行地运行。

一旦到达终点，就从头开始，继续下去，直到这个布尔值不再为真。

一旦这是假的，那么它将停止这个循环，然后转到 while 循环之后的下一行。

而这个过程也发生在 Python 中。

如果我在这里打印，然后到达，比如说，这个 while 循环会继续处理代码块中的每一行，直到这里的条件为假。

如果它永远不会为假，那么猜猜这个代码永远不会被到达。

我可以证明。

但首先我需要设置几个变量，我会到这里来，然后初始化一个整数。

我们姑且称之为 a 为什么不，同样 t 等于零。

所以在这个布尔值中，我们要做的就是，如果 a 小于 100，那么我们希望你运行，然后每次运行，我们可以简单地做一个加号，使用递增运算符。

然后让我们也做一个控制台点写线。

打印出实际的整数值。

所以这里将要发生的是，当我们的主程序运行时，它会运行下去，然后到达我们的 while 循环。

因为 A 小于 100，所以它会运行这段代码一次，然后把它写出来。

然后它会到达终点，再做一次。

然后再重复 100 次，直到 a 不再小于 100。

一旦它大于 100 或等于，那么它将不再运行，然后它们将到达我们右边的线，到达这里。

我们可以把它打印出来。

我可以证明给你看。

但首先我需要下来这里做一个控制台点读取键。

所以当我们点击这里的开始按钮时终端不会关闭。

我会把窗户打开。

如你所见，我们得到了我们期望得到的结果，while 循环会一遍又一遍地运行，直到布尔条件不再为真。

一旦为假，while 循环就会退出，转到下一行。

然后在那一行，在 while 循环的末尾，我们得到了打印输出。

用 Python 写的。

这是用 Python 语法以完全相同的方式完成的。

因此，为了设置它，我只需到这里来。

哎呀，我刚才做了什么？哎呀，我想我是的，折叠起来。

好，上来这里，我要做一个等于零的算式。

然后当 A 小于 100 时，我们就打印 A 等于什么，而不是 pass，这样就不会认为它们是一样的。

我按了这里的播放键。

如你所见，我们忘记增加了，对吧？我会把 a 递增到+1。

现在我们的代码应该被修复了，我点击播放按钮，然后嘭的一声，你就看到了，和 C 调完全一样的场景。

最后，还有一种方法可以停止 while 循环。

那就是使用 break 语句。

第一个问题，break 语句是什么？break 语句只能用于循环，比如 for 循环和 while 循环。

当你使用 break 语句时，它所做的只是中断这个循环，并移动到循环后的下一行。

为了证明这一点，我将用一个普通的布尔值来代替这个布尔值，真值。

然后我会在这里写一个 if 语句。

所以现在我要说，如果 a 大于或等于 100，那么我们要做的就是分解。

为了证明这是可行的，我来到这里的播放按钮，点击开始，然后打开窗口。

你可以看到它和另一种方法完全一样。

为了在 Python 中实现这个方法，我将简单地用真正的布尔值替换这个布尔值。

然后我会到这里来，如果 A 大于或等于 100，哦，100，那么我们要停下来，点击这里的播放按钮，证明它是有效的。

你可以看到我们有同样的东西，可能甚至没有注意到它变了。

这就是为什么它是给你的。

在某些情况下，它们会很方便。

但是说实话，你可能更经常使用 for 循环。

现在你知道了。

这就是开始编程 while 循环所需要知道的一切。

终于到了说方法的时候了。

因此，方法是令人惊奇的编程工具，简单地说，就是让你不必一遍又一遍地重复代码。

并且能够更好地理解您的代码。

例如，假设您有一些库存类。

在这个库存课程中，你有一些可能发生的事情，你可以关闭库存，你可以打开库存，你可以清空库存，这里只举几个例子。

每次你想做这些事情的时候，不需要改变一系列的库存变量，你可以把所有的代码都打包到一个方法中，然后在你想做的时候调用这个方法，这样可以节省你大量的时间，避免你一遍又一遍地写同样的代码，也避免了你将来的麻烦。

所以现在让我们来看看如何在代码中使用方法。

我们将用两种不同的语言来做这件事。

因为我认为看到它们之间的对比会对你很有帮助。

另外，你几乎可以同时学习两种语言。

左边是使用 IDE 的 C 语言 sharp，这是一个 Visual Studio，如果你想做 windows 开发或者想用一个叫做 unity 的流行游戏引擎制作游戏，你应该会感兴趣。

右边是使用 it Visual Studio 代码的 Python 语言，如果你想进入普通编程或数据科学领域，你应该会感兴趣。

好了，这里我有两个全新的控制台项目。

如果你不知道如何制作一个新的控制台项目，也不知道是 C sharp 还是 Python，或者两者都是，一定要看看描述中的视频。

叫做如何安装广告。

在视频的最后，我会告诉你怎么做。

所以在我们使用方法之前，我们首先要学习如何初始化它们。

让我们先看看它们的语法，在左边和 C 调，我会去掉这两行，因为我不需要它们。

然后我会到这里来，这样我们可以初始化一个新方法，语法如下。

你只需输入 void，然后你想给你的方法取什么名字，我就称它为 ABC。

然后你需要在括号里放一个开始。

最后，简单地添加一个代码块。

现在，您可以在这个代码块中放置任何您想要的命令。

例如，我只打算把控制台点写线，和比萨饼，因为比萨饼是最好的。

我们已经初始化了一个方法。

现在用 Python 来做这个，只是有点不同，但是概念还是一样的。

所以在 Python 中初始化一个方法，你要用 type out 代替 void，你要用 def，def，然后不管你的方法叫什么名字，或者不再调用，ABC。

然后你要在括号里开始，这个约定保持不变。

但是你不用去新的一行，用括号来做代码块，你只需要在这里用一个冒号，然后去下一行，确保它是缩进的。

我使用的是 Visual Studio 代码 ID，当我转到下一行时，它会自动为我添加一个缩进。

现在就像在 C 调中一样，我可以在这个代码块中添加任何我想要的命令。

所以我要写出指纹。

还有，披萨，因为披萨是最好吃的。

这就是在大多数 C sharp 和 Python 中初始化方法的方式。

但是如果没有更多的上下文，我不认为它们有什么帮助。

因此，让我们后退一步，检查一个场景。

想象一下，你正在用一个有三个空间的库存系统写一个程序，这个程序需要你把这三个空间里的东西换出来。

因此，为了减少混淆，我将去掉我们在 Python 和 C sharp 上的初始示例。

现在我要用一些变量来设置这个场景。

所以我继续写下了屏幕外的场景，因为否则可能会有大约五分钟的沉默。

但我会带你走一遍。

在这里，我们有所有的字符串变量。

这是我们的库存代表我羡慕插槽，一，二，三个 Python 一样。

我们首先将它们都设置为空。

然后我们可以在变量名称下选择几个项目，pick up，one，two，第一个是 pizza，因为 pizza 是最好的。

然后我们有糖果。

当然，Python 也有同样的功能。

接下来，我们有几个 if 语句来模拟一个事件的发生，假设你想拿一些物品。

嗯，首先，你要看看它是不是空的。

如果是空的，你就把它捡起来。

同样的事情也发生在 Python 中。

好了，我们已经设置好了我们的场景，我将在这里添加一个控制台点写入行，这样我们就可以在这里打印出结果。

让我们先在 Vin one 中打印出来。

然后我就懒了，我要做 Ctrl，C，Ctrl，V，再复制粘贴几次，然后在 Vin 和 inven 中打印出来。

三个。

当然，我会到这里用 Python 做同样的事情，除了语法更简单一点。

在 Python 中，我可以做 inven，one，there，然后是逗号，还有 vim three 中的 one，two，和逗号，这是一个更简单的 Python 打印函数，在我们运行它之前，我们应该期待什么呢？好吧，再说一遍，我们所有的库存，插槽，东西都是空的。

然后我们来到这里，检查第一个位置是否为空，然后我们将把取货位置分配给库存位置 1，即披萨。

然后我们再做一次检查。

如果库存位置 3 是空的，那么我们将把取件分配给它，也就是糖果。

所以我们应该期待的是比萨饼，空糖果。

让我们看看我们是不是会在这里按下开始键。

哦，那会离我们很近的。

我忘了加上，当然，控制台点读键。

好的，我要再次来到这里，点击开始按钮，打开窗口。

你可以看到我们有一个库存槽一个比萨饼，槽二空和槽三糖果。

完美。

也要用 Python 来运行，点击这里的播放按钮。

你看我们有披萨，空糖果。

完美。

好吧，这还不错。

这非常简单，正是我们想要的。

但是现在想象一下这样一个场景，当你在你的库存中分配一个新的物品时，它不仅仅是改变该物品的位置。

也许，你知道，它在这里做一些用户界面的东西。

然后，它确实像一些特殊效果，我说的是 v 效果，然后可能像更新，其他像更新。

例如，槽一，库存槽一也更新库存，例如，槽二，槽二。

每次你想这么做的时候都会这样，对吧。

所以我要把这个复制下来，带到这里。

你可以看到我们的代码开始变得很长，有点费力。

现在，这是方法的用例之一重新发挥作用的地方。

使用方法的第一个好处是能够压缩代码。

我们为什么要这么做呢？代码块中的所有东西都有这个主函数，这是我们的主程序。

在 Python 中，只是为了比较，脚本中的每一行都算作你的主程序。

但是随着你在主程序中添加更多的代码，它会变得越来越长，变得越来越混乱，难以阅读和理解。

因此，我们可以把这些代码转换成一个方法，然后在我们的主程序中调用这个方法，而不是这些代码。

让我们开始吧。

为了做到这一点，我先来看看我们的第一段代码，它本质上封装在一个方法中。

在它的正上方，我将添加一个开始大括号，在结束大括号的底部，Visual Studio 会自动为我重新格式化，谢谢 Visual Studio，然后我可以简单地添加我们的 void。

然后我们现在可以进入所有编程中最难的部分。

现在，我们必须给我们的方法命名，谢天谢地，我提前想到了这一切，所以你不必看着我挣扎，但我要把它叫做拿起比萨饼。

我赢了。

然后，当然，我们的括号，关闭它。

我们已经提前完成了代码块。

这就对了，我们已经成功地把它转化成了一个方法。

现在我要对第二段代码做同样的事情。

我的过程有点不同，拿起这是什么糖，我三个做那个。

哎呀。

我们要处理掉这个。

然后我们将使用底部的自动格式。

谢谢你。

视觉公司，我们有它。

同样，在 Python 中，假设当你拿起某样东西时，你也喜欢一些 UI 和视觉效果。

你也可以更新你需要的任何信息，更新你的库存，也可以更新正在发生的任何事情。

第二张幻灯片，等等。

我只是要复制这个，然后把这个也复制到这个，并稍微修改一下格式。

你可以看到剧本变长了很多。

所以让我们把它们转换成方法。

现在，你已经知道了 Python 中方法的语法，我们只需用 def 替换 void，然后我们将变量命名为 pick up pizza，Inventory Slot，one，以括号开始，然后是分号。

然后我们来到这里，点击 tab 键，这样它就成为了这个方法的代码块。

让我们对第二段代码做同样的事情。

def，它是什么拿起糖果，库存，三，在括号中开始，然后我们的冒号，然后下来所有这些行和缩进的人。

所以它们算作这个方法的一个代码块。

现在，我们需要暂停一下，如果你看一下我们的库存，三个变量，甚至我们的库存一个变量，它们带有红色下划线，这意味着我们有一个问题，这将归结到我们的控制台并按下问题，我们看到 word 告诉我们这些变量没有被分配。

这是怎么回事？嗯，这和所谓的范围界定有关。

Python 对于简化你的作用域是非常奇特的，这个代码块是独立的，只包含在这个方法中，拿起 pizza inventory one，他没有对这个代码块之外的任何东西的引用。

当然，这个代码块也是如此。

我们可以做一件简单的事情，让这些代码块知道，有一个变量叫做 iron v one，在它的作用域之外，也就是这个，我们可以简单地在它的顶部输入 global，然后输入 I N v one。

这里也一样，我们可以输入 global，然后输入 ion v，你可以看到我们的问题已经解决了。

问题解决了。

我们现在能做的就是压缩代码，在这里抓取所有这些行，它们包含了我们的两个方法。

我要按 Ctrl+x 或 cuts。

现在我要把它移到整个程序的底部，在最后，我会意识到刚才发生了什么，差不多我们的程序到此结束。

所以在那之前的都是我们实际的程序，看看它有多短。

在 Python 中，我们可以做同样的事情。

我要选择所有有这两个方法的行，按 Ctrl，x 或 cuts。

然后我会把它向上移动。

然后，在我们的打印函数下面，我将按 Ctrl+V 或粘贴，并将其放在下面。

同样，就在这里，我们的程序在这里结束，非常非常短。

然而，我们的代码现在被压缩了。

是的，但是我们还没有完全完成。

在 C 调左侧，你可以看到，如果我点击顶部的开始按钮，打开窗口，你可以看到我们的整个库存仍然是空的。

Python 也是如此，如果我点击播放按钮，您可以看到我们有空的、空的、空的。

原因是我们从来没有调用过我们的方法。

在左边的 C 调中要做到这一点，你所要做的就是写出你的方法的名字。

所以我们的第一个，我们想做一份外卖披萨。

然后你还需要包括你的开始和结束括号。

这表明它是一种方法。

然后我们需要做捡糖果库存，我三个括号开始，然后我们的分号。

为了向您证明这是可行的，就像我说的那样，我将点击“start”按钮，然后打开窗口，您可以看到它们现在已经设置好了，请查看。

现在在 Python 中，它实际上非常不同，尽管概念仍然是相同的。

所以之前，我把我们的方法移到了 Python 脚本的底部。

但是如果我们进入程序中间，然后输入，pick up，what is it pizza item one，然后在括号中开始，然后运行我们的程序，你会看到我们得到一个错误，因为 pizza pickup pizza item one 还没有定义，或者说，inventory one 还没有定义。

因此，关于 Python 的一个事实被揭示了。

在这种情况下使用方法并不是很有用，因为操作的顺序在 Python 中非常重要。

所以要使用我们的方法，我们必须在脚本中第一次调用它们之前对它们进行初始化，但也要在初始化我们在其中使用的变量之后。

你问的甜蜜点在哪里？嗯，就在这里。

所以我要选择所有包含我们方法的行。

然后我要按 Ctrl，X 来剪切，然后按 Ctrl，V 来粘贴。

现在你知道了。

为了证明一切都如其所愿，我将在 Visual Studio 代码中点击播放按钮。

你可以看到我们买了披萨，空的，空的，等等，这不对。

当然，这样做的原因是，我们必须调用第二个取件，在保存播放按钮的括号中选择三颗星，然后，瞧，我们得到了披萨，空糖果。

这是方法的一个用例。

但是我想给你们看另一个，我要从移除我们做的所有代码开始，以及 C sharp 和 Python，这样我们可以建立一个新的场景。

现在想象一下，你有一个程序，里面有某种 boss 战。

每次你成功击中老板，就会有两件或更多的事情发生，所以我会快速编写这个场景。

好的，所以我继续在屏幕外编码这个场景，因为否则会有另外几分钟的沉默，但是我会带你们看一下。

这里我们有几个整数初始化，我们有我的 HP，敌人的 HP 和一些钱，然后我们有一些 Boolean 来检查我们是否击中了腿，手臂或头，把它们都设为真。

然后我们只需要一些 if 语句来检查这些卷。

如果他们中的任何一个是真的，那么我们将简单地从老板那里减去 HP，然后在我们的银行账户上增加一些钱。

Python 也做同样的事情。

同样的变量可能在 PHP 和金钱中，然后一些击中腿，击中臂和击中头金条。

我们只需检查这些叶片。

如果它们是真的，那么我们将再次从敌人那里减去 HP，并在我们的银行账户中增加一些钱。

仅此而已。

所以方法可以给你的下一个优势是减少重复，如果你还没有观察过，两个脚本，我们有这一段代码，敌人 HP 减等于 1，money 加等于 5。

这在两个脚本中都重复了三遍。

我是说，这很好。

总的来说，我的意思是，这将做你期望它做的事情。

然而，我们在编程中有这个规则，它是这样的。

永远不要写两遍同样的代码。

此外，假设您想要向这些点击事件添加其他内容。

举个例子，假设你想用你的生命值为每一次成功命中增加一点治疗，对吧。

例如，你可以到这里来做我的 HP+等于 2。

但是问题是你现在必须为每一个点击事件写这个。

所以我的 HP+等于下一个，我的 HP，哎哟，我的 HP+2 等于 2。

现在这里有一个清晰的模式。

你没有理由一遍又一遍地重复这些。

让我们把方法带回来，我们要做的就是在我们主函数的最后写出 void，然后命名我们的函数，就叫它 hit event 吧。

听起来不错，从括号开始，然后到这里，你的代码块。

让我们把这些都写出来。

任何 PHP 负等于一，完美金钱，正等于五，然后我的 HP 正等于二。

因此，现在我们要做的就是简单地删除所有这些，并用点击事件替换它们。

以括号、分号和 bottom 开头的所有其他空格也是如此。

参加热门活动。

以括号开始，分号，哎呀，去掉那一行。

当然，这里也有关于炸弹和巴达冰的事件。

你看，我们已经大大减少了重复。

当然，只是为了向你们证明这一切都是可行的，我们到了脚本的末尾，同样，在我们的方法被调用之前就结束了。

所以在脚本中，然后我要添加一个控制台点写线。

我们真的只想知道我们所有的变量，所以敌人的 HP，然后我就懒了。

所以我要按几次 Ctrl，C Ctrl，v，然后用钱来代替它。

然后把这个换成我的 HP。

来到这里，按下开始键。

向上，它将关闭当然，我们必须点击控制台，读取键总是忘记这一点。

可能有一点。

因此，使用 Python，点击 play，打开窗口，您可以看到我们已经完成了计算。

我的意思是，没有人会确认它们是什么，但是相信我，它正在工作，现在在 Python 中，这种技术有很多好处，但是它仍然受限于操作的顺序。

同样，要在 Python 中实现这一点，我们只需要初始化我们使用的变量。

但是在我们调用它之前，甜点就在这里，所以我们可以定义，我们叫它什么？我忘记了点击事件，点击事件，然后在括号里开始。

然后我忘了一些，只是普通的冒号。

在我们的代码块中，我们可以把它放在这里，我很懒。

所以我只是复制并粘贴了 being，然后我们可以用 hit 事件替换所有这些。

然后我要复制这个，因为我很懒，也要复制这个，因为我很懒。

您可以看到我们又有问题了，您可以看到我们又有范围问题了。

您可以看到，我们又遇到了范围问题。

您可以看到，我们又遇到了范围问题。

为了解决这个问题，我们所要做的只是再一次，增加我们的全球，然后我们需要什么敌人，惠普和钱。

完美。

所以现在为了证明所有这些工作，我只是简单地重复我们在 C 调中所做的，然后做 print 和 PHP。

然后我们也想印钞票。

然后我们还想打印我们的 my HP。

我嘣，当然除了 HP，因为我们没有加上那个。

但是加东西就简单多了。

现在，我们不必遍历每一个事件，而是直接使用实际的 hit 事件方法。

然后我们可以做我的 HP plus 等于，我相信它是，我只是再检查一下。

是的，它也是。

我们需要将它设置为全局的，以便从我们的脚本中获取它。

然后我可以再次点击播放。

你可以看到我们有相同的变量 2715 和 16。

这就是减少重复的方法。

现在，我想向你们展示最后一个方法技巧，那就是返回值。

来做这个。

同样，我将删除所有我们已经用 Python 唱过的代码。

现在想象一下这个场景。

所以我继续写了这个场景的代码，因为否则，这可能会是大约 10 分钟的沉默。

但和往常一样，我会先带你浏览一下，考虑一下你正在开发某个应用程序或某个社交媒体或类似的东西的场景。

并且您希望为所有用户生成一个用户排名。

老实说，现在我们程序中发生的事情一点都不重要。

但这里我们有我们的用户排名变量。

假设我们想用这些变量来生成这个用户排名。

嗯，我刚来这里做了一棵很大的树，做了一些检查。

因此，如果客人然后减去一些如果页面，加上其他东西。

另外，如果这是任意的，我只想做一棵大树来演示下一个例子。

这么说吧，所有这一切，实际上从这里开始，所有这一切就像是你和你的社交媒体你的应用团队开发的算法。

你们得出的结论是，这是一种非常准确的评估用户排名的方法。

我们可以把这个算法转换成一个方法，让这个方法返回值，而不是把这个算法硬编码成一个变量。

为此，我们只需要做两件事。

首先，我们把这个算法转换成一个方法。

现在，这仍然是硬编码我们的用户等级变量。

这和普通的方法没什么区别。

接下来我们需要做的是创建一个临时变量，我们称它为 temp，然后把它的值设为 0。

然后我们需要把所有的用户等级都改成 timps。

或者临时变量，我很抱歉。

所以我必须用等级来表示临时变量。

我将在这里使用，这里也是。

然后这里用 rank 也是 temp 然后这里用 rank 也是 Tim Hahn 这个用户 rank 是 temp 是 well。

最后一个是，我们要做的是，结束我们的方法，然后返回提示。

现在，你会看到我们会在这里得到一个错误。

这其中的原因需要解释一下。

因此，当您初始化方法时，您也在转换它的数据类型。

因此，如果我们检查我们的 score 方法，您可以看到我们的 score 方法的数据类型是 void，它本质上是数据类型列表。

所以我们的得分基本上只是遍历它的代码块，运行它，然后退出，但是如果我们想返回某种类型的数据，我们所要做的就是用那个数据替换它。

类型。

看到我们的使用等级是一个浮点数，我们将把它替换成一个浮点数。

你看我们的错误已经消失了。

那么这让我们能做什么呢？好了，我要去开大会了，我们的节目到此结束。

然后我要下去找到这两个东西，然后 Ctrl+X 把它们切掉，然后把它们放在编程之前。

现在我们所要做的就是给我们的用户排名变量赋值。

这不是很好吗？假设我们有另一个想要计算的用户排名，对吗？这比较容易，秩二等于零。

好了，我们现在要做的就是去用户排名二等于分数。

在 Python 中没有任何地方，我们可以做同样的事情，只是首先把我们的算法包装到一个叫做 score 的方法中。

这样做，现在我们需要选择整个算法，然后点击 tab 键。

这就是分数的所有代码块。

因为这是一种方法，希望这里有什么东西坏了，什么东西坏了。

现在我不完全确定这里发生了什么。

但是要解决这个问题，我们只需简单地添加一个制表符，修正语法，错误就消失了。

完美。

接下来，我们将添加回我们的临时变量。

让我们初始化这个 temp 等于零。

然后用 temp 替换所有的用户等级。

我只是复制粘贴，因为我现在很懒。

温度，温度，温度，温度，温度，温度，温度。

最后我们出来了，我想这就是了。

返回提示七仔细检查，这是正确的地方把它。

是的，它是。

因为这是 Python，所以这个方法对于压缩来说不是很有用，这也是最有效的方法。

但是我们可以简单地通过调用 user rank 然后给它分配分数来得到我们的用户排名。

现在为了证明这些和我说的完全一样，在 C 调的左边和右边，我要复制并粘贴右边这条线，因为我很懒，然后加到那里。

然后我们读了密钥。

所以现在我要按下开始键。

然后我会把窗户打开。

你可以看到我们有 49 和 49。

为了用 Python 证明同样的事情，我在这里初始化一个新的用户等级，使用等级 2，这也等于分数 2。

所以我们有相同的场景。

然后我们祈祷用户排名第二。

然后我要点击这里的播放按钮，你看到我们有 49，和 49。

是的，你知道了。

如果你想学习更多关于方法的知识，我认为你能做的最好的事情就是开始更多地使用它们，因为在这个过程中你会学到更多。

但这是你开始编程所需要的一切，关于方法，函数，函数，让我们步入函数的奇妙世界。

现在我要公开声明，这些函数无疑是最有价值的编程工具。

这是为什么呢？函数和方法有很多共同之处，因为它们允许你重用代码，让你有能力写更整洁漂亮的代码，这实际上让你更容易阅读，让你更好地掌握你的代码。

但是函数相对于方法的优势在于，您可以传入数据并稍微修改在函数中运行的代码，从而使它们变得非常强大。

例如，假设您有一个可以容纳四件商品的 inventory 类，您想删除这些商品。

不是创建四个不同的方法，你可以调用它们来移除不同的项目，库存点移除项目一，库存点移除项目二，等等。

运行完全相同的代码，只是可能有一两个变量，你可以传入函数，例如，物品编号，使用相同的代码可以移除物品。

现在谈论函数的理论是很棒的，但是让我们看看如何在代码中实际使用函数，因为我保证这会变得更容易理解。

我们将使用两种不同的语言来做这件事，因为我认为这对于你了解它们之间的比较和对比非常有帮助。

另外，你几乎可以同时学习两种语言。

所以左边是使用 ID Visual Studio 的 C sharp 语言，如果你想做 windows 开发或者用一个流行的游戏引擎 unity 做游戏，你应该会感兴趣。

右边是使用 IEEE Visual Studio 代码的 Python 语言，如果你想学习更一般的编程或数据科学，你应该会感兴趣。

好的，这里我有两个全新的控制台项目，如果你不知道如何制作一个新的控制台项目，或者 C sharp 或者 Python 或者两者都有。

请务必查看描述中的视频。

它叫做如何安装一个广告。

在视频的最后，我会告诉你如何做，保罗·怀特，他已经准备好享受一些功能了。

在我们开始之前，我们需要复习一下 C sharp 和 Python 中函数的语法，在左边，我要去掉这两行，它们是每个新控制台项目的标准，但我不需要它们。

然后在这个代码块中，我将初始化一个函数，如果你对方法很熟悉的话，你会觉得很熟悉，我将输入 void，然后输入我们函数的名字，现在叫做 F，然后在括号中开始。

现在，如果你对方法很熟悉，你可能会看着这个，你会想，等等，这只是方法而已。

是的，你是对的，方法和函数有很多重叠。

但是让函数变得非常酷和非常有用的是你可以传入一些输入。

如果我在这里输入，如果我初始化一个 int，然后我调用这个 x，对于 x 的 f，这是函数的标准符号，我现在可以在这里写代码块。

我们可以在代码块中对这个输入做一些事情。

例如，假设我们想要输入 x，我们只是想乘以 2。

所以我将使用乘法运算符，然后乘以 2。

然后，为了查看这一变化，让我们到这里添加一个控制台点写入行，以便打印到控制台。

然后我们要把 x 变量放在这里。

好，现在我们可以用这个函数做一些很酷的事情，如果我来到脚本的开头，然后我要初始化一个整数，我要调用它，比如我不知道我的数字。

然后我们就定在九点。

所以现在我只想打电话给 F，然后传入我的号码，9。

它要做的是在这个函数中接受我的数字，然后这里是乘以 2 并打印出来。

为了向您证明这一点，我将在我们函数的正下方添加一个控制台点读取键，这样终端就不会对我们关闭。

我们一运行它，就按开始键，我们的终端在这里。

你会发现 9 乘以 2 实际上是相等的权重。

现在在 Python 中，语法有点不同。

我先初始化我们的整数，我的数字，给它赋值一个 9。

然后我们的，我们的函数必须在我们真正调用它之前出现。

所以我要初始化它，首先，我要说 def，名字是 F，然后我们在过去九个输入 X，然后我在这里用一个冒号，向下进入代码块，然后我要做 X 乘以等于，和我们在 C 调中做的一样，然后简单地打印出 X 是什么，最后， 我们可以调用我们的 F 函数，说 F，然后传入我的 num，当我做 num 的时候知道我的 num，现在你知道了，我要按下 play 按钮来证明这是有效的。

砰的一声，你看到了网络 18。

现在，我们仅仅触及了函数的表面。

但是希望，你已经可以通过这个简单的例子看到我们有多大的能力。

请允许我再给你看一个例子。

在我们继续之前。

假设我们有一个名字列表，我们想添加 Mr。

或者以 Mrs 开头的那些名字，那么，我们可以简单地去掉这个整数，不再需要那个。

还有什么是我们不需要的，我们其实已经不需要这个功能了。

因此，我们不需要这样做，实际上，这将保留控制台，让我们去掉整个程序，从头开始，对 Python 做同样的事情。

所以让我们初始化一个字符串数组。

姑且称之为名字吧。

然后这等于一个新的字符串数组的大小。

让我们来初始化大小，让它变得简单。

然后我们可以在这里手动设置数组中的变量。

所以我会做零等于，假设他的名字是约翰尼。

然后假设元素一的名字等于卡拉。

然后元素 2 的名字等于，假设扎克，我假设扎克，谢谢。

现在让我们写出这个过程的函数。

我要下来，我要做一个空，然后让我们称它为姓，我相信它们是姓。

然后我们就可以进去了。

让我们看看，一个字符串，这将是它被称为 sir 的变量。

现在我们可以这样做，零的名字等于 sir，加上零的名字。

当然，让我们把它打印出来，这样我们就可以看到控制台点写线上发生了什么。

我要找的是，我们可以打印出零的名字。

快点，我们到了。

然后我们可以在这里称呼我们的姓。

然后让我们进去。

那里有个空格。

当然，还有控制台点读键。

所以终端不会对我们关闭。

另外，我就写在这里，因为区分我们的程序在这里很有趣。

所以我继续用 Python 写了这个程序，所以你不必坐下来听我沉默几分钟。

但是它做了同样的事情。

我们这里有一个无名的，用强尼·卡拉和扎克初始化的。

然后我们初始化我们的姓氏函数，它接受 sir 的输入。

我们实际上只是指定 sir，加上元素 0 的名字，来命名 velleman，0，所以我们实际上是重新指定它，然后打印出来。

这是我们调用实际函数本身的时候。

如果我来到这里，按下播放按钮，你可以再次看到，我们有 Mr。

约翰尼除了我把句号放错了应该去的地方，再按一次，吧嗒嘣。

好吧，酷。

所以现在我们有了一个姓氏函数，它为我们的名字添加了某个名字，然而，我们有三个名字。

我真的不想为此而写三个不同的函数。

谢天谢地，我们不必这样做，这要归功于下一个功能，它进一步使函数变得更加强大。

因此，对于函数可以传递多少个输入，并没有真正的限制。

事实上，我们在这里使用了一个数组，我们可以传递一个整数来改变名称数组中我们想要的任何元素。

为了做到这一点，我要在这里的开头添加一个整型变量，我们称之为，我只是调用 ind 来表示 index。

然后我们可以简单地用 ind 代替这里的零，我们再传入一个整数，然后再传入。

最后，我们来到这里，然后传递 0。

因此，我们希望更改名称数组中的零元素，它对应于 Johnny 再次运行它，通过点击顶部的 play 按钮，打开控制台窗口，您可以看到什么都没有更改。

现在我们已经增加了灵活性，我可以简单地用这一行，复制并粘贴，复制并粘贴，复制并粘贴，只需将索引更改为一个是卡拉，两个是扎克。

当然，卡拉不是一个谜。

所以我们可以在这里把它变成一条信息。

当我们运行这个程序时，它会把这三个都打印出来。

因此，我将再次按下播放按钮，并打开窗口。

你能看看这个吗？

约翰尼夫人。

卡拉和先生。

Zack 功能运行良好。

在 Python 中，我们可以做同样的事情。

所以我要再次传入一个名为 ind 的输入变量，用逗号分隔它们，这对两种语法都很重要，用逗号分隔变量。

然后我们可以用 ind 替换这里所有的元素号，然后当我们调用函数时，传入一个整数。

就在这里。

所以先生。

零。

当然，我们可以复制、粘贴、粘贴。

然后一个两个。

卡洛斯错过了保存按钮清除所有错误，然后点击播放按钮。

你能看看那个先生吗？

约翰尼夫人。

卡拉先生。

扎克，我会一直说下去的。

但是函数是最伟大的东西。

如果你认为到此为止。

好吧，你会有点吃惊的。

但是首先，让我给你们介绍一个问题，我们想在程序的底层初始化一个新的字符串，对吗？还有这里的名字。

这么说吧，这是老板的名字，等于海伦。

现在如果我们想在海伦的名字上加一个姓呢？嗯，因为这个姓函数是直接绑在名字数组上的。

我们现在还不能，我的意思是，当然，我们可以进来手动添加。

海伦，或者我们可以去掉这个，然后直接进入下一行，做一些像专横的酷。

什么事，夫人？

加上这里的老板。

然后我们还想做一个控制台点，是的，我们可以重做相同的代码，如果我们真的想想象你是作为一个自动化的任务在做这件事，那么，你不会想手动去添加姓氏到你所有的名字。

你不会真的想一遍又一遍地重写代码。

所以解决这个问题的方法是，把这个函数从一个变量依赖函数变成一个变量独立函数，我们可以通过使用 return 语句来实现。

为此，首先我要去掉这两条线，因为它们只是一个例子。

然后我要进入我们的姓函数，我们要返回一个字符串。

所以我将初始化一个字符串，我称它为 temp，然后我将它暂时标记为空字符串。

现在我们必须把所有对名称数组的变量引用替换到临时变量中，因为这是函数变量依赖的原因。

所以我要把这个改成 temp。

然后我会把这个也改成 temp。

然后我要把这个改成 temp。

实际上，因为我们需要一个原始名称的输入，我们不需要在这里初始化它。

所以我要去掉那条线。

我们现在可以把整型变量输入变成字符串变量输入，并把它叫做 temp。

所以这里我们需要做的最后一件事是，我们需要把函数的数据类型从空的改为字符串。

然后你会看到我们得到一个错误，说它没有返回值。

当您在函数上强制转换数据类型时，它们必须在返回中返回相同类型的数据类型。

所以我们在这里添加，return 和 temp 是一个字符串。

所以我们将返回开始时用 sir 修改过的 temp。

现在，因为我们将姓氏的输入从数据类型 integer 更改为 string，所以您可以看到这里有三个错误。

对于我们调用它的时间，它们现在是整数，它们需要是字符串，这没问题，我们可以在这里输入我们想要改变的实际名称。

当然，第一个是元素 0 的名字，我很懒。

所以我要复制粘贴这个，粘贴粘贴，把这个改成 1 和 2，也对应这里。

如果我点击“保存”,然后点击这里的“开始”按钮，然后打开控制台，您可以看到一切都没有改变，功能完全一样。

然而，为了使它完全起作用，我们必须做的最后一件事是，就在这里，它打印出 temp，所以这只是暂时的，我们得到一个对它的引用，然后我们改变它，然后我们返回它。

但是你可以在这里看到它没有被分配给任何东西。

这是我们在这里要做的最后一件事。

但是在我们进一步说明我的观点之前，我将简单地在底部写一行控制台点。

然后我们要打印出 name zero，然后我会复制粘贴几次，因为我懒，改成了 one in two。

如果我来到这里，点击“开始”按钮，打开控制台，您可以看到，某些名字被添加到了前三个名字中，这就是实际的姓氏功能。

但是最后三个被打印出来的，在 surname 函数之外的，实际上是 0 个元素的名字，一个元素的名字和两个元素的名字，仍然和我们初始化它的时候一样。

所以要解决这个问题，我们只需要把这里返回的值赋给它。

所以 0 的名字等于输入的姓氏，1 的名字等于输入的姓氏，名字必须等于输入的姓氏。点击开始按钮，打开窗口，你可以看到它们实际上已经被赋值了。

让你看看这里发生了什么。

在这三个元素上，我们将指定零元素名称，一个元素名称和两个元素名称，这三个元素的名称与一些输入的姓氏相同。

所以我们进入函数，传递输入。

temp，这是现在的名字，而 surname 是我们想加在名字前面的名字，所以我们把姓氏加在名字的前面，然后打印出来，再返回 temp。

因为这个函数的返回值是 temp，也是在这里计算的，我们把返回值赋给元素 0 的名字，值 1 和 L 的名字。

现在终于回到原点了。

现在我们有了这个灵活的变量独立函数 surname，我们可以在这里初始化我们的 boss 字符串，它等于 Helen，简单地通过使用 boss 等于 surname 来重新分配它。

然后我们要在这里传入 boss。

我们可以让夫人进来。

就这么简单。

为了向您证明这一点，我将去掉这三行，使它不那么令人困惑，点击星号按钮，将窗口移过来。

如你所见，我们有先生。

约翰尼夫人。

卡拉先生。

扎克，最后，夫人。

海伦。

这就是你写好代码的方法。

在 Python 中，我们也可以很容易地进行这种更改，我们只需简单地将它改为 temp，然后将它改为 temp，再将它改为 temp。

当然，我们想把输入改为 temp，oops，然后到这里。

然后我们想把这个命名为 0，然后这个命名为 1。

然后这个被命名为。

当然，然后我们需要给这个 0 的名字赋值等于这些输入的姓，然后 1 的名字等于这些输入的姓，然后我就懒了。

所以我要复制并粘贴最后一个更改，即命名姓氏等于最后一个输入。

当然，我在这里得到了一个错误。

它说的是赋值给一个不返回的函数，我忘了在这里返回。

所以我们只是简单地退还小费。

当然，我们也不能忘记我们的老板海伦。

所以我们可以这样初始化，老板等于姓。

因为我们正在初始化它，我们只是简单地传入 Helen。

像这样。

然后夫人。

来到这里，按下播放键。

瞧，先生。

约翰尼夫人。

卡拉先生。

扎克和夫人。

海伦函数简直不可思议。

我喜欢这些东西。

我们仅仅触及了函数的表面。

但是希望你的创造力源源不断，你在想如何应用一个功能的不同方法。

我以前说过。

我将再次郑重声明，函数可能是编程中最强大的工具。

事实上，如果你对机器学习领域正在发生的所有突破性研究都很熟悉，那么，这些都是由函数驱动的。

因此，我希望我对让您对一些功能感到兴奋有所帮助。

但是这是你开始编程所需要知道的关于函数的一切。

现在，注释可以决定你的编码成功与否，特别是如果你在一个开源项目或一个项目的团队中工作，你计划长期使用注释，正如你希望我们以一种人类可读的格式在你的代码中留下注释，这样你就知道代码的不同部分发生了什么。

在任何时候，有时你会写出极其复杂的代码，而这些代码在当时是完全有意义的。

但是当你几个月后再回到那个代码，或者把它给其他人，这样他们可以添加到它上面，尝试逆向工程代码可能真的很困难或者浪费大量的时间。

所以让我们来看看如何在两种不同的语言中使用注释。

因为我认为看到它们之间的对比会对你很有帮助。

另外，你几乎可以同时学习两种语言。

所以左边是使用 ID Visual Studio 的 C sharp 语言，如果你想做 windows 开发或者用一个流行的游戏引擎 unity 做游戏，你应该会感兴趣。

右边是使用 ID Visual Studio 代码的语言 Python，如果你想进入更一般的编程或数据科学，你应该会感兴趣。

好了，这里我有两个全新的控制台项目。

如果你不知道如何制作一个新的控制台项目，无论是 C sharp 还是 Python 或者两者都不知道，请务必查看描述中的视频。

叫做如何安装 ID。

在视频的最后，我会告诉你怎么做。

好的，我想看的第一件事是行注释。

所以在 C 调和左调中，我将删除每一个新的 C 调项目都会有的这两条线。

我将开始创建一个小的假项目。

所以我要初始化几个浮点数。

我们称这个位置为 x，要做一个 2.345 F，然后我们再做一个浮点位置 y，我们要做 123 点 456。

哦，对不起。

好的，这些变量只是作为例子的命运坐标。

所以我要创建一个更复杂的布尔值，我要说 bull in oops，in position，让我们说 in pause equals，让我们做位置 x 大于，我不知道，让我们只说 12。

没错。

然后我们会检查，如果位置，y 小于 30。

但是除此之外，我们还会检查，或者如果我要离开屏幕，我会移过去，或者如果位置，x 大于位置，y，我们会加上分号。

现在我们有了一个更复杂的欺凌事件。

好的，这里，我们有一个很好的例子，说明了行注释是非常有用的，或者说是非常有用的。

更确切地说，我要在我们的欺凌之上，我要做正斜杠正斜杠，就在那里，它开始我们的注释，我们可以写任何我们想写的。

所以我会尽力解释这个布尔函数的作用。

如果位置 x 大于 12，那么 12。

和位置为什么为什么是大于，不小于，小于 30。

或者如果位置 x，x 大于位置，y，y，那么我们就就位了。

抱歉，它有点长，而且不完全适合代码的其余部分。

但这就是你写一行注释的方式。

它的工作方式是当你的代码被编译时，任何时候编译器看到一个双正斜杠，它知道忽略它后面的所有东西。

所以差不多会转到下一行。

在 Python 中，也是一样的，我要做 loke，哦，loke，x 等于 1，2.345。

看，y 等于 1 2.412 3.456。

然后我要做停顿等于，让我们看看 loake x 大于 12。

loq 是什么？为什么不到 30？嗯。

或者我们已经看 loq 已经忘了 x 大于 x 大于看为什么？伙计，太残忍了。

为了增加我们的注释，我会把它放在位置变量的上方。

我不再使用正斜杠正斜杠，这种货币形式在 Python 中是不被接受的，我将点击英镑符号或标签，不管你怎么称呼它，然后我将写出我们的消息。

所以我不写了，因为这又要花很长时间，我要从这里复制，然后粘贴。

我们开始吧。

评论，评论。

抱歉，我刚意识到 Python 的字体应该大一点。

所以我要把它放大一点。

是啊。

这是行注释的一个用例，可能是行注释最有用的用例之一，但是，你也可以做其他事情。

比如说，你知道，我们尝试了这个布尔值，但不管出于什么原因，它都不能正常工作，好吧，我们可以在赋值运算符后的所有内容上做一行注释，然后就像，例如，等于 1，只是为了确保哎呀，我们实际上需要把我的 true 放在这里，只是为了确保这个布尔值实际上是工作的，只是举例来说，然后在 Python 中，都是一样的，使用散列标签，后面的所有内容都被忽略，你只需把 true，哎呀，true。

哎呀，没有分号。

是的，这是另一个用例，在调试代码时也很方便。

所以我想给你们看的最后一个用线注释的用例是，假设我们要把这些返回给那个赋值。

让我们到这里来。

然后让我们添加一个 if 语句。

所以如果就位，对，我们希望有事情发生。

然而，我们还不知道我们想要发生什么。

我们有一个想法，也许也许我们不知道如何去做。

也许你想让别人来做。

我们能做的只是添加一个注释。

就像，在这里，我想显示一些东西，你知道，一些通知，我不知道，我只是想在这里显示一些东西。

你可以把这个评论留在这里，也许以后会回来看，也许会给一个朋友，也许会做一些研究，然后你知道，学习如何做，然后实施。

这个注释只是一个简单的方法，让你知道，你把 if 语句放在这里的意图是什么。

而在 Python 中，真的很像，只是有一点点不同。

假设我们做了 if 语句，如果就位了，我们就下来，我们可以写注释，也就是，复制粘贴，复制粘贴。

然而，我们不能让它像这样，你看，如果我们保存了，我们将有一个问题，那就是，我们有一个解析错误，一个语法错误。

原因是，如果你做 if 语句，然后你缩进，这是你必须做的，当它运行 if 语句时，什么也没有，它不会运行任何东西。

这是 Python 中的语法错误。

所以我们可以简单地把，比如 print，你知道，比如零，这样就可以清除语法错误。

从技术上来说，你甚至不需要打印，例如，你可以做空引号，哦，你可以做空引号。

这也是一样的，但是它必须在缩进中运行一些东西。

因为这一行又被忽略了。

好了，这就是行注释。

接下来，我想谈谈块注释。

为了演示，我将删除我们刚刚编写的所有代码。

然后哎呀，是啊。

然后我会，这么说吧，你正在创建一个复杂的算法，你很明白，但你有点不明白。

所以我要创造一些东西，这只是一个高级算法的表示，发挥你的想象力。

让我们做 loq y，除以等于 4，对吗？然后我们需要归还它或者类似的东西。

所以我们要做局部 New 等于 loake y 除以 loquax。

比如，哦，我得初始化这不是 Python floats。

最后，让我们把它退回去。

我们开始吧。

让我们做这个控制台。

哎呀，控制台点写线。

对吗？线，位置，新。

好吧。

这么说吧，这是某种高级算法，你只需在你的程序中实现它。

但是突然之间，就像所有的东西都坏了，什么都不能用了。

你就像，怎么回事？这不是我所期望的行为，我知道这是有效的。

很明显，你知道，这很简单。

但是也许算法的这一部分没有正常工作。

现在，你可以，你知道，走过去，像这样，你知道，正斜杠正斜杠正斜杠四斜杠。

如果你有一堆这样的，一行一行地做。

但是一个非常方便的方法是使用块来，在 C 调中，它只是一个正斜杠，然后是一个星号。

after 的结尾正好相反，一个星号，然后一个正斜杠，这是一种快速注释整个代码块的方法。

现在有一件非常重要的事情需要注意，块注释必须有一个开始，也必须有一个结束。

因为如果你没有结尾，这里发生的是它注释掉它后面的所有东西，包括括号。

如果括号有开始，但没有结束，你会得到一堆错误。

所以它们都必须有一个开始，它们都必须有，现在在 Python 中，我将在这里复制我们的超级高级算法，我将做 loake x 乘以等于 2，然后 loake y 除以等于 4。

然后我们要做 loake nu 等于 loake y 除以 loake。

x，那我们就把它打印出来，因为为什么不看起来像新的。

在 Python 中添加块注释，类似，但也有很大不同。

我的意思是，你想上来，或者如果你想做你的块，注释，只做三个单引号。

再说一次，它需要一个开始，他需要一个结束。

如果你没有结束符，你将会得到一个语法错误，因为它需要一个开始符和一个结束符给三重引用的字符串文字，也就是块注释。

现在，我想讲的最后一类注释是总结注释。

为了演示，我会去掉所有不再需要的代码。

然后我会初始化一个浮点变量，比如说，叫它 loke 等于零。

接下来，让我们创建一个快速的小函数，我将在我们的主函数之外，然后做静态浮动。

姑且称之为 ret 吧，一。

然后让我们，让我们传入一个 float，然后我们称我们的 float 为一个 sum，简单点。

然后我们要做的是返回一个，让我们看看一个加号。

这就是我们要返回的。

然后让我们快速使用它。

所以我们做 loke 等于 rat，然后我们就直接传进去。

所以它本身会加一。

为了证明我们会做控制台点写行，尽管这不是为了证明，而是为了注释，但我们还是会做。

控制台点读取键。

然后我会按播放键。

嘣，我们开始了。

零加一等于一。

所以我们的函数起作用了。

但是，假设您知道，在项目的后期，当您回到脚本来升级或修复某些东西时，您会看到 Loki 向右移动，这是做什么的，然后您向下移动到 read one 函数。

假设这要复杂得多，你并不真正了解发生了什么。

好吧，你可能会浪费很多时间逆向工程来找出这个函数是什么。

但是能解决这个问题的方法是添加一个总结评论。

所以在 C 调中添加总结注释其实真的很容易。

你所要做的就是在你想要添加摘要的函数或方法的上方，点击反斜杠，反斜杠，反斜杠，如果你在 Visual Studio 中，它会自动完成，这意味着只要填写你的摘要。

所以我要在输入上加一。

现在，如果我看红色的，你会看到这里有一些描述。

它告诉我它在哪里加，一，二，不管输入是什么，输入是 Loke。

所以它是局部的，再加上一就是它要返回的值。

简单，容易，切中要害。

它们还有一些其他的变量，你可以填入，比如参数 A 是什么，你可以输出它是什么，你可以输出任何返回，在正确的上下文中，它也会为你显示这些信息。

在 Python 中，我将删除所有内容，快速设置我们的场景。

所以 loke 等于零做一个 do def read，然后我们需要 a 输入。

然后我们来看看 return，呜呜，return a 加 1。

然后我们只需要简单地做 loke 等于正确的那个，然后传递 Loke。

然后我们可以打印低输出，然后我们不需要输入。

为了证明它可以工作，我将在这里单击 play，您会看到我们有一个。

现在，要向 Python 方法和函数添加摘要注释，您所要做的就是在 Python 中的方法或函数下，点击三个单引号字符，然后再点击一个。

所以你几乎屏蔽了评论。

然后你只需要输入函数。

我要抄一下这个，因为我很懒。

然后我要把它粘贴到这里。

现在，如果我们转到这里，它会向你展示它是做什么的，红色的那个，它给用户加 1，对不起，给输入加 1。

如果你需要，我们还可以添加更多，比如像 equals，float，为用户提供更多信息。

差不多就是这些了。

好了，伙计们，这差不多是你开始喜剧编程所需要知道的一切。

好吧，祝贺你，你完全通过了课程。

现在，你觉得你表现如何？如果你真的不确定，你应该回到你的第一个程序的第四课，并在不同的层次重试。

如果你能在他最困难的时候完成你的第一个程序，那么你知道，你已经正式准备好进入这个世界去寻找你自己的问题去解决，并用你新发现的技能编码你自己的想法。

但在我完全放开你之前，我只是想给你一些灵感，让你从这里通过更多的练习可以利用这项技能获得一些很酷的可能性，当然，我只是想让你想想这些天你做的每一件事。

想想这个事实，在这个新的数字时代，几乎所有你与之互动的东西都是由代码决定的网上银行、线下银行、短信、推特、观看 YouTube 视频甚至现在驾驶你的智能汽车或使用你的智能牙刷。

用你的智能锁锁住你的家，甚至用你自己的智能温度计，这样的例子不胜枚举。

当然，从今天开始，这个名单只会越来越长。

我们的世界正变得越来越依赖于运行节目的程序，这是一个完全不同的话题。

但是，再次祝贺你，因为你至少现在有了更好地理解这种情况并为之做出贡献所需要的东西。

我祝你和你的编码冒险一帆风顺。

最后，我想给你们一点建议。

只要你遵循这最后一条规则，你一定会用代码做出惊人的事情。

记得永远满足你的好奇心。