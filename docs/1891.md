# 了解如何在 Figma 中创建设计系统

> 原文：<https://www.freecodecamp.org/news/learn-how-to-create-a-design-system-in-figma/>

设计系统是一组标准、可重用组件和模式，用于在不同的项目和页面中创建视觉一致性。Figma 是一个矢量图形编辑器和原型工具。

我们刚刚在 freeCodeCamp.org 频道发布了一门课程，教你如何在 Figma 中建立一个设计系统。

蒂姆·沙利文创立了这门课程。蒂姆是一名前 Airbnb 设计师，他是一名优秀的教师。

如果你想让你的项目看起来专业，重要的是要创建一个设计系统，这样就有一个一致的风格。

以下是本课程涵盖的主题:

*   介绍
*   创建颜色系统
*   在 Figma 中建立一个类型系统
*   海拔
*   产品和系统图标
*   文本易读性
*   州
*   选择(互动)
*   了解布局
*   像素密度
*   间距方法
*   响应式网格布局
*   应用程序栏-底部
*   应用程序栏-顶部
*   背景
*   旗帜
*   底部导航
*   按钮-浮动操作按钮
*   小跟班
*   卡片
*   炸薯条
*   对话
*   日期选择器
*   圆规

观看下方或 freeCodeCamp.org YouTube 频道 (8 小时观看)上的[。](https://www.youtube.com/watch?v=RYDiDpW2VkM)

[https://www.youtube.com/embed/RYDiDpW2VkM?feature=oembed](https://www.youtube.com/embed/RYDiDpW2VkM?feature=oembed)

## 视频抄本

(自动生成)

设计系统是一组标准、可重复使用的组件和模式，用于在整个页面和项目中创建视觉一致性。

Figma 是一个矢量图形编辑器和原型工具。在本课程中，蒂姆·沙利文将教你如何使用 figma 创建一个设计系统。

这是怎么回事你们在这里辅导蒂姆。

今天，我会给你们一个简短的概述，在接下来的这个系列中，我将创建一个设计系统。

这个设计系统将成为 figma 的材料设计系统。

我将尽我所能把目前在这个网站上展示的东西翻译成 figma 本身作为一个设计系统。

所以我们先来看看基金会。

这个基础需要一些东西，比如布局，什么是布局，就像对创建这些风格和图形的响应网格布局的响应。

了解如何使用适当的空间间隔方法来构建这些组件，以及如何在这些组件上进行布局。

所以我们要复习一下。

我们还将讨论如何利用材料设计颜色系统应用颜色系统，并将其应用到 figma 中，以及设计颜色样式。

此外，我们将复习排版，并建立排版系统，在材料设计中有详细说明。

但在 figma 中，我们如何做到这一点，并将其转化为 figma。

此外，我们还会复习肖像学。

对于 figma 中的图标，我们将基本上应用产品图标中指定的字母结构。

当我们复习时，figma 中已经有了这组图标，但我将向您展示如何组织它们，以及如何通过实例下拉菜单访问它们。

因此，您可以在通用组件和其他组件图标之间交换，它们也是组件。

另外，我们还将讨论一部分交互，也就是状态，状态是某些组件的状态。

在建筑设计系统的这一部分，我们将讨论基础。

接下来，我们将讨论组件。

我们将利用我们在 figma 中创建的基础，从头开始构建这个页面上的每个组件。

因此，我们将从应用程序栏、应用程序栏、底部到顶部，一直到最后一个组件工具提示。

我们会讲解如何通过它的解剖构造它，对吧。

所以我们有所有这些信息，如何使用它的位置和行为。

基本上，我将教你如何查看这个文件，并在 figma 中从头开始构建它，并应用适当的约束。

所以它是有反应的，以防设计师需要修改。

这基本上就是我们将在 figma 建立的设计系统，也就是材料设计。

这就是简要概述。

我将从一个颜色系统开始，是的，所以它将从参考网站开始，让你们了解我们将在 figma 中构建什么。

我们在 figma 要做的，基本上是翻译，我现在给你们一个简短的概述，我们在整个视频过程中要做什么，让你们有一个大概的了解。

所以我们将从颜色系统开始，实现颜色，然后把它作为一个库添加到 figma 中，所以你可以在这里看到它们。

对于那些已经使用过库的人，对于那些能够使用库的人，你可以在这里看到这个调色板。

以及在该库中创建的所有样式。

因此，我的材料设计，系统颜色文件和重命名，真正快速的材料设计。

我们这里有材料。

现在，如果我转到这里，我们将能够将它添加到这个最终将发布的库中，使用所有这些颜色，但当前文件已经发布。

但我要做的是很快删除所有这些。

好了，我们有这些颜色，我们有颜色样式，但是我们还没有添加到库中，我要告诉你怎么做。

但我将通过开发《创建这种颜色系统》这本书向您展示如何做到这一点，我们将使用基线材料颜色主题，展示如何将这些颜色构建为风格和 figma，供您的整个团队在 figma 中使用。

例如，如果你在为一个组织工作，或者只是一个团队，这是一种更有效的工作方式。

所以基本上，我要做的是截屏所有这些值，所以我不想在浏览器之间来回切换。

完成后，我将删除这里的所有内容。

不需要那个。

不需要那个。

我要开始了，这就像一个全新的文件，所以你们可以跟随。

从头开始跟着我。

我已经有了一个页面，实际上我要标记这个原语，因为这个组件是一个原语组件。

因此，我们要构建所有这些组件的方式，就其复杂性而言，是从最简单的形式，即被标记为原语，一直到组件，然后是由几个组件组成的模式。

稍后我们将对此进行更深入的探讨。

但是让我们从颜色开始吧。

所以现在我把截图贴在这里。

我要做的基本上就是把这些颜色转化成风格。

所以没有任何进一步的麻烦，我只是要创建一些矩形。

实际上，我要按 Ctrl，C，来复制这个值。

请注意，当我使用 Ctrl，C 时，十六进制值并不是我选择复制值并应用于矩形的屏幕截图中显示的值。

所以我必须手动输入并输入这里显示的值。

我将继续这样做，直到我完成所有的颜色值。

完成后我会去看你的。

好的，现在我已经在 figma 中为您创建了所有这些色块，只是为了直观地向您展示并应用正确的颜色行，因为实际上，我最初想做的是使用一个控件选择器并应用这些值，但由于我抓取的截图，它实际上并没有复制图像中指定的精确十六进制值。

无论如何，长话短说，让我们现在做这些颜色的样式。

所以现在我们要把这个加入到我们的颜色系统中。

目前，我们的颜色系统中没有任何东西。

所以基本上，你能做的是，我们已经为我们的颜色样式设置了命名约定。

因此，我们可以去我们的颜色和选择样式，添加新的样式，它将基本上抓取我们选择的矩形的十六进制值，然后我们可以将其标记为主要。

当我们单击“创建样式”时，它将创建该样式。

当你将鼠标悬停在它上面时，它下面会出现一个工具提示。

它会说明你要寻找的风格的名称。

此外，如果你想添加标签，比如说，如果我们创建一个类似的主要变体，哎呀，我们选择这个小，这四个小图标，然后单击添加样式。

然后我们做主斜线变体，这实际上会添加标签。

我们将开始，您将开始看到，随着我们在您的颜色样式名称中使用斜杠，该斜杠实际上为您创建了这个标签，以便您真正轻松地解析您的颜色样式中的信息，这非常好，例如易于使用，而不是仅仅生成所有这些颜色。

然后把它们放在一起，悬停，就可以准确地找到你要找的颜色的名称。

至少把它们分成几部分，让你离找到你需要的颜色更近一步。

我要把它分开，因为这不是我想要的方式。

我实际上要去这里，如果你如果你也注意到了，还有你的颜色风格，如果你还没有发表，会在本地风格下面。

然后，您可以访问您的库，如果您按下 Command Shift，问号，您可以键入 library，然后它会将您切换到团队库。

还有一个快捷键，选项三，它会带你去那里，还有一个快捷键选项命令 o 来访问库。

你会注意到我有 19 个未发布的修改要发布。

我可以发表这些，但我们会在完成这个后再发表。

我将继续创建其余的颜色，并相应地标记它们。

次要的，创造风格。

然后我会把它作为主变量来创建。

创造风格。

在颜色样式中你还可以做的一件事是，你实际上可以拖放来重新排序样式。

因此，我将实际上右击这一个并删除样式。

对于颜色样式，您还可以做的一件事是，您可以进入编辑并添加描述，比如说，这种辅助颜色用于 x 组件，x 表示组件的名称，或者也可以用于 x 组件的表面。

完成后，您可以点击“返回”,它会保存您的更改。

然后，您可以返回到该样式的组件中，您会注意到这些更改现在已经存在。

这是颜色的另一个好处，在使用颜色时，可以了解颜色背后的用法，比如说，你正在为一个产品进行设计，我将暂停这个视频，并生成其余的颜色。

一会儿见。

所以现在我把一切都搞定了。

你可以看到我的颜色风格有地方风格。

所以我得到了材料主题的颜色基线，我把它们都标在这里。

从截图上看，我是从网站上抓拍的。

我基本上只是把它翻译成了 figma。

然后我把这些风格应用到错误表面，很多都非常相似。

但是有一点我不喜欢这个方法，我将向你们展示。

下一种应用颜色样式的方法是使用一致的命名约定，清楚地标记所有内容，当你试图通过下拉菜单访问它时，这不是很清楚我试图改变颜色，对，我只需悬停以找到值。

对于有描述组件，将会出现一个描述，这就是为什么在这个实例中工具提示对于那个颜色样式来说是相当大的，只是为了提供一些上下文。

无论如何，这对于我来说添加颜色样式还不够清楚。

因此，我要做的是在 2014 年材料设计 2014 年在这里建立，他们的调色板，它只是由颜色组成，和谐地一起工作。

基本上，这就是我要使用的一致命名约定和结构，基于，这里给出的颜色，我喜欢称之为变量和它们的十六进制值，然后我将在 figma 中应用。

我将向你展示前三名是如何做到的。

所以我要回到菲格玛。

我之前会展示给你们看，我会展示给你们看，我会移除所有这些，因为我再也不想要这些了，我会真的把它留下。

因此，您可以看到如何使用一致的命名约定导航颜色(在命名这些颜色时使用斜杠)和创建样式时使用样式之间的区别。

不管怎样，我要去这里，拿起我的长方形。

我真的要把这个模块复制到背景标签上，红色，红色，红色 50，来模拟我们这里的布局。

然后，我将按命令 D 进行复制，然后按选项 D 向右对齐，然后键入这个十六进制值。

你们从我这里看到的整个过程是，我只做一次，然后暂停视频，在剩下的时间里继续做。

然后我将向您展示如何在 figma 中将所有内容正确地标记为颜色样式。

然后我会回来的。

好了，现在我复制了这张我从网站上截取的屏幕截图，它包含了材料设计必须提供的调色板的一部分。

我刚刚画了红色、粉色和紫色。

现在，我将开始向大家展示这种差异，以及将这种一致的命名惯例应用到我们即将为红色、粉色和紫色创建的所有颜色样式中会有多大帮助，您可以将这些颜色样式应用到您的系统中。

我将从红色开始，我们有这个红色 50 值，所以我们有红色 50，我们已经有十六进制值，它将单击“添加颜色样式”,我将它标记为 read，因为我们将有一个 rat 部分，范围从 1 到 900，从亮到暗，然后还有 100 到 700。

不确定这些值代表什么。

但是，呃，你知道我们要说什么了。

我将执行 red 100，然后指定十六进制值。

和颜色样式，这总是很有帮助的，所以你不必实际进入并手动应用样式，然后取消链接以查看十六进制值，我觉得这很烦人。

我们得到了这种格式，我将单击“创建样式”,然后按回车键。

现在我们有红色的了。

所以在悬停时，我们会看到红色 100，然后是十六进制值。

然后，如果你点击离开，你还会看到我们命名它的方式是红色斜线 100 破折号，十六进制值，这很好，很容易解析。

同样，这取决于你和你的团队决定如何给它贴上标签，就像为一个组织做这件事以供团队利用一样。

现在，我将进去创建其余的这些用于，用于红色。

也是同样的过程。

所以我们得到了，我们得到了什么，我们创建了红色 100 实际上应该被标记为红色 50 实际上也写了错误的十六进制值。

所以我要编辑一下。

现在是红色 50，可以走了，按回车键，返回。

现在我们看到它被细化并反映了实际价值。

好的，我们再做几个，这样你就知道如何命名你的颜色风格了。

现在，我将暂停它，完成红色，然后再做几个粉红色，然后做几个紫色，看看在尝试访问颜色以应用于组件时，利用斜线和您的颜色样式可以如何有效地解析信息。随着系统变得越来越密集，我们将继续实施更多此类系统。

所以我们这里有红色 100，要添加颜色风格。

红色 100，适合创建颜色样式。

我们有两个属于红色类别，因为我们使用红色空格斜杠来保持命名约定的一致性。

所以我只是要复制这个十六进制值，命令点击我的矩形的颜色，我想成为一种风格。

然后我要贴红 100。

但是将其更改为 200，然后重新应用红色 200 的十六进制值，并创建该样式。

所以现在我将继续创建另一个类别，这是粉红色的。

我们得到了我要再次复制这一点，命令点击并添加颜色样式。

我将使用粉色空格斜杠空格 50，然后是破折号，然后是十六进制值。

创造那种风格。

你会注意到，现在在颜色风格中，我们有了新的我们自己的粉红色部分，以反映我们想要在视觉上模仿的东西，但在我们的颜色风格中，一旦你发布了这个，每个人都可以访问，因为目前这些都是本地风格。

正如您所看到的，当您在画布(空白画布)上随机单击某个位置时，您会看到这里的颜色样式。

但是他们只是坐在那里，但是我们做完炖肉后会推他们。

我现在要用紫色，因为我很确定现在你们已经知道怎么做了。

因为我已经做了多次，我要复制这个十六进制值，选择背景，添加颜色样式，我要去紫色空间斜线添加十六进制值，然后输入第五个 50。

好吧，进来。

现在我们有红色、粉色和紫色的部分。

我们还没有从材料设计网站实现的其他部分，我将自己完成，如下。

正如你所看到的，我浏览了很多，这将花费很多时间，但都是同样的过程。

但是我会暂停这个视频，完成那个，然后给你们看，一旦我完成了，我们就会发表它。

这将是视频无鞍的结束。

好吧，好吧。

好了，我用我指定的暂停这个视频的方法完成了剩下的颜色样式的创建。

你可以看到这种风格变得多么密集。

所以每一种颜色都是由红色、粉色和紫色组成的。

还有很多要做的，我会把它们都制作出来供你参考，我指的是所有这些，这些截图会转换成 figma 文件。

花点时间，但是，呃，这不是问题。

所以现在，最后一步是发布这个。

那我们该怎么做？我们如何做到这一点？我将向您展示几种方法，我们可以选择资产，我们可以单击这个库图标，右上角的这个蓝色小图标表示有发布者要进行更改，或者有发布者要进行更改，您可以选择它，您可以看到您可以发布 69 个更改，这 69 个更改是我们应用到该库的所有颜色样式。

我想向大家展示的一件事是，我们在开始时标记这些颜色的方式，与我们以一种更有条理、更清晰简洁的方式标记这三个部分的方式之间的区别。

因此，如果我们到这里，创建一个矩形，并应用颜色样式。

我们只是喜欢这个小图标，你会看到我们有这些颜色，你能真正说出它们的标签是什么的唯一方法是，如果我们悬停在一个母亲身上，等待看到工具提示弹出。

我们对颜色做的是用斜线表示标签，斜线的左边是读数，然后是应用于样式的值，读数为 50。

悬停时，也可以查看十六进制值。

我们这样做了，以一种更加清晰和简洁的方式将红色、粉色和紫色分开，以便用户导航和使用。

同样，这仍然有点麻烦，因为这个调色板是如此的密集。

但这是我认为 figma 为这个和我自己创造的最好的解决方案。

如果有其他更好的方法，请在下面的评论区告诉我。

我希望你们觉得这个视频非常有帮助。

感谢您的时间，请留下喜欢，评论，并在 GitHub 上订阅和关注我。

如果没有，我会在下一个赶上你。

你们在干什么，我差点忘了，我们还得出版图书馆。

因此，我向您展示了如何发布它的多种方法，并使用“转到视图”“切换到团队库”或您可以在“资源”面板中选择此图标。

另一种方式是我的最爱。

我的快捷键是选项命令零。

然后您可以选择发布更改。

然后你可以查看每一个修改过的样式。

所以在这种情况下，有一堆被删除的，你知道，颜色不是很明显。

然后是我们添加的颜色，当然，我将点击，然后您也可以添加对您添加的内容的描述。

因此，我将它作为一种类似 git commit 消息的消息，一旦发布，它将显示在版本控制历史记录中。

所以我在添加颜色，给居里夫人添加一部分颜色。

标志，颜色系统到图书馆。

然后你有一条信息，解释你做了什么，然后发表。

然后它会将此添加到您的库中，这是一个指示流程成功更新的小零食栏。

然后你可以查看你的资产。

它的好处是你现在可以查看，如果你去材料设计，你可以在这里查看你所有的颜色。

在 hover 上，你可以看到一切。

它没有这里的颜色面板布置得好。

但是，这就是你将颜色发布到你的库中的方式。

这是最后一步。

今天我将向您展示如何创建一个类型系统 figma 特别是材料设计类型系统，链接将在描述中引用材料创建的类型系统。

基本上只是给你一个如何真正开发一个用于你的内容的类型系统的一般概念，特别是你的组件和我们将在 figma 中创建的模式。

我也推荐使用这种字体，这种字体学校生成器，他们在网页上有一个很好的互动元素，可以让你真正看到字体技能和不同的字体，可能更像是你正在使用的字体家族，你可以看到它的规模。

如果这是个很好的资源，我想我会分享的。

再说一遍，我建议你看完所有这些。

因此，基本上，我们将从头开始创建它，并将它们开发成 figma 中的典型纺织品，这是一个非常合理的防撞设计系统，也是我们将要建立的系统。

所以让我们开始吧。

我在这里打开了 figma，你也可以访问这个练习文件，如果你不想这样做，只是想获取值，我会有那个 figma 文件，一个描述中的链接。

因此，我们在这里总结了所有标题或副标题、正文和内容的属性示例，例如行上的按钮标题，我们基本上只是要将所有这些属性应用到我们的纺织品上。

所以事不宜迟，让我们开始吧。

好了，现在我们得到了这个，我将在这里打开我的类型工具，我将在 h1 中选择类型。

我将把它设置为自动宽度，因为除了为类型指定的空间之外，我不喜欢任何不必要的行外空间。

我将把这个大小设置为 96，它已经是 96 了，行高设置为自动，但是我们将把字母间距修改为指定的负 1.5。

当我确定我在这里选择了，选择我的类型，然后输入负 1.5。

点击输入它收紧这里，字距和或我们的字母间距更好的词，因为这里表明。

但无论如何，我将继续在这里建立这些类型的风格。

我将输入 h2，选择我的字体，确保我得到了正确的字母间距，这样我就不会忘记，并将其设置为负 0.5。

再次，如果你有任何困惑，想了解更多的属性，你可以点击这个图标，在这里选择你的文本，更多的图标，并查看这种字体提供的所有属性。

需要注意的一件重要事情是，如果你使用不同的字体，但是创建了这种类型系统，有提供和不提供的功能和特定字体。

因此，如果您在这里看到一些功能，而这些功能并没有以您在本视频中使用的单独字体提供，这可能是因为它没有提供该功能。

请注意，我也建议大家通读这些非常有用的信息。

但这是另一个视频，如果你想让我制作它，请在下面的评论区留下评论。

不管怎样，我们指定了字母间距，我们需要改变这里的大小。

而且分量不错。

线的高度，你可以开始看到这个过程，非常简单。

你重复这个动作，继续前进。

在我快速浏览之前，我要说的唯一复杂的细节是按钮属性。

因此，如果我继续键入全部小写，我实际上可以在文本属性中应用全部大写的句子大小写，这太棒了。

我将通过选择大写属性来实现这一点。

此外，还有其他属性，如小写标题、小型大写字母和小型大写字母。

我只想指出这一点，因为我认为这对这些纺织品非常重要，要真正优化它们。

我们将转到字母间距，并在 1.25 中实现 add，我们已经设置了字母间距。

现在我们只是要改变大小和重量设置为中等。

现在我们已经把所有的大写字母都按在按钮上了，我们甚至不用按住 Shift，这太棒了。

这里还有一分钟的变化。

对于这种纺织品，我们当然要改变尺寸和重量。

但最重要的是，我们转到文本属性，我们要将大小写更改为与此处的大小写一致，因为字体大小设置为 10 非常小。

除此之外，你们可以继续，使其余的这些也将有这个文件和下面的描述。

所以我马上带着这些纺织品回来。

好了，现在我知道我已经创建了所有具有适当属性的纺织品。

我希望你也做了那个练习。

无论如何，现在我们要开始组织我们的风格，然后发布它们，这将是视频的其余部分。

因此，我们将利用 figma 结构访问纺织品的方式，这是一种非常好的方式，可以将所有标题内容与正文中的副标题以及其他类型内容的纺织品彻底分离，如按钮、标题和 over 行。

因此，我将转到这里，选择我的纺织品，单击此图标，单击加号按钮创建一种风格。

我将输入标题斜杠。

使用斜杠命名约定，它实际上允许我按类别区分我的纺织品。

因此，我们将有我们的标题和副标题，正文和单独的内容，如按钮，标题或行。

所以我要在这里指定这个 h1。

然后它会在这里加一个小点把它分开。

所以我拿到了 h1。

你也可以指定字体的粗细，这个 h1 是一个轻的 h1，以防你以后因为某些特殊的原因有了 h1 的变体。

我们还可以指定尺寸，这里是 96 号。

我喜欢你所能做的就是使用这个分隔线，指定与字体大小相关的行高，这也是非常重要的。

但是我在 figma 把这些都设置成了自动模式。

但是，例如，您可以编写 auto 或指定行高。

同样，当你创建一个样式时，当你试图应用一个样式时，它会出现在标题下。

说我现在要应用样式。

在这个分类标题下。

一旦内容更加充实，就更容易看到内容的组织。

因此，我将继续在这里为我的头创建完全相同的东西，我的头的其余部分再次使用斜杠约定，指定它是 h2。

它使用重量轻。

字体大小是 60。

行高设置为自动。

同样，对于您的系统，可能需要更具体的行高，但在本例中，我不会涉及这方面的细节。

因此，创建该样式时，您会注意到它在此处更新，我们将再次填充它，获得 h3，再次指定重量，并指定大小。

当然，还有行高。

所以你可以看到人口在继续增长。

现在我要继续完成剩下的部分。

我将创建一个副标题，以便您可以看到两个不同类别之间的分类差异，这将是我们的标题和副标题。

所以我要去这里，添加样式，标签，副标题，副标题和一个，指定权重，只是普通的。

当然，还有字体的大小。

我不知道，我想了一下，但是我会把它设置为 6，副标题设置为 16，行高设置为自动。

现在你可以看到，你的纺织品已经整理好了。

很明显，在这里，我要继续完成剩下的部分。

我马上回来。

好了，现在我们已经设置并组织好了所有的内容。

所以你可以看到我现在已经把它们拼接成了 344 个类别，我们有了标题和副标题，还有正文和内容。

我实际上缺少的一个纺织品是这个副标题，这里发生了什么，让我快速地说一下。

我们有字幕，字幕，字幕想，哦，我们走吧。

我会很快组织好的。

我们走吧。

因此，您创建纺织品的顺序就是它在层次结构中的显示方式。

所以你可能需要组织一下。

现在，当我们继续创建纺织品时，我的意思是创建，当我们需要应用纺织品时，我们可以到这里，查看我们的纺织品。

现在它们以清晰、简洁的方式组织得非常好。

不仅它们井然有序，每件纺织品的命名方式也很容易指定尺寸和线高。

再说一次，我只是设置了自动。

嗯，如果你想的话，你也可以根据你真正需要的技术水平来设定字母间距。

这就是你要做的。

现在我们的最后一步是将它发布到我们的图书馆。

所以现在我有 26 个改动要在这里发布。

对于那些不知道如何使用它的人来说，这基本上就像 git commit。

对于那些忽视所有这些被移除的纺织品的开发者来说，这些都是旧的。

在这里，你可以看到你创造的所有变化，也就是你添加的所有纺织品。

我们可以写一份描述。

向设计系统添加文本样式。

这是非常模糊的，但给你一个如何做到这一点的一般想法。

同样，您可以在下面查看所有更改，以及所有未更改的内容。

然后你就可以出版了。

留下评论的好处是它留下了跟踪任何历史的线索。

因此，如果我们在这里显示版本历史，通过单击这个胡萝卜，单击“显示版本历史”,我们可以看到我们刚刚发布了我们的纺织品并留下了该消息。

所以我们实际上可以通过点击它转到那个时间点。

它会带我们去那里。

或者我们可以回到更远的时间点，那时这些东西还没有被创造出来。

因此，如果我单击“6:47pm ”,它将会加载，我会看到自动保存的时间是 6:47pm。

你可以看到这里没有纺织品。

而且你不能编辑这个版本。

但是如果你需要的话，你可以复制这个文件，然后去那个复制的文件，获取你需要的，然后把它放到当前版本中。

如果你点击退出，你可以回到你当前的版本或者你可以点击按钮编辑当前版本。

如果你迷路了，我会在键盘下按两个快捷键，让你的画布上的一切都适合你的框架，现在我们发现自己回到了这里，所有的纺织品都在这里，不再是本地的了。

现已出版。

所以现在设计师们可以在他们的面板上看到这个，我们去，我把我的材料设置在这里，上面有我所有发布的颜色和我的纺织品。

今天我们将讨论立面和材料设计，以及如何利用它们。

因此，我们将对立面进行概述，包括立面描绘、理解立面描绘、立面层次结构以及组件的默认立面设置。

我有一些从材料设计网站上下载的文档，你也可以点击这个和练习文件，然后点击打开链接。

然后，您将进入材料设计中的立面页面，在那里讨论所有这些内容，并在此处深入讨论。

实质上，仰角是两个表面之间沿 z 轴的相对距离。

那么 z 轴是什么呢？好吧，我们继续谷歌搜索。

对于你们这些初学者，不理解 z 轴的人来说，这是第三个通道，通常代表三维网格上的深度。

它可以用在图表或图形中，通常在笛卡尔坐标系中，z 轴垂直于 x 轴和 y 轴，用于绘制 z 的值，这是数学中的第三个未知数，知道这一点很好。

不管怎样，这在 figma 中的翻译基本上是等级制度。

在图层中，你可以想象图层面板中的层次结构，比如说，我这里有一个框架。

然后我有几个形状。

我要在这里抵消这种颜色。

目前，形状基本上是利用 Z 索引。

你会注意到，因为这个矩形，当我将它重叠在它上面的矩形上时，这个灰色的矩形，我在那里添加这个红色的矩形，你会注意到这个红色的矩形在 Z 索引上更高，你也可以认为在层的顺序中，我甚至可以改变它，并将其拖动到这个灰色的矩形下面，这意味着它现在在 Z 索引上更低，红色的矩形， 您可以继续移动它，并了解这是表示此处图层顺序的 Z 索引。

基本上，材料设计所做的是它有一套组件。

在这里，您可以看到各种组件的一组默认高程值。

这里，我们有所有的高度。

值将转换成样式，因为它们已经在本地的 figma 文件中。

我们将从头开始创建几个，以便您了解它们是如何作为样式构建的。

我们还将根据阅读文档的情况，简要概述这些功能是如何实现的。

所以在这里，同样，海拔是用倾角来衡量的。

而在立面中立面材质设计就是测量表面之间的距离。

正如你在这里看到的，他们给了你一个 z 指数的表示，通过我们利用这个图表，这个格式来表示一个表面离另一个表面有多远。

但是，对我们来说，视觉上看起来很可怕，我们在屏幕上看到这匹马在这个平面上。

但是，如果你将视图向侧面倾斜，你可以说，这就是第二个例子中的场景，其中一个表面的倾角设置为一个倾角，另一个表面的倾角设置为八个倾角，从侧视图来看。

并且从侧面角度看，两个表面之间的差异是七度倾角。

你可以用阴影来表现。

这将显示这两个表面之间的高度和距离。

这是这个矩形中的一个倾角，这是从侧面看的。

这里是在一个更高的高度表达的高度，使用了八个倾角，更多地强调了投影，正如你所看到的，这也是从侧面表现的。

这里表示距离。

还有一个更复杂的例子，表面 A 和 B 都在相同的高度，nBw 设置为 8 度倾角，它们投射不同的阴影，因为表面 B 在另一个已经有高度的表面前面。

正如你所看到的，表面 c 已经设置了四个倾角。

然后在表面 B 的顶部设置八个倾角，你可以从侧面看到，而表面 a 下面没有任何东西。

它被设置为八个仰角。

所以 a 和 b 基本上在同一轴标高上，只是它下面没有其他曲面。

同样，您可以理解高程系统在组件中使用，所有材料设计表面和组件都有高程值，不同高程的表面有不同的功能。

这些东西允许表面在其他表面的前面和后面移动，如应用程序栏后面的内容滚动，并反映空间关系，如浮动操作按钮阴影如何表明它与卡片分离，并将注意力集中在最高的高度。

这在组件(如对话框)中很常见，它们暂时出现在其他表面的前面。

接下来，我们将看一些很好的例子，看看这些组件在一些运动示例中是如何工作的。

但这是一个很好的静态提升示例，默认情况下，此卡组件在默认应用程序中将提升设置为一次下降。

一旦对它采取了行动，有时高度会改变。

因此，了解这一点很重要，例如，对于一个按钮，当您按下它时，高度会发生变化。

在这个例子中，当有人按下这个按钮时，你会看到从侧面看，它会根据将它提升到表面的动作从两次下降到八次下降，这使它成为一种更加身临其境的体验和互动，也在用户与他们的产品交流时向他们传达某些关键的互动和进展，例如，这是基于材料设计的。

在这里，你可以看到这张卡的其他提升示例，当它被拖动时，它从一个倾角提升到八个，然后重新访问，然后上升到上一张卡之上，因为它在 Z 索引中位于它之上。

这是另一个例子，当这些卡片滚动时，你可以利用仰角。

在那个基于高度的系统上，你可以在你所看到的表面上滚动。

而且你还可以阅读其他重要的东西，比如了解表面重叠在其他表面之上，以及何时使用它们，这真的很重要。

以及何时在用户界面中使用稀松布背景来表达其上方的内容处于更高的高度，以便在该时刻更加强调最主要的行动号召。

同样，更深入地分析海拔高度。

当您可以一起使用运动仰角时，例如，显示阴影变化或显示重叠，或推动内容或缩放内容，或使用视差滚动。

我们现在要做的是理解海拔等级。

基本上是查看默认高程值的图表，这非常重要。

我们在 figma 也有。

既然我们已经谈过了。

因此，我们有了这组标高值，它准确地告诉我们哪些组件有默认标高值。

这对于融入我们的色彩风格非常重要。

在这个我已经创建的 figma 文件中。

我已经创建了一组高程值，它们已经是你的 figma 文件中的效果样式，这个练习文件。

再说一次，这是我们将要烘焙的最终结果，但是我不只是想把它交给，实际上向你展示如何创建这些效果风格。

因此，我们将继续创建其中的几个，然后我将暂停此视频，让您自己构建其余部分。

首先，我需要教你如何实际评估每个阴影的值。

这里你可以看到这些都是在材料设计中使用的标高值。

再说一次，这个不是。

这是在表面水平，这没有利用海拔，正如你看到的，设置为 00 倾角。

这不可能是特效风格，因为它不使用任何特效。

那只是平坦的基线，就当 00 是基线吧。

然后，当然，默认情况下，一个 dip 用于处于静止高度状态的搜索条卡，以及处于静止高度状态的搜索条。

和开关元件。

和文本按钮被设置为零，并且标准侧页组件也被设置为零。

让我们继续，我要复制这个。

您会注意到它是附加到样式上的，我将继续将其分离。

而且你会注意到这些款式都是投影。

所以我要把这些都去掉。

现在右边这里是平的，我们可以做的一件事是手动进入并单击这些单独的样式，这非常繁琐。

我们可以继续这样做。

例如，如果我单击这个值，我可以截屏所有这些，因为我不想来回点击所有这些阴影，复制，粘贴这些属性。

所以我要向你们展示一条漫长的道路。

然后我会告诉你最快的方法，这样你就不会再用那种方法了。

让我先撤销这个。

然后我要粘贴这个，开始。

所以我要抓住这最后一个阴影样式。

然后用阴影样式设置，我有所有三个阴影，我将重新创建 01 倾角的高程设置。

因此，我将继续点击“效果”。

哎呀，我要点击我的创建效果样式图标，默认情况下，它选择投影，它会给你一些默认值。

所以我要在这里创建一个。

所以我要把模糊度改为 1，这个和那个，我要把 y 轴的值设为 1。

然后我要保持传播为零，改变不透明度为 14%。

它用的是黑色，000000。

现在我已经创建了第一个投影样式，你可以开始看到这个投影被应用到这里，但是它的投影颜色非常浅，相比之下，它在矩形的边缘更加清晰和黑暗。

我将继续创建第二个投影。

就这样，我要模仿这个阴影的属性。

模糊设置为 1，扩散设置为 0，y 轴设置为 2，不透明度为 12%，按 enter 键，我知道应用了第二个投影属性，然后我将删除这两个属性，这样我就不会混淆，并创建我的最后一个效果，这是另一组投影，我将再次应用这些值。

因此，一个在 y 轴，三个在模糊，零蔓延和 20%的不透明度。

一旦我创建好了，你会注意到我现在有了一个精确的阴影的复制品。

给你。

唯一不同的是，这个阴影是全新的，它仍然是材质设计系统的阴影。

只是它不是为了学习的目的而附加到一个样式上，具体来说，就是为了让你知道如何创建这些阴影。

我们可以继续把它变成一种风格。

但是在我们把它变成一种风格之前，让我们先向您展示一种简单的方法。

因此，如果我们现在已经有了这种风格，也许我们可以继续复制这种基于八度倾斜的版本，它仍然附属于一种效果风格。

所以我要脱离这种风格。

我要复制这个矩形。

我要删除所有这些阴影。

我要做的是确保我的矩形被选中，我要按住 Option Command C。

这允许我复制与我选择的对象相关联的所有样式，我选择的对象是矩形，然后我可以将它们粘贴到我的新样式新对象上，方法是单击这个矩形，然后按 option Command V。

它粘贴那些精确的投影值。

你会注意到，当我粘贴它时，它实现了那些新的投影值。

这比我手动操作并参考截图要快得多。

所以我不必逐个点击每个投影属性面板，然后一个接一个地做。

那是非常讨厌的。

也就是说，现在让我们利用这种方法。

在这里，我们可以复制过来，我按住 command 单击所有的 command 单击来选择矩形，然后按住 Shift Command 单击这里的所有矩形。

基本上，我要做的是抓取所有这些内容，然后按住 Option 并拖动它们来复制所有内容。

你会注意到我们这里有所有这些高程值或效果，所有这些矩形都与效果样式相关联。

但是我们不想这样做，我们想利用创建投影的方法快速制作属性的副本并粘贴它们。

要做到这一点，我们需要继续下去，解除所有这些阴影样式的链接。

现在我们开始，我们将继续分离这些样式，我只是单独选择每个样式，因为很遗憾，对于效果样式，您不能批量选择这些样式并将其分离。

但是你可以用颜色风格做到这一点。

现在我们已经应用了，我知道这些都是按顺序的或者正确的顺序。

因此，它从一个倾角，海拔，234-689-1216，和 24。

所以我要做的就是快速复制这个。

在我分离所有这些阴影值之前，我要再复制一次，在我没有创建的矩形上复制它们。

暂时放下阴影。

你会注意到它们都非常平坦，这意味着它们基本上使用的是零倾角仰角。

我将继续点击这些命令选项，选项 C，然后选择我要粘贴阴影的对象，然后点击命令选项 v。

你会注意到我现在只是复制和粘贴这些效果样式属性。

所以按住 option，Command C，选择新的对象，然后执行 option Command V。

我只是抓取了所有这些阴影属性，并快速粘贴它们。

这也可以用于颜色。

因此，如果我改变这个的颜色，它会复制那个颜色值，它仍然有阴影，但是如果我选择命令 C，然后选择我的新对象，然后选择命令 V，它也会复制那个填充值。

所以它复制了所有的属性，而不仅仅是效果样式，在我们的例子中是投影。

所以一定要注意对象，命令 C，选择菜单对象对象，命令 V，然后对最后一组立面做同样事情。

这一点非常重要。

现在我们已经完成了所有这些，我们可以继续用它们来创建效果样式。

如您所见，我们已经创建了它们。

所以我们可以继续模仿一对夫妇。

我将继续，这是我们的零蘸效果风格。

因此，我将继续操作，单击我的样式图标，然后单击添加新样式。

然后我输入 0 来蘸一下。

这是我们新的特效风格。

我们现在将有两个版本。

然后它被创建并被放置在底部。

最新的样式会在效果样式和属性面板的列表底部生成，我甚至可以继续创建另一个样式，您会注意到它现在链接到了一个样式。

这些还没有，因为我还没有创建颜色样式。

我将继续单击另一个，然后单击样式图标并创建新样式。

这是我的 24 度倾角仰角基础值。

因此，我有 24 个倾角，我们将单击创建样式，我现在有零倾角和 24 倾角创建。

但这只是为了向你们展示如何用这些高程值来创建样式，因为你们会一次又一次地重复使用它们。

所以我要删除这些，因为我们的文件里已经有了。

既然你已经知道了如何在 figma 中创建效果样式。

也就是说，这些特效风格中唯一缺少的就是文档。

基本上，我所指的是，在这些效果风格中，我们可以继续下去，并标记这个高度将应用于何处。

我将单击 24°倾角，因为我们有默认高程值的表格，它会告诉我哪些组件使用该高程集。

因此，我可以点击这个编辑样式图标并添加描述。

你和我可以键入对话组件的使用，我将点击 tab，它将保存更改，我将点击 back，我将继续选择我的 16 dip，修复样式，单击 Edit style。

我会继续添加描述。

和当前符合导航抽屉组件和模态底板。

和模态侧板构件使用此默认标高值。

因此，我将继续输入用于这些组件并已成为导航抽屉的内容。

以及莫代尔底片和莫代尔侧片。

现在我有了那个输入，它将保存那个，你甚至可以回到你的，你的风格，编辑它，检查它是否保存了描述。

我将继续输入描述，这样当您发布这种样式，其他设计师开始使用这些样式时，他们可以继续点击此描述，了解您实际应用这种立面样式的位置，这是与您的设计师交流的非常有用的工具，尤其是当您不知道这些样式在哪里使用时。

在这里，您可以看到许多组件使用默认的八度仰角值。

我将继续播放并暂停此视频，我希望大家继续，对于其余的八种 dips 64321 dip 熔化效果样式，我希望大家继续参考此处的表格，并将其输入到描述中，就像我们刚才对以下效果样式所做的那样。

我要暂停这段视频，完成后我会通知你。

现在，我已经完成了对其余高程值的所有描述。

正如您在这里看到的，它直接反映了默认高程值和材料设计表中标注的内容。

同样，我从材料设计立面页面获得了这些截图，如果你想仔细检查，也可以在这里找到链接和练习文件，并了解更多关于立面本身的信息。

因为这不仅适用于材料设计和设计系统，也是你实施新系统的基础。

所以这也是需要考虑的重要因素。

这里再次，如果我去编辑样式，我只是再次检查我所有的描述。

一切准备就绪。

今天，我们将讨论产品和系统图标，以及它们的不同之处。

以及它们之间的区别。

我们还将回顾图标在材料设计中的制作原理，我们将简单回顾一下。

然后，我们还将使用一个您将在整个课程中使用的工具，如果我在练习文件材料设计图标中选择此链接，我可以单击该复选框并单击该链接。

你会注意到我们有这个资源来访问这些图标。

因此，在本练习文件中，我已经在“材质设计”中导入了一些但不是所有的系统图标。

我是故意这么做的，因为我想让你自己也能做到。

不仅要自己去做，还要了解如何利用两种方法去做。

因此，在您的练习文件中，您将有一个名为“材质设计图标”的 zip 文件夹。

在这个文件夹中，有一组 Android 的图标，用于开发 Android。

因此，如果你是为 Android 设计的，你可以在这里看到这些图标的正确格式，就像你在点 XML 文件格式中看到的那样。

此外，我们还有这些图标的字体基础版本。

然后我们有基于 iOS 的版本。

然后，我们还将它们导出为各种尺寸、各种不同尺寸的 png，从 18 度倾斜、2436 度倾斜和 48 度倾斜，每个尺寸分别为 1 倍和 2 倍，如您所见。

这很有帮助。

但这份文件里也有很多材料。

如果这对你来说太难了，你可以继续使用这里的资源。

同样，点击 figma 中的材料设计图标链接可以访问该资源。

打开它，你就可以访问这个神奇的资源，我们在本课程构建组件时都会用到它，以防我们错过了 figma 中的一些图标。

正如你所看到的，从我最初的导出中，我对这些图标进行了视觉分类。

不仅如此，在图层面板中，它们在导出时也是如此。

这些是每个图标的名称，这很好，所以我们不必自己给它们贴标签。

但是如果我们愿意，我们也可以稍后调整命名。

也许我们想去掉那些下划线。

当我们实际发布这些器件以在我们的设计中使用时，我们就会明白这一点。

这里我们有一组 10 个类别，用户操作、图标、字体图标、特定于设备的图标。

在这里，你可以看到这在 Android 和 iOS 设备中被大量使用，指示电池的状态、飞行模式、它是开还是关，或者你是否设置了闹钟，或者你的蓝牙是启用、连接还是禁用，以及其他类似的事情。

这就是这个设备类别。

然后我们有一些其他类别，如一些编辑器图标，可以看到常用的和其他谷歌产品，如谷歌文档，像其中一些是选项，当你选择文本并在这里编辑它们时，一些对齐图标等等。

还有一些硬件图标、内容图标、文件图标和一些其他图标，例如通信警告图标。

再说一次，这只是触及图标的表面，因为在这个工具中，有一个重要的功能。

在这个主题类别下。

在左上角，你会看到所有这些图标都有不同的变体。

所以有填充版。

这是大纲版本。

你会注意到，这个填充的版本，肯定不同于轮廓的版本，你可以利用同一个图标的这两个版本来表示状态。

这些图标还有一个圆形版本，角更圆，正如你在这里看到的，与轮廓变型相比，元素变得更圆，更平滑，你可以说，我们也有这种双色调变型，你会注意到有这种第二色调的颜色，这是一种浅灰色，作为界面中几个元素的背景色。

然后我们有图标的锐利变体，在那里角落在边缘上绝对非常锐利，诸如此类。

因此，考虑到这些变化，我们还可以切换特定于特定操作的类别，或者如果我只希望显示警报，或者只显示 V 图标，或者只显示通信图标。

因此，基本上有能力单独查看所有这些图标或一次查看所有图标，因为有大量的图标，我建议你只是浏览一下这种理解，你认为你会和不会使用，为什么不会将是一个很好的测试，这意味着你对材料设计的理解，以及当我们开发组件库前进时所需的图标。

同样，如果我们没有这些内部 figma 文件，也不用担心。

如果它不在这里，我们可以在这里创建一个新的类别。

当然，假设我在社交类别中没有图标，因为有很多社交图标，就像点击这个图标一样简单。

然后在你浏览器的左下角，你会发现你可以导出这个图标。

它会告诉你要导出的大小和格式，所以这是基于 SVG 的图标格式。

我还可以单击它，打开这个面板。

你可以下载 SVG 格式，或者基于 PNG 的格式，我们将坚持基于 SVG 的格式。

还有一些关于如何使用 CSS 来设计你的图标的说明，这里给你一个小例子。

你也可以改变下载的大小，SVG 会分 24 次下载。

因此，你也可以得到黑色版本的图标或白色版本的图标，我们将坚持使用黑色版本的图标。

因为我们将在内容中设计浅色主题，比如字体，图标将是深色而不是浅色。

你也可以下载基于 iOS 的版本，安卓版本。

同样，所有这些图标都已经在你的文件里了，在你下载的练习文件里，你可以在 source 或者你正在设计的任何设备下找到它们，你在这里有所有格式的所有这些图标，轮廓圆锐化双色变体和填充变体。

话虽如此，我们可以继续讨论，因为我们已经讨论了所有的系统图标，这是我们刚刚讨论过的，我们刚刚讨论了所有的系统图标，同样，这也在此处的文档中，也可以在 figma 文件中访问。

我会确保它在里面。

所以你可以点击这个链接。

所以基本上，这象征着文件，设备和目录的共同行动。

整个概念是简单，现代，友好，有时古怪，因为每个图标都被简化为最简单的形式，表达你试图用图标相关的设计元素采取的行动的基本特征。

你可以看到，如果你也要创建自己的图标，它会教你如何使用与文件构造相关的适当参数来创建图标，利用这里的网格和关键线条形状的规范，以及如何在密集的布局中使用它们，只是图标本身的布局，这真的很有趣。

此外，还有其他一些规范，比如分解图标的组成，讨论图标的颜色和状态。

我们有这些图标，在这里指定的亮背景上的状态，这很好，我们可以将它合并到我们的系统中。

它也谈到了方差。

图标主题，我们已经在这里的工具中过了，主题被填充，勾勒，圆形的色调和尖锐。

这些都在系统图标中讨论过。

我们现在也可以在 figma 和他们一起玩。

如果你想继续点击它们，复制，粘贴它们，把它们扔进组件。

但是当我们开始构建我们的组件库时，我们会更加注意向前发展。

我建议你通读这篇文章并做笔记，因为这肯定会告诉你如何系统地使用图标。

作为一名设计师，你也可以玩得更好。

话虽如此，我们现在可以继续讨论产品图标了。

所以产品图标和系统图标是分开的。

因为这是品牌产品及其提供的服务和工具的视觉表达。

例如，在谷歌浏览器中，如果你有一个谷歌帐户或 Gmail 帐户，你会注意到这些是产品图标，而不是系统图标，就像这个浏览器中的一样。

这些是系统图标，是为界面上的常见操作和项目而设计的。

而这些产品图标在这里代表产品本身、服务或工具。

所以这是需要注意或区分的重要问题。

因为如果我们回过头来谈论产品图标，它会谈论它如何表达该产品以及它如何与制造它的公司相关联，所以在这里你可以看到这是在这里说的，这里的图标在这方面 Gmail，Google Calendar 和其他几个图标传达了产品的核心思想和意图，它们简单，大胆和友好的方式，而每个图标仍然是独特的。

一个品牌的所有产品图标应该通过这些概念和在执行中统一起来。

在这里，你可以看到他们实际上把他们的设计看作是一种物理性质的材料，比如纸，在那里，在那里，每一片被切割、折叠和点燃。

它也利用光和图标联系起来，用数字的形式表现出来，真实地展现出这些表面和光的互动。

你可以看到这些产品图标上微妙的高光和一致的阴影。

因此，如果我们继续前进，回到谷歌的产品图标套件，你可以在 Gmail 图标中看到，你可以看到这些边缘应用了阴影，所有产品图标都实现了一致的照明。

你可以得到关于这些图标是如何建造的规范等等。

但是这不是我们将要做的特别具体的事情，但是我强烈建议你浏览一下。

如果你将来想开发你自己的图标，这是非常有帮助的关于如何对待图标，以及它们应该如何与元素表面齐平，等等。

以及如何通过不使用过多的手风琴式折叠来对图标进行适当的关注。

例如，在这种情况下，您会看到这些手风琴式折叠，或者看起来像是这张纸在折叠，或者这个图标在折叠，而不是有太多的手风琴式折叠，使用不超过两个或一个手风琴式折叠。

两个确实为你的图标提供了一个清晰的焦点。

当然，无论是产品图标还是系统图标，都不能扭曲或变形。

因为这不是设计的目的。

这就是我今天为大家介绍的关于产品图标的全部内容。

我建议你只是练习点击其中的一个，下载成 SVG，然后把它保存在一个对你有意义的文件夹里。

同样，你已经有了这些图标。

但是我建议也养成这样的习惯，因为也许我在这里漏掉了一个图标。

我们可以保存这个 SVG，我可以打开 figma，访问下载文件夹，我的 finder，然后把这个图标拖到画布上。

现在我有了这个图标，它实际上已经在这里了，你会注意到我有了这个图标的填充版本，就像它在这里声明的那样。

所以我得到了一个 24x 24 的 3d 旋转图标，宽度和高度设置为 24 度，在这个例子中是像素。

或者，thigma 称之为点，但不是为了混淆你，只是在这里称之为 dips，并参考材料设计，测量术语。

这就是你刚刚添加的内容，你可以将它添加到你的库中，并对其进行相应的组织。

随着课程的进行，我会继续建立这个图书馆。

但是当谈到肖像学时，这就是我能给你的全部。

今天，我们将讨论如何构建基础，特别是文本易读性，以及这对于确保我们使用正确的颜色值有多重要。

因此，在本教程中，我们将概述材料设计网站上的文字易读性。

所以在这里，它讨论易读性标准。

在之前的视频中，我们讨论了莱昂纳多色彩工具，并简要讨论了设置对比度，或者利用这个工具来获得适当的对比度。

我们将深入探讨这一点的重要性。

这里有一些非常重要的文章，我推荐你们阅读。

这篇文章是关于材料设计网站的，涵盖了一般的可访问性，我强烈推荐我们，你浏览一下，全部内容。

这里讨论的是颜色对比，可以用来帮助用户看到和解释应用程序的内容，以及它是如何在你将要构建的应用程序的所有界面中大量使用的。

不仅仅是颜色和对比，还包括布局和排版。

我建议你自己读一遍。

我们将触及 WCAG 标准和 WCAG 代表的网络内容可访问性指南。

对于那些不知道的人来说，这需要双 a 符合才能达到 4.5 比 1 的正常印刷字体的文本和背景之间的颜色对比度，正常印刷字体是你的正文，比如我强调的这个网站中的正文。

此外，三比一利用大文本，大文本可以是这些标题，确保它们具有三比一的对比度。

这里你可以看到它实际上是白底黑字。

所以那些对比度会非常非常强。

我们将继续下去，只是浏览背景上的文本，因为这基本上是我们的排版将在。

它会出现在各种组件的背景上。

正如您在此图中看到的，此文本位于应用程序栏上，并且位于应用程序栏背景色上。

你也会注意到，这里我们有一个各种各样的通知横幅，字体覆盖在通知的背景颜色上。

有一点非常重要，那就是谈论文字不透明度是如何在材料设计中应用的，因为他们非常依赖它来表示状态。

所以，同样，黑色文本被推荐用于浅色背景，白色文本用于深色背景。

这一点很重要，尤其是当你为浅色主题或深色主题开发应用时。

只要确保文本具有如上所述的适当对比度即可。

我们将继续讨论构建一些这样的例子。

现在我们已经讨论了文本不透明度的使用，或者如何在截图中使用它以及如何避免使用它。

在这里你可以看到这种排版并没有改变文本的不透明度。

但是在这里你可以看到不透明度被修改了，因此，发出不透明的灰色，这是不清晰的，在彩色表面上很难看到，正如你所看到的。

接下来我们将讨论浅色背景上的深色文本。

所以材料利用了这些原理。

因此，对于，如果您愿意，您可以将其视为活动状态，或者如果您想要对某些信息应用高度强调，可能您想要对标题应用高度强调，例如，此网站，使用黑色，不透明度为 87%。

然后，如果您想要应用中等强调，这可能是中等强调，如排版，或者是默认状态，对于在组件中键入更高的费用，您使用 60%不透明度的黑色。

然后禁用状态使用 30-38%的不透明度，颜色为黑色。

我们将继续讨论如何在背景上稀疏地使用彩色文本。

所以彩色文本通常不会到处使用。

它被有选择地用来引起注意和应用。

非常有选择性的强调是理想的颜色文本应该保留给文本元素，如标题、按钮和链接。

这里你可以看到对颜色的谨慎使用。

跨越排版。

这里你可以看到黄色和黑色，这种粉紫色的背景是非常不推荐的。

相对于谨慎，我甚至不建议这样做。

请注意，如果你对某些设计元素有疑问，最好不要利用它们，坚持标准。

这里你可以看到大标题和简短的技术片段最适合彩色文本。

接下来，我们将继续讨论辅助文本。

辅助文本基本上给出了关于字段输入的上下文，例如输入将如何使用，它也可以采用品牌颜色，但应该清晰易读。

无论如何，根据 WCAG 的标准。

同样，它利用了相同的原理。

唯一的区别是辅助纹理使用 60%不透明度的黑色，然后错误文本使用 100%不透明度的红色。

你可以在这里看到十六进制值。

你在哪里能看到这个？你可以在材料设计文本输入框中看到。

因此，如果我们真的去组件这里，去哎呦，我们搜索文本输入。

让我们看看能否在这里找到文本字段文本字段。

我们可以选择规格，您会注意到，此处有一条针对此文本输入字段的图像的错误消息，它利用应用于排版的错误文本此处此颜色属性来提供有关该字段输入的上下文，就在这里。

在这个例子中，这是用户的错误输入，会出现一些错误信息。

在这个文本字段中，有一些帮助文本显示在这里，反映在这里。

这个文本输入辅助文本的变体，这是非常重要的。

接下来我们还可以讨论选择的文字，这样选择的文字可以反映你的品牌。

它可以利用你的原色或二次色的强调，在这种情况下，选择色应该清晰可辨，选择色应该与背景色形成对比。

这里需要注意的一点是，您可以看到材质设计使用主要的变体颜色来突出显示文本选择颜色，突出显示所有选定的文本。

此外，另一个需要注意的重要事情是，图标和其他符号利用了与排版相同的原则来表示图标和其他符号。

当我说排版时，我指的是浅色背景上的深色文本和深色背景上的浅色文本利用了同样的原理。

它只是坚持一个特定的不透明度水平。

并且它们具有相同的不透明度级别，其中它们都在不同的不透明度级别使用相同的黑色。

因此，活动状态被设置为 87%的黑色，活动状态为 60%。

而禁用状态为 38%。

你可能会问自己，如何将这种状态应用到你的排版中。

所以我们能做的一件事就是抓取这些截图。

因此，我可以抓取一个屏幕截图，然后打开我们的练习文件。

文本易读性突然，他们将继续下去，并在这里引用文本类型。

基本上，通过所有这些截图，我们将证明我们需要多少种颜色样式，以便在创建组件时提供适当的强调，然后将它们构建为颜色样式，这些样式将在以后发布以供重用。

这里我们可以看到，我之前说过迭代器。

对于浅色背景上的深色文本和深色背景上的浅色文本，图标和符号都有相同的原则。

它使用相同的原则和不透明度。

这意味着我们只需要在这里创建三种风格。

它是图标和符号的活动，非活动和禁用，对不起。

此外，只提供高强调，中等强调和禁用状态的深色文本亮背景。

因此，我们可以做的是让这一切都生活在一个颜色样式的内容部分。

所以我们可以把它定义为扩展图像。

这基本上就是这三张截图所涵盖的内容。

所以我们能做的就是创造一种纺织品。

我们可以从这张截图开始，我会把它们移开，这样就不会让你们任何人感到困惑。

因为这方面的信息很多，所以我实际上只是想找一个纺织品和材料设计，只是使用 Roboto。

类型设置是什么并不重要。

我会很快改变这一切。

现在，我们要做的就是定义颜色。

并且该活动状态被设置为 87%。

目前，颜色设置为 100。

因此，我将更改该输入字段，我们可以做的只是选择该样式图标。

您会注意到，我们可以点击“创建样式”。

然后我会输入文字和图像。

这将会是这个命名约定将会是标题。

所以我们知道如何在颜色面板中利用这一点。

因此，我将在这里选择主动主动。

然后，您还可以指定百分比，如果您希望设计人员能够在查看颜色样式的名称时立即阅读，或者您可以删除它，我将删除它并选择“创建样式”，我现在已经创建了该文本样式，这正是我想要的任何活动图标或其他符号的样式。

然后我们继续复制这个。

复制完成后，我将分离样式。

并且只需指定非活动状态，这非常简单。

我们所要做的就是将路径城市再次更改为 60%。

并且表示任何图标或其他符号的非活动状态。

我要继续创造另一种风格。

同样，我刚刚标记了文本和图标，该类别使用 flash 命名约定。

现在我将指定 inactive。

然后，我们还可以指定这种非活动状态，如果我单击这个编辑样式图标，我可以添加一个描述，并继续为图标和此处指定的其他符号进行说明。

如果设计师不知道如何使用这种颜色，他们想知道这种颜色的用途。

这非常重要。

您也可以在以后继续修改这些属性。

我将继续复制这个，分离样式，设置不透明度为 38%。

现在，我们不仅禁用图标和其他符号，还禁用版式。

我将继续操作，再次单击样式图标，创建该样式，并将其标记为文本和图标。

现在我们有了它，我们可以继续将它标记为禁用。

现在我们有了三种颜色款式。

如果我继续操作，再次选择一些文本，并转到我的颜色样式，您会注意到我有文本和图标部分，它不仅提供了该颜色样式的名称，而且还提供了描述(如果有描述的话)，正如您在工具提示中看到的，当您将鼠标悬停在颜色样式上时，您可以了解该颜色样式的用途或描述。

这太棒了。

现在，既然我们有了这种活跃的状态，这可能是一个问题，我们可以做各种各样的，有各种各样的决定，你可以和你的团队一起建立你的设计系统，因为当你开始在你的设计系统中实施这些基础元素时，术语是非常重要的。

因此，我提出的一个问题，也是你现在可能会问自己的问题是，如果这些都是相同的值，相同的十六进制值，相同的不透明度，所有的东西都是浅色背景上的深色文本，图标和其他符号，我应该重新创建其他样式吗？答案是否定的，你不必这样做，你可以做的是继续，编辑它，并在描述中指定它，你可以指定它也是高度强调的，对吧。

因此，您可以指定这是在浅色背景上形成深色文本高度强调。

或者，更好的是，我们可以做的是，对于这种颜色样式，我将直接修改它的名称，而不是让用户去查看颜色样式的描述，以确定它的确切用途。

所以我要做的是明确这是重点。

我将把活动状态放在括号中，或者反过来也可以。

然后，我将保存它，现在已经保存了，这是重点，我将继续选择非活动状态，并确保我也编辑了名称。

然后指定中等强调。

一旦我指定了它，我就会保存它。

现在，我将继续选择它，禁用就是禁用。

这太棒了。

我们实际上不需要重新命名。

这些都是需要做出和理解的重要决定，正如你所看到的，设计系统很快会因为你如何命名而受到严重污染，不仅是在你的设计系统或设计中，而且在开发中，与你的工程师真正同步并理解他们在调色板中实现了什么是非常重要的，或者无论那个 CSS 文件是那个 sass less 文件，无论那个调色板在 react 中是什么，例如，这是现在非常流行的框架，真正试图理解那些十六进制值在哪里。

如果它们是十六进制值或 RGBA。

基地。

这些都是非常重要的问题，如果您还没有这样做的话，请向您的开发人员询问，并真正开始将您的系统与当前正在开发的系统保持一致，以真正增强设计师和工程师之间的工作流程，并真正开始巩固和创建跨设计和工程的统一语言。

这就是浅色背景上的深色文字、图标和其他符号。

接下来，我们将继续讨论文本类型。

我们确实忽略了一件事，就是我们已经将这个状态标记为中等强调，而对于这个状态，它是不活动的，中等强调。

和 inactive，我们也可以在描述中指定这用于帮助文本。

所以我可以在这里指定。

这解决了我的问题。

然后我们可以我们需要的最后一个文本颜色样式是错误文本。

所以我们需要继续复制这个。

分离样式，设置不透明度回到 100%。

然后将颜色设置为红色。

现在我们有了要读取的颜色，我们可以开始了。

我们所要做的就是点击样式图标点击创建样式，然后确保它和其余的颜色样式在同一个类别下，并输入空气，文字和创建样式。

我们是摇摆和滚动的人，我们已经创造了所有的颜色风格，我们将需要利用我们继续建立我们的材料设计系统的基础。

因此，如果我们继续在本地检查这些颜色样式，假设我像矩形一样拉出，然后查看我的调色板。

这一切都生活在文字和图标和我的颜色样式面板，并把它拖过来。

你可以看到，现在我有这个文本，高强调，颜色样式，中等强调，这也是不活跃的状态。

我们还有高度强调，这是图标和符号的活动状态，高度强调与浅色背景上的深色文本相关。

因此，我们确保所有这些内容都得到重用和简化。

因此，没有颜色样式的重复，这是不必要的，在开发中也是不必要的，如果你可以巩固并简化它，我们也有我们的错误文本，我们的禁用文本，这都在这里涵盖，这就是我为你提供的文本易读性。

我建议你仔细阅读网站内容可访问性指南，以真正理解这一点，我知道很多信息。

但是你读得越多，做的笔记越多，你就越会有所收获，并让你成为一名设计师，因为易访问性非常重要。

我们将在《基础》这一节的交互作用部分讨论状态。

随着我们继续构建我们的基础，我们将查看状态，如果您继续操作，单击此练习文件中的链接打开一个链接，您可以在此处访问这些状态。

一旦页面可见，就可以看到状态是用来传达组件或交互元素状态的可视化表示。

我们将介绍本页内容的概述，以及如何将其转化为 figma。

在这里翻译。

然后，我们将讨论状态的用法，以及何时应用它们，了解这一点。

我们还会讨论所有类型的状态。

然后，我们将分解一个状态的结构，理解它的用法和一些需要遵循的基本原则。

同样，再一次，实现状态和 figma，以及它们如何被用作颜色样式，或者是的，figma 中的颜色样式。

我会在我为你们创造的这些例子中，讲述如何实现它们。

我们还将继续构建其中的几个。

事不宜迟，让我们开始讨论状态的用法。

所以状态传达 UI 元素的状态。

在这里，您可以在这个组件上看到一个悬停状态，这是一行选项卡。

在这里，你可以看到按钮被按下的状态。

这里您可以看到芯片组件的拖动状态。

这里你可以看到一个按钮的焦点状态。

在这里，您可以看到行项目的选定状态。

还有其他州，当然这里也是。

同样，这向用户传达了状态 UI 元素。

每个状态状态在视觉上应该是相似的，不会彻底改变组件，但必须有清晰的启示，将它与其他状态和周围的布局区分开来。

因此，记住这一点，以实现材料设计坚持这三个原则的区别，确保这些状态有一个附加的原则，其中状态一次出现，如选择和悬停。

当采取这些动作时，无论是选择还是悬停，都会显示两个状态指示器。

当然，一致性是另一个原则，为了提高可用性，状态应该一致地应用于组件。

当然，这些状态是不同的，或者它们有明确的启示，将它们彼此区分开来。

关于周围的用户界面，它与根据它们的级别、它们的优先级来强调相关联。

现在我们要涵盖所有这些类型的状态。

这里您可以看到有一个启用状态。

并且它利用了前四个状态的按钮示例，其中启用状态传达交互式组件或元素。

所以本质上，启用状态只是组件的默认状态。

因此，您可以将启用视为默认设置。

只是交流，这是一个你可以点击的按钮。

这里的禁用是一个按钮的禁用状态，这只是表明它是一个非交互式组件或元素，所以实际上你不能点击它，因为它是禁用的。

这里，我们有一个按钮的悬停状态。

并且当用户将光标放在交互式元素上时进行通信。

我们还有一个焦点状态，当用户使用键盘或语音等输入方法突出显示某个元素时，它会进行通信。

如果我按 tab 键，你会注意到我可以在这个界面上跳转。

我跳转到这个链接，我跳转到这个按钮，这是这个按钮的焦点状态。

这就是一个例子。

如果我们回到这种状态，哦，等一下。

因此，如果我们回到这些类型的状态，这里，我们有选择的状态，它传达了用户的选择，他们选择了一个行项目，或者你也可以称之为列表项目。

在这里，我们有一个激活状态，无论是由用户启动还是默认启动，它都会向目的地传达一个突出显示。

这里我们有一个按下状态来传达用户的点击。

你可以看到一个涟漪效应，这个圆圈被称为涟漪，当你按下一个按钮或一个元素时就会产生涟漪。

和按下状态，它只是传达用户的点击或轻敲。

这里我们有一个拖动状态，当用户在元素中按下并移动时它会进行通信。

我们还有 on 状态，通常用于按钮、复选框和单选按钮，或者开关、单选按钮和复选框。

并且 on 状态传达两个选项之间的切换，这两个选项是 on 和 off。

“off”是另一种状态，它只是在两个选项之间切换。您可以看到,“off”状态显示在“switch”、“radio”和“checkbox”组件中。

同样，我们也有错误状态。

正如您所看到的，空气状态表示用户或系统错误，用红色表示。

这个图标中的标签，这个信息图标和这个文本输入字段的红色下划线，还有一些帮助文本来指示 air 是用户创建的还是系统创建的错误。

那么这些状态是如何沟通的呢？这是一个重要的问题。

既然我们已经讨论了所有类型的状态，我们就要分解结构了。

因此，figma 中的状态是通过一个覆盖图来传达的。

所以你可以在这里看到这些状态。

在本节中，它用表面颜色表示表面上内容的状态。

以及从内容继承的状态。

它本质上是一个覆盖层，只是一个半透明的覆盖物，覆盖在一个元素上，表示它的状态。

覆盖图提供了一种通过使用不透明度来可视化状态的系统方法。

在这里，你可以看到这种状态利用了黑色覆盖。

这是焦点状态。

它为您提供了覆盖图、百分比基础值、不透明度，并且它在此处使用黑色作为表面上内容的焦点状态覆盖图。

覆盖层的结构就在容器之间，你可以把它想象成表面。

这是这里的表面。

然后覆盖层就在内容和容器之间。

所以它从最上面的内容开始，然后是覆盖图，这是表示状态的地方，它就在中间。

最底部是容器，覆盖层和内容都位于其中。

所以这一点很重要。

并且该图中的相同结构适用于这些容器的其余部分。

在这里，您可以看到表面容器状态，无论它是在主要内容上，该状态看起来像什么，该状态看起来像从内容继承的，然后表面本身以及该状态在被选择时看起来像什么。

你会注意到在禁用状态下。

不管表面容器是在主表面上，还是在普通表面上，白色表面上，你会注意到内容，即排版的内容是实际被操作的状态，而不是处于禁用状态的背景本身。

为此，该文本被设置为 disabled。

再一次，只是为了继续分解在我们的设计中代表状态的覆盖的解剖。

它提供了一种通过使用不透明度来可视化状态的系统方法，并且可以应用于整个元素或圆形。

正如你在这里看到的，当它被按下时，它被应用在圆形中，覆盖颜色与它所应用到的元素上的文本或图标的颜色相匹配。

在第三个示例中，您可以看到选择的单选按钮列表使用了设置为蓝紫色的原色，然后与之关联的状态是“不使用相同的颜色，但该颜色的值不透明”。

正如您在 figma 中看到的那样，state 层使用原色作为覆盖层，并使用不同的不透明度来指示悬停的焦点新闻拖动和选择该颜色的状态。

您可以看到，这取决于组件的类型和状态，因此在此行项目中，选中的复选框使用的是原色。

您会注意到该行项目的选中状态也使用了该颜色，但是该颜色的值不透明。

所以知道这一点很重要。

您会注意到在这个复选框中有同样的事情，这里使用了复选框本身的轮廓，启用状态设置为黑色。

然后该复选框的悬停状态使用黑色的不透明值。

您可以在 figma 中看到，hover 上使用了 4%的覆盖，这只是 hover 上的一个黑色覆盖，这就是我们在此复选框中看到的。

这一点非常重要，一次只能应用一个状态层。

正如您在这里看到的，例如，如果一个元素首先被聚焦，然后被悬停，悬停状态层将只显示到悬停完成，然后返回到焦点状态层，如果元素仍然被聚焦，所以您不能同时做两件事或同时两个状态。

也就是说，这里还应用了叠加不透明度值，我们可以在 figma 中自己实现。

所以我甚至可以重做表面，去 figma 粘贴截图。

通过屏幕截图，我实际上可以在这里以层的形式重新创建状态，例如，我可以继续创建一个矩形。

我知道悬停状态使用的是设置为 4%不透明度的黑色，所以我可以继续应用黑色，十六进制值为 0000，设置不透明度为 4%。

然后我可以把它变成一个框架，并把它标记为悬停状态。

这个矩形正好代表了这一点。

也许我可以取消那个框架的组合，把它的名字应用到矩形上，我们可以在这里添加一些文本，只需输入悬停，然后增加它的大小。

一旦这些都完成了，我就可以将它们放入一个框架中。

同样，垂直对齐这个文本，然后指定不透明度，我也可以指定十六进制值，这样就不会有人混淆了。

因此，用户将知道这是一个覆盖状态悬停状态，但实际的颜色是什么呢？首先，设计人员实际上必须单击矩形形状，以了解十六进制值和应用于它的不透明度。

或者我们可以在这里指定。

如果你愿意，就贴上这样的标签。

我要继续把它展开。

这是一个很好的例子。

给你。

这是一个你如何指示你的状态的例子，我可以继续，甚至复制它，将它更改为焦点状态，然后添加 12%的覆盖，并再次使用相同的黑色。

然后有一个被选择的状态。

因此，我甚至可以选择使用原色值的状态，原色值，但我将分离此样式。

正如你所看到的，它只使用了原色值的 8%的不透明度。

我可能，我会想要复制它。

将十六进制值粘贴到这里。

你会开始看到我们在系统中指定状态，这太棒了。

然后我们可以继续对激活状态做同样的事情。

所以你开始看到我们如何在 figma 中为我们的组件创建精确的状态，这很棒。

不，让你养成习惯。

因此，激活状态，我可以继续改变不透明度为 12%，略暗的阴影。

我甚至可以代表新闻界，这实际上是一种连锁反应。

所以我们能为连锁反应做的就是创造一个圈。

在前列腺上，颜色设置为白色，不透明度为 100%。

但是，我们将使用，我们将在这里创建一个圆，椭圆来表示，我将把它放在这里的排版后面，并确保它在这一层中，我将剪辑内容，以便圆不会超出框架的界限。

如果它超出框架边界，它将被隐藏。

对于这个圆，我将继续把它的颜色改为黑色，然后设置不透明度为 12%。

这是我们的受压状态，非常棒。

然后我们甚至可以拖动到状态，拖动状态使用 8%的覆盖。

尽管焦点和悬停状态具有不同的悬停，但在焦点中使用 4%的覆盖，使用 12%的覆盖。

我将继续创建拖动覆盖，改变不透明度为 8%。

点击回车键，现在是一个更暗的阴影。

我将指定，确保它在文本中是准确的。

我现在已经创建了我所有的状态。

接下来你要做的就是点击命令来深度选择你想要创建的三角形。

您可以转到样式图标，然后单击创建样式。

你要做的是确保你正确地标注了这些。

你可以查看这个文档，确保你创建了正确的状态。

或者你可以参考我已经建立的所有这些状态。

你会注意到它是从州开始的。

然后它使用斜杠命名约定对这些状态进行相应的分类。

它将这些黑色状态标记为黑色覆盖。

然后它有这些紫色的主要状态，标记为主要重叠。

然后，当您在主要内容的 pri 之上创建状态时，我们有白色覆盖层，它利用紫色背景。

值得注意的是，这正是利用这些白色叠加态的地方。

我建议您继续在练习中构建其余的这些状态，我将暂停此视频，自己创建它们。

我希望你也能为自己创造这样的机会。

请注意，我遗漏了启用状态。

因此，请在此处为其添加启用状态。

然后，在主服务器上以及在本部分中，也创建这些。

暂停此视频，我将创建它们，完成后再见。

现在，我创建了所有这些状态，当你的内容在主表面上时，利用 62 双零双 e x phi 的颜色，就像紫色的紫色变体。

不管怎样，重点是你会注意到我最后做了什么。

如果你得到一个相似的结果，实际上最后复制所有这些，然后把它们粘贴到这里，我添加了一个背景来表示它在主表面上的样子。

一旦我做到了这一点，我所要做的就是将这些值更改为白色，您会注意到系统中的一些不同之处，当涉及到在主表面上使用状态的不透明度时，与在表面上不同， 这只是表面上的白色背景，是原色上的白色背景，使用这种紫色，不透明度在原色激活状态和拖动状态下更强，你会注意到我在这里增加了一倍，拖动状态从 8%不透明度增加到 16，利用白色覆盖而不是黑色，这里设置为 32%，那里设置为 24%，这是值得注意的重要事项。

差不多就是这样，我在这里创建了这些颜色样式，我要求你通过选择这些矩形并点击样式，创建样式来创建这些颜色样式。

然后是命名约定，你会用一个州的，然后空格斜杠空格。

这是一张白色的覆盖图。

以及白色覆盖的类型作为焦点状态。

我只需点击“焦点”,点击“创建样式”,我就创建了焦点状态样式，实际上，我将右键单击并从我的本地样式中删除该样式，它将出现在最底部，因为这是一个新创建的样式。

它按照创建时间的顺序出现。

右键单击并删除它，因为我已经有了它。

再说一遍，你有这些风格可以参考，我建议你进去制作这些，然后根据需要创建它们。

我们将讨论选择，这应该很简短，我们主要只是讨论和忽略材料设计网站的例子，因为一旦我们开始讨论状态，选择将被大量讨论，这是我们在本课程基础部分互动部分的下一个视频。

因此，如果我们继续操作，在练习文件选择中单击，这是一个我们可以访问的链接。

本质上，在选择中，它只是谈论它如何涉及用户如何指示他们打算采取行动的特定项目。

当他们对物品采取行动时，有几种方法可以对物品采取行动。

我们会在州的部分深入探讨，这是本课程的下一个视频，你们接下来会看到。

所以我们现在只做一个简单的概述。

你会注意到很多文档都涉及到了移动设备上的交互，但是我们将特别讨论桌面部分。

但是当我们开发我们的组件时，我会在 figma。

但在这里，你会注意到我谈到了移动和桌面上的选择。

我会非常重视桌面部分，尽管移动部分同样重要，如果不是更重要的话。

不管怎样，让我们开始吧。

因此，在触摸设备上，选择项目需要您在设备上长按触摸或双指触摸。

或者，您可以利用可用的选择快捷方式，如点击头像。

和带有复选框的桌面项目。

复选框不应该默认显示它们的复选框，或者永久显示，除非项目选择是 UI 中的主要活动。

相反，这些复选框只应在悬停时显示为该项目的单个复选框，或者在选择第一个项目后，为该组中的其余项目显示复选框。

这里我们可以看到用户在点击，然后他们点击来选择一个项目。

正如你在这里看到的，他们在这里选择了第一个项目。

他们进入选择模式，选择一个项目进入选择模式。

然后再次点击以退出该模式。

这里还有一些其他的例子。

只是用户选择了多张卡。

一旦每个项目被选中，他们可以点击它们来切换它的选择状态。

所以用户一次一张地选择所有的卡片。

然后他们再次选择它们，然后选择那些项目。

所以这些都是选择。

这是另一个例子，我们可以看到另一个选择的例子，用户长时间按住并拖动多个项目，以便一次快速选择几个项目。

您会注意到他们选择了几个项目，因为在左上角和应用程序栏中，当用户使用选择时，它会更新选择。

通过长按并拖动多个项目，这些项目是照片，这里的例子。

这是一个很好的例子。

然后在桌面上，这是非常重要的。

你会看到更复杂的状态变化，你会看到用户正在选择，你会看到悬停时，它会显示复选框。

他现在选择，你会注意到，当用户再次悬停时，该项目会显示一个复选框，然后可以单击复选框，再次单击以选择该行，该行项目。

这一点非常重要，我们还可以看到这里记录的其他变体。

这是一篇简短而甜蜜的文章。

我强烈建议你通读所有内容，只是简单地理解选择。

今天我们将讨论如何理解材料设计中的布局。

我们将回顾一些非常简单的原则，这些原则将贯穿整个课程。

我们将复习一些例子和组件。

这里我有几个链接。

我们首先打开这个链接，如果你想通读信息，或者你可以跟我来，我已经打开了这些链接，基本上材料设计为了确保所有平台或环境的一致性，以及他们设计的屏幕尺寸，他们遵循三个简单的原则。

确保当您在所有这些平台上实现可预测、一致和响应的布局时，您可以在这里看到这些定义。

因此，他们通过使用直观和可预测的布局来确保它们是可预测的，这些布局具有一致的 UI 区域和空间组织，通过使用这些度量，当然，还有一致的布局，同时使用网格、关键线和填充。

因此，我们将大量使用网格和填充，当然，当然，确保这些布局是自适应的。

它们对用户的输入做出反应，无论是设备还是其他屏幕元素。

因此，在这里你可以看到一些基线测量，材料使用和材料线布局在视觉上是平衡的。

为了实现这种平衡，在设计时，它们的大部分尺寸都是元件，与应用的八倾角网格对齐，这既对齐了间距，也与整体布局对齐。

而较小的组件(如图标或字体)可以与四个 dip 网格对齐。

把它想象成四个像素。

那到底是什么？因此，如果我们转到 figma，我们可以继续构建一个框架，也许我们想选择一个通用的 MacBook Pro 屏幕布局，我只需选择 macro pro pro 屏幕布局，我将继续添加一个网格。

并且通常大多数内容与八像素网格或八倾斜网格对齐。

为此，我可以选择我的实验室网格的网格类型，并将大小设置为 8。

这是我的第八个。

这是我的基线网格，设置为八个倾角，或者说八个像素。

这是一个 8，这是你的 8 像素网格，你可以开始设计你的网格，这是非常重要的。

我们会在需要的时候使用它。

当然，当你设计几个元素时，我们这里有一些文本，这是非常低的水平，不准确，这只是给你一个总体的想法，这种排版当然会与这里的网格对齐。

然后，我可以确保它的间距为 8 个像素，正如您在这里看到的。

然后，如果我复制 8 个像素的空间，请注意，当你设计时，你打破了间距，当你设计组件时，它总是能被 8 或 4 整除。

这里，我们有一些测量值，从零开始，然后每次增加四个倾角。

所以 04 812，一直到 80。

通常情况下，你会在顶部的间距中使用这个间距，对于一个广告，这个间距可以设置为 816 20 430-240-4856 647，当你需要将更紧密的间距应用于较小的组件(如图标或排版)时，你可能会不时地使用这个间距。

至此，我们将在下一页讨论这些测量。

但在此之前，我们要讨论一下响应登录路径。

因此，成熟的设计响应式布局网格是放置组件和元素的首要指南，以便它们适应屏幕大小和方向，确保布局的一致性。

当然，他们在响应网格布局中实现的三件事是，列、空白和边距。

在这里，你可以看到页边距在屏幕的最右边和最左边，在这个绿色的地方。

然后我们有我们的柱子，这里的红色阴影不是红色的，我一定是色盲，所以我道歉。

这是我们的专栏标签。

然后，我们的空白，当然，是列之间的间距，在屏幕的最右边最左边，这当然是空白。

因此，我将在 figma 中快速构建一个示例，我们在这里有 8 个倾角基线网格，所以我将标记该帧。

我们有八个倾角的基线网格，然后我将继续复制它。

在一本苹果笔记本上。

Pro 屏幕。

我将继续创建一个响应式网格布局，我将删除它，如果我想添加另一个网格布局，我可以保留它，并将其更改为列。

我们能做的是确保这种类型的网格被设置为拉伸，因为这意味着它将是一个响应性的布局网格，或者你可以称之为自适应的。

或者你可以说是流体。

这是另一个常用术语。

我将在这里添加 12 列，或者我们可以检查在此网格中实现了多少列 368 列，看起来它们可能是为平板电脑设计的，这里我们有一个网格，您会注意到这里的所有列都设置为读取。

我可以改变它的不透明度来指出它。

我们这里有八列，但实际上我们还没有边距，我们要做的是添加这些边距，在这里用绿色标出。

我们将它设置为 24 像素，这里有边距参数和 figma。

所以我设置为 24 像素。

你会注意到，它把那些柱子压扁了，所以我把那些柱子压扁了。

现在我要将这个装订线从默认的 20 像素或磅改为 16。

因为这也是常用的。

它也与我们的网格完全一致，我们的网格也是八像素基线网格。

这是一个如何构建的例子。

但现在，我将实际拉伸屏幕，因为这是响应式的。

因此，当我移动这个框架时，你会注意到，你设计的组件将附着在屏幕上，当你开始拉伸它时，坚持屏幕上的列布局或网格布局也会做出响应。

因为大家都知道，我们有网络浏览器，我们可以扩展这些浏览器，它会变得有反应。figma 本身，这个工具是有反应的，正如你所看到的。

但这只是一个响应网格布局的例子。

既然我们已经明确了这一点，那就太棒了。

我们将继续并开始了解如何在组件中实现测量。

因此，在我们的练习文件中，如果我们单击应用程序栏组件，我们可以打开该链接，这将打开一个材料设计页面。

如果您继续跟着我，并单击内容类别中的规格，您会看到我们有一堆底部应用程序栏，它们是仅移动组件。

最重要的是，我们看到了许多关于如何设计这些元素的规范。

所以在这里你可以看到这是汉堡菜单被包裹在一个 24x 24 的框架里。

我们有一个浮动操作按钮，设置为 56 乘 56，宽度和高度倾斜，它利用 Z 索引上的高程上升到底部应用程序栏上方，如您在这里所见，然后您可以看到这些图标之间的间距也是 24 度倾斜，然后它有一些填充，或者您可以说右边的边距设置为 16 度倾斜。

它是这些元素的高度，这些图标位于底部应用程序栏的中心。

其高度设置为 24 度。

这里有一些测量值。

您可以继续向下滚动，查看这个浮动操作按钮的其他变体。

正如您在这里看到的，这里提供了更多规格的其他变体。

因此，在这个浮动操作按钮和底部应用程序栏背景的空白之间有八个下降。

再次，更多，注意所有这些都可以被八或四整除。

所以理解这在很低的水平上是一致的。

所以一切都要从头开始。

或者你可以说非常原始的水平，因为这些是原始的，比如图标，一切都可以被八或四整除。

并且所有这些元素之间的间隔可以被 8 或 4 整除。

以及覆盖在底部应用程序栏顶部的其他组件的大小，这是一个非常棒的信息来源，供您浏览。

我们很快就会谈到这一点。

这就是我对你和理解布局的全部。

我们将讨论像素密度。

那到底是什么？

因此，我们将对此进行概述，然后了解 iOS、Android 和 web 等不同平台的像素密度。

以及这对每个平台意味着什么。

我这里有资源，我们可以点击这个链接并打开它。

一旦你打开它，你会发现它把你带到材料设计文档。

我们只是通读一下，我会向你们解释这一点，以及你们需要理解这一点的某些场景，如果你们已经不理解像素密度，也只是为了拓宽你们作为设计师的视野。

举例来说，一旦你必须为特定平台设计，你就确切地知道如何传达测量结果。

所以说，在 Android 上交流测量结果与在 web、iOS 或 Android 上交流开发人员的测量结果完全不同。

因此，举例来说，了解如何进行转换，你可能会说，举例来说，我正在建立一个 Splunk。

我工作的公司的产品，这个产品已经发布了。

这是一款面向 iOS 的应用程序。

我自己曾经是一名网页设计师。

当时我不知道如何将像素密度从网络转换到 iOS。

本质上，这是一个你需要理解的场景。

所以我举了这个例子。

所以让我们开始吧。

剩下的我们以后再谈。

那么什么是汉克斯像素密度，屏幕像素密度和分辨率因平台而异，像素密度只是一英寸内的像素数量，称为像素密度。

所以是每英寸的像素，每英寸多少像素。

所以，你可能已经看到，如果你以前打印过纸，或者打印过一些图形或其他东西，它会在打印前的设置中要求你指定每英寸打印多少像素。

所以每英寸的像素数越低，密度就越低。

每英寸像素越多，密度越高，当你看到打印出来的图像时，或者当你在显示器上看到时，他们会在文档中给你一个计算像素密度的公式。

就是这个等式，你的屏幕密度等于屏幕宽度或高度和像素除以屏幕宽度或高度(英寸)。

如果你这样做，你会得到你的像素密度。

这里有一个直观的例子，这是一个非常好的例子，高密度显示器比低密度显示器每英寸有更多的像素，它可以放大它。

你会注意到你这个 UI 元素，因为像素尺寸在低密度屏幕上显得更大，在高密度屏幕上显得更小。

所以像素越多，在高密度屏幕上显示的就越小。

像素尺寸越小，看起来就越大。

因此，我们可以继续讨论密度无关性，这是指 UI 元素在不同密度的屏幕上的统一显示，以及密度无关性。

在 material design 中，通常读作 dips，这里是 dp 的缩写，读作 dp，读作 Dibs，它们是灵活的单位，可以缩放以在任何屏幕上具有统一的尺寸。

它们可以提供一种灵活的方式来适应跨平台的设计。

所以举个例子，Material Design 不仅为 web 设计，也为 Android 和 iOS 设计。

因此，他们使用这个术语 dips，这是他们在设计和工程中拥有一个通用语言的方式，以适应跨多个平台的设计。

材质，UI 方面，使用密度独立的像素，是为了在屏幕上一致地显示元素，在那些不同的平台上使用不同的密度。

在这里，你可以看到一个低密度的屏幕，显示的密度与密度无关，低密度使它更明显，你可以看到所有这些像素，都是正方形，在低密度下更明显。

它变小了，仍然很明显，但在高密度下更难看到，因为屏幕显示的密度很高。

我们现在可以开始讨论每个平台上的像素密度，当您在 Android 上开发或为 Android 设计时，Android 上的像素密度可以使用 dips 在不同密度的屏幕上统一显示元素。

这就是你测量它的方式。

它的倾角等于密度为 160 的屏幕上的一个物理像素。

本质上是什么，这里有一个等式，再次被利用。

所以本质上，一个倾角是一个像素。

所以不要让我迷惑你。

你可以把它想得这么简单。

一次倾斜是一个像素。

但是 dips 是用来在 Android 上测量的。

所以这一点很重要。

以及可缩放的像素。

as SP 的缩写起到与密度无关像素相同的作用，密度无关像素是 dip。

但是对于字体，可缩放像素的默认值与倾斜的默认值相同。

但是对于字体来说，可缩放像素和密度无关像素之间的主要区别是缩放像素会保留用户的字体设置，这一点非常重要。

具有较大辅助功能文本设置的用户将看到字体大小与他们的文本大小首选项相匹配。

同样，iOS 上的像素密度。

基于 iOS 的平台使用逻辑分辨率来确定密度，这意味着它只是以点来测量其单位，而不是像素或倾角，它使用点来测量。

在这里，通过逻辑分辨率，点使用原生比例因子进行缩放，这是一些技术知识，如果您愿意，可以理解，它只是映射到器件的原生分辨率。

举例来说，如果你正在为 iPhone x 设计，你会设计一个 375 点乘 112 点的逻辑分辨率。

并且当它被渲染时，元素被图形硬件处理。

现在使用这个原生比例因子功能来填充 iPhone Xs 的尺寸，因为屏幕要大得多。

因此，它能够理解这些点，并根据显示设备相应地缩放它们。

单位，再次，iOS 使用点，记住这一点，然后，当然，网络上的像素密度与设备，屏幕分辨率成比例，但当你为网络设计时，用像素代替倾角。

所以当你在网上设计产品的时候，除非特别说明，你可以通过和你一起工作的工程师等等，你可以用像素来测量东西。

我们将再次在材料设计中讨论一些间距方法，听听它们如何处理间距，它们的元素，以及它们使用的非常有用的术语，我们将在文档中讨论这些术语。

在我们的练习文件中，我们可以点击这个链接和 figma，在这里我们可以打开 figma 中的间距方法页面。

我们将在这里检查所有内容。

而间距方法本质上只是使用基线网格、关键线、填充和增量间距来调整某些比率、容器和触摸目标。

很多这些都非常重要。

我知道，可能有点无聊。

但这非常，这是我们学习的一个很好的资源，不仅仅是材料设计系统的空间，还可以应用到我们创造的任何其他系统或我们设计的产品中。

这里是我们的基线网格，它只是讨论了所有组件如何与移动平板电脑和台式机的八倾角正方形基线网格对齐。

所以我们可以继续在这里复制它。

在这里，你可以看到这个正方形是 8 乘 8 的倾角，本质上就是 8 乘 8 的像素。

如果你要为网络设计，在 figma 这里，我们将继续创建一个设置为 MacBook Pro 屏幕大小的屏幕。

我将把它标记为 div 基线网格。

然后我会应用一个布局网格。

然后在这里将尺寸设置为 8，我现在有一个 8 倾角的基线网格，我总是可以利用它来测量这个框架上的元素。

所以这一点非常重要。

很高兴能拥有。

所以这是如何在文档中写出，如何在 figma 中创建的直接翻译。

我们还有一个四倾斜网格，用于更密集的区域，如图标类型，以及一些具有可以与四倾斜网格对齐的组件的元素。

如果我们想创建一个四倾角网格，老实说，我们所要做的就是复制这个，我可以将其重命名为四倾角。

然后从那里，我需要，哎呀，我把它设置为 18，应该是 8。

既然说了八个，我道歉。

既然已经设置为 8，我就可以把这个改为 4。

现在我们有了一个四倾角基线网格和一个八倾角基线网格。

这里详细说明了它们的用法。

因此，印刷术与四位数基线网格对齐。

当它在组件(如按钮或列表项)中居中时，这种版式可以放在四个 dip 网格之外。

当它被放置在网格之外，但在组件内居中时，文本仍然可以垂直居中对齐。

在这个场景中，您也可以看到，它在这个列表项的中心垂直对齐。

尽管字体被放置在四个 dip 网格之外，但这是需要注意的重要事项。

然后我们会谈到一些术语，在我们从头开始构建这些组件时，我们会在整个课程中经常用到这些术语。

所以他们用这些按钮作为例子。

因此，间距方法比响应式网格布局更加精细，响应式网格布局是一组关于如何在布局和组件中放置元素的规则。

因此，这里的填充指的是组件内元素之间的间距。

所以你可以看到这个图标的左右两边都有空白，右边这个按钮的左右两边也有空白。

这个按钮是垂直居中对齐的，在这个例子中表达了这一点。

以及此对齐间距方法示例，其中带有加号图标的按钮垂直对齐到按钮组件的中心。

当然，我们有两个网格维度，我们很快会讲到，它描述了组成元素的宽度和高度。

因此，我们将确切地知道如何通过利用尺寸来相应地调整组件和其中的元素的大小，然后现在它讨论具体的填充，以及它如何引用 UI 元素之间的空间，以及它如何作为关键线的替代间距方法，并以八个或四个倾角的增量进行测量。

所以你会注意到它总是能被八或四整除。

并且可以垂直和水平测量填充，并且不需要跨越布局的整个高度。

当然，我们在这里为组件指定了尺寸，如您所见，这里的高度设置为 8，它为您提供了应用程序栏的高度规格，设置为 56 度，状态栏高度设置为 24 度。

当然，这份清单列出了 88 个项目。

因此，当我们真正需要确保我们的元素是像素完美，或者在这种情况下是密度无关的像素完美时，这是很重要的参考。

然后，当然，我们有对齐，这是元素在这里的位置，它只是给你突出显示元素的例子，并讨论它们如何居中。

这太好了。

然后我们也有关键线路。

它是一种工具，可以在布局网格之外实现一致的元素放置。

它们是垂直线，显示当元素没有与网格对齐时元素的位置。关键线帮助您确定元素与屏幕边缘之间的距离。

在 figma 中，您还可以以 8 为增量进行测量，然后移动，这对您有很大帮助。

如果我打开 figma，在这里创建一些文本。

一旦我创建了文本，我就可以继续创建一个矩形。

在这里，如果我按住 Option，你会注意到关键线出现。

我看到左边有 240 个像素的地方，还有这段文字。

我可以从屏幕的边缘测量。

从上边缘，左边缘和下边缘之间的距离，甚至最右边的边缘，以及这两个按住选项。

或者在 Mac 上选择 Windows 上 Alt。

这些是我的关键线，帮助我指定元素之间的距离。

我们在构建组件时会一直使用这个快捷键，以确保它们是完美的像素。

这就是关键线路。

你也可以在 figma 中使用标尺。

如果我按 Shift R，你会注意到在画布上，这些标尺出现了，你也可以测量标尺之间的距离。

您可以通过从标尺中拖动到画布上来创建垂直或水平。

你可以测量标尺线或关键线之间的距离，只要你把鼠标悬停在上面，我们可以称之为标尺线或关键线，你可以通过按 shift R 隐藏它们。

这就是我们使用关键词的方式。

接下来我们将讨论容器和比率。

实质上是指封闭区域的容器。

容器本质上是由 UI 元素组成的，比如图像、图标或表面。

这里我们有一个图像容器。

这里我们有一个图标容器，当然还有一个表面容器，它封装了一个按钮，容器可以是刚性的，可以限制其中元素的大小或裁剪。

因此，如果图像在图像容器中，甚至在图标容器中，您可以利用容器来裁剪图像。

但是，它通常更多地用于需要裁剪图像的图像容器。

但是它们可以灵活地增长，以支持它所容纳的内容的大小。

正如你所看到的，在这些例子中，我们有一个刚性的图像容器，第一个例子，它裁剪图像的原始大小，或者我们有一个灵活的图像容器。

第二个例子，它在缩放时保持原始图像的大小。

还有一些重要的事情需要理解，我们在这里讨论了长宽比，长宽比是一个元素的比例，它的宽度与高度之比。

它的长宽比被写成这样的宽高比。

为了保持一致性，您的布局材料设计在图像、表面和屏幕尺寸等元素上使用一致的纵横比。

建议我们在 UI 中使用这组纵横比。

你可以在这里看到它们。

所以你可以看到一些景观格式，这个比例是分级的。

然后是一些肖像格式。

它还谈到了灵活的比例，这是由布局网格决定的，其中容器宽度由屏幕布局决定，并增长以填充最大可用空间。

所以只是指响应式屏幕。

而一个容器的高度是由容器中图像的高度用这些灵活的比例原则决定的，你可以用一个灵活的比例来让内容形式决定容器的高度，这是很常见的。

需要理解的最重要的事情之一是触摸目标，以及触摸目标如何应用于接收触摸和非触摸输入的任何设备。

这些原则是用来平衡信息密度和可用性的。

并且触摸目标应该至少是 48×48 的尺寸，或者 48×48 的像素，每个目标之间至少有 8 个凹陷的空间。

你可以看到这里指定的维度。

虽然看到这个图标容器被设置为 40。

通过 40 次倾斜，敲击目标被设置为 48×48 次倾斜。

所以这非常重要。

例如，如果你在它旁边有另一个图标容器或任何按钮，它必须在目标之间有至少八个凹陷的空间，所以我可以继续这样做。

假设我有一个 40 乘 40 的图标。

我可以我可以指定我可以创建一个框架，然后按住 option 命令将它缩放到 48 乘 48。

然后我可以把它放在中间，你会注意到我把这里的背景改成白色，可能稍微有点红色。

而红色参考引用被敲击目标。

这里的图标是这个圆，点击目标设置为 48 或 48。

但是图标本身是 40 乘 40 的。

这些是敲击目标的尺寸。

然后，如果您要在它旁边创建另一个元素，您至少需要有八个倾角的间距。

这就是这里所指的。

您可以看到，该按钮的高度为 36 度，但点击目标高度设置为 48 度。

所以这非常重要。

您可以在第二个示例中看到，有一个图标设置为 24x 24 倾斜，但点击目标设置为 48x48p 倾斜。

因此，这是一个标准的可访问性原则，只是为了确保你可以在界面上正确地点击这些元素，并坚持不仅要平衡屏幕上的信息和可用性，特别是对于受损的用户，而且它只是允许一个更有凝聚力的设计。

今天，我们将讨论材料设计中的响应式网格布局。

我们将在 figma 和 as 中创建这些样式，以便我们可以根据需要在所有设计中系统地重用它们，并将它们实施到我们的系统中。

在这里，你可以看到我从 material designs 网站上截取了一些截图，该网站引用了所有断点，因此它在这里讨论了断点系统，以及它们如何为 iOS 设备设计的断点。

我们不仅要学习如何创造这些 istyles 和 figma。

但是在我们开始之前，我们将会讨论一些关键的概念和材料来应用到我们的心智模型和创建网格和设计中。

当你创建你的网格的时候，其中的一些东西是列，空白和空白，我们已经知道并且已经讨论过了。

我们将再次回顾这一点，并在您的头脑中记住这一点，以及一些定制网格的方法，无论是水平还是垂直，定制这些访问的网格布局，并讨论您能做什么和不能做什么，或者至少是一些好的最佳实践和不太好的实践。

然后，当然，创建样式，理解网格行为，主要是固定网格布局和响应网格布局之间的差异，然后讨论 UI 区域。

他们的网页上有一些例子，然后是白色框架，所以我们可以继续点击这里的资源，打开链接，它将打开材料设计网站上的响应布局网格页面。

本质上，这种响应性布局网格允许我们的设计适应多种屏幕尺寸和方向，确保所有布局的一致性。

总图描述了一个响应式网格布局或只是一个总体的网格布局。

它由柱子组成，正如你在这里看到的，还有柱子之间的间距。

然后是页边距，在屏幕的最左边和最右边，这里用绿色显示。

这些是边际利润。

它深入讨论了列，以及内容如何放置在包含列的屏幕区域中。

因此，您会注意到，通常，当您在网格布局上进行设计时，您的设计或组件会吸附到这些列。

如你所见，它吸附在这两列的左右边缘。

你可以说这张卡片占据了两列，还有它右边的那一列。

并且它利用其间的间隔和沟槽来提供一致的间隔。

所以事物被有组织地分类。

所以列宽是使用百分比而不是固定值来定义的，这允许内容灵活地适应任何屏幕大小。

所以你会注意到，当我们在 figma 创建这些网格时，它们会非常敏感。

网格中显示的列数由断点范围决定。

并且指定了此处的断点范围。

因此，从零下降到 359 提示，或者你可以调用这些像素，如果你是为网页设计。

您可以看到，我们有从移动设备到大屏幕的各种产品。

我们将创造所有这些。

在这里，我们也将创建所有这些 iOS 网格布局。

这是针对列的，这里有一些关于 mobile 的示例，在 360 dip 断点处使用四列网格布局，您可以看到四列、Scott scoters 和指定的边距。

您可以看到，在断点宽度为 600 的平板电脑上，它使用八列的布局网格。

随着屏幕的扩展，它从四列增加到八列，这也允许在屏幕上实现更多的内容。

所以你可以看到这一排只有两张卡片。

然后，当扩展到更宽的屏幕尺寸时，在本例中为 600 dips，该行从两张卡变为四张卡。

这里讨论了檐槽，这些是在这里突出显示的栏之间的空间，它们有助于分隔内容。

并且这些装订线宽度在每个断点处都是固定值。

并且更好的适应屏幕尺寸。

装订线宽度可以在不同的断点处改变。

因此，更宽的装订线更适合更大的屏幕，因为它们会在栏之间产生更多的空白。

这样一来，屏幕上的信息就不会像大尺寸屏幕没有足够宽的空白区时那样密集和杂乱。

因此，您可以在移动设备上看到 360 度倾斜的断点。

该布局网格使用 16 个倾斜槽，在断点处的平板电脑上倾斜 600 度。

布局网格使用此处指定的 24 个倾斜间距，并使用这 24 个倾斜间距来分隔内容。

现在，我们可以讨论边距，它是屏幕左右边缘的内容之间的空间，在左右两侧以绿色突出显示，这些边距宽度被定义为每个分隔符处的固定值。

点。

这可以让你更好地适应屏幕，在不同的断点，页边空白宽度可以改变。

较宽的页边距更适合较大的屏幕，因为它们在内容周围创建了更多的空白，正如你在这里看到的。

这是一个小设备上的移动页边距示例，使用 16 的布局网格，下页边距。

然后在平板电脑上，在一个稍微大一点的设备上，移动设备上，你会注意到边距从 16 增加到 24，左边距和右边距有所下降。

现在，我们可以继续讨论网格的定制以及定制方式，这样可以满足产品和各种器件尺寸的需求。

所以有时候，你可能需要自定义间距，在内容之间留出更多的空间，以创造更多的一致性。

如果间距有时太窄，或者间距太大，正如你在这里看到的，在这个实验室网格中，这里使用了八个倾斜槽，非常窄的间距，更窄的间距可能表明图像彼此密切相关，因此它们被视为集合的一部分。

在这里，你可以看到这个布局网格使用了更大的间距，设置为 32 度，以在列之间创建更多的间隔。

这些额外的空间有助于将相册视为收藏中的独立实体。

有一件事你不应该做，那就是不要把装订线做得太大，比如和栏一样宽。太多的空间不会给内容留下足够的空间，会妨碍内容看起来统一，而且看起来很奇怪。

所有的间距都太大了。

您还可以自定义边距，边距可以调整，以便在内容之间创建更多或更少的空间。

并且屏幕边距的边缘对每个断点使用一个固定值。

这里给出了一个排版的例子，正文的理想长度是每行 40 到 60 个字符。

因此，如果你超过了每行那么多字符，这可能意味着你必须增加更多的深度，增加边距空间。

不要把页边距做得太大，以至于没有足够的空间容纳内容。

所以你必须找到平衡。

这是一个移动断点大小为 64 的例子，移动断点的宽度和内容看起来是相称的，但不会淹没用户。

然而，这是非常密集和薄，只是没有很好地利用空白和太多的空白空间。

那个例子。

在这里，我们还可以讨论水平网格的利用，其中可以定制材料设计布局网格，以触摸您水平滚动的眼睛。

因此，如果你正在浏览相册，或者只是在平板电脑或移动设备上的浏览器上滚动，当你创建水平布局网格时，列、装订线和边距是从左到右排列的，而不是从上到下。

在这里，您可以看到页边距在水平网格布局的顶部和底部，这里的列设置为四列，高度为 400，深度为 400，高度为 400，这很好，我们已经指定了。

此外，我们还有水槽，同样的原理只是方向不同。

并且可以定位水平网格以适应不同的高度，从而为应用程序栏和其他 UI 区域或其他固定在屏幕上的内容留出空间。

所以你可以把它塞进去。

现在我们已经讨论了所有这些，我们将讨论在 figma 中构建断点。

最后，还要介绍一些 UI 区域。

本质上，UI 区域是一个布局，由几个 UI 区域组成，比如固定的侧边导航。

所以 UI 区域通常是固定的内容，像侧边导航、内容区域和这个应用程序栏。

这些区域可以显示动作、内容或导航目的地。

并且这些 UI 区域应该在不同的设备上保持一致。

你会注意到，它给出了一个缩小的例子。

因此，它开始喜欢桌面布局，然后是平板电脑布局，然后是移动布局，它向您展示了它如何在缩小规模时隔离用户界面区域，这对于增加您在放大和缩小规模时如何跨设备实现这一点的熟悉度非常重要。

当你设计的时候，你可以确保你能正确地适应不同的断点有不同的屏幕尺寸。

同样，我将会谈到这些 UI 区域是如何固定的。

您可以将它们视为永久的 UI 区域，也可以显示在响应网格之外，就像导航抽屉一样，这些区域不能折叠。

如你所见，在上面的例子中。

此外，我们还有持久的用户界面区域，可以在命令下显示，例如汉堡包菜单，当您按下它时，菜单会滑入，它们可以保持可见，或者可以切换打开或关闭，以显示或消失。

当它们出现时，它们浓缩了两者的内容。

它们压缩了内容和网格。

因此，您会注意到，一旦持久用户界面区域打开，它就会压缩网格，在本例中，用户按下它，然后内容就会压缩。

并且附着在网格上，这就是内容被浓缩的原因。

还有临时的 UI 区域，它们不会影响响应网格。

这是一个覆盖示例，当它可见时，可以通过点击区域中的某个项目或区域外的任何空间来隐藏它们。

所以这个临时区域只是暂时出现，并不影响响应网格。

然后我们有了白色框架的概念，这是一种结构化的布局，提供了一种一致的布局方式，以及分层和阴影。

这是一个很好的起点，可以被修改以满足产品的特定需求。

举个例子，你可以把它想象成一个模板。

所以你不必设计一个设计的整个骨架结构，你只需要抓住一个白色的框架就可以开始了。

或者这只是给你一个很好的开始来设计你的产品，而不是从头开始。

这里你可以看到一个白色框架的移动例子，你可以把它看作一个模板，然后你可以把你需要的东西插入到这个界面中。

它已经从基本结构中为您构建出来了，也是一个桌面示例。

现在说了这么多，并允许涵盖很多内容，我们可以继续打开 figma，开始创建这些网格样式。

我们可以这样开始，我可以抓取这个截图，然后我们一起做一组。

然后在这个练习的其余部分，你也要自己实现这些。

这里我们有第一个断点，设置为从 0 到 359，dips，我将创建一个帧，指定宽度设置为 359，这是真正的瘦大小设置为纵向。

我现在真的不关心高度，因为这是我们只是为这个宽度开发网格布局，而不是高度。

我将在这里单击“创建样式”,这是一种网格样式，单击“布局网格设置”图标，将其更改为“列”。

你可以看到它被设置为四列。

边距和装订线设置为 16。

它会有反应的。

所以我要把这种类型设置为拉伸。

所以我要把边距设为 16。

请注意，我的页边距现在已经增加了，现在有页边距，然后将页边距也改为 16，这样当我们在这个网格实验室中对齐内容时，会加厚列并收紧内容之间的间距。

现在我们有了这个断点范围的网格布局。

我可以继续，现在我可以把它变成一种风格。

创建好这个实验组后，我可以单击这个样式图标，然后单击“创建样式”。

我能做的是指定这是一个额外的小网格布局。

我甚至可以指定 0 到 359 度倾角的超小尺寸。

我甚至可以指定我可以对此进行分类，因为在这里您可以看到我们有一系列屏幕尺寸，从本栏中的超小型到小型到中型到大型。

因此，我将把它指定为 extra small x small，这是一个斜杠命名约定，用于对此进行分类。

现在我可以这样命名它，所以现在它特别小，这是从 0 到 359 倾角的第一个断点。

这是网格的样子，我可以创建这个样式。

我甚至可以编辑并添加描述，并讨论这是用于小型手动装置。

是的，这是我真正需要传达给设计师的所有信息，他们需要了解这个网格布局的用途。

现在我已经应用了网格布局。

现在，我将复制这个框架，并删除这个布局网格，并确保我将它设置为 360 像素左右，这样我就可以有一个任何宽度的框架，从 360 到 399 倾角。

我甚至可以把这个从 359 改成 360。

和宽度，或者我可以把它增加到 399，只要在这个范围内，没关系。

因为这是一个流体布局，它将自动维护和计算这个诱饵断点范围之间的四列，并确保这些边距和间距始终设置为 16，以及这个断点范围的四列。

因此，我可以继续创建另一个布局网格样式，单击网格设置，指定列，选择列，确保有四列，我甚至可以更改颜色，如果我想，我可以将其更改为蓝色，并确保类型设置为拉伸 16。

像素是 16 下降到我的空白和檐槽，我现在已经指定。

我可以继续关闭它，并使用斜杠命名约定在额外的小类别下创建另一个网格样式。

然后，我将继续指定断点范围，即 360 到 399。

而且我要写 dp。

因此，人们理解这是材料设计的格式，这是 dips，密度独立的像素，我要创建这种风格，我现在有两种网格风格。

我要和你一起再创造一个。

然后我要挑战你自己再做一个剩下的。

所以我要复制这个，分离这个样式，从头开始创建一个，改变这两列。

在这里，我需要指定从 400 倾角到 79 倾角的宽度。

所以我就在宽度上选 479 lipsi 479。

现在我已经指定了 479，我将单击我的网格设置，并确保有四列，再次更改此网格的颜色，更改为另一种蓝色，因为我喜欢蓝色，然后将边距和边距也更改为 16。

它被设定为拉伸。

所以会有反应。

现在我可以把它变成一个网格样式，并指定这是一个非常小的，非常小的网格样式。

然后我可以指定断点范围。

所以是 400 低到 479。

快速，伟大的停止。

我甚至可以继续添加描述。

这款手机通常用于大型手机，即大型设备、大型手机。

现在我有了描述，我有了三种风格。

现在我向你挑战，继续从 480 度向下做剩下的这些样式。

我将暂停这段视频，自己制作它们。

所以我马上回来。

好了，现在我已经创建了所有的样式，你可以和我比较一下你创建的样式，和网格样式相比。

在这里，我有这个截图中指定的所有格式。

所有这些断点范围都清楚地作为网格样式直接反映在单个文件中，您可以看到所有这些断点都是按窗口大小划分的。

因此，我们有四个额外的小断点大小，指定为具有适当的列和边距的网格大小。

当然，这已经在练习文件中了，您可以查看。

如果你没有从头开始做，我强烈推荐你做，这是练习构建网格样式的好方法。

我几乎可以向你保证，当你创建完这么多的网格样式时，你就会知道怎么做了。

对你来说，这只是非常非常简单的事情，从现在开始，你就拥有了作为设计师的技能，这非常有用。

你知道，就像你的手背。

正如你在这里看到的，我有所有这些样式，一直到超大尺寸的任何断点，宽度为 9 个 1920 像素或更宽。

我们在这个文件中列出了所有这些例子。

作为额外的练习，如果你愿意，你甚至可以创建所有的 iOS 断点，就像这里指定的那样。

一个提示是，如果你创建了一个框架，你去框架，你有各种格式的 iPhone 格式在这里。

所以你可以选择一种 iPhone 格式，然后实现网格布局。

只需选择您的列布局，指定需要多少列，其中大多数使用四列，对于此紧凑版本，此处仅适用于前三组 iPhones，它们都是第一组。

在纵向和横向中，你会注意到方向是不同的。

所以你可能还需要为水平风景创建一个网格。

因此，在不使用列，而是使用行来指定网格的情况下，一定要考虑到这一点。

因此，考虑这一点非常重要。

当你建立这些的时候，我建议你挑战自己，同时也创造这些风格。

因此，您甚至可以继续创建网格样式。

通过提供命名约定，您可以指定这是一个 iOS 网格布局，他们使用斜杠将它们归类为 iOS 断点的网格样式。

然后你可以指定 iPhone 纵向。

现在你有了 iOS 变体网格样式。

因此，如果我进入并创建一个框架，然后转到我的网格样式，你会注意到我已经对它们进行了分类，我有额外的小网格，有小网格样式，从中等网格单元和大网格单元中选择，还有额外的大网格单元，当然还有 iOS 网格单元。

因此，您也可以继续构建它们，这非常有帮助。

对你来说是很好的锻炼。

你会注意到有些人可能会问，为什么要做这么多网格样式。

举例来说，如果所有这些小版式都使用相同数量的栏、装订线和装订线。

这是一个很好的问题。

由于这是一个响应式布局，从技术上来说，你甚至不需要制作所有四个，我只是制作了所有四个，以复制材料设计指定的表格中断点系统的精确性。

因此，如果你想继续下去，并实际上巩固这些最少数量的网格样式，你可以继续这样做。

在现实中，你实际上只需要三种网格样式，你需要额外的小的和小的。

实际上，你需要三个网格样式。

所以你需要这些断点的网格样式，这里的前四个断点，这些额外的小块有四列，边距和装订线设置为 16 度。

然后你会有第二个小的断点，设置为 8 列，16 页边空白和空白。

然后，对于列数为 8 的大型平板电脑，将第三个设置为断点。

但是装订线的边距设置为 24。

然后，对于每一个其他设备，您将有 12 列和 24 个下凹边距和檐槽。

所以，本质上，你只有 1234 个网格样式，你可以整合它们，甚至进去，如果你整合了它们，你可以为它们应用到什么断点写一个描述，这也是一个很好的练习，我建议你也这样做。

今天，我们将构建应用程序栏的所有变体，特别是应用程序栏的底部，您可以将它视为一些底部导航。

我们有这个描述的链接。

它是一个底部地图，应用程序栏，在手机屏幕底部显示导航和关键操作。

既然你已经掌握了和我一起，甚至是你自己，建立这个的窍门，我希望你已经掌握了，我希望你现在已经掌握了。

那太棒了。

如果没有，我们就要经历这一切。

你会注意到这个应用程序栏有四种变化。

这个底部的应用程序栏，我们标记为没有浮动操作按钮。

它只是一堆访问不同操作的图标，我们将浮动操作按钮放在中间，你会注意到这里有一些间隔，那里有一些复杂的间隔，这将有助于适应新的设计。

你，然后我们也有浮动的行动按钮中心底部的应用程序栏和这些行动。

所以我们要做的是在 figma 中，有所有的截图，我们要做的是创建这个。

我要挑战你们创造这两个，因为它非常简单。

我们将创建这一个，获得所有的间距，对吧，然后我要你暂停视频，我自己构建这些，然后让你构建这些，挑战自己理解这里的规范来构建这些。

然后我们再讨论这个问题。

然后，我们将一起构建中心切割变体，因为这可能具有挑战性。

但是对我们来说没有什么太大的挑战，因为一旦这个课程结束，我们将是 fig maestro 的。

所以让我们开始创建浮动操作按钮居中的变体。

对于那些不知道的人来说，这可能是一般为 Android 设计的，因为这是材料设计系统。

规格设为 360 度。

宽 640。

但本质上，你不必担心背景的实际大小，你需要理解你头脑中有反应的心理模型。

所以它会遵守这个间距等等，正如你在这里看到的，它在宽度方面被扩展了。

举个例子，互动的例子。

让我们开始为这个中心，这个应用程序栏指定背景的高度，现在我们已经指定了这个高度和 Android 设备的宽度，我可以继续将高度更改为 56。

现在我们有了 56 个，我要改变背景颜色。

我们需要记住的一件事是启用我们的材料设计系统库。

指定之后，进入我的背景色，选择原色。

我们现在需要我们的图标。

因此，如果我们转到我们的资产面板，我们可以搜索它们或键入 hamburger，没有关于 hamburger 的结果，这令人失望。

去我们的材料设计系统，检查我们的图标，我们可以寻找它们。

这就是我需要的图标，它显然叫做重新排序，我可以继续点击这个框架，点击命令 V，它将粘贴它，我们需要做的是删除这个填充颜色，分离样式，选择样式图标，并应用适当的颜色，这是主要内容。

然后，我将按住选项 A。一旦我选择了此图标，选项 A 会将它对齐到框架的左侧。

然后我将应用适当的间距，这里设置为 24。

所以我要把这个移动几次。

现在间隔设置 24 居中。

我甚至可以复制这个图标来慢慢展示占位符的用法。

如你所见，如果我继续，设置为 16。

我没有再复制了。

我将确保这两个图标之间的间距再次设置为 16。

所以我们把它们设置为 8，我们想把它们设置为 16。

所以我把它推过来。

我们准备好了。

我们有我们的图标授权，如果你想特别指定我们的图标，你可以搜索它们，我们也可以在这个库中寻找它们，我将暂停这个，很快为你找到那些图标。

如果您找不到图标，您可以搜索图标和材料设计图标工具。

因此，我通过单击 SVG，然后选择 download，分别下载了所有这些文件，但我已经为您下载了它们，它们在这个练习文件中。

因此，我们实际上可以在您的文件夹中，如果您转到正确的部分或组件，那么 2.1，您将可以访问它。

我们可以做的是打开我们的 finder，将这三个组件都拖到画布上。

现在，我们可以继续添加适当的组件。

但在我这样做之前，我要改变这些颜色，删除那里的填充，去选择颜色，点击样式图标，并设置这些内容为主。

我们就可以出发了。

所以我可以替换这两个。

所以我能做的就是把它们分别拖到画布上，然后点击这个。

您会注意到，使用著名的智能选择，我可以按住选项 W，它会将这一层捕捉到顶部，然后我可以交换它们，这非常有用。

这样我就可以，这样我就不用重新调整大小了。

保持调整大小。

我可以将这两个图标拖到画布下，并分别交换它们，我将点击选项 w，这样就没有间距了，它们都在该轴上对齐。

然后我将切换它们，然后删除。

我要将这一个班次居中，单击它，然后交换它们。

现在我有了所有需要的图标，它们都反映在这里。

我们可以继续，甚至把我们的脖子，我们的浮动操作按钮。

我们想要常规的，也就是这个，它更大，缩略图，正如你所看到的，我们可以直接把它拖到画布上。

完成后，我们要做的是确保它被捕捉，并在这里水平居中。

因此，如果我们将其分组到我们的框架中，然后我们要做的是单击剪辑，取消选中剪辑内容，单击此处，然后按住选项 h。

这将使它居中，因为它没有居中，选项 h 现在居中了。

我们已经正确实现了这些图标。

我们希望这些图标具有向右对齐和垂直居中的约束，因为它们位于底部应用程序栏的右侧。

我将把它设置为而不是左和左上，并在轴上垂直居中。

这样，当我调整它的大小时，元素的背面，在垂直方向上与背景保持比例，所以它是平衡的，图标是水平的，比例的，大小是存在的，然后在垂直方向上，它将保持比例以及高度的比例。

然后我们有一个动作按钮。

规格都很好，因为我们已经建立了这个组件。

由于它是我们库的一部分，我们只需使用“资产”面板并将其拖入构建组件，这非常方便。

我们知道它是这个组件的一部分，因为我实际上是在点击父层，按住命令 Shift H 并隐藏所有内容。

所以所有隐藏的东西都与所选的父层相关联，这让我知道这是我想要的图标。

我的意思是，我想要的组件和它相应地分层。

同样，这些都应该是图标。

所以我能做的就是把这些复制到我的材料设计系统中，我可以根据需要把它们粘贴到我的系统中。

我是，然后我可以应用这个通用的默认颜色作为填充，所以我可以输入灰色。

如果你需要长话短说，这个应该设置成，我可以设置成。

我基本上是要确保这些都是主要组件，分别发布到这个库，可以看到我已经添加了这三个新图标。单击“发布”,稍后我会组织它们。

我们实际上要把它们交换出来。

有了这些，这些组件，我要在这里给你展示一个技巧。

所以如果我输入搜索，就会出现我的搜索图标。

如果我按住，向上，如果我继续做所有这些组件，创建多个组件。

我再次转到“assets”面板，拖动这个“Hold OPTION”命令，它应该会在适当的位置交换它，但不是这样。

因此，我们将继续手动操作，我很抱歉。

所以只要抓住这些图标。

一旦我们有了所有的东西，我们将继续把它们拖到画布上，进行完全相同的操作。

一旦这些都被交换了，我们就可以走了。

好了，现在所有的工作都完成了，只需要再次应用正确的颜色，设置表面的内容，我们就可以开始了。

所以现在我们只需要相应地命名这一层。

它属于应用程序栏类别。

因此，这里有几个类别的酒吧，空间斜线空间，然后它属于底部类别的应用程序酒吧。

这是带有浮动操作按钮的应用程序栏，底部的应用程序栏带有居中的浮动操作按钮。

一旦完成，我们就可以开始了，我们甚至可以在右边应用约束。

所以它总是保持居中。

或者实际上，我们希望它水平居中。

所以当我们拉伸宽度时，它会一直保持在中间。

现在我们有了宽度拉伸，我们需要做的是将约束再次应用于垂直居中和向右，然后垂直居中和向左。

所以现在，当这个元素被选择，它拉伸，它也相应地拉伸。

一般来说，高度不会拉伸，因为我们已经完美或正确地设置了这些规格。

然后，当我们调整宽度时，一切都会相应地受到约束，就好像这是一个应用程序响应布局一样。

所以我们可以继续把它作为主要组成部分。

那就好办了。

现在，我将暂停这个视频，我要求你们在这里制作这两个组件，这实际上非常简单。

不要从头开始制作它们，只需复制这一层，分离实例，然后开始删除和添加这两个变体所需的内容。

我马上回来。

好了，现在我已经创建了这些，需要指出的一点是，练习文件中已经有了，所以你可以检查这些，加快速度，或者注意约束条件。

如果你从头开始建造它。

请注意，这个浮动动作会在顶部和右侧按下约束按钮，因此它会相应地移动。

然后这些被设置在左边，垂直居中，所有这些在左边的图标都是这些动作，所以它们被限制成比例。

然后我们有底部的应用程序栏，没有浮动的动作按钮。

我们希望确保约束设置正确。

举个例子，如果我点击这里的这组图标，并把它们设置在右边和中间，它们会相应地水平和垂直缩放。

还有这个，这个菜单或者汉堡菜单的动作。

这是我希望你喜欢自己做的两个。

最后但同样重要的是，我们将继续构建底部的应用程序栏，它带有一个浮动的操作按钮，该按钮具有中心切口。

所以背景中间有一个圆形切口。

这实际上很容易复制，我们所要做的就是创建一个 72 乘 72 的椭圆。

一旦你指定了这些设置，确保它有正确的背景颜色，所以表面的背景，但不是为中风呜呜，以便为背景本身的填充。

然后我做的就是确保我为背景色创建了一个合适的矩形，设置为原色。

我为 Android 设备的宽度和底部应用程序栏的高度设置了合适的尺寸。

我所做的就是把它们分组。

因为它们是分组的，而且上面覆盖了，我们可以做的是利用减法选择。

所以当我点击减法时，它会在这里创建适当的间距。

此椭圆设置为 72 的原因是，它在此处的空白周围提供了 8 个像素的填充，刷卡空间周围是 8 个像素。

所以当我们继续建造它的时候，现在我有了这个背景。

我已经做了，我所做的是在这里复制这个组件，并将其附加，它几乎有所有相同的图标，除了心脏，稍后将交换。

嗯，我可以继续做的是删除条例草案，并实际上添加一个矩形，因为我们有背景颜色应用于框架。

但是在这种情况下，我们实际上可以将减去选择应用于帧。

我们接下来要做的是将它添加到父层中。

现在它是该框架的一部分，我可以直接点击选项 A 和 W 来捕捉选项 s。哎呀，我需要快速撤销，我需要选择我的背景。

中心，我要离开所有的中心切割背景。

我将按住 Command 左括号，这将一直推到底部，并点击选项 s 和选项 W，这将相应地捕捉它。

我现在有了需要的外观。

我们可以继续并仔细检查，将宽度设置为 8 的像素。

这太棒了。

我们在四周设置了适当的间距。

这就是我们如何用底部应用程序栏的浮动动作按钮创建中心切割浮动动作。

确保所有这些都正确分组，确保间距正确。

我们的罐头直道应用得当。

你会注意到背景现在需要适当地设置约束。

如果我继续拉伸，它会保持这个间距。

但是有了这个中心切口，你在发展中交流就容易多了，在那个背景下到底发生了什么。

所以，这是需要注意的重要的事情，我们能做的是，因为这是一个选择，我们可以确保水平居中和光滑的矩形混合物，一直延伸到左边和右边。

所以现在，当我们继续移动它时，它实际上会随着浮动的操作按钮移动，这太棒了。

现在已经完成了，我们只需要给它起个合适的名字。

因此，重新命名的 fab 中心削减，并使其成为一个主要组成部分。

我们已经准备好了，所有东西都在适当地缩放，我们只需要添加那个心形图标，我们可以继续检查，看看我们是否真的有那个内部库，我没有。

但同样，我们可以只使用材料设计工具，图标，工具，材料，图标工具。

它就像在心里输入一样简单，我可以继续点击，您已经在 finder 中可以访问它了。

现在，我要打开 figma，打开我的 Finder。

我们有一个最喜欢的，我要把它添加到我的图标库中。我的图标可以放在这里。

稍后我会用那个收藏夹来组织这个，他们会把它作为一个主要组件，publish。

实际上，因为我没有，如果我输入 favorite，实际上图书馆里已经有了，我道歉。

所以我有最喜欢的图标。

我将把它拖到那里，分离样式，在表面上添加新的，在主要内容上添加内容样式内容，将它拖到我的组件中，确保它垂直居中 shift 单击，然后您智能选择快速交换它们。

我现在复制了这个截图。

所以我们现在可以开始组织所有这些。

因此，我将整理所有这些组件并暂停这段视频。

一切都准备好了。

我希望您喜欢观看此视频，并不断了解约束条件，稍后在本课程的自动布局部分，我们将根据需要在元件上实现自动布局。

我会告诉你如何定义和解决这个问题。

我们将讨论如何构建顶级应用程序栏组件变体。

具体来说，我们有两个这样的例子。

如果我们继续操作，单击练习文件，在这里，如果您打开该链接，我们就可以访问文档。一旦您打开该链接，您会注意到我已经在那里了。

这是用来提供与当前屏幕相关的内容操作，也是利用您的品牌、屏幕标题导航操作的绝佳时机，然后可以转换为上下文操作栏，这只是另一组操作。

基于你互动的内容。

在那个屏幕里。

Pan，我们将创建两种类型的 AP 栏。

我们不会从头开始构建这些按钮，我们实际上会参考我们已经创建的按钮，它们是底部的应用程序栏按钮。

我们可以继续并结束这个练习文件。

如果你保存了它，你可以继续，我已经出版了。

如果您获取此练习文件，您将不得不再次启用材料设计系统库。

启用后，我们就可以继续搜索应用程序栏了。

我们有所有的应用程序栏。

因此，我们可以更具体地了解我们需要什么类型的应用程序栏。

我们显然需要没有浮动操作按钮的底部应用程序栏。

现在我们有了这个，我们有了所有合适的颜色样式，因为这让我们领先，我只是要分离这个样式。

我已经分离了这个实例，很抱歉，我把它从主组件中分离出来了。

我们拥有的是一个伟大的起点。

这是页面标题。

我们拿到了所有的规格。

所以这个菜单在左边设置为 24 像素，它的高度设置为 56。

您会注意到，顶部还有一个小部分，它指的是系统 UI 栏，我们不需要为此进行设计，它在此屏幕截图中只是一个占位符。

因此，当我们工程师实际构建它时，这些组件将被放在顶部，就在系统 UI 栏的下面。

所以忽略这一点，这就是为什么规格在这里指定 256，不考虑系统的高度，UI 栏，只是为了一些背景。

我们这里有三个图标，更垂直的图标，搜索图标，这个共享图标，我相信这是标题，这是页面标题，然后我们有菜单图标。

我可以删除第四个图标，因为我们不需要它。

我要给它重新命名。

所以我不会搞混，因为这又是一套 app 栏。

但它不是在底部，而是在顶部，我们正在创建常规，我们只是要标记这个常规，正如我们已经知道的，这是常规的顶部应用程序栏。

也就是说，我们需要找到合适的页面样式，了解使用的是什么材料，然后我们只需输入页面模糊我，页面标题，添加合适的样式。

因此，如果我选择标题，我们会看到标题和副标题的变化。

我们可能需要参考文档，我将继续单击填充，应用主要内容颜色，确保调整大小设置为自动宽度，并垂直对齐画布，我没有正确的颜色、样式或字体样式。

因此，我将参考文档，找到此处印刷字体的确切字体大小，以获得一个大概的概念，如果您密切关注，就会发现这种 Pog Rafi 字体较小。

在这个扩展的顶部应用程序栏变体中，它稍微大了一点。

也就是说，我们需要做一些调查。

我们有它，我们有我们的类别，字体大小设置为 h6 在这种情况下。

我们再次引用了排版，声明它设置为 h6，h6 就在那里，有一堆例子。

又一次，给我们举了 h6 的例子。

所以这非常重要。

既然我们已经有了，我们可以继续，只是我们有这个颜色的风格。

因此，我可以直接选择它，转到我们的纺织品，然后选择 h6。

所以我们这里有 h6。

我们继续把这个放在中间。

这个设置为菜单栏图标左边 30 像素。

所以现在设置为 32。

我们把它设置为 16。

在左侧，我相信它当前设置为 24，所以我将 shift 单击文本，然后我将单击此 shift 单击菜单图标，将它移动一个位置，我的微移量和首选项设置为小微移 8，大微移 1。

因此，我可以快速地以八倍的增量有效地移动东西，因为这些是这个材料设计系统的测量原则。

所以现在我在这里设置了页面标题。

我将重点关注这里的对齐方式，确保右边的填充设置为 16。

图标之间的间距是 24。

如果我继续检查设置为 24。

我选择了这些，它被设置为 16。

在右边，填充，约束都准备好了，我可以通过将垂直轴设置为中心来应用适当的约束。

而这个图标已经有了左右居中。

所以我们可以走了。

所以如果我缩放它，它会保持它的位置，或者随着高度的增加，它会保持在 y 轴上的位置。

所以我们有了这个变种。

唯一缺少的是合适的图标，你可以自己添加。

我将暂停并添加它们。

所以我已经确定了所有合适的图标。

我遗漏了一个图标，它是属于“操作”类别的“共享”图标。

所以我就去了材质设计图标工具，输入 share，我找到它，点击它，保存为 SVG，我把它放在练习文件里，省去了你那一步。

所以你可以把它拖到你的画布上，然后 figma 把它发布到你的材料设计系统库中，这正是我所做的。

我只是发布它，在这里添加它，使它成为一个主要组件，发布它，一旦我发布了它，我就是我，因为我在这个练习文件中启用了该库，我能够从这里的资产面板中拉出它，然后我只是相应地交换它们。

现在所有的规范都准备好了，如果需要的话，你可以仔细检查一下间距什么的，确保约束设置正确。

现在，我们可以继续检查我们的图层，它的标签是正确的。

我要把这个作为主要的组成部分。

现在这是一个哎呦，我将选择顶部的父框架，并使其成为主要组件，我们现在有了我们的主要组件。

一旦有了，我们可以继续，甚至复制这个。

复制好之后，我会把它拆下来。

我们将把这个元素的高度设置为 128。

但在此之前，我会有意设置一些约束。

因此，约束将应用于所有图标。

因此，我将命令单击所有这些图标，这允许您单独单击这些图标，并保持对上一个图标的选择。

所以我把这些都选中了，我要把这个设置为 top，因为我不想在设置高度的时候这些移动。

我要做的是将它设置为底部，然后按住 shift 键单击此约束以移除它，并确保文本设置为底部。

所以将要发生的是，当我改变这个元素的高度时，图标会粘在顶部，文本会粘在底部。

所以我准备把高度改成 128。

它确实做到了。

现在已经创建好了，我要做的是确保基线间距也相应地设置好。

你可以看到他们正在指定文本的基线，这不同于文本的边框，它被设置为 28 像素。

我们可以很容易地做到这一点，按下 shift 键，在字体的基线上加一条红线。

按住，我们可以按住这里的选项，我们可以单击文本，它在这里被设置为 16，但如果我们继续创建一个文本框，会注意到它的高度被设置为 21。

因此，我们还需要确保的是，我们正在为这个扩展的顶部应用程序栏使用正确的文本样式。

那么，我们该如何做呢？让我们继续参考顶部应用栏上的文档。

你会注意到印刷字体只提到这是一个 h6。

如果我们再向下滚动一些，这个标题是一个 H 6。

但是扩展的顶部应用程序栏呢，它是不同的，它是不同的字体。

所以我们可以继续谷歌顶部应用程序栏文本大小。

因此，我们可以继续前进，继续通过谷歌搜索来确定字体的大小，如果你曾经困惑，但如果不打算再次打开该页面，一旦打开，我们可能希望能够访问开发人员指南，并在那里窥探。

它会给我们密码。

但我们想要的是这里的使用权。

了解地形。

所以文本外观，我们只是在这里读一些属性。

我们真正想做的是了解应用的纺织品。

所以我要暂停一下，寻找那个值。

因此，我无法通过窥探这种排版来识别实际的标题，或者至少在这里打印代码，不要混淆。

同样，我刚刚经历的这个搜索特定属性的小练习是一个常见的练习，我建议你开始理解，因为它真的会帮助你通过浏览不同的文档集来尝试剖析和理解如何识别设计的某些元素，这是一个很好的技能，并且会改进你的设计实现，因为你可以更好地向开发人员阐述它，以及你继续深入这些类型的 Google 搜索并尝试理解它是如何实现的。

因此，我只是参考了开发者文档，并访问了组件页面，其中谈到了顶级应用程序栏。

我正在浏览这段代码的结构。

所以机器人用这种叫做 XML 的格式编写。

这是他们写代码的语言，或者是实现 UI 的语言。

我正看着突出的顶部应用程序栏。

在突出的顶部应用程序栏中，我向下滚动，发现了一组已传达的属性。

它谈到了他们使用的标题属性。

因此，我继续寻找顶部应用程序栏，任何与顶部应用程序栏属性相关的内容，看看我是否可以识别大小。

这里还有很多其他的设置，它们是可以交流的。

但具体来说，我们寻找的是类型标题属性。

所以利用了一个标题。

材质工具栏的标题设置为六岁，这是我们指定的。

然后素材字幕设置为字幕一，他们也有一个折叠标题变体。

正如您在这里看到的，他们还有一个扩展的标题，但我们将继续使用现有的标题。

同样，如果你知道这种风格是什么，请继续完善它。

因此，我将把它改为副标题，因为这是我试图识别的标题。

如果我们继续下去，并确保基线设置为 28，我们应该可以开始了。

所以我要把这个矩形增加到 28 像素，稍微向上推，这样基线就对齐了。

所以当我画红线时，地形的基线现在距离底部 28 像素，这是在扩展的顶部应用程序栏中指定的，就在这里，28 像素，这条绿色水平线。

所以现在我们可以走了。

上面的图标上的所有间距都是正确的。

我们只是设置了一些限制，这样它就能保持下去。

设置了页面标题后，我将确保约束也设置为 left。

所以当我们输入一个新的标题，我们就可以开始了。

也就是说，我将确保我的间距设置为相应地设置在左侧。

所以设置为 72。

我们准备好了。

我们将继续将其命名为 extended，因为它是我们的扩展顶部应用程序栏变体。

现在，所有的约束都已设置好，我们可以继续做下去了，我将继续让它成为一个主要组件。

你可以在这个框架中继续组织。

这就是我们如何构建顶级应用程序组件的方法。

今天，我们将制作一些背景组件。

因此，我们将在观看的基础上构建背景组件，首先应用适当的约束来构建它们，并且确实需要首先理解和使用这些组件。

因此，如果我们继续操作，单击那里的背景文档，它将在您的默认浏览器中打开。

一个背景出现在应用程序中所有其他表面的后面，显示上下文和可操作的内容。

有一件事没有，目前，截至本记录的日期，2020 年 9 月 10 日，星期四，这是目前在测试中。

因此，对于参考此的开发人员来说。

目前，还没有关于这将在以下平台 Android，iOS web 和 flutter 框架开发的状态。

所以这一点很重要。

此外，背景由两个表面组成，对吧，所以我们有后面的层，就是看起来蓝色的层，就在这里，这个顶部的条形元素，基本上就是这个样子，然后是前面的层，它有这个白色的背景，并利用了一点点的仰角，不管你能不能看到它。

后一层显示动作和上下文，这些控制并通知前一层的内容。

所以背景上的内容决定了前面层的内容。

这一点很重要。

当我们继续分析这个背景组件时，它还利用了副标题下的可选分隔线。

这是这个背景组件的解剖图。

它由一个后层、一个前层和一个可选的子标题和分隔符组成，正如您所看到的，后层或前层可以同时处于活动状态。

所以只能有一个东西是活动的，要么是后层，要么是前层。

当后面一层活跃时，这个前面一层利用运动下降。

在这里，您可以看到这是一个固定区域，或者至少，这是一种可选方式，可以将子标题用作一个固定区域，其中包含滚动内容等等。

我强烈建议你通读下面的内容，阅读它并理解它的行为。

就在这里。

您可以看到前层，因为后层正好位于表面上，前层利用高程，在 Z 索引上有一个倾角。

Z 索引上的一个像素也是在 web 上引用它的另一种方式。

这对于我们的前层来说很重要。

我已经抓取了这些设计规格的屏幕截图，您会注意到，在这个示例中，它使用了一些列表项，我们将继续进行构建。

因此，我们可以在这里进入 figma，这些截图所引用的内容，我们将继续创建这个通用示例。

好的，我们可以在系统中搜索 app bar，确保您的库启用了材料设计系统库，然后您可以搜索 app bar，我们有所有这些变体，对吗？所以我们要使用的变体很可能是这个带有页面标题的，你可以看到它也有其他的动作。

我真正想做的是把它放在一个父框架上，这个父框架将利用 Android 手机的尺寸。

所以我要给这款安卓手机重新命名。

因此，我将把它拖到这里，并将其吸附到父框架的顶部和左侧。

太好了，我们有我们需要的开始。

这也表明背景仍然设置为蓝色，所以我们可以做的是添加一个主要的背景颜色来表示我们的例子。

Android，Android 手机背景，为了准确起见，我将很快对其进行重命名。

我们能做的是这个背景一直出现在这个框架的顶部。

因此，我将在 figma 中添加一个标尺。

与这里的选择一致。

得到完美的像素。

现在指定了标尺，我可以继续创建另一个框架。

在这一帧中，我将宽度设置为表面表面，背景颜色和宽度跨越屏幕的整个主体，这是 360 度倾斜，我将把它吸附到顶部。

我将继续选择独立的角，并确保顶部和左侧设置为 16 像素圆角。

因此，我们在这里实现了相同的外观，正如你所看到的，就在那里，它利用了一个字幕，该字幕从前面层的顶部开始具有 32 个倾角的基线。

所以我将标记这一层，这个框架前层，因为这是背景，将有一些文本生活在其中。

我要确保我指定了合适的纺织品，这是副标题。

我要确保调整大小设置为自动宽度。

为了保持简洁明了，我可以把它靠上，靠左 16 像素，向左倾斜 16 度。

我可以把这个推下去。

我想做的是确保基线设置为 32 位。

所以我可以继续做的是抓住，哎呀，抓住一个矩形，抓住一个矩形，在那里指定 32 个提示。

然后，我将把这些内容向上推，使其与基线对齐。

我可以用尺子来证明这一点。

我们准备好了。

基线就在那里。

其中一个可选的工具是分频器。

因此，我们将从一开始就添加它。

我要继续在我的资产中寻找分割线。

然后，我可以从系统中取出这个分频器。

并且分割线大约是从字幕向下倾斜 12 个像素。

我要做的是，我按住 option command 并缩小它，以便它设置为在另一个子标题的左侧和右侧填充 16 个倾角，这反映在我们上面的示例中，正如您在这里看到的。

它就在那里对齐，那个分割线。

这种一致性很好。

现在我们有了它和我们的纺织品。

一切看起来都很好。

我们准备好了。

我们现在可以做的只是将它指定为我们的基本布局，因为设计师将继续添加该组件所需的实际项目。

所以我们这里有这个前层，如果我继续缩小这个前层，我们现在有一个画布，让设计师开始添加内容。

所以这一点非常重要。

因此，这本身就是它自己的组成部分，我要做的是确保这种颜色样式指定为激活我们的高强调，也称为高强调，并确保它具有正确的高程设置为一个倾角。

所以我们可以稍微看到现在使用的高程。

有了这个，我将继续让它成为一个主要组件，因为这是一个变体，设计师可以使用这个准系统布局，复制它，然后开始在它上面添加他们自己的元素，我可以继续并指定约束，因为现在当我拉伸它时，它不一定会正确地坚持，指定它总是好的。

所以我要设置左右的约束，所以现在当我拉伸它时，它会适当地拉伸，即分割线，所以现在我们扩展了前面的层，你可以看到这里使用的高程。

一开始可能会有点困惑，创建这个扩展的前层是因为它是一个空白的画布，所以当设计师进去使用它时，我会在这里对齐它。

所以当一个设计师去使用它的时候，一开始可能会有一些误导。

因为设计师需要做的就是复制这个。

哎呀，我要交换一下，我把实例放在这里。

不过没关系。

我们可以做的是继续前进，移动主现在这个框架，前层扩大。

扩展后，我们可以继续创建隐藏的变体。

我最初试图得到的是设计师将复制它，复制这个副标题，并继续添加其他元素。

例如，我们在上一个视频中创建的列表项，我可以继续，也许我想添加到这个组件中，对，我只需要确保指定了正确的间距。

所以我想做的是，我要把它包在一个框架里，对吧。

因此，它将是我的前端层，扩展了内容，所以你必须将它包装到一个框架中，确保一切都相应地对齐。

我要把它拉伸到整个宽度。

然后你能做的就是，根据需要继续复制这些项目。

这很好，并确保剪辑内容是指定的。

指定了剪辑内容后，现在就有了一个包含内容的示例，只需确保在顶部设置了适当的填充。

所以我们可以继续检查，看看从这个列表到分隔线顶部的填充指定了什么。

我们能做的就是始终参考我们的规格。

因此，参考我们的规格，看起来这里有一些间隔。

不一定指定。

但是我们可以继续观察，它看起来大概有 24 个像素。

这样，我们就可以确保设置为 24，这几乎是完全的，我只需要将此内容再向下移动一个像素。

我们应该准备好了。

那里。

我们有自己的例子。

如你所见，如果我们看这个框架，它包裹了前层，前层是背景，上面添加了所有这些列表元素。

由于可能有几种情况以及您希望如何使用扩展的前层，您可能希望在此背景上的扩展前层上添加其他类型的内容。

所以我们会给你一个基本的布局，让你可以自由地在上面添加东西。

你可以通过分组和作为内容的框架来做到这一点。

我们将继续检查隐藏版本，隐藏版本指的是这个，这个前层现在被最小化，这就是我所说的隐藏材料设计称之为隐藏，整个这是设置为，让我们看看 48。

因此，高度设置为 48，我们可以在这里指定它，并将其捕捉到底部，我们现在将它设置为 48。

我还在用我刚刚去掉的那个版本。

所以我要做的是分离这一点，并把这个前层隐藏。

移除顶部约束，将其设置为左下和右下。

所以分别延伸。

在这个版本中，不需要分隔线，需要的是一个动作，胡萝卜和我这里有一个，向上箭头，键盘向上箭头，所以我要选择粘贴它，确保它在这里居中。

右边的填充设置为 16 度，这样我们就可以开始了，现在我们有了隐藏的前层变体，我将继续让它成为主要组件。

复制这个，抓住主要组件，把它拖到我们的画布上。

它仍然利用一个倾角仰角。

所以这一点很重要。

我们甚至可以把它扔进去，就像你看到的，它现在是一个例子。

这里我们没有指定的是它的内容。

所以我把它重新命名了。

本质上，你可以做的是在这里创建和添加你需要的任何内容。

所以当这个被隐藏的时候，背景，后面的层被利用。

因此，如果我们继续关注材料设计文档。

我们看这个背景的结构，它有一个背层，对吧。

而背景幕上有一个具有原创性的动作。

并且前层具有与该后层上的动作相对应的动作。

所以我们可以继续，在这里举例说明。

因此，我们可以继续创建这些列表项。我可以，我将继续拉。

我要去换衣服。

让我们看看这个应用程序名称。

我要把这些图标藏在这里。

我甚至可以把这个图标换成 x。

所以我可以找到退出图标。

或者更好的是，它可能被标记为接近。

这里我们有一个关闭图标。

我们想仔细检查一下，看看我们这里是否已经有了，我们已经有了。

所以其实叫清场。

所以我要在这里寻找 clear，我的材料设计库。

我将把它拖到这里，按住 Option command 并将鼠标悬停在该图标上，它就会交换它。

我会把它改成原色，因为它在主表面上。

我们可以移除隐藏或关闭的填充。

所以我们指定了。

我们现在要做的是，我们可以在这里引入一些列表项，我们可以引入利用图标的列表项，这是一个单行变体。

我们甚至可以做的是移除背景，我们可以将文本的颜色改为主表面上的内容。

现在我们有能力将这些内容推上一两个台阶。

所以现在这个直接贴在顶部的应用栏上。

我们现在可以交换图标了。

因此，我们可以不使用占位符，而是使用男性，我可以在这里交换图标。

在原色上添加适当的颜色。

如你所见，我们现在有我们的项目了。

我想确保该状态设置为非活动状态，抱歉。

因此，这应该是利用中等强调，这不是一个颜色的风格，现在我们可以继续进行，并在我们的图书馆指定。

因此，如果我们如果我们分离的风格，并达到 68 那里，我们现在有我们的媒体强调颜色风格。

因此，如果我继续指定主要内容，中等强调，这可能是我们的新颜色样式，这将与其他相关联。

长话短说，现在我们已经创建了这个列表项，我们需要将它的高度改为 40。

所以你会注意到我是如何开始把我们设计系统中的元素结合起来，在其他组件上使用，比如这些背景，你可以把它想象成这些基础层。

因此，我将继续复制这个，我们现在已经复制了我们的，我们的背景与隐藏的前层。

因此，这个前端层隐藏在那里，我们甚至可以将它作为一个组件。

因为通常情况下，设计师可能会使用这个，如果我们把它作为一个组件，设计师可以继续下去，只需要交换这些元素，并根据需要重命名这些列表项中的文本等等。

或者我们可以给他们提供，我们也可以提供两种基本正确的变体，这样我们就有了前层隐藏，他们可以根据需要将其拖放到设计上，或者我们可以给他们提供我将要创建的整个示例。

所以我要把它重新命名为隐藏的，隐藏的。

我将把它标记为隐藏的，这只是隐藏的前一层。

现在一切都准备好了，这是一个主组件，我们也准备好了，这就是你如何为材料设计构建背景变体。

今天，我们将构建横幅组件。

唯一进一步的麻烦是，概述将讨论如何通过查阅文档来使用这些组件。

然后，我们将自己创建它们，应用适当的命名约定，甚至添加适当的约束条件，可能还会添加一点自动布局，但我们将实际保存该自动布局和有关自动布局的深入视频，因为课程的下一部分将恢复我们在本部分中创建的所有组件的自动布局。

但是对于约束条件，唯一的区别是组件已经制作好了，我们将在课程的下一部分对它们实现自动布局。

因此，不再费什么周折，让我们来看看这个横幅文档资源，一旦它在您的浏览器中打开，继续点击，将不会再次显示该网站的复选框，因为您会经常查看它。

这里你可以看到我们的这个有点放大了。

哎呦。

与此同时，横幅显示对手的信息和相关的可选行动。

因此，在这里，突出的信息出现在顶部，然后我们根据突出信息中的内容采取相关的可选操作。

你可以继续理解这里的用法。

它还讨论了类似的组件，并且基于所传达的消息的优先级，您可以利用某种类型的组件。

因此，对于横幅，通常当根据严重性级别有突出或中等类型的优先级时，如果该优先级为突出或中等，则通常您会使用横幅，横幅会一直显示，直到用户取消或导致横幅的状态得到解决。

这里描述了小吃店和对话框组件的一些不同之处。

横幅显示重要性。

重要性简洁的消息并为用户提供操作来解决或消除横幅要求用户操作来消除。

它们应该显示在顶部应用程序栏下方的屏幕顶部。

并且它们是持久的和非模态的，允许用户在任何时候忽略它们或者与它们交互，在那个时候应该只显示一个横幅。

这里有一些原则和解剖一个最复杂的旗帜。

正如您所看到的，最复杂的横幅有一个带有支持插图的容器，您也可以将其作为引导图标或信息图标来引用。

这些也是常用术语。

所以辅助插图。

这里我们有一个容器，它封装了这个设计中的所有元素，当然，还有文本。

然后我们有按钮是可选的，我的意思是我们的支持行动。

这里有一些如何使用和如何不使用它的例子，一次只能显示一个横幅，任何横幅都可以出现在内容的上方和下方。

这些图片下面的内容是这些图片的集合。

这里是顶部的应用程序栏，它正好位于两者之间。

所以你总是可以把它想象成它会出现在顶部应用栏的下面。

这里还有一些用法，我建议你仔细阅读，真正理解放置和行为是非常重要的。

然后我有这些关于如何实现这一点的规格截图。

我们将继续参考我们需要的纺织品。

我们需要的纺织品，正如你在这里看到的，是纺织品的身体。

我们现在将进入 figma，开始构建这些。

我们要做的第一件事是启用我们的材料设计系统。

为了开始引入我们已经构建的某些组件，我们构建了这个文本按钮。

也就是说，让我们继续建造它。

所以我们有一个框架。

在本练习文件中，我已经创建了命名约定，说明我们希望如何对这些横幅广告进行分类。

如你所见，有移动和桌面贝雷帽。

我们将继续创建所有这些，我将继续在每个部分创建一个或两个。

然后，我会暂停视频，让你测试你的技能，并以系统化的方式自己建立这些技能。

所以利用我们已经在材料设计系统中创造的资源。

所以事不宜迟，我们开始吧。

所以我们有这个移动变体，对，所以我们有内联的文本和动作，这意味着它们都在同一水平上。

它们没有叠放在一起。

它们在水平线上，对，它们都在同一轴上，x 轴只是水平的。

也就是说，我们可以继续创建一个框架，我将对其进行重命名，因为我已经在该文件中复制了该命名约定，所以我的图层现在将在移动类别下标记为“live ”,作为一个单行列表，其中包含文本和动作，它是成行的，不是堆叠的。

这是堆叠的。

但这是一个两行的例子。

正如你所看到的技术栈，文本在这些动作之上。

我们首先需要的是我们的文本按钮。

所以我们只需输入文本，就可以在那里找到文本按钮。

在我们的“assets”面板中，我们可以将它拖到这里，然后将它重新命名为 action。

您会注意到，我们已经将自动布局应用于此按钮。

所以我们不需要手动调整它的大小，这非常方便。

我们暂时把它放在一边。

让我们调整这里的宽度，将其设置为 360，这符合 Android 的移动原则，因为材质设计最初是为 Android 创建的。

指定之后，让我们继续指定适合四个像素的高度。

现在我们已经指定了高度，我们还需要一个东西，就是分隔线。

因此，如果我在我的资产面板中找到我们已经创建的分隔线，我可以将它拖到这里，我们已经指定了宽度，并且它具有我们需要的适当颜色样式，我可以直接删除，我将快速转到主要组件，并分离这个不必要的关闭的纺织品，填充样式，填充颜色。

我会继续出版这个。

我们就可以出发了。

添加实际方差。

既然已经发布了，而且成功发布了，我可以继续更新这里的组件，如你所见，它现在已经更新了，不包含填充的背景层，因为它是不必要的，我们在这里使用了一个笔画。

我们可以继续，如果你密切关注设计，你可能需要也可能不需要这个横幅，有一个分隔线，正如你所看到的，在内容和顶部栏之间有一个分隔线。

所以记住这一点。

我们能做的就是提供这个，然后设计师可以复制这个组件，如果需要的话就关掉它。

所以我们可以提供。

这是我要添加到所有元素中的第一件事，我刚刚将它捕捉到这个框架的底部，按住 Command S，option s skews，因为它是父框架的子元素，所以我可以根据需要轻松地将它捕捉到顶部或底部。

现在我已经设置好了，我要设置约束条件是左下和右下。

这样的话，如果这个被拉伸，这个分隔器也会随之拉伸。

但是我们在这里已经定义了这个，我们真的不需要，我们不需要修改它，因为我们在这个场景中为 Android 开发系统。

也就是说，我将拖动这个按钮，靠左对齐，距离右侧 8 个像素，在 y 轴上垂直居中。

所以那是相应地设定的。

然后，我将把约束添加到父框架的右侧，约束将贴在父框架的右侧，然后垂直居中。

所以每当它被拉伸时，它总是保持在相对于高度的中心，或者降低或者增加。

然后我们将继续指定文本。

因此，文本再次使用，我们将在这里添加颜色样式首先文本和图标，并使用高强调文本颜色样式。

然后，我们将继续为这种版式指定正文。

然后，如果我们只输入一行，只是模仿那里写的，并且指定一行字符串和一个动作，我不能在这里输入一个动作，我们应该准备好了。

指定之后，我将把它放在这里的中心，用快捷键选项 A 和选项 B 把它向左对齐，垂直居中。

我要把这个移到 16 楼。

因此，填充设置为 16 个倾角，就在那里，这是中心。

以此为中心，我们的内容就可以开始了。

你可以看到 material design 指定 textbox 在这里有 36 个像素的间距。

我们现在能做的就是把它设置为 48。

从文本按钮本身，所以我们可以继续按住命令，并延长文本框设置为，所以间距设置为 36。

所以现在设置为 36。

在这里，我们需要做的间距，以确保这是固定的，我们可以继续进行约束，并将其设置为左和右。

因此，如果它被拉伸，这些约束将保持不变，我们现在不必担心这个问题。

我将排版也设置为在 y 轴上垂直居中，以便与右侧的动作对齐。

因此，如果设计师需要扩展这一点，这不会打乱他们的设计。

所以有了这个说明，我们就可以开始了。

我没有向你们展示 autolab，因为我将在课程的下一部分向你们展示，这将是它自己的重点，你们将掌握，我真的希望你们理解这部分课程中的约束的概念。

仅仅是这些组件的一般构造，以及将 autolayout 添加到 mix 中，就可能会非常费力和令人困惑，这是您目前正在学习的所有这些密集知识的基础。

有了这个，所有的排版都被指定并附加到它们适当的颜色样式和所有利用实例的东西，我们现在需要进入这个框架的背景并指定颜色。

因此，使用的颜色是表面背景，我们可以指定表面表面或表面背景，表面表面表面表面，我们开始，因为表面覆盖在背景之上，它不是整个页面的初始背景，它只是横幅组件的表面。

既然我们已经创建了它，我们将继续复制它。

然后我们要把它做成一个组件。

现在我们有了主组件，我们创建了第一个父组件。

让我们继续，用动作构建这两行变量。

需要注意的一点是，我们可能需要修改文本元素的行高，因为一开始我在我们的系统中创建这些纺织品时，我特意让我们修改并注意我们排版的行高，以确保正确完成，因为如果没有正确完成，我们排版的间距将在行式变体等组件中关闭。

让我们继续创建。当我将此屏幕截图移过来时，创建一个新的。

因此，我将继续复制这个命名约定。

当它抓取时，我将从头开始创建一个新框架，宽度设置为 360，高度设置为 112。

然后，我将粘贴命名约定，我们有两行文字和动作的移动版本，横幅变体，这很好。

现在，它使用一个纺织品主体，但我将继续复制这个动作按钮，粘贴它并按住选项 s 和选项 D，它将吸附到底部和右侧，然后向上移动，直到底部的填充设置为 8。

没错。

现在，我将重复这一点，并确保填充设置为 8 从该元素的权利。

这两个元素都在右边。

我们希望它们也能粘在这个组件的底部。

因此，如果它曾经被拉伸，它将坚持底部和右侧间距分别对应其父母，这是背景。

当我们在这的时候，让我们继续改变表面的填充表面的背景色。

现在我们要做的就是调整合适的间距，以适应我们的排版。

所以我要在这里创建一个文本框，选择正文颜色样式。

然后我会模仿里面的内容。

在此组件中，该组件中的文本。

一到两行在移动和平板上是有利的。

好极了。

所以我们有了我们需要的文本。

现在我们需要指定一些东西。

因此，我们需要指定的是，这两行文本中第一行文本的基线设置为 36 dips，左侧填充设置为 16。

在这种情况下，右边的填充设置为 16。

让我们继续，把它拉到左边，把这个内容稍微往下推。

所以我们把它设置为 16。

我要把它推过来，确保它设置为 16。

在右边，将约束设置为左、右、上、下。

然后，我们需要调整这些按钮和文本之间的间距，这是我们开始修改行高的地方。

让我们继续，在这里拉出我们的标尺，移动 R，然后我将创建一个 36 像素高的矩形来引用此文本的基线位置。

因此，我将向下推送此内容，实际上，目前声明文本框在 22 个倾角的顶部有一个填充，以实现从顶部开始的 3、6 个倾角基线。

正如你所看到的，我们甚至可以调整这个文本框，以确保它不会重叠或干扰下面的动作。

它展示了从基线到按钮顶部有 12 个像素的间距。

所以让我们继续仔细检查一下。

如果不是这种情况，这是一个潜在的迹象，我们的行高在打字风格。

继续创建这个集合，设为 12，我们非常接近，有一个像素偏离基线一个像素。

所以本质上我们能做的就是进入我们的纺织品并对其进行改良。

所以设置为 20。

所以我进去修改，转到定义样式，修改为 20。

然后将其发布到我的库，发布，成功发布，然后我可以更新它，一旦我更新了它，请注意这里的文本下降了一个像素，它实际上下降了不止一个像素。

所以我要取消这种风格。

把我的身体和纺织品联系起来，确保它在那里，我要重新发布那个变化，并确保它再次更新，这样它就在那里。

虽然我认为它是正确的，但我会接受它只差一个像素。

嗯，有了这个，实现了，外观和感觉，你有所有的间距设置正确。

以及文本上适当的颜色样式，加上高度强调的纺织品应用，我们应该可以很好地去做。

所以，我甚至可以隐藏我的标尺，或者直接从这层中移除它们，我们缺少的是一个分隔线。

所以我要在这里复制这个分隔线，然后粘贴它，然后点击选项 s，这将捕捉到这个元素的底部。

它有我们之前构建的组件的约束。

一旦我完成了，我们就可以测试约束了。

你会注意到，当我展开这个元素时，约束和间距保持不变，分隔线固定在需要的位置。

一旦我对这个组件的响应性感到满意，我就可以开始了。

如果你真的想变得清晰、简洁，你可以在你的组件中创建重新排序的元素，这是一件很棒的事情，这样会更容易消费组件。

我将继续制作它，我们可以将它制作成主组件，我将复制它并暂停此视频，为移动版本构建第三个变体。

所以你可以根据需要修改它，你可以拖动一个占位符图标。

所以我们可以输入占位符。

我们有那个占位符。

我们可以把边缘弄圆。

现在我们有了那个圆形图标，你就有了一个好的开端和一条线索。

所以我马上回来。

现在创建了第三个变体，我只需要增加高度到 120。

添加 40 乘 40 的 dip 图标，设置与此处规格中所指示的内容相关的填充透视。

我觉得这非常简单明了。

没什么，没什么难的，我只是确保我设置了正确的约束。

我和你应该可以走了。

也就是说，我将继续让它成为一个主要组件。

我们将继续创建桌面版本。

有了这个单行变体，我们可以继续指定高度，我刚刚复制了我们在移动设备上创建的第一个版本，因为它本质上是完全相同的。

但是对于桌面，宽度被指定为一个更大的值，这里的宽度要大得多。

我们能做的就是将宽度设置为 720。

好的一面是，我们的约束会随之改变。

所以仍然有需要的空间。

所以这些文字可以占据这个信息的大部分，如果需要的话，我们有一个分隔线供设计师使用，让这个信息变得如此独特。

我们的排版属性保持不变，或者在这些元素上仍然保持相同的间距，只是宽度现在发生了变化。

我还需要将它更改为这个组件的命名约定。

所以是桌面。

桌面区只有一行文字和动作，所以我可以把它改成桌面，然后重命名，我们就可以开始了。

因此，继续把它作为一个主要组成部分。

我们将从头开始创建另一个，这里，所以我们有，我们没有，这个三线元素的宽度看起来可能设置为 80 像素，它实际上并没有给我指定的高度，我们只是要把它们扔掉。

因此，我将再次参考该文档。

是的，被切掉了，很难看清，但我会，我很确定是在因为它能被 8 整除。

因此，对于这个三行列表，让我们继续创建一个新的框架。

然后单击，在背景中应用适当的表面表面颜色，我将继续抓取这个命名约定，它们组织得很好，然后发布这些组件。

所以我已经有了命名约定。

我将继续将宽度指定为 720。

抓住一个分隔线，因为它会抓住，竖起，把所有普通的东西都去掉，指定宽度为 720。

将约束设置在左下方和右下方，只需使用我的快捷键，就可以将它捕捉到最下方，就像你在这里看到的那样。

那就好办了。

我们只需要仔细检查这里的高度，将其设置为 80。

并且该分隔器仍然停留在需要的地方。

我们可以继续添加这两个操作，将它们去掉。

所以我要复制这个文本按钮，粘贴它，把它贴在底部和右边，然后向上移动。

因此，右下角的填充设置为八，复制这个，并确保这是八个像素远离按钮的权利，应用适当的约束。

我们应该准备好开始行动了。

现在我们只需要找到我们的三行文本字符串的三行列表。

因此，我将继续创建选择要纺织的身体。

我们需要的是，我将暂停这段视频，快速复制这段文字。

我一字不差地写下了横幅信息。

我使用正确的纺织品。

实际上，我需要按住 Command 键，确保织物包裹住整个信息，然后将其垂直居中，然后检查左侧的间距，设置为 24 度。

所以我现在有 25 个，把它推过来，我们就可以开始了。

以及这里和这里之间的距离。

和文本框是 90 度倾角。

所以我能做的就是把它拉长，这样它就被设为 90。

现在我有 90 英镑来支付这笔费用。

那不是 94，现在我把它移过来，设为 90。

现在我们在文本边界框和动作之间有了 90 度的间距，这非常重要。

现在我们已经准备好了，我们已经定义了它。

我将继续创建这个组件。

我要挑战你们自己制作这最后一条桌面横幅信息。

所以只要按照这些规格，这里的高度说 72，只是因为它被切断，可能很难。

这段文字的基线距离底部 20 个像素，但实际上只有 8 个像素。

因此，与我们在这里为这些实际按钮所做的规格相同。

无论如何，我要暂停视频，制作这个，我希望你喜欢这个练习。

现在，我们已经为横幅广告创建了第三个桌面版本，一切准备就绪。

现在我们有了这个组件，设计者可以继续复制，然后开始根据需要替换图标占位符的图像，用正确的文本边界框修改版式。

应用指定的这个组件，然后这些按钮的适当位置，以及添加和修改文本，这是伟大的。

今天我们将讨论底部导航的构建，底部导航只有两种变体，分别是纵向版本和横向版本。

的版本，我们将为基于 Android 的格式进行设计。

我们有文档资源，所以你可以打开链接。

如果我们点击它，很明显在底部导航允许在主要目的地和应用程序之间移动。

这很关键，也很常见。

它被用在每一个应用程序中，无论你是在手机上使用脸书移动，无论它是 iOS 还是 Android，或者你正在使用 YouTube，或者你正在使用谷歌套件中的某种谷歌应用程序。

我强烈建议你通读所有内容，就像其他视频一样，真正深入了解这些组件做了什么，与这些元素相关的一切，以及背后的研究。

也要理解状态以及如何与之互动。

在 figma 中，我截取了规格的截图。

还有一点要注意，非常重要的是，每个按钮的容器的最小宽度设置为 80 dips，最大宽度设置为 168 dips。

因此，您会注意到，它们都利用了此处指定的最小宽度，即 80 度倾斜宽度。

在纵向版本中，纵向是指如果我们创建一个框架，然后我选择 android，我现在有了我的 Android 版本。

所以我要输入，我要正确地标记它。

因此，我将继续前进，并键入底部导航空间斜线基地，纵向。

一旦你正确地标记了它，我也要复制它，并将它设置为横向。

一旦你指定景观，现在景观，这是景观，所以我们要在这里设计景观。

接下来，我们将开始这个聚会，将这个设置的高度指定为 56 像素，它的底部应用程序栏是图中的仰角，如果你不相信我，你可以自己看看，但仰角默认设置为 8 度。

你会注意到，这里还有一个框架，我把它拆下来，重新指定这是材料设计尺寸的框架。

我继续下去，只是机器人只是标签的机器人肖像。

你会注意到，这是底部的导航设置为纵向，内容被剪辑。

现在因为父帧已经检查过了，所以你在另一边看不到阴影。

所以现在我们需要继续并开始添加内容。

我们这里有这三个图标，但是它们将存在于一个特定的容器中。

所以这些图标被设置为 24 度宽和 24 度高，并且在轴上居中。

如果我继续选择这三个，我可以继续确保它们水平居中，然后相应地隔开它们。

你能做的就是确保它在 120 像素帧 120 dip 宽帧内居中。

因此，如果你感到困惑，我们可以做的是，因为这是 363 60 除以 3 是 120 倾角。

所以我们所要做的就是在每个图标周围加一个框，设置宽度为 120，高度为 56。

我们要做的是，我点击选项 W 和 a，将它捕捉到顶部和左侧。

然后，如果我按 Enter 键，并按下选项 V 和 H，它会在屏幕上完美地居中显示。

然后我们也可以对这个做同样的事情。

现在我可以把它分开了，因为它是居中的，它的位置是向左填充 48，垂直于 y 轴。

所以我们要做的就是把它放在一个框架里，设置为 120 像素宽，56 像素高。

我将点击选项 w，使捕捉到顶部，然后我将使其水平居中，然后点击 enter 并点击选项 D 和 H，使其在此框架内居中。

所以你会注意到这是 120 像素宽，它左边的部分也是如此。

我们现在可以做的是，把它放在中间，我可以把那个框架解组。

我甚至可以在这里应用间距，只是为了再次检查。

或者我可以把这个包在相框里。

同样，如果你不是那么有信心想要非常精确，设置宽度为 120，高度为 56，然后点击选项 D，这样，捕捉到左边，选项 W，所以它捕捉到我要输入的父帧的顶部。

现在我选择了我的图标，点击选项 D，然后 h。

现在，它位于 120 dips 宽框架的中心，距离下一个图标 48 个像素。

我可以取消分组，现在我的图层面板，现在一切都相应地居中。

所以现在我已经把图标正确地放在底部导航的中心了。

所以我们遗漏了一件事，当然，就是这种排版。

因此，我们可以再次参考文档。

它向你展示了这种类型，我们可以在主题中检查排版。

它使用标题样式。

因此，随着标题的使用，基线距离图标框架底部 12 度，这对我们来说是一个很好的参考。

因此，如果我们继续，现在创建一个纺织品，我将继续创建这个纺织品，并标记为哎呦。

我只是要输入调整为自动宽度的特色设置，应用纺织品标题。

并确保 fill 设置为 on primary，并确保全部大写。

我要仔细检查一下，哎呀，不全是大写字母。

而且我写错了，不好意思，这里标着 favorites done 应该注意。

本质上我要做的是设置行高。

有了这条基线，我将把它放在中间，移动它，你会看到它捕捉到红色箭头指示，我将把它向上移动，看到这里有一条红线，我将应用到框架的底部。

我现在需要基线设置为 12 像素。

它在基线上设置为 11，或者我们的基线设置为 11。

因此，如果我们需要将它向下推一个像素，在图标的顶部留下一个像素间隙，即外部边界框。

现在我们将基线设置为 12。

这太棒了。

我们也可以引用给定的时间，我们在这里的排版，我们需要做的是选择这个图标。

我们可以继续分组，然后我可以保留选项 v。

这实际上会使内容居中。

然后我要再次取消它的分组。

我想做的是，确保文本对齐在中心。

因此，每当用户重命名它时，它就会自动位于该组件的中心。

所以他们不需要处理排列混乱之类的事情，那会很烦人的。

因此，现在，这是指定的，我们可以走了。

你会注意到，当文本被应用时，它会推高图标。

当前未激活的图标保持垂直，在应用文本的轴上垂直居中，考虑并计算它，然后在图标和文本上垂直居中。

现在，我们已经创建了该变体，我们可以继续创建它作为主要组件。

现在我们有一个我们的风景或肖像的主要组成部分。

因此，我们可以继续实际复制它，将它放入我们的 landscape 变量中，我将它重命名为 landscape，并使用快捷键选项 command B 分离此实例。

我们能做的是，确保我们有适当的约束。

因此，在这个例子中，它在这个例子的六个框架中利用了 Android 手机的横向版本的整个宽度。

所以我们能做的是手动确保它被正确应用。

通过这样做，我们需要确保我们有三个设置为 168 倾角的框，或者你有其他更好的计算方法。

请这样做。

因为这是最好的方法。

我知道一切都是垂直居中的，正如你所看到的，但我要做的是我们对纵向版本所做的相同的方法。

因此，我将宽度设置为 168 度，高度设置为 256 度，并向右上方对齐，选择我的图标，按住 v h 选项，确保居中。

我们已经正确地实现了这一点，我将为下一个做同样的事情，我将分离顶部，并将其水平对齐中心。

所以现在，我只需要对图标做同样的事情。

既然已经应用了，我们可以说我们想要捕捉这个，我们想要这些帧的边缘彼此紧挨着捕捉，因为它在这个风景变体的中间居中，做完全相同的事情。

树应用这些值，我要确保这个文本和他的收藏夹和图标也被分组，并在这个框架内居中。

然后我要把这个扣到顶部，然后把这个推过来，直到它扣到看到那条红线。

现在，我已经相应地对齐了所有三个元素。

我可以根据需要取消这些属性的分组。

所以我要取消这些框架的组合。

所以只要利用这一点来完美或恰当地测量它。

也就是说，我们现在已经将所有这些都调整好了，我们可以开始了。

我们需要做的就是确保我们把它作为一个主要的组成部分。

这是我们的主要组成部分。

现在我们要做的就是，我们甚至可以把这个框架放进去，这是每种格式的一个例子。

或者你可以实际上，我们只是想继续，把它扔在那里。

今天，我们将讨论如何构建组件。

这将非常简单，我们将从这个文件开始，转到我们的团队库，您可以按住选项三。

打开这个库，你将要访问的库被标记为材料设计系统，忽略上面标记为材料设计的库，我们需要材料设计系统。

我要把它关掉，这样你就不会困惑了。

现在我们已经启用了这个库，它将具体化一些无法在我们的资源面板中访问的库。

我们首先要看到的是，你可以点击这里的文档，这是 figma 中的一个链接。

你可以打开材料设计网站上的组件页面，了解这个组件的实际功能。

这里有很多深入的信息，我完全推荐你去阅读，因为这只会让你作为设计师受益。

你会注意到，我们有这个互动浮动操作按钮，实际上我们将自己制作。

有一个默认版本，有一个迷你版本设置为 40 度，默认设置为 56 度，宽度和高度为 56 度，迷你版本的宽度和高度也设置为 40 度。

然后，我们有了这个浮动操作按钮的扩展变体，它要么有图标，要么没有图标。

今天，我们将创建这些组件中的大部分。

我所做的是从规格中抓取截图。

这实际上告诉你如何在 figma 中构建这些。

我们将把它们变成主要组件，然后发布到我们的库中。

它谈到了位置。

同样，这真的很重要，因为这肯定会教你如何区分某些类型的按钮，因为我们创建了几种类型的按钮。

这只是其中一组按钮。

这些是浮动操作按钮。

如您所见，这有多种变体，我们有一个两个变体，即前导和文本变体。

然后，我们有两个常规浮动操作按钮的变体。

所以我们总共有四个变体。

现在，你可以随意查看。

我们将继续检查我们的 figma 文件。

我把这个练习文件和截图放在一起，供大家参考。

现在我们需要的第一件东西是图标，我们已经可以把它放在图书馆里了。

这是一个加号图标。

如果我输入加号，也许我们可以点击它，然后继续搜索那个图标。

如果它没有出现在您的资产面板中，您也可以扩展此处的所有图标，并且您也可以按类别进行搜索。

如果这对你来说太难了，你会注意到我正在慢慢地向你展示这个来展示 Guess 的功能。

同样，如果我们愿意，我们可以继续搜索。

但同样，这实际上非常繁琐，对我们的设计过程没有好处。

所以最好的方法是直接进入所有这些组件所在的文件。

因此，您可以将这些组件拖到画布上。

如果您拖动它，然后可以单击“转到主组件”,这将直接带您到这些主组件。

因为我们所有的组件都在同一个位置，所以我们要自己去寻找它。

我们将寻找加号图标，它应该位于我们的操作类别下。

我知道即使在这里，可能也很难解析和找到。

所以我要暂停一下，快速寻找一下。

由于我找不到这个图标，它实际上不在我们的库中。

再说一次，这是我们的图书馆，我故意没有把所有的图标放在这里，虽然随着时间的推移，它会。

我这样做是因为我想让我们利用图标工具。

所以我要在网上输入材料设计图标工具。

基本上，这个工具是下载我们需要的特定图标的资源。

我们还可以搜索类别，就像我在之前的视频中讨论的那样。

我们可以通过滚动来查找，虽然这可能很难，或者我可以通过名称来过滤，我只需输入加号。

你会注意到在动作类别下，这些都是图标的加号变体。

所以我一直输入加号。

我实际上寻找的是添加图标。

因此，如果我可以再次检查，回到我的图书馆，并搜索添加，我的图标，你会注意到它在那里，它只是对我来说很难找到，因为这一切都非常密集。

哦，在那里。

让我想想，有时候你会被那些内容淹没。

所以我可以继续复制这个。

它叫做内容添加。

我将继续搜索它作为示例内容。

如果我输入斜杠 add，它甚至会进一步划分。

同样，我们准备好了，我可以将它拖到画布上，我现在有了这个图标的适当尺寸，我只是再次检查这个图标的宽度和高度是否设置为 24x 24。

在这种情况下。

因此，您会看到这是在属性面板中指定的。

所有这些浮动操作按钮的变体都使用 24×24 的宽度，24 的宽度和高度。

我是来找圣像的。

我们将首先创建常规变体。

我们要做的是，现在我们有了这个图标，我实际上要按住 Option Command G 来创建一个框架。

我们要给这个按钮贴上标签，斜杠空格斜杠空格，浮动动作。

哎呦，动作按钮，常规。

这是浮动操作按钮的常规变体。

它位于按钮类别和浮动操作按钮类别下，是常规的变体。

因此，重要的是要注意，我们从一开始就设置了命名约定，这将使我们的生活变得容易得多。

当我们开始将这些组件发布到库中时，您就会知道我的意思了。

好的，现在我想做的是拿着一个选项，想象你又把它变成一个框架，这就是框架。

我要做的是在拖动帧时按住 Shift 键，并确保将其设置为 56 乘 56。

如果不是，如果你不想这样做，这要快得多，你只需将这些设置为 56 乘 56。

您会注意到图标设置为顶部和左侧。

默认情况下，当我将它向外扩展时，我要做的是在图层面板中，单击该图标，按住选项 V，然后按住 H，这将使我的图标完全居中，这是我的浮动动作按钮所需要的，以便创建它。

现在我已经创建好了，你会注意到图标左右两边的间距应该设置为 16。

因此，填充应该设置为 16 左右倾斜，这是在这一帧。

你可能很难看到，因为默认情况下填充是关闭的，我按住 option 来获得这些面包线测量值。

因此，您会看到这些规格设置正确。

并且已经根据需要垂直居中和水平居中。

框架尺寸正确。

在我忘记之前，我需要做的是确保这个图标的颜色将关闭构建图标和选择颜色，这是应用于图标的颜色，需要正确设置。

因此需要在主服务器上将其设置为内容。

你现在看不到，但没关系。

因为我们要通过点击样式来指定这个填充的背景颜色，转到我们的内容。

然后我们将选择应该在背景上还是在表面上。

它们都是相同的色值。

同样，如果对应用什么颜色有任何困惑，我们可以随时参考此处和材料设计中的文档，以更好地理解行为，并获得如何更好地构建这些元素的线索或提示。

但是现在我知道那是什么了，我要确保这个组件有圆角。

所以我要改变这里的角半径，使它变圆。

你会注意到它现在变圆了，当我在拐角半径上设置它为 32 度时，我现在有了我的圆形。

我们已经应用了想要的颜色。

这个图标也实现了，我需要做的最后一件事就是给这个按钮添加一个效果样式。

请记住，当我们在立面上工作时，我创建了一个立面图，我们可以再次参考文档，并访问材料设计网站上的立面。

如果我们转到组件，或者转到设计，选择环境并选择立面，然后转到立面层次结构，这是默认的立面描述，我试图找到一个很好的立面示例，以及它在浮动操作按钮上的位置。

所以一个浮动的操作按钮。

让我们在这里找到例子。

好了，我们有了，就在这里。

所以你在这里看到这个浮动动作按钮，当它上升到 12 度时，这个浮动动作按钮就打开了。

你会注意到它在这条轴上有 4 到 8 个倾角。

它位于 y 轴的六个倾角上。

这就是浮动动作按钮的默认位置。

因此，如果我们继续前进，实际上进入我们的风格，进入效果并应用六度，这是它已经在描述中的颜色系统中被标记的，这太棒了，我们现在已经创建了我们的浮动我们的常规浮动动作按钮。

一旦您根据需要创建了所需的组件，我们就可以继续将它变成主组件。

您可以单击工具栏中间的“创建组件”。

但是我总是使用快捷键来创建一个主组件，那就是选项命令 k。

如果你注意你的层面板，一旦我点击快捷键，我现在有一个主要组件。

这太棒了。

我们现在需要的是一个迷你浮动操作按钮，这就是 FA B 的意思，我们实际上要做的就是继续复制它，因为本质上这是一个普通的缩小版，只是缩小了而已。

我们需要做的是分离此元素，我将执行选项命令 B，我没有附加常规框架，或者您可以右键单击并单击“分离实例”,将它从主组件分离。

我们现在需要做的是指定间距并收紧它。

所以你会注意到周围的填充设置为 16 度。

你所要做的就是将这个的高度和宽度设置为规格中指定的 40 度，我可以继续点击这个约束比例，键入 40。

它会这样做，我可以做的是选择图标，点击选项 V，然后选择选项 H，我只需按住选项，然后点击 D，然后 H，然后它会自动将它放在父框架的中心。

因此，现在一切都设置为八个倾角，在这里指定的顶部、左侧、底部和右侧。

我们准备好了。

我们唯一需要做的事情是，实际上继续进行并重命名它，我们将把它命名为 mini fab 而不是 regular。

所以我们不需要改变这一层左边的命名约定，只需要改变最右边的部分，这是常规的。

所以改成迷你 fHb。

完成后，您可以将它转换为主组件，右键单击“创建组件”或“主组件”。

现在我们有了这两种变体。

太好了，我们可以走了。

下一步我们需要创建另一个按钮，这是一个扩展的浮动操作按钮。

这是我们需要在这里创建的最后一个元素。

创建完成后，我们将继续引用这个迷你绑定。

我将复制此按钮，并将其分离，分离此实例。

我要去设置命名约定。

而且是很长的一段。

所以我要输入 x 凹 FA B。

所以扩展浮动操作按钮。

现在我们有了正确的名称，我将继续确保我们指定了父框架，我们将利用父框架来设置按钮的参数。

所以我所做的就是抓住右边，按住 command 键，然后展开这个元素。

我需要确保我的约束设置正确，也许我希望我的约束以 y 轴为中心。

所以如果我不把它放在中间，它会设置顶部，并在约束上向左移动，这只会停留在那里。

我不想这样，因为我需要将这个高度设置为 48。

我仍然需要这个图标垂直居中对齐，我可以只应用该属性，然后将高度更改为 48。

并且保持居中，你会注意到这就是设置顶部和底部像素比例的原因。

你可以看到利用材料设计原理是多么的强大。

事物总会被四或八整除。

现在我已经指定了高度，我需要将左边的间距设置为 12，而不是 8。

现在我把它设置为 12。

所以现在间距是成比例的。

实际上我需要创建一些文本。

我们已经有了颜色样式，因为我们为按钮颜色样式启用了库。

我将指定，我将在这里创建一些文本，在键盘上按 t，创建文本工具。

我要输入 create。

我将确保内容、颜色和样式设置正确。

所以我有主要的内容。

首先，你会注意到它使用了 Roboto mano，所以看起来还不像这种纺织品，但我们需要应用一种风格。

选中该文本后，我可以单击纺织品图标。

然后选择按钮。

现在我已经创建了集合。

创建好之后，我就可以继续了，等一下，一旦指定了颜色，我就可以继续指定我需要对这种颜色样式做什么，确保我按住 command 并抓住文本的边缘。

我可以把它放在中间。

所以选项 v 垂直居中，我的间距设置为 12，我需要右边的间距设置为 20。

因为这是指定的，所以文本在它指向这里时居中，通过图标的内容，文本在这个扩展的浮动操作按钮中居中。

我现在把它放在这里的中心。

我们需要在这里将间距设置为 20，因此我可以继续单击此处，我可以按住此处的选项，这将允许我查看间距，我按住 Command 并将其向左推，以查看我是否获得了正确的间距，我差了一个像素。

所以现在这里设置为 20。

现在我已经创建了扩展的浮动操作按钮，并具有所有适当的属性。

因此，我们可以将这个拉到它旁边，继续仔细检查。

我们可以看到，我们将图标设置为 24x 24，间距设置为按钮边缘左侧和文本右侧的 12。

它垂直居中。

按钮本身的高度设置为 48 度。

文本左边的填充设置为 12，右边的填充间距设置为 20 度。

我们准备好了。

那就是我们的浮动按钮浮动动作。

浮动扩展浮动动作按钮对不起，很多正在进行的话迷惑自己。

所以我要把它作为主要组成部分来创建。

有一点你会注意到，如果我在这个组件中输入内容，我没有我需要的自动布局，因为它会自动适应我输入的内容，并在我输入时根据需要实现适当的间距。

所以我们要做的是把它变成一个自动布局组件。

课程的下一部分将详细介绍用于实现这些组件的自动布局，我认为这需要单独讲述，因为它包含大量内容。

和理解，通过在层面板中相应地对其进行分层和命名来提供该组件的命名。

然后利用提示和技巧来创建组件本身可能需要一些时间。

因此，我们将根据需要专门关注这一点和约束条件。

因此，我将把它放在中间，并确保它贴在左右两边，但请记住，当我们最终发布这些组件时，我们将使用自动布局。

我们不会用这些约束，只是用约束来练习理解约束，一旦我们一起交互地构建所有这些组件，你们就会成为大师。

所以这是我所有的。

最后一件事是组织和发布这个 so。

既然我们已经组织并发布了所有这些，那么现在我们需要组织它，因为我们已经有了这些主要组件。

因此，我们可以做的是，使用这个常规的浮动操作按钮，我们可以指定确保它相应地对齐，我们可以根据需要更改间距。

哎呀，我要把它拖到这里来。

把它调到 16 档。

类型之间的间距设置为 72。

我将继续拖动最后一个按钮，并更改它，确保它设置为 16。

我们现在有三个浮动动作按钮的按钮变体，如材料设计网站上指定的规格所示。

这就是我们刚刚创造的。

您可以继续阅读此网页的内容，可以在底部的练习文件中找到，只需单击该链接，选择“打开链接”。

它会为你打开那一页。

你就可以走了。

再说一次，我会尽量简短。

非常感谢您的观看。

在其他视频中，您会注意到我们实际上会将这些内容发布到中，作为一个整体发布到我们的材料设计系统文件中。

但是我要把它留到最后。

所以我们可以一次完成。

这样，你不是你的头，不是类型之间的上下文切换，这是两个独立的过程，构建这些组件，然后组织和发布组件。

今天，我们将讨论构建一些通用按钮。

如果您单击我们资源中的链接，我们可以继续并访问按钮文档，我完全建议您深入了解所有这些内容，但我们主要将重点放在构建这些按钮上。

你会注意到，我们这里还有另一个互动场，一个很好的小操场，让我们看到我们将要建造的东西。

我们还会谈到，我们有不同的变体。

在这里，我们有包含按钮，我们有大纲按钮，我们有文本按钮。

所有这些按钮都有一个前导图标。

所以他们也都是这种类型。

所以我们有三个，三个常规变体。

所以我们要在那里好好创造。

然后，我们也有这些变量的每一个领先的图标。

所以我们今天要创造六种类型的按钮。

如你所见，它们也是与此相关的状态，我们将在本课程的不同部分讨论。

我们能做的就是，我们这里有所有的规格。

因此，我们可以继续检查规格，看看如何实际构建这些按钮的尺寸。

一个屏幕是的，我这里有所有的截图，有所有的五个变体。

请注意，所有这些变体都有一个带前导图标的，这将考虑在内，这也是我们查看文档的原因。

正如你在这里看到的，它们也有前导图标。

因此，如果我愿意，我也可以继续截图。

我可以将它粘贴到我的画布上，单击每种类型，然后为所有 Mac 用户截图。

如果你按住 Ctrl Shift 命令，它将允许你对你的界面进行快速截图，然后你可以把它粘贴到画布和 figma 中。

现在我有了所有这些变体。

我们将从包含的按钮开始。

由于自动布局用这些按钮非常容易实现，我们将继续讨论这个问题。

但是，自动布局将是本课程的一个单独部分，创建这些组件时，只需了解如何构建它们以及如何利用颜色样式等等，这本身就很耗时，这就是为什么本课程中有另一个部分专门讨论自动布局，以及如何将自动布局添加到我们当前正在构建的这些组件中。

所以现在我们可以从一个通用框架开始。

我要去打那个。

所以框架的高度是 36 度。

所以我把它改成 36。

现在，我们已经将高度设置为 36 步，我将继续进行，并确保在主要背景色指定，如果我们去效果和去填充，点击样式图标。

我忘记了，我们需要做的一件事是，由于这些是单独的练习文件，我们必须重新启用库，我们需要重新启用材料设计系统库来访问这些颜色样式。

既然我们已经指定了，我可以去我的 Phil，我在这里有可视材料设计系统库。

我将继续单击原色，这是背景色按钮的颜色，同样，我们需要再次检查该按钮的角半径，我可以这样做，实际上有几种方法可以做到这一点。

还有边框半径，我刚刚查了一下代码，边框半径在那里设置为四个像素。

所以我可以把这里的角半径改为 4 个像素。

既然已经设置好了，你会注意到我们需要添加一些文本，一旦我们添加了文本，就会调整这个组件的宽度。

因此，如果我键入内容，应用适当的颜色样式，转到内容，然后选择主表面上的内容。

因此，如果我键入按钮，或应用适当的颜色样式，我的意思是纺织品，选择按钮纺织品，我现在有按钮。

我注意到这种风格的一点是，我们单击了“类型细节”按钮，并确保为调整大小选择了“自动宽度”,这正是我们想要的。

我能做的一件事就是按住 V 和 H 键，使它水平和垂直居中。

但是一旦我们添加了自动布局，这就不重要了。

所以这只是我经常使用的一个技巧，在父框架内水平垂直居中，这是选项 A v。

现在我们已经为背景和文本选择了海岸，我可以继续添加自动布局了。

那你是怎么做到的呢？我不知道这两种元素都需要用到。

这两个是什么？我所指的两个元素是什么？嗯，这两个元素是文本层和这个框架，我将命名为按钮。

空格斜杠空格，然后标记这个包含的按钮，因为这是我们包含的按钮。

现在，我已经正确标记了它，我将按 shift a，这样做只是添加了自动布局。

我要做的是，默认情况下，自动布局广告，默认情况下，水平和垂直填充，我们不希望这样，因为我们希望它保持垂直居中。

所以我只是再次点击选项 B 来垂直居中，然后确保这个自动布局设置为水平。

然后我将选择父层，并添加自动布局。

我只需按下 shift a 键。

或者您注意到您可以单击这个框架，右键单击并选择 Add auto layout，或者您也可以从画布中执行此操作。

我点击右键并选择添加自动布局。

你会注意到我们把它设为垂直。

那么如果我输入它，vertical 会做什么呢，只是 vertical 参数允许你在垂直方向上调整它的大小。

然而，如果我水平设置，我会改变水平方向的大小，我是说垂直方向，很抱歉这里有些混乱。

因此，如果我将此自动设置为垂直，您会注意到，我实际上只能调整水平轴上的宽度。

如果我选择水平，我实际上只能指定修改高度。

但这不重要。

我们已经指定了我们的身高。

认识到这一点，并应用适当的垂直填充，使它与文本保持一致，所以德州中心。

现在我需要做的就是确保填充水平填充没有设置为 162，正如你所看到的。

它被设置为 16。

所以我要把它改成 16。

现在 autolayout 做什么，我现在有这个按钮。

如果我使用这个按钮，如果我键入 hello 并一直键入，它会自动调整填充。

这正是我们所需要的，它将为设计者节省大量时间，这是一个非常有用的功能。

创建完成后，如果您仔细观察，唯一缺少的是这些按钮的一些高程数据，我们可以再次参考材料设计文档。

我们需要做的是继续前进，访问环境，良好的海拔，并单击描绘海拔向下滚动一点。

我们将继续寻找这个图表。

你会看到，我们需要找到我们的按钮，我们包含的按钮位于仰角的两个倾角轴上，当它被按下时，它上升到八个倾角，或八个像素和仰角轴。

因此，我们可以继续应用双下降效果样式，就在这里。

买了双份蘸酱。

那里。

现在我们已经为这个按钮设置了合适的高度。

我们可以按下这个按钮。

我们甚至可以将它作为主要组件来创建。

现在我们有了这个主要组件集。

现在，我们可以创建另一个主要组件，它是一个包含有前导图标的按钮。

你会注意到这个图标被设置为 18x 18 像素。

但是，我们也可以仔细检查转到我们的按钮，单击前导图标，我们可以检查在命令中收到的代码，shift C，要查看此按钮，我可以将其分解并尝试查看此按钮的规格。

但是如果这对你来说太难了，那就不要做。

但这是看待这些规格的一种方式。

但是我们已经有了。

因此，我希望我没有让你感到困惑，只是提供一些技术提示和技巧，告诉你如果你怀疑事情没有做好，应该如何分析。

因此，我现在已经删除了自动布局和分离的实例。

因此，我可以继续相应地调整该组件的大小。

所以我需要在图标和文本之间有八个像素。

所以我能做的就是在这里使用约束，把它设置到右边，在垂直轴上居中，在水平轴上居中。

我可以把它拉长，它会在右边保持适当的间距到 16。

它仍然居中，这给了我一些空间来添加新图标。

我们真正需要的是一个占位符图标。

因此，我们可以继续点击我们的资产。

你可以随便输入你想输入的，我刚刚输入了星号。

它可以是任何类似于添加按钮的东西，就像我们在浮动操作按钮中所做的那样。

我们能做的是去掉这个填充层，因为它是不活动的，没有这个目的。

所以这只是令人困惑。

我们可以点击内容并选择主要内容。

如果是在原色风格的表面上，那就是背景。

现在，我们要做的是把它缩小到 18 倍。

到 18 岁时，我所做的就是按住 Option Shift，当我向下拖动手柄时，它会按比例缩小，我可以将它放入我的层，并通过按住 Option v 来确保它垂直对齐。

因为我知道这不是它在父框架中。

仔细检查一下 12 度倾斜的间距。

现在我可以把它移动一个像素。

我能做的就是把它变成一个自动布局。

和父框架合并到自动布局中。

我们可以开始指定项目之间的间距。

因此，我们可以在这里做的是删除自动布局设置的初始填充总是增加一个额外的水平和垂直像素，这很烦人。

我们希望确保左边的值设置为 12。

这对于自动布局来说可能有点棘手，因为它不允许我们在这里指定这个布局上的每条边。

所以这可能很棘手。

同样，我们可以在下一个视频中讨论这个问题。

如果这对你来说太难了。

实际上，我要做的是，因为我们只关注这个组件的构造，而不是 autolayout，所以忽略这一点，这样我们就可以得到完美的规格。

我们可以在课程的下一部分重点讨论这一点。

所以我们有 12 个。

我要把这个挪过去，确保它设置为 8。

并确保此按钮右侧的填充设置为 16。

现在，它被设置为 16，在父帧中，它被设置为从右到左 8，从左到右 12。

这个按钮构造得很好。

因此，一旦正确地构造了它，我就暗示这是一个包含了图标的按钮。

因此，我们可以做的是指定右边有图标的容器按钮，并使其成为一个主要组件。

现在我们在这里指定了两个组件。

我们将继续创建这两者的大纲版本。

因此，我实际上是要节省一些时间来复制这两个。一旦这两个被复制，我们知道这些不使用高程，所以我们可以删除它。

我们要做的是按住 Option command B，这将分离这些样式。

注意其中一个有自动布局，就是这个。

因此，我将点击选项 Shift a，将其从自动布局中分离出来。

所以它只是一个框架，这就是我们想要的。

我们知道高度设置正确。

我们需要的是这个按钮周围的边框轮廓。

因此，如果我继续键入，概述按钮，我将继续，做同样的事情。

我只需要在这里指定间距，确保它设置为 16，我就可以开始了，我们真正要做的就是修改颜色和添加一个笔画。

也就是说，有一件事需要仔细检查，就是这个按钮上的高度，这个高度已经被修改了。

所以我要把它改成 36。

同样，要确保这些内容仍然居中。

所以这是我们在构建这些元素的时候，应该注意的一件事，就是要确保它们相应地居中。

当我们实现这些东西时，我要取消那些不应该分组的，应该在画布上居中的，仍然设置为 8，所有的，这个，这个按钮都有一个框架，因为我们添加了一个自动布局。

所以当我拆下这些东西的时候，我忘了加上这个框架。

这就是发生这种情况的原因。

所以现在它确保那里的像素是完美的。

所以我们可以走了。

现在，我可以继续从这两个页面中删除账单样式。

我们要做的是添加一个笔画，并将其留在一个像素处。

对于颜色，我们需要继续实际查看这个轮廓按钮。

您会注意到“轮廓”按钮设置为“原色”。

但在规格中，我们指定了不同的颜色，但这没关系，所以为了构建这些元素的唯一目的，我们将把轮廓保留为原色，尽管可能并不总是如此。

因此，我们继续单击笔画，转到样式，选择在原色上，我们将再次检查此按钮，颜色样式也设置为在原色上。

因此，我可以命令单击该文本，并命令 Shift 单击第二组文本，然后继续选择原色样式。

我可以用这个前导图标做同样的事情，确保它设置为在底漆上，而不是在表面上。

现在我们有了这两个按钮变体，只需确保间距设置为 16。

它是垂直居中的，这是通过按住 option 键实现的。

我们可以将这个按钮从包含按钮重命名为大纲按钮。

现在我们有了，我们就可以开始了。

我们可以把它作为主要组成部分。

您会注意到，它仍然会根据所有适当的参数调整大小，这很好。

但是要让它成为一个主要的组成部分。

我们也很喜欢这个组件。

我要继续按住 Option command K。

所以我们有这两种变体。

接下来我们只剩下三个按钮组件了。

所以我们有包含变量和轮廓变量。

所以我们甚至可以删除那个屏幕。

我们不能删除截图。

但是现在我们有了文本按钮。

所以我们的按钮本质上只是文本，但不会让你困惑。

对于那些质疑为什么我们不能在这个按钮上使用文本的人。

当你使用这个按钮的状态时，比如当你悬停在这个按钮上时，它周围实际上有一些参数。

这就是为什么我们需要做这个，这就是为什么这不仅仅是一个真正的文本按钮。

它周围实际上有透明的参数。

所以当你悬停在它上面时，这些颜色会出现，你可以看到它实际上被封装在一个矩形中。

如果我们回到 figma，我们可以创建该变体，我将在这里复制这个按钮。

没有必要担心从零开始创造会让你的生活更困难，没有必要让你的生活更困难或工作更聪明，而不是更努力。

然后，我们将分离样式，因为它不利用海拔，实际上只是删除后面的填充颜色，这是背景，将命令单击文本，并将该文本设置为原色样式。

现在我们有了文本按钮，我将把它标记为文本按钮。

唯一的区别是，我们需要将自动布局水平填充从 16 更改为 8，因为填充较少，所以如果我们单击它，它的左右水平填充设置为 8，文本垂直居中，其高度设置为 36。

并且根据按钮的宽度，按钮的最小宽度可以被设置为 64 像素。

那么这个标记为 A 的按钮会起作用吗？因为文档中规定的最小宽度是 64 位，也就是 64 个像素。

在这个场景中，我们有一组 100 个肌腱，所以我们准备好了。

这就是我们文本按钮的构造。

我接下来要做的是把它拆下来。

我将把它重命名为文本按钮。

因此，我们有文本按钮，我将选项命令 K，使其成为一个主要组件。

我们准备好了。

唯一缺少的是另一个带有前导图标的文本按钮。

如您所见，这是一个带有前导图标的文本按钮。

所以我能做的是在这里复制这个按钮，这个带有前导图标的大纲按钮，前导意味着它在文本的前面，在左边书写。

我将分离此实例，因为它是另一个族的一部分。

并附加实例，确保左右的填充设置为 8。

一旦完成，我需要非常小心我的框架，确保它干净整洁。

只需将这些框架解组即可。

所以我们可以走了。

我需要做的是这里的填充设置为 12。

它被设置为 16。

所以我可以把它缩小。

但是我们也可以参考这里的代码。

所以在这里，你可以清楚地看到，填充仍然设置为 12。

我的意思是八个在这个图标的左边和右边，设置八个按钮在八个右边。

然后它仍然保持图标和文本之间的间距，如你所见。

所以只要试着检查一下那里的代码，就能让我对元素的间距有个大概的了解。

所以衬垫留在了那里。

我们准备好了，让我们，我将它指定为文本按钮，通过按住来缩小，缩小到第四组八个。

然后这个按钮被设置成合适的参数。

所以现在它可以运行了，我们只需要重命名它。

所以这是一个带有图标的文本按钮，并使其成为一个主要组件。

我们准备好了。

我们需要创建的最后一个按钮是这个切换按钮变体。

也就是说，现在有了这个切换按钮，我们要做的是这里有三个部分，这里有三个按钮和这个切换按钮。

它们都是 48×48 尺寸的盒子。

中间还有一个笔画，我们可以称之为笔画，这两个笔画设置为一个像素。

它周围也有一个边框，这是一个笔画。

所以我们会考虑的。

当我们建造这个的时候。

我将继续创建一个框架，这样我们就可以计算了。

因此，48 乘以 3 等于 144，我们可以将宽度指定为 144，并确保高度设置为 48。

所以我们有，并确保这个填充的背景颜色设置为表面。

表面。

一旦这一点浮出水面，我们现在就可以抓住占位符图标，我们不知道您是否想深入了解细节并找到它们。

现在你可以通过访问你的图标库。

事实上，是的，我们可以继续这样做，因为它是免费的。

在我们的图书馆很容易找到。

我们有本例需要选择的所有格式。

我刚刚进入了我们的材料和系统库，它也在这里启用。

我粘贴了这三个，我需要做的是添加适当的颜色样式。

所以添加删除填充关闭，这是隐藏的。

现在，我可以转到表面内容，然后在表面上，因为背景设置为表面，他们已经设置为 24 乘 24。

我需要做的是设置拐角半径。

所以一般来说核心半径设定。

对于这个场景，它被设置为 2。

设置为 2 后，我们就可以把它们放进去了。

因此，这个图标，我要垂直居中，因为高度正确，请设置好，它只是填充设置为左边 12 个像素，右边 12 个像素。

我们需要做的一件事是添加一个边框。

所以我们可以做的是添加一个笔画，并设置为三 E，按回车键。

它已经设置为一个像素，我们希望它设置为内部。

因为如果我们把它设置为内侧，那就是我们想要的合适的粗缝，否则它会把它设置在框架内，如果我们把它设置为外侧，笔画现在在我们的框架之外。

我们希望设置为 inside，因为这是这里指定的。

现在，在里面设置，我将创建一条线，它是一个像素的十六进制值三个 E 或六个 E，x 值，然后设置为 48。

我要做的一件事是通过按 command 或 Command Shift O 来拉平它，然后我要按 option W，这会使它对齐。

然后，我将确保填充设置为 12。

所以现在这个图标在 12 左右和上下有了填充。

我们可以把它拖到这里，我可以把它垂直居中，然后复制它。

确保也设置为 12。

所以现在左右设置为 12。

然后我们要做的就是把它拖到这里，确保它也设置为 12。

所以现在左边设置为 12。

这里有一点是不正确的，左边最后一个图标间距填充为 10，所以是 12。

然后，我们可以通过按住右边手柄上的 command 键，向外移动两次来展开这个按钮。

现在按比例设置为 12。

所以现在这些都准备好了。

我们能做的是继续下去，并正确地标记它。

因此，我们可以标记它，如果我们重命名这个层按钮，空格斜杠空格，标记切换按钮。

进来，我们可以走了。

我们甚至可以通过点击 option command K 来使它成为一个主要组件，我们已经准备好了。

现在我们有了所有的按钮变体。

现在，我们可以根据需要将它们添加到我们的文件中。

这就是你在本课程中需要学习的所有内容。

还是为了这个视频，不好意思。

所以现在我只是拖着这些。

所以我要暂停一下，整理一下这些。

所以现在我把所有这些都组织在我的按钮层。

它们因此而得名。

今天我们将讨论构建卡组件。

我们将对卡片的用途和需求、何时使用卡片以及如何利用我们已经制作的组件制作卡片进行概述和了解。

因此，我们可以从打开我们的团队库模型开始，我们需要打开我们的材料设计系统。

然后我们可以访问这个练习文件中的 cars 文档。

因此，如果我们继续并打开该链接，您将看到卡片包含关于单个主题的内容操作。

这里我们有一个很棒的互动演示，展示了卡片的类型。

提升卡有几个选项，还有一个轮廓卡，在我们的 figma 文件中。

我们将继续构建四个变体，其中三个是提升的。

所以你可以看到这里的高架类型，在这里有一个投影被用在这个高架变体上。

然后在轮廓变体上，它实际上只是一个轮廓，而不是使用投影。

这就是两种卡的区别。

这两种卡类型，提升型和轮廓型，都有媒体支持文本和按钮的选项，您可以混合和匹配它们，继续和那些熟悉这些类型卡的人混在一起。

关于如何使用卡片有很多很好的上下文，从技术上来说，卡片是显示单个主题的内容和动作的表面。

它们应该易于扫描相关和可操作的信息，如文本和图像等元素。

如你所见，在交互式演示中，有文本和图像示例。

我们对一些按钮有一些操作，这些操作专门针对卡片中的单个主题。

这里有一些包含这张卡的原则。

它们被认为是一个独立的单元，它是独立的，可以独立存在，不依赖于周围的元素。

而且是个别的，一张卡不能和另一张卡合并或者分成多张卡。

这是解剖结构的分解图。

这里，正如你所看到的，卡片容器是卡片中唯一需要的元素。

显示的所有其他元素都是可选的。

这里的容器保存了所有的购物车元素。

这实际上是唯一需要的元素。

因此，这表明卡片布局非常支持它们所包含的内容类型。

在制作卡片时，它会根据您的具体需求而定。

所以当你试图在 figma 中把它实现为一个系统时，所有这些可选的变体在你发布它们的时候实际上应该是它们自己的元素。

这样，你就可以开始把你的单独制作成你自己的卡片。

所以它具有所有定义的功能。

因为如果你只是创建这些特定的卡片，可能有一些你不需要的东西，你需要删除它们，因此，需要从头创建你自己的卡片，这就是为什么我们要利用元素从头开始构建它，而不是重构已经构建好的卡片。

从这个意义上来说，它更加模块化。

这是一些很棒的，你知道的，行为用法的分类等等。

我抓取了这些规格的截图。

我真正喜欢的是它对卡片的说明，这一部分不应该被解读为规定性的，或详尽的，或明确的，这些卡片没有单一的布局，图形类型或图像大小。

所有这些都不是特别明确的。

因此，他们试图说明的是，它是为满足他们所展示内容的需求而设计的。

所以，所以设计师试图呈现的内容。

因此，从这个意义上说，它是灵活的，你可以根据自己的需要制作一张卡。

它显示了多种布局，有助于展示它的灵活性。

因此，我们将继续说，跳转到 figma。

现在我们在 figma 中，我们将继续创建第一种卡类型。

关于这一点很重要的一点是，汽车其余状态的高度被设置为一个倾角。

在我的下方，在材料设计指南的下方，有一个默认值表，之前已经讨论过几次，它显示处于静止状态的卡设置为右侧的一个 dip。

当它被拿起时，当这张牌被交互或被拿起时，它利用八倾角默认仰角值。

这里只是一个旁注，因为我们需要知道，要实现我们建立的卡的提升部分。

所以说，让我们继续，并开始破解这个提升卡你的。

所以我们有一个标题。

但是我们将在这里继续定义框架。

这里我们有表面的背景颜色样式，所以它是白色的，圆角设置为 4 实际上是设置为 8。

我们将继续复制它的名称。

所以我已经给这些命名了。

所以这些卡片生活在提升的类别下，这有一个标题，一些副本，正文副本，图像和动作，这些都在那里被陈述。

所以我要点击重命名这一层，或框架尖塔。

现在，我们可以根据宽度 148 倾角和高度将该搜索的宽度和高度设置为 344 倾角。

既然我们已经指定了，我就更关注这个季度，因为它应该设置为四个季度。

现在我们可以开始了，只是当我们没有把它调整到合适的尺寸时，它看起来不一样，这很好。

现在我们知道这利用了两个按钮。

我们可以从我们的资产中提取一个占位符图像。

要了解我们的材料设计系统库支持什么，我们可以继续在我们的资产面板中搜索 QA 类型，我只需键入占位符，就可以了。

我也可以使用这个图标占位符作为图像占位符。

所以如果我把这个拖到这里，我可以设置宽度和高度来添加下沉。

然后，我可以确保我正确地定位它，16，在顶部和右侧倾斜，我肯定会将约束设置为顶部和右侧，以便它保持不变，它在调整大小时保持顶部和右侧的间距。

现在，我们可以在资产面板中搜索按钮。

这里我们有文本按钮的变体，我们所能做的就是给这个动作贴上标签。

我可以再次选择它，并适当设置间距。

因此，从本文开始，此间距设置为 16。

这意味着我拿出标尺，移动了 16 个像素。

现在它在帧的左边 16 个像素，或者说在帧的左边倾斜。

但是它希望排版成为文本的开头，距离文本 16 度。

所以我可以适当地利用这个空间，关于按钮的边界框，我可以在这里添加这个标尺。

有了这把尺子，我就可以把这段文字向内推。

现在，该文本在父框架的左侧倾斜 16 度。

然后，我可以继续复制它，它被设置为向右倾斜八次，第二个动作在这里是第二个按钮，或者我们也标记该动作。

我们应该很好。

现在我们只需要添加正文和标题。

因此，我将在这里取出我的文本，确保它设置为文本和文本和图像颜色样式，这已经得到了高度强调，我将标记这个标题，哎呀，我将选择适当的纺织品。

这应该设置为一，让我们看看，副标题一，我已经指定了。

所以标题放在这里。

指定后，我将确保 type details 中的类型调整大小设置为 auto width，因此它会相应地对齐，并符合我们的类型系统中指定的正确行高。

我将使用快捷键选项 w n A 来捕捉到顶部和左侧，然后按两次，再按两次。

所以我把顶部和左侧的填充设置为 16，因为我已经把小挪动的量修改为 8，而不是大挪动。

所以我把这个反过来了，我强烈建议你修改你的判断，这样你就不用一直移动它了。

由于这个系统可以被 8 整除，所以每次我使用箭头键时，我可以以 8 为增量移动它，这非常高效。

所以我要在这里复制这种风格。

我们在这里看到的是我们的文本框应该，让我们指定正文到正文一，正文二，正文二，正文二，然后对颜色样式应用中等强调。

应用媒体强调后，我将只复制该规范组件中的内容，即第二行文本 lorem ipsum。

德勒，坐好了。

不确定这是不是一个词。

没关系。

现在我已经指定了，我要把它放大 8 个像素。

我想确保的是文本框在图像右边 16 个像素，现在它被设置为 21。

所以我可以继续增加 5 倍。

现在设置为 16。

如果需要的话，我可以将约束设置为左上，右下。

然后你可以在这里看到，我们的按钮距离这个 bount 文本边界框的顶部有八个倾角，所有可分的东西都有合适的尺寸等等，这太棒了。

现在我们要做的就是再次检查我们的参数，所有的东西都被设置为 16。

我们准备好了，我们现在已经创建了我们的第一张卡，实际上，我们需要做的最后一件事是继续前进，并应用我们的效果和提升。

这样一来。

现在我们可以走了。

所以我们制作了第一张卡片。

我们可以仔细检查一些东西，通过参考材料文档，看看它是否使用了不同的副标题。

因此，如果我们去我们的规格，只是向上滚动一点，它会告诉你，标题实际上使用一个 h6 和它使用身体一。

所以如果我说如果我选择了肉体一，哎呀。

但那是为了某个主题。

所以请注意，我们应该坚持使用和引用的样式，即使在另一个主题中属性可能不同，它仍然使用那些样式。

所以现在我要把副标题改为“六岁”。

这是一个很好的开始。

我现在相应地附上一切。

是的，我们很好。

很明显，这只是使用了 Body Body 和 metadata，它使用了 Body 2。

因此，让我们继续进行相应的修改。

现在我们已经修改了，我们的卡应该可以使用了。

但它实际上看起来不像是一个适当的纺织品，应该更小，字体，所以我们会让它保持原样。

我们将正确设置所有约束和度量。

我们可以继续复制它，并将其作为主要组件发布。

现在我们已经创建了第一个组件。

我们甚至可以创建另一个版本，它实际上是一个大纲卡。

因此，对于轮廓卡，我们可以做的是，我们要做的第一件事是删除这个海拔样式，添加一个中风，它被设置为一个像素作为一个倾角，正如你在这里看到的，一个倾角，和颜色设置为下表面表面覆盖。

这就是卡片的轮廓。

如你所见，这里被放大了。

这些特性实际上是相同的，但是我们的卡的高度更高。

我在这张卡上搞砸的一件事是，这两个按钮下面的间距应该设置为 8 个倾角，而不是 16 个倾角。

我将继续操作，在更改这些尺寸时，单击“保持”命令。

现在我把它设置为倒数第八次死亡。

这张卡应该可以用了。

现在继续摸摸这里。

我可能只是重复这个。

所以我不想修改那里的尺寸或间距，也不想按住 option command B 来分离它。

然后，我将重新标记这个图层，并删除该高程。

我们将选择我之前在这里指定的命名约定。

这个命名约定清楚地说明了这是什么类型的卡。

这是一个轮廓卡，上面有一条线，标题上有一条线，包括正文，复制血液，然后是一个图像和一些动作，这些动作是我们的按钮。

因此，现在我要继续和重新应用中风的颜色样式，这是表面表面覆盖。

这被设置为一个倾角或像素。

我们要做的是快速增加这张卡片的高度。

我们需要将这些内容向下推，因为这个标题的基线距离上划线文本的基线有 40 度的倾斜。

所以我能做的就是把这个内容向下推，复制这个纺织品，或者这个文本，然后用指定的重载把它改成上划线。

我要把它写出来。

然后，我将确保它从顶部向下倾斜 16 次，事实就是如此。

现在我们已经指定了上划线，接下来我将确保基线距离，从这里的排版到这里的标题有 40 度的倾斜，这个标题使用 h5。

所以它稍微大了一点，如你所见，现在我要继续仔细检查，添加一些，将这条红线添加到第一条基线，将一条红线添加到标题的第二条基线，这就是我要将其命名为标题五。

然后我会拿一个方块，手动计算。

所以目前设定为 41。

所以我们想做的是，我们已经很接近了，默认情况下向上推一个。

现在设置为 40。

就在那里，如你所见，这正是我们想要实现的。

这太棒了。

我可以让我的尺子按下 r 键。

我们在这方面做得很好。

所以让我们继续，从标题和这段文字中去掉这个间隔。

标题的基线比正文的基线高 24 度。

所以我们可以继续，在这里做同样的过程。

只需在这里添加另一条红线，抓取一个矩形，并确保这一切都是正确的，目前的高度为 27。

所以我们要做的是把它放大几个像素，并确保它设置为 24。

现在，基线距离标题基线不到 24 个点，标题五，特别是 h5 和我们的纺织品。

我们将继续仔细检查正文和按钮之间的间距。

由于某些原因，我们的规范中没有规定，但是在这里规定了，所以看起来像是设置在字体下面 16 像素，然后在按钮下面。

我继续将这些按钮重命名为按钮，因为在另一张卡中的按钮类型之间感到困惑，我可以继续仔细检查这些元素，然后将它们推过来。

因此，左边的填充现在设置为 16，以便更加一致。

您会注意到，现在按钮类型中实际推入的按钮与这里的正文类型不一致。

因此，请务必注意，这将是缓慢的，它会在视觉上出现轻微的缩进，然后我可以将两个按钮之间的间距再次更改为八个 dips，因为它应该是一个 dips，然后在按钮下方八个 dips。

现在，我将继续操作，抓住这辆车，按住 Command 键，然后拉起它。

现在是九度冰沙。

如果我再放大一个像素，我们就可以开始了。

现在是八个下降以下，现在一切都符合规格，因为需要河流覆盖标题和主体副本和图像，我们可以在这里替换填充样式。

在那里选择一张图片。

所以当设计师复制这个的时候，我们就有了按钮动作。

现在完成了，那真是太棒了。

我们将继续创建下一张卡。

所以现在把它作为主要成分。

接下来，我们将继续处理这个更复杂的组件，我们需要做的是，我们确实需要这个组件，这里我们有一组星号，所以规格中的星号，我将从这个组件中忽略它，因为我实际上对这个组件有一些特殊的用途，我希望稍后使用。

因此，我将在没有明星的情况下进行建设，因为我不需要传达这些评级。

我能做的是创建这个不同的年份，指定宽度为 344 度，高度为 510 度，角半径为 4 度。

这是一辆高架汽车。

因此，我要去我的影响，并适用于一个倾角海拔，然后我们将前往我的菲尔，并选择表面表面颜色样式。

我将继续操作，确保我的组件中有该组件的正确名称，然后我们有一个图像占位符，如您在此处看到的，我可以抓取我们系统中的占位符，并将宽度指定为 344 度，然后高度指定为 294 度。

然后按住选项 W 和 a，根据需要将其正确地嵌入到组件中。

然后我们将约束设置为左上和右上，这样当需要拉伸时，它不会变得很奇怪。

它会贴合所有的边缘，所以看起来很干净。

也就是说，我们有一个标题是 44 个倾角，这个标题的基线是从这个图像的顶部开始的 44 个倾角。

因此，如果我继续在这里添加这个标题，然后应用副标题一的适当文本样式。

或者实际上，它是 h5 标题，我相信。

我们走吧。

现在，我将设置选择调整大小，并设置为自动宽度，我将按下这个按钮，然后再按下一点，将那个按钮推至 30。

仔细检查基线。

因此，图像的基线应该设置为 44，实际上就在这里。

所以实际上是 24。

从图像顶部倾斜，基线从顶部倾斜 44 度。

现在我想忽略这些星星。

甚至，我可以添加一些次要文本。

因此，在次要文本中，我将使用标题纺织品。

如果我复制它，更改文本样式并转到标题，我可以选择它，将其设置为次要文本。

确保从卡片容器的左边算起有 16 个 Dibs。

我将确保这两种填充样式都设置为文本和图标。

高度重视。

完成后，我将继续复制这个二级纺织品，按下它并展开文本框，这样，它就是汽车集装箱右侧的 24 个死亡。

这样，它会将三个设定为 24 的音符从右边推到限制，左上和右上。

然后，我将添加已经在这张卡中的正文副本。

我会很快的暂停一下。

这种版式指定，你会注意到它也使用了分隔线。

因此，我们甚至可以拉进我们已经在那里创建的分隔线组件，拉进它，您会注意到它是 16，左边有 16 个下沉的填充，右边有 20 和 16 个下沉，因此我们可以进行计算，我们希望将此组件的宽度设置为 344。

然后负 32 就是 312。

因此，我们可以将分割线的宽度改为 312。

所以当我们把它放入我们的组件时，有点难以理解。

因此，如果需要的话，我可以直接转到我的图层面板并拖动它。

然后按住选项 H，这将使其水平对齐，然后按住选项 v，以使其垂直对齐，正确对齐，我可以按下它，确保它与正文正文文本的第二条基线有 24 度倾斜。

所以，如果我把这个向上推，我需要确保它在 24 处，所以如果我抓住一个文本框，我可以从底部测量 24 个倾角。

我很粗鲁。

快到了。

我很接近了。

现在离基线还有 24 次倾斜。

所以我要把它压得更低。

哎呀，我要抓住这个分隔线，再向下推一个像素，它就来自文本框了。

在这个主体副本上，它被设置为 20。

这是值得注意的。

我们需要改变这个文本的颜色，从高度强调到中度强调。

现在这是准确的。

有了分隔线，我们可以继续添加另一个标题，我们实际上是一个副标题。

所以我可以复制这个，把它向下推一点，标上副标题，然后把文本样式改成副标题。

现在我们有了副标题，必须使用一个更小的副标题，看起来只是为了保持层次结构。

就像副标题一一样，我相信，然后确保间距适当。

基线距离分隔器 36 度，我们可以继续测量。

从这个分割线。

目前，我们有 41 个倾角，所以我可以把这个元素向上推。

现在我们离分割线有 36 步了，这很好。

我将继续添加一些芯片组件。

所以我们在这里使用了一些动作芯片。

因此，如果我输入行动筹码，或者只是在我们的资产面板中输入筹码，我就可以拖动筹码。

我要用这个芯片做的是，我可以复制它几次，对吧，它们之间相隔 8 度。

所以我要在这里扔三个筹码。

它们都有不同的标签第一项。

我们需要确保的一件事是，这些芯片利用自动布局，现在看起来不像是利用自动布局。

因为我需要给我的芯片添加自动布局，因为它们不能适应文本的大小。

所以现在如果我进去输入第一项，它会保持左右适当的间距。

这里你可以看到我们有四个筹码。

因此，让我们继续选择它们。

这是 123 号商品，还有四个。

所以我会相应地给它们贴上标签。

现在，我可以继续调整这里的间距，确保所有八个倾角都分开。

我要把它拖放到我的 v 分量里。

并且它指定基线 14 偏离芯片的顶部。

我假设这实际上离边界框和排版有八个倾角，我可以继续仔细检查。

从技术上来说，这里的基线是 15 个点，但我们将保持字幕顶部的间距，保持在 8 个点。

现在我们已经指定了，我们所缺少的是下面的一个动作，离芯片 20 步远。

因此，如果我转到“我的资产”,在“我的资产”面板中键入“按钮”,搜索该按钮。

它没有出现。

因此，如果我继续重新输入，它会搜索，我可以抓住这个文本按钮，只需将它拖到画布上。

我可以把它改名为行动一。

一旦这个动作被重命名，我就可以将它拖动到我的新卡片组件中，并确保相应地设置间距。

确保设置为 20。

这在规格中有所说明，在 20 处，距离按钮的外框 8 个数字像素，距离父容器 8 个倾角，因为它将排版与芯片对齐。这就是为什么按钮在 8 个倾角处，因为它将按钮的排版与卡中的其他内容对齐。

然后我只需要指定这是间距下的八个倾角。

因此，我可以在向上拖动时抓住父容器的按住命令，确保设置为 8，现在是 5。

把这个向下推三个像素，我们就成功了。

太棒了。

现在，我们已经在材料设计中创建了一个更复杂的卡片组件，这也是一个例子，所以我将要求你们自己创建这个汽车组件。

然后我将暂停视频，向大家展示组装这些汽车部件的最佳方式。

好吧，我暂停一下。

现在我已经完成了所有元素的创建，我在这里创建了这个变体，一模一样的复制品，这次我做的有点不同。

我想在向你们展示所有内容之前，以我刚才的方式来完成这个练习的原因是非常有意的，我想向你们展示构建这个组件的手动过程。

然后是我刚刚创造的这些元素的力量，作为设计系统的构建模块，不仅为你自己，也为你所迎合的设计师，如果你曾经在设计系统团队工作。

这里，我们这里你可以看到我们已经创建了我们已经手动创建了我们的卡片，这里有一些有更多合成卡片的例子。

然后我在这个休息时间继续，不仅在这里制作了这些元素，还把它们恰当地组合起来。

所以当我说我正确地组合它们时，这些都是卡片的特定元素，这些卡片的宽度由 344 定义，它们的某些变体。

就像这样，我们把它们标为元素，纺织品，你可以把它们想象成瓷砖，当你在地板上添加瓷砖时，你可以排列它们，添加它们，堆叠它们等等。

所以这正是我们要做的。

有了这些元素，我要去分解它们的组成。

然后它被包裹在一个有适当背景颜色的框架中，背景颜色被设置为表面，表面然后是适当的高度，其余的高度被设置为一个倾角。

然后，当然，提升的状态设置为八个下降，正如你可以看到的两个不同之处。

在这个框架中，如果我单击这个框架，我会继续检查。

哎呀，我继续检查这个框架中的元素，我在这个组件中有三个部分。

所以我可以把这些拖出来。

请注意，它们没有应用任何背景颜色，除了这是一个图标，我的意思是图像占位符。

看这是由这三个元素组成的。

因此，注意和理解这一点非常重要。

所以用这些元素开始构建并添加到卡片上。

这些都是你在设计系统中可以拥有的元素。

很明显。

这就是它在那里的原因。

所以我们可以继续构建一个示例。

所以你可以举几个例子。

所以我们可以从这个标题开始，我可以复制那个标题，也许标题有支持文字，对吧。

然后我可以继续添加图片，标题上方的支持图片。

所以我们有媒体和标题。

然后我们有下面文字的支持。

现在我可以做的是，将它们全部连接起来，然后将它们包装在一个框架中，选择它们，然后点击 option Command G 来创建一个框架。

现在，我将单击填充颜色，转到曲面并选择曲面。

确保我有适当的圆角半径，这四个像素或下降，然后单击剪辑内容。

我们将转到 effects，在 elevation styles 上选择一个 dip，然后快速查看，我们能够使用元素构建一个卡片组件，而不是进入并确保所有东西都像像素一样完美地拖放。

因此，如果你这样做，如果你建立了这些元素，一旦像素与像素精度像素完美，你应该很好去。

今天，我们在一个设计学院，我们将创造一些芯片组件。

让我们给你这个视频的大概介绍。

我们将首先概述芯片组件，它们是如何使用的，它们是什么，然后还要构建这些芯片组件。

我们将在练习文件中打开 chips 文档的链接。

一旦打开，继续，一旦加载，您可以看到芯片是表示输入属性或操作的紧凑元素。

这就是我们的设计。

正如你所看到的，这是我们的芯片，它本质上是在这个文本中，指定向谁发送电子邮件。

这个人叫佩顿·史密斯，名叫平·史密斯，不好意思。

如你所见，一旦电子邮件或用户名被打出，并被选中，它就变成了一个芯片。

还有一个小图片来表示该用户是谁。

考虑到这种情况，我们可以继续了解用法。

它允许用户输入信息、做出选择、过滤内容或触发操作。

而按钮应该一致地出现，并带有熟悉的动作调用。

班次应该动态地显示为一组多个交互元素。

在这里，您可以看到这些原则，它们是有意精简的、相关的，因此它们具有清晰且有用的中继信息，即与 UI 中呈现的内容或任务相关的信息。

芯片应该使任务更容易完成或者内容更容易分类。

大家可以看到，这里有所有类型的芯片。

这里我们有一个最复杂芯片的解剖图。

所以我们有一个通用的容器来保存所有的芯片元素，它们的大小由这些元素决定。

在这里，您可以看到我们有一个可选的缩略图，它是这里的主要图标，可以通过在那里显示头像徽标或图标来识别实体，如个人资料。

当然，我们还有文本，用来显示实体名称、描述、标签动作或对话。

第四个因素是输入芯片可以包含一个移除图标，表示如果需要，您可以移除整个芯片。

我们对不同类型的用法进行了分类。

但是我们不会深入讨论这个问题，我建议您根据需要浏览和使用这些信息。

在这里，我们有所有我们将要创建的类型。

但有一点我想让你注意的是，这些芯片中有三个是相同的，但在本文档中明确表示为不同的芯片。

我只是希望我们能注意到这一点并集中注意力。

在规格中，我们有这个动作芯片，轮廓芯片选择芯片滤波器。

我们有输入芯片和芯片，例如分组芯片，每个芯片之间有 8 个倾角。

你会注意到，动作选择和过滤芯片都是完全相同的芯片，但底层功能可能会有所不同，这就是我想说的一点。

有了这个声明，我们就可以直接进入我们的手指文件，开始构建这些芯片。

接下来，我将确保在此练习文件中启用我的材料设计系统库。

也就是说，我们可以继续前进，开始创造这些芯片。

有了这个芯片积分，我将继续创建一个框架。

在这之前，我要复制这个名字，给这个标签加上动作框架的名字，因为这将是我们的动作芯片。

我将继续添加背景色表面下的表面覆盖。

然后我将圆角设置为 16。

所以那里很好很圆。

我要把高度设为 32。

然后，我们将根据需要修改宽度。

好的一面是，我们将能够快速添加这方面的功能。

因此它会动态适应其中的文本字符串。

所以不用手动调整大小，非常有帮助。

所以我想在这里添加这个文本样式。

通过添加这种纺织品，我可以继续选择要纺织的身体，因为这是利用芯片。

我可以给这个动作芯片贴上标签。

我希望默认情况下，我的我的字体自动调整大小。

然后我会继续把它放在中间。

我要做的是按住 shift a，创建自动布局，然后再按 shift a，在父帧上创建自动布局。

现在，您会注意到，我可以将 dip 填充调整为左右 12 个 dip，而不是适合这里的一个，这是当前指定的。

所以如果我输入 12，我现在把它缩小到 12。

它目前显示为 13，因为 autolayout 默认情况下会在元素的水平和垂直方向增加一个额外的像素，这在我看来很烦人。

因此，如果我们重置，您现在将会看到，它的左右两侧设置为 12，垂直居中，这在 y 轴上非常好，我们现在已经创建了第一个芯片，我们可以继续在这里敲除其他两个芯片。

滤波器中的选择芯片，这里有一些关于设计系统的复杂问题需要理解。

这是每个团队做出决定的地方，我喜欢这样称呼它。

所以我们有了这艘行动船。

有三种类型的这种动作船，它们在视觉上看起来都一样。

但是其功能可能会有很大的不同，并在不同的场景中使用。

比如说这个组件，内置代码以不同的方式表示，这是否意味着你也想在你的设计中反映出来？或者你只是想利用相同的设计，称之为动作芯片，而不是实际的芯片选择和滤波芯片？为了保持一致，根据设计文件中的命名约定，以及工程师在前端开发的内容，您可能需要创建三个或三个变体。

因此，为了做到这一点，所有的造型都是一样的。

所以我要复制这个，我要让这个动作成为一个组件。

作为一个组成部分，我要给这个标签贴上标签。

然后，我将继续将它重命名为“choice ship ”,然后输入并将该框架重命名为“label”。

然后我将重命名这个滤波器芯片，我将标记这个滤波器。

我要将此文本自动布局框架重命名为标签层 2 标签，我可以选择这两个，这两个元素，然后单击并选择创建多个组件。

现在我有了所有三个组件变量，这非常重要，我需要做的是重命名它们。

所以这是选择芯片。

这是过滤芯片。

现在我们正在摇摆和滚动，我们已经把这三种变体打得落花流水。

而我们现在能做的就是专注于轮廓芯片和输入芯片。

也就是说，我将在这里拿起这个筹码，抢占先机。

这个芯片现在的情况是，我要把它拆下来，标上动作轮廓。

正确标记后，我将通过按住 Command Option 键来删除 autolayout，实际上只需按住 Option Shift a，然后我将对另一个 autolab 帧层执行完全相同的操作，然后点击 Option Shift A，再点击 Command Shift G，将该帧从不需要的 Texas 中删除。

有了这种版式，我们现在需要的是一个图标占位符，因为这是一个轮廓芯片。

我现在可以做的是，在这个文件中，我已经有了一个图标占位符，复制它，我将在左边的边界框上按住该命令，以拉伸它并粘贴它，我将把它捕捉到左边。

确保它在左边四个像素，垂直居中，然后移动这个文本，添加这个约束来设置这个文本的约束，在 y 轴上向右居中，然后推这个，直到排版距离图标八个像素。

所以它必须移动大约三个像素。

所以我要把它再移动一个像素。

哎呀，我们走吧。

一旦在那里指定了，我们就可以正确地设置这些规格。

我们有一个，填充设置为右边 12 个尖，左边 8 个尖，我们准备好了。

这就是我们的行动纲要行动芯片。

我们现在要做的是指定一个像素的笔触，并将其设置为默认的内部，我们将添加表面叠加颜色到笔触，但我将删除那里的颜色样式。

我将转到“surfaces Overlay ”,现在我已经指定了它，如果图标占位符太难看到，我们可以更改它的颜色。

现在我们有了动作轮廓芯片，我们甚至可以添加自动布局，这样间距就是动态的，所以您不必一直手动操作。

因此，我按 shift a 进行自动布局，然后我将删除默认的水平和垂直填充，然后重新对齐它，当你这样做时，你必须重新对齐它，然后我将继续移动它，确保这里向左倾斜四次。

然后，我们将添加到这个，并删除，一个像素填充添加，并重新调整这些元素，这是恼人的。

接下来我要做的是，在图层面板中按住 shift 键同时点击这两个图层，然后按 shift 键 a。

我所做的基本上是通过创建父元素来保持这两个元素之间的间距。

现在我能做的就是选择我的动作轮廓框架父框架，然后点击 shift a。

我还可以继续为该元素指定间距。

而且它会是动态的，但是有一点要注意，这可以自动布局会变得棘手，因为你不能单独指定其间距。

所以如果我想让这一边是 12，现在不可能完全做到。

所以你能做的就是继续前进，撤销那些动作。

您可能需要手动重新调整。

我们会在下一个视频中解决这个问题，在课程的下一部分，不好意思。

既然我们已经创建了这个轮廓动作芯片，我们可以继续复制它，然后创建它作为一个主要组件。

一旦指定，我们就开始处理这个输入芯片。这里稍有不同，我们将不得不取消帧中的自动布局。

然后。

所以我使用快捷键选项 Shift a 来移除自动布局，然后命令 Shift G 来移除框架。

因为那是剩下的。

一旦你创建了删除自动布局，它会变成一个框架，然后你必须删除框架。

因此，有了这个设置，我将标记这个，并把哎呦，并把船舶。

所有适当的填充都已指定，现在我们有了图标占位符。

现在我们需要在这里添加一个可操作的图标。

在我们的设计系统库中，我已经有了一个名为“取消”的图标。

所以我可以复制。

选择你的父框架，然后粘贴到那里。

如果需要，哎呀，我要把这个拖到这里。

我要把尺寸重设为 18 乘 18。

继续，把这个拖到这个组件中，t 恤垂直居中，然后向右拉，把它推过来，大概八次。

指定之后，我们现在就有了输入芯片。

但是我们需要做的是将这个动作图标的约束设置为垂直居中。

然后，我将继续推进这些内容，以便在文本和此操作图标之间只有八次下降。

我们将把图标占位符设置为左侧，垂直居中。

然后让我们将它设置为左侧和垂直居中。

然后把它移过来，确保我的间距设置正确。

所以我在这篇文章的左边和右边有八个倾角。

我缺少一样东西。

我需要删除中风，并添加表面覆盖颜色到这个芯片。

一旦我指定我们现在可以开始了，我们已经为我们的设计系统创建了所有的芯片类型。

我们需要做的就是标记这个输入。

一旦我们确定，我们现在已经创造了芯片所需的所有主要组件。

这就是我今天要展示给你们的全部内容。

非常感谢观看，下一集我们再见。

我想我差点忘了，除了我们的 autolayout autolab 倾向于调整组件的高度。

因此，我们要确保进入我们的组件，因为我们有适当的约束集。

我们希望再次检查我们是否为组件设置了适当的约束。

在一些组件上指定了自动布局后，我们需要调整一些元素的大小，确保它们能够正常运行。

确保所有这些芯片的高度都设置为 32。

我将继续对其余组件进行同样的操作。

由于约束被设置为垂直居中，它们将适当地动态调整。

其中一些有自动布局。

所以我不能在实现自动布局的情况下手动调整高度。

所以我能做的是，移除自动布局并重新调整高度。

用这个组件做同样的事情。

请注意，当我移除自动布局时，您看不到它，我使用了快捷键选项 Shift a，它保留了主组件图标。

但是看着，看着这个不能看着边界改变，我会在这之后改变高度。

所以，我只是点击了选项 Shift a，它现在改变了边界框，我将继续在每一边增加一个像素。

所有的间隔都保持不变，我们准备好了。

我们会建立对话。

我们将从理解什么是对话开始，以及它们是如何被使用的，为什么。

我们将构建这种对话的变体。

我还会要求你们也构建一些变体，我这里有所有指定我们将制作的屏障的截图。

然后，我们将打开此链接，开始阅读文档。

一旦你在这里打开它，你可以看到对话框通知用户一个任务，包含关键信息，需要决策，或者在对话框中涉及多个任务。

在这里，我们可以看到这个对话是基于一个右边的按钮触发的。

你会注意到这里有一层背景覆盖层，我们称之为稀松布。

在我们的设计系统中，这不仅是一种颜色风格，也是一种我们可以从系统中提取的稀松布。

如果我们转到 figma 文件，就在这里启用它。

要访问我们的团队库模式，请打开材料设计系统库，我可以继续搜索稀松布，我们已经可以访问它，我们可以将它拖放到这里以供以后使用。

所以这是一个稀松平常的词，用在这里。

然后你可以看到这个对话框的边缘有一些凸起。

这里有一个分隔线，在这个确认变体中，你会看到这个菜单有一些溢出的内容。

所以我们将继续构建这个变体。

接下来，我们将在这个交互式演示中构建一个名为“简单对话框”的对话框，这里有列表项。

你会注意到这些状态并没有占据对话框的整个宽度，这很有趣。

如果我们检查代码，你会看到这是一个列表项。

在这个元素的左边和右边有一些填充设置为 16。

这一点很重要。

除此之外，在父元素的左边和右边也有一个 8 的填充。

所以这个对话框左右两边的填充并不适用于列表项。

这就是为什么状态没有占据元素的整个宽度。

我将关闭该 Inspect 元素。

现在，我们也有了一个警告对话框变体，这是我们将要构建的。

不仅有这些变体，而且你可以看到有一个只有正文，只有文本和标题。

或者，你知道，我们有并排的按钮，或者我们可以选择堆叠两个按钮。

对我来说，我更喜欢并排变体，这似乎是一种更常见的模式。

您会看到，在这个对话框中还有其他选项，不是简单的，而是确认的。

还有并排的堆叠式按钮。

我们将继续下去，了解一下用法。

因此，对话框是一种模式窗口，出现在应用程序或应用程序内容的前面，提供关键信息或要求决策的对话框，当它们出现时禁用所有应用程序功能，并保留在屏幕上，直到确认取消或采取所需的操作。

这些都是有意打断的，也就是说，这些在应用程序内容前面的显示是有意打断的。

所以应该谨慎使用。

围绕这个组件有一些关注的原则。

它集中了用户的注意力，以确保他们的内容在那个时间点得到快速处理。

对话应该是直接的和交流的，在交流信息和致力于完成一项任务时，对话应该出现在对一个用户任务或一个带有相关或上下文信息的动作的响应中。

所以很有帮助，也很直接。

在这里，您可以看到何时使用小吃店横幅或对话，以及在这些情况下使用对话的方式，手头的任务需要非常集中，因为这被视为高优先级。

因此，它会阻止其下的应用程序内容，直到用户在对话框上采取行动或退出对话框(如果选项可用)。

在这里，您可以看到一些手机和的例子，以及详细的分析。

我们在这里有容器标题，一些支持文本和按钮，背景中的纱布，我们已经在他们的手指文件中拉。

我们这里有一些规格，我们将直接进入，开始构建其中的一些元素。

因此，我们可以从桌面移动警报开始。

我将从这里拿起这个，我已经将它们贴在了我们的标签页上。

我要做的是选择这些图片，移动它们。

在这里，我也引用了类型样式，所以我们知道利用什么。

指定后，我将宽度设置为 560 度，正确指定宽度后，我将高度设置为 182 度，并使用 4 度角半径。

然后它使用彩色表面作为背景，填充，然后当然利用了 24 度倾斜的效果样式。

现在我们已经指定了基线，我们将继续将此组件重命名为桌面，正斜杠移动警报，因为这是对话框变体标签材料设计。

我们将继续创建一个 h6。

创建了 h six 后，我将在截图中的文件中指定这个对话框标题，设置自动，大小调整为自动宽度，调整大小，继续选择我的 H six。

指定之后，我们可以继续在这里摇摆，我将它捕捉到顶部，然后向左，因此从左侧开始倾斜 24°，基线设置为 40°，我相信从顶部开始也是 24 分钟。

我们可以在这里继续仔细检查，只需创建一个矩形并将此内容向下推即可。

所以基线应该是 40。

所以我实际上需要把这个推高一点。

并进而使衬垫 21 从顶部开始下沉，基线设置为 40。

如你所见。

既然已经指定了，我们继续为这个标题指定颜色样式。

进入我们的颜色样式、文本和图标，并选择高强调颜色样式，我们将继续复制该文本，并选择 body one 颜色织物。

现在我们已经有了合适的纺织品，我们需要指定颜色风格到中等强调。

我们能做的就是指定这个的边界框。

通过在这里指定边界框，我将开始注意规格。

并且基线应该被设置为距离头部的基线 36 度倾斜。

所以我们基本上可以这样做，创建一个哎呦，创建一个推这个矩形到基线，这里设置为 36。

所以我只需要把这个内容推进一点点。

现在基线设为 36。

这个文本框左右两边的边界框设置为 24 位。

所以我能做的就是把它变成一个固定的文本框。

目前是 23 岁。

让我推三个像素。

现在是 24 岁。

我们希望将约束设置为左和右，所以我按住 Shift 并选择右约束。

然后我们可以做的是，如果我继续输入 lorem ipsum to lorcet Matt，继续输入，就会出现新的一行。

新的一行出现后，我们将开始我们现在只需要证明我们需要的艺术，我们需要证明我们的文本按钮。

我们的文本按钮可能会从底部开始与基线倾斜 28 度。

从集装箱里。

所以在这里，我想解释的是对话框底部的高度被强调了。

所以它本质上是在，你可以认为它是在它自己的一行中，其中的内容是底部一行中的按钮，就像这个对话框的页脚使用了两个按钮，高度设置为 52。

它在最下面一行的中间。

这段文字实际上就是所说的，这段正文从这段文字的基线向下倾斜了 28 度，你可以看到这段距离。

因此，要真正做到这一点，我们可以做的是，我要在这里创建一个框架，你可以调用这个对话，你可以我们都把它称为页脚，我要把那里的高度指定为 52 度。

当然，把它贴在左边和底部，它会按住 command 键，把它拉长，指定约束在左下方和右下方。

我们不需要背景。

我们需要在其中实现按钮。

因此，如果我们抓取打开我们的团队库，去文本按钮。

如果我输入 button，我得到了文本按钮，它在动作一的规范中被标记。

我重复了一遍，它离这个按钮有八次倾斜，然后修改文本，说动作我们在摇摆和滚动，继续，把它拖到那一行，并指定它如何居中对齐，所以基本上，我们的间距都是正确的，很好，我们有正确的间距，两个按钮之间有八次倾斜，向右，向下，和向上。

因为底部页脚的高度被指定为 52，我们可以开始了。

我们需要做的就是证明这里和 so 28 之间的距离。

所以我们基本上差了两个像素，但这已经足够精确了。

它离文本框有 23 度，我们可以继续修改它。

所以设置为 24。

现在，离 2024 年还有一段时间，我们已经准备好了。

这都是适当的间隔。

我们现在有了这个移动提醒对话框变体，它符合规范，我们可以将它作为主组件。

在此之前，我要重复一遍。

这是一个主要的组成部分，我道歉。

所以我们有了主要的组件变体。

我要把它拖到这里。

现在，我们已经有了主要组件变体，我将继续下去，看看我们接下来要解决什么，接下来我们应该解决什么？那么，我们可以继续创建这个带有操作的确认对话框。

这没什么特别的。

宽度实际上并没有在这里被指定，并且它只是一种单调乏味的建立。

所以让我们从这个开始。

实际上，我们没有所有必要的细节，不管我说了什么，我们将建立移动确认对话框。

所以我要把它复制到这里。

继续并重命名它。

现在我已经重命名了它，我需要指定宽度。

我们有两个动作。

我们能做的就是调整大小，因为我们已经正确设置了约束。

我们需要为这两个按钮设置约束。

这些按钮目前被贴在它的父框架的顶部和左侧，但我们需要贴在底部和右侧，这样当它调整大小时，它会适当地调整大小。

所以我们能做的就是正确地指定所有的属性。

所以我们可以走了。

我们所要做的就是将宽度从 560 度倾斜设置到 280 度倾斜，看看我们有没有成比例的限制。

唯一的事情是我们需要把这些内容推下去。

随着内容的下推，我们应该指定文本和页脚行之间的间距。

如你所见，它离基线有 28 个点。

因此，让我们继续努力，确保这一点得到正确实施。

会变成 28。

我们要做的是，确保当我们调整它的大小时，这个父框架是在这个框架内设置一个标尺。

因此，当我调整它的大小时，我知道它当我调整父框架的大小时，我知道我需要将它再推高一个像素，我希望那条红线可以帮助我对齐内容，但它实际上没有这样做。它没有响应，那些红线，它们留在原地。

然后，我将向下拖动此文本框，确保它设置为 24。

这就是我们要做的，在左边，右边和底部适当的间隔。

现在我们有了确认对话框变体。

同样，这是一个单线变体。

这也表明了一个两行的对话框标题。

因此，如果我们愿意，我们甚至可以继续这样做。

所以我可以很快完成。

所以我们必须从根本上减少这些内容。

就这样，我把这个拷贝按下去。

然后直接到 bash line dialog header，我们现在有两行，我要把它增加到 14 行，因为这给了我适当的间距。

从一条基线到另一条基线有 36 次倾斜。

正如我所说的，它被设置为 36，这太棒了。

因此，这是适当的，成比例的实施。

现在我只需要调整这段文字和动作之间的间距。

因此，它被设置为 28 深度，所以我们可以做的是，这应该更容易，实际上是继续推进此内容，创建 28 倾角矩形以对齐事物，只需在那里向上推进四个倾角，然后删除它。

现在我们的基线已经按照规范中的规定对齐了。

我能做的是选择调整大小或快捷键 Shift 选项命令 r。

这应该会根据父容器调整内容的大小，这太棒了。

或者你可以按住 Command 直到它与你的容器对齐。

现在你有了两个两行的对话框标题。

我们可以继续创建这个确认对话框，其中包含堆叠的长操作。

所以这一个的区别是这些按钮是并排的。

这些是堆栈。

我们要做的就是不要在这里多走动。

在这种情况下，不需要使用页脚的概念，这可能是我要删除它的地方。

我要做的是将其重命名为打开速度提升。

指定之后，我会把它们对齐。

然后确保它们相距 12 度，并确保它们从这个画面的右边开始倾斜。

所以右边是一条沟，应该离底部也很深。

现在我们已经指定了，我们有两个按钮。

但是我们需要知道的是基线。

所以 828，所以 36 从这里的框架下降。

因此，当您指定从框架开始的 36 次倾斜时，我所做的基本上是计算距离，从基线开始的距离，以及从这个按钮到这里的距离，这是 28 加 8，也就是 36 ^ 5，如果我正在计算，对，我可以继续拖动它，直到它捕捉到那里。

我们是一伙的。

所以我是那个合适的间距。

现在我需要做的就是确保我拖动了这个元素的底部。

所以离按钮底部有八个深度。

我可以适当地应用我的约束，我们就可以开始了。

我将指定这是一个带有长操作的确认对话框。

我将使它成为一个主要组件，接下来我们可以创建移动警报对话框，我刚刚附加了我的实例，标记这个警报对话框。

警报对话框非常简单。

它甚至没有标题，只是利用了正文复制。

所以我们能做的就是把 52 加上 28 加上 38。

哎呦，数学 28 加 38 你不会。

我不知道为什么它不为我计算。

所以 28 加 3866 加 52，就是 118。

所以这个元素的高度是 118。

所以我们能做的就是把它设置为 118。

现在我们可以把这段文字放上去，把它变成一行。

现在我们有了那一行，我将设置排版来自动调整宽度，然后我将确保基线设置正确。

因此，它被设置为从顶部向下倾斜 38 度，现在是 40 度。

我们到 38 岁了。

在那里，我可以把这个内容推上去，失陪了。

现在我可以创建一个固定的文本框。

我希望它从右边倾斜 24 度，或者 624 度。

所以如果我在 22 点把这个再放大一点。

现在我把它设置为 24。

在指定的情况下，我还希望文本框从底部向下倾斜八次，这是按钮外框的底部。

所以目前，它被设置为 4。

现在那里设定为八点，我们可以出发了。

我们已经为这个警告对话框指定了所有适当的间距。

我们准备好了。

就是这样。

也许我们可以把它们重命名为取消和丢弃。

我们在这里可以做的只是确保正确的间距设置为 12，当前设置为 8，然后将它推过来。

蘸着吃。

现在它被适当地隔开，在按钮之间和按钮的底部和右边有八个。

那太好了。

现在我们有了手机版的提醒对话框。

现在我们已经创建了其中的三个，我可以，我想挑战你在这里构建这个对话框标题。

我们可以看到它使用的是，嗯，如果我们去我们的列表项，我们可以很容易地看到，我们可以在这里使用这个变体，我刚才拉了图标和文本和动作，这是另一个图标。

我们可以相应地调整它的大小。

我们可以继续，通过修改一些现有的列表项组件来实际构建它。

也就是说，我们要完善这个列表项。

所以我们要标记这个图标和文本，我已经得到了列表项。

当然，我们已经对此应用了约束。

因此，这将是值得挑选出来的，因为因为我们已经定义了这一点，我们只是要设置这 256 个下降的高度，内容根据需要保持居中。

不出所料，我们将这个 240 的高度设为 40。

但是我们希望图标的高度为 40 乘 40。

所以我继续实现它，图标当前设置为 33 点 33 分。

所以我们能做的是，我可以把这个解组，我只是想确保我正确地缩放了向量。

并将帧设置为 40 乘 40。

如你所见，我们可以暂时使用一个占位符。

所以，如果你知道这个，实际上，你知道，以后改进这个图标，你可以继续做。

但是我将使用这个占位符，这样就不会干扰对系统构建的图标的修改。

用于材料设计。

我将更改图标占位符上的四分之一速率，使其变为 40 乘 40。

从这个列表项的左边算起是 24 个倾角。

现在我已经把那个专栏移走了，这很好。

文本也在图标的左边倾斜 20 度。

我将确保约束设置为左侧和垂直居中。

这里的约束条件很好。

我所要做的就是，我可以在这里重命名它，这样文本就可以匹配规范中指定的。

我们所能做的就是复制这个，并将其与下一个列表项的底部对齐。

把这个用户标记为 2。

然后我可以再复制一次，输入一个广告账号。

您可以随意修改它的填充，将其更改为图像类型，并根据需要添加图像。

由于它使用了图标，现在这三个列表项上指定了间距，让我们继续创建父容器。

所以我们的父容器，我可以想象设置为，在这个例子中，设置为 320，或者这里指定的两个。

这看起来和一个简单的对话框一样宽。

因此，我将创建一个框架，转到我的标签页并复制该命名约定。

并将其重命名为移动斜线简单对话，宽度为 280 角半径为四种颜色样式的过滤器表面，然后效果为具有使用 24 倾角的效果样式。

这样，我们就可以将这里的宽度修改为，这个宽度应该是合适的。

我们要做的是在这里添加一个，我能做的就是复制这里的。

然后或者我可以获取我已经创建的这个 liner h6。

他们会把它放在这个元素里。

我们需要检查并确保它的基线是从这个框架的顶部开始的 40 个深度。

这样，让我们看看，设置为 40。

只是推，基本上只是稍微向上推。

现在一切都好了。

我们有自己的对话头，它被包装在一个高度为 64 的 a 帧中。

所以我要将垂直约束设置为文本居中。

因此，当我调整框架大小时，我只是把它包起来，它会根据需要保持在适当的位置。

所以设置为 60。

现在它被设置为 60，高度被设置为 64 度，它并没有正确地做我想要的。

因此，我将确保它在该框架内居中，然后将其推到顶部，按住 Command 并调整该标题框架标题的大小，我将标记该标题。

现在我已经设置好了。

然后所有这些列表项将堆叠在这个框架的顶部。

现在我可以将它们堆叠起来，它们的大小也合适，我只需要调整宽度。

我们准备好了。

我需要做的就是调整底部的大小。

我可能需要在那里添加 16 个 dip，底部目前设置为 870，增加 17%，我们要走了。

所以我们现在已经实现了简单的对话框变体。

如果我们想的话，我们还可以再创造几个。

实际上，我要挑战你们去构建最后两个元素。

你会发现你自己在创造这最后两个的时候，是我们一起创造了这四个。

但在这些对话中，你会发现自己在这里构建一些复杂的对话，你会遇到这个，这些项目或项目溢出，并且因为溢出而被隐藏，因为我们有这个，这些动作出现在上面，用户可以滚动这些项目并选择一个。

所以我要挑战你去建立它，也挑战你去建立这个全屏对话框。

您没有在界面中构建这些文本输入字段。

你可以，但对于全屏对话框，我们可能会，这只是你创造和挑战自己的额外收获。

是的，我将暂停这段视频，然后构建这个滚动确认对话框。

我马上回来。

所以我现在已经为手机开发了我的滚动确认对话框变体。

本质上，我所做的只是重新创建它，让我来抓住它。

所以我们的另一个移动变体，我们的简单对话，我做的是复制它，分离它。

我去我的资产那里拿了我的无线电清单，万豪酒店。

然后我把它包在一个框架里，我把高度设为 48，正如你在这里看到的。

我只是指定了这个列表项中所有元素之间的适当间距，然后复制它们。

因为它们应该叠在一起。

由于我们的按钮被正确标记，我能够在两种状态之间交换它们，这很棒，将约束设置为垂直，如果这个高度被修改，则居中。

然后我抓住页脚，把它移到图层顺序的顶部。

如此看来，你似乎可以滚动浏览这些内容，如此处所示。

这个页脚已经创建好了，我所要做的就是在这个元素中添加一个分隔线。

我将分隔线对齐到页脚框架的页脚元素的顶部。

这就是我所要做的。

这就是我在那里创造对话的方式。

是的，我就是这样建造的。

这就是我今天要展示给你们的，建立对话的全部内容。

我希望你学到了很多，并喜欢这个视频。

我向你挑战，作为创造这个全屏对话的奖励。

我们将回顾一下日期选择器，我们将大致了解什么是日期选择器，以及它们的使用时间和方式。

然后我们要自己建造一些日期选择器。

这是我们的文件。

我将继续打开链接。

一旦你的链接打开，你可以看到，日期选择器让用户选择一个日期或日期范围，正如你在这里看到的。

这将指定当前日期，然后在此处指定所选日期。

我们有一个标题小编辑图标来编辑这个字段中的日期，这个标题，那里的下拉菜单和一些箭头来在月份之间移动。

上菜。

有一些海拔被利用。

这是一个移动日历日期选择器的剖析。

在这里，您可以看到它上面有一个标题，然后这里显示的是选定的日期。

然后它有切换到键盘输入图标，这就是所谓的。

然后它有年份选择菜单。

我们也有向前或向后的月与月之间的分页。

然后我们这里有当前的日期。

这用这个轮廓来表示。

然后，我们在这里选择了日期，并指定了原色作为背景，还有一些取消或点击 OK 的操作。

您可以看到移动日期范围选择器的外观。

这是一个移动日历日期选择器。

这是一个日历日期选择器。

然后我们有日期范围选择器。

因此您可以指定开始日期和结束日期。

这是选择的区域，背景是较浅的原色。

然后，我们有一个移动输入选择器的变种，那里的解剖，等等的一堆故障。

这个视频会非常密集。

在这个日期选择器组件中有很多事情要做，我们要做的是，我们要分解它，我们要真正简化它，从组成它的小元素开始，然后相应地构建这些元素。

记住这个心智模型，我们就能着手构建这些复杂的组件。

这太棒了。

我们不仅要构建移动变体，还要构建桌面变体。

所以这很好。

在这里，如果我们查看我们的规格，您可以看到我们为移动日期选取器指定了触摸目标，其中日期选取器的触摸目标大小设置为 32 x 32 dip。

这是这里推荐的方法，外容器设置为 40×48 nips。

这里我们开始看到日历标题的规格。

所以这非常重要。

这里指定了一系列规格，所以您不会错过任何东西，因为有很多事情正在进行。

在这里，您可以看到移动日期选择器的日历视图和年份选择视图。

诸如此类，我们将在 figma 中大量讨论这些内容。

所以让我们直接开始吧。

这里，我有一张，我们即将要做的贴纸。

这是我们将要构建的几个元素。

因此，我们在这里有我们将要构建的移动日历标题，以及我们的移动日历选取器工具，日历选取器图像。

所以我要把这些图片移开。

把这些放在这里，把这张图片放大。

有了这些，我们就可以开始了。

所以我要继续复制这个，这个文本，因为这将是我们的主要组件的名称，一旦它被创建。

我们可以继续并开始计算这个日历视图的宽度应该是 328 度。

所以我们输入 328。

我们知道宽度是如何指定的，但是我们接下来需要确定的是这个日历标题的高度。

在这里，你可以看到它被设置为 120。

然后我们有 32 加 24，所以 100 然后再加 120。

因此，我们得到 176。

它的高度设置为 176。

现在我们有了正确指定的高度，我们还需要在父框架上指定圆角。

所以把圆角设置成独立的角。

因此，我们需要将其设置为左上角和右上角，并将其设置为 4。

现在我们的角在顶部变圆了。

我们需要做的是在这里添加上划线文本属性。

如您所见，基线设置为 32 度。

因此，如果我们继续操作，调出我们的文本工具，只指定选择日期，然后将文本样式更改为 over line，我们现在指定了选择日期，我们的所有内容将向左倾斜 16 度，并确保实际上是 24 度。

我的道歉设置为 24 下降到这个标题的左边。

然后我们要检查基线。

当然是设置为 32。

所以 30，我们几乎是准确的。

现在我们有了这个设置，基线设置为 32 就可以开始了。

我们需要的是在这个练习文件中，这里有一些图标，我将复制这个编辑图标。

我需要把它放在右边。

这个字段，所以我需要指定的一件事是，我可以在这个组件的 my 中做什么，如果我在这个元素中正确地命名它，这是一个日历标题，我可以相应地开始包装这些元素。

所以，也许我想把它变成一个框架，并标记这个选择日期行。

有了这一行，我就可以，我想做的是，确保我按住 command，将它捕捉并拖动到父框架的顶部和右侧边缘。

这样，此内容就与此处的文本字段隔离开来，因为您会看到此处指定的框。在整个屏幕中，当您在开发中实现此功能时，您可以将其视为一行。

然后我们在它下面有一个大标题的另一行。

因此，我们将继续添加，我相信作为 h1，我将指定确切的日期，11 月 17 日。

周一，转到“我的文本样式”并选择 h1，它显然不是 h1，我们要将其更改为 h3。

这看起来实际上是一个 h4。

所以我要把它放到右下方，我们要做的是确保我们的文本从左边倾斜 24 度。

不仅如此，我们还要在这里对齐我们的图标。

但是最重要的是，我们这里的排版，你会注意到在下面的这个组件中，我们有一个白色背景的页眉部分。

所以我们要做的是在这里指定距离。

所以我们有 128 72。

所以我们有一个距离，我相信我有大约 16。

在这里，不是很清楚。

所以如果我们减去 72 加上 3200 420，mm 是 104，我们得到 16。

所以，是的，我们想确保我们已经指定了底部的高度。

所以我们可以做的是，如果我们改变这个背景，或这些颜色的文本，我不能在主要内容，然后我可以改变这一点，以主要内容。

接下来，我选择这个容器和组件的背景填充，并选择主要填充，我所要做的就是复制这一行，删除该框架中的文本，并指定将此背景设置为 surfaces surface。

我只需要确保高度设置合适。

所以它被设置为 32 加 24，我们得到 56，这个元素的高度被设置为 56。

我可以确保它扣在底部。

有了这种对齐，我只需要确保这种排版，这种排版，图标是我们从底部开始的 16 次倾斜，我们现在已经对齐了，左右各 24 次倾斜。

正如你在这里看到的，哎呀，排版是这个图标左边的 64 度倾斜，现在一切都成比例地设置，我们准备好了。

所以我只需要确保这个图标与日期选择器中的文字基线对齐。

所以你把它向下推一个像素。

现在这正在被校准。

这太好了。

我们在摇摆。

所以我们差不多完成了，我们需要继续在这里建立我们的排版。

因此，在这里您可以看到，在这部分的顶部和底部有填充，这是日期选取器行。

所以我可以具体说明。

一旦在日期选择器行中指定了，我就需要确保指定了正确的版式。

这看起来像是利用了字幕纺织品。

所以这基本上是居中的。

并带有标题文字文本样式，通知类型 2018 年 11 月。

选择我的标题文本样式。

居中，因为它被包装在一个高度为 56 的行中，并且指定基线距离顶部 32 个像素，所以我将它居中，因为它明显地在这个元素中居中。

确保设置为左侧 24 度。

我们还利用了一个向下箭头下拉箭头下拉图标。

所以我可以把这个复制到这里。

继续选择这个日期选择器行，粘贴并开始移动它，直到它在这个文本的左边下降四次。

所以现在这是符合规格的，从左到右。

现在已经实现了，我们可以继续添加在月份之间移动的图标。

所以这两样我都有。

我将复制这些图标，并将图标之间的间距指定为 24，这里的间距也是 24。

所以我可以把这些拖到这里来。

一旦这些被拖动进来，就相应地移动这些元素。

现在我们准备好了，我们创造了我们的头球。

我们可以根据需要继续检查间距，16 个倾角位于我们元素之间的顶部和底部。

如果需要，我们甚至可以将这两个包装在一起。

您可以相对于其父元素应用约束和。

因此，我将这里的所有内容设置为左垂直居中，然后我将这两个图标设置为右垂直居中。

因此，如果他们保持侧卧，如果他们移动，宽度会发生变化，我还将选择日期选择器行，并指定它始终保持在底部，并设置为左右，以便按比例限制。

我们应该准备好了。

现在创建了这个日历标题，我们可以开始在其他移动元素中使用这个元素了。

因此，如果我继续，我可以复制它，以防万一，我想创建它的一个变体，但将它建立为一个主要组件，然后复制它，我们可以继续处理我们的第一个日期选择器，这是移动输入选择器。

所以我们要做的是我们有了头，所以我们可以复制头。

我们需要一个文本输入字段。

因此，如果我转到我的资产，并确保我们的材料设计系统库始终处于打开状态，我将继续搜索文本输入。

和哎呦文本字段。

抱歉。

然后，我可以直接访问文本输入字段。

我们可以选择这个组件。

这个组件指定了什么，我们也要把它包装在一个框架中。

请注意这里的规格。

因此，规范从顶部开始设置为 16 个倾角。

我们需要做的是创建一个父容器。

因此，我们将把它建立为移动输入选择器。

因此，这个父容器将被称为移动斜杠输入选择器，然后我将指定高度为 264。

你会发现那里有一些空间。

然后我将继续使用这个组件的变体。

参考一下。

将它放入容器，左右两边都有合适的填充，只需要确保顶部的填充设置为 16，就是这样。

现在，我们只需要实现一个背景，这是设置为城市表面表面。

还有一点要注意的是，我们需要指定父容器的拐角半径来进行四周。

此外，由于这是一个模态，在这个场景中，我们需要在父容器上指定高程 224 24 dip 高程。

现在我们可以继续使用这个示例 textfield。

请注意，它不是正确的文本字段类型。

但这只是实现的一个例子。

这里你会注意到我们可能需要另一个头变量。

这就是我在这里复制这个的原因。

所以我想实际上可能继续删除这个日期选择器行。

在我删除该日期选择器之前，我将继续选择父容器并向上移动该内容，确保它捕捉到日期，然后删除下面的一行，使其成为主要组件并复制它。

然后我们用这个代替。

所以如果我继续删除它，我能粘贴它并把它吸附到顶部吗，我能把这个元素上移。

所以从顶部开始有 16 次挑落。

我们现在有了日期选择器，宽度仍然设置为 364。

因此，我需要将其更改为 264，我们现在将继续实现一些纯文本按钮。

所以我要去找我的资产。

把那个文本按钮拉进来，我需要两个。

我需要一个可以的，所以我要把它拖进去。

我们要做的是，确保排版与文本输入字段的末尾对齐，这意味着按钮的父框架是 16，从容器向右倾斜，它们也被添加到从底部倾斜的 16。

所以这是等价的，大小相等。

我要在这个文本框里输入取消。

指定后，我们只需将它与另一个按钮对齐，它必须在“确定”按钮的左侧倾斜八次。

所以现在，我们已经快速创建了我们的移动输入选择器。

这是一个更容易实现的日期选择器。

因此，我将把它作为主要组成部分，这很好。

现在，我们有了第一个手机日期选择器。

接下来，我们将处理一个更复杂的组件。

所以在这里，你会开始看到这些是由什么组成的。

也许我们会从你的选择视图开始，开始时会简单一点，不会太难。

但这是我们分解这个组件的方式。

那么我们在这里看到了什么？嗯，我们看到我们需要创建单元，我们可以称这些单元或这些单元格，这将是我们的元素，日期选择器的 Id 元素。

这些元素包含了文本，当然，它给了我们这个尺寸，这个我们要创建的单元格，它的高度是 52 度，宽度是 88 度。

所以我们本质上，它有一个选定的状态变量。

所以我们也需要选择那个状态。

这是这里的百思买。

72，倾角宽和 36 倾角高，它利用了主要的背景色。

那太好了。

这里我们已经有了头组件。

所以我们可以带一份拷贝过来。

您会注意到，它没有在移动输入数据输入选择器的年份选择视图中使用，也没有使用那里的图标。

所以我们可以直接移除它们。

因此，当我们删除这些实例时，在我们的实例中，它只是隐藏它们，这很好，不用担心。

我们可以开始创造这些细胞。

我们要做的是创建一个框架，将这些元素标记为空格斜杠空格、年份选择、单元格。

然后我们可以将它标记为默认单元格，因为我们将有一个活动单元格或选定单元格，实际上，我们可以将它标记为未选定。

然后我们将创建另一个名为 selected 的。

现在我们有了这两个，确保法案是一套表面表面，我们要去，我们要做的只是指定这两个 250 的高度，以一石二鸟。

并且它们的宽度都被设置为 88 度。

太棒了。

现在我们在中间有了印刷术。

在单元格中，我们有文本，我们可以，让我们看看 2011 年，我可以选择 2011 年。

我要确保这个字体设置为高强调，然后确保调整大小设置为自动宽度。

现在我需要应用适当的文本样式。

所以看起来像是用了标题。

所以它看起来像是使用了一个标题，我只是想验证它们看起来是否准确。

如果我们对所使用的纺织品感到困惑，我们也可以参考文档，但是我没有找到任何相关的规范。

这就是字体设计。

对于文本，也许我们要申请。

让我们仔细检查一下，看看我们是否需要利用正文样式。

那看起来好多了。

因此，我们应用身体织物只是中心垂直和水平的命令，我的意思是，快捷键，选项 V 和 H，我们应该准备好了。

所以我们有了细胞。

现在我们诚实地拥有了，那是我们唯一缺少的东西。

我们只需要这个的变体。

所以这个的变体，我要复制那个文本，粘贴它，用这个单元格，让我们只设置垂直和水平居中的约束。

然后在这个容器单元里做同样的事情。

所以只要保持居中，但不会出现我们需要调整它们大小的情况。

只是为了最佳实践。

因此，对于所选的变体，我们必须选择原色上的颜色内容，我知道我们看不到它，但我们最终将创建一个宽度为 72 倾角、高度为 36 的矩形。

我们要把这个角弄圆，我们可以把角半径改成 24。

在这里，我将把它居中，放在排版的后面，使用命令左括号，这样它就改变了分层的顺序。

并将其标记为状态覆盖。

现在，我们可以继续将填充颜色改为原色。

我们现在已经达到了我们选定的状态。

我们还应该指定排版，两种对齐方式都设置为文本居中对齐。

所以当你的设计师重新命名它的时候，它会从中间重新调整大小。

我们应该准备好了。

现在我们有了这两种单元格变体，让我们继续构建今年的选择视图。

在这里，你可以开始看到我们是如何构建这些模块化元素来构建整个界面的。

因此，我将创建这些多个组件，以便已经有了这个组件。

我们将继续进行，只是把它们向上移动。

我将在选定的变体上重复这一点。

重复一下，我们要指定的是，假设我要把它放在一个标有 this 的框架中。

我将为其命名约定，移动这是移动年选择视图，日期选择器变体。

所以这一帧将被准确地标记。

我将应用一种背景色作为表面的填充色。

所以现在，我要把这个高度拉长，因为我们还没有准确地定义它。

但是我们会的。

您会注意到，在这个组件中还使用了一个分隔线，然后内容在它下面被切断。

所以我们有三个，五个。

选择这个组件后，让我们确保它在我们的框架内。

我将把它拖到这里，靠左对齐，确保它的左填充为 24，并靠紧日历标题的底部。

我们能做的就是继续复制这个。

你知道，元素之间有八个倾角的间距。

我只是按了 D 命令来复制它。

现在，我们可以在这一行中同时选择这三个选项。

我可以继续向下复制这些，按住命令 D，它们都排成一行，正如你在这里看到的，它们之间没有垂直间距，只有宽度，有一个槽，所以是五个，然后我有六个，但我会留六个以后再用。

如果这里有太多的元素，你很难读懂，我们可以做的是，也许我们可以通过选择行的形式来清理它们。

或者实际上，我们想把这些以列的形式分组，因为在移动设备上，用户将在 imagine 上垂直滚动这些列，而不是水平滚动。

所以你可以用这个列布局在 figma 中创建这个组件的原型，所以我要把它放在一个框架中，如果你愿意，把这个列标上，第一列，单元格第一列。

只是也许，也许，也许只是把它标为第一列，这可能更容易。

然后我会对这组元素做同样的事情。

哎呦，按住 Command Shift G 重命名，然后重命名为 column。

现在我们将着手解决第三个问题。

很直白。

将其分组，重命名为第三列。

关于这些列，有一点是这里有 5 行，在每一列都被截断，有第 6 行被截断，但我们将保存它，并继续执行我们的按钮。

我们已经在这里创建了取消和确定按钮。

所以我要命令点击这些，按住 Shift 返回选择父层，并复制。

填充设置为 16 和 16。

哦，每一面，我将在这里选择父帧，粘贴它们并向下移动，确保它们设置为 24。

我相信，哦，右边是 24，下面是 16，或者下面是 8。

让我看看，那看起来是正确的。

看起来是的。

然后，我们将确保我们有一个以上八个倾角的分割线。

我们将转到“assets”面板，将该分隔器拉进来。

并在这里指定宽度，我们要确保我们有指定的宽度，并确保我们的父容器是为倾角半径。

因此，使用此分隔线，需要将宽度更改为 328，以跨越日期选择器的整个宽度。

我要把它拖进去。

把那个推上去。

所以离按钮有八次倾斜。

现在，我们要做的就是选择我们的列，按住 Command 并将其拖动到，以对齐分隔线。

并选择剪辑内容。

在每一行中，我们要做的是，我按住 column，Command D，然后拖动它，使它捕捉。

所以你可以看到，当这个内容被下推时，它哎呀，内容被下推，它被忽略。

所以这很重要。

但是你实际上看不到我们目前实现它的方式。

例如，我们需要添加的一件事是一个选定的状态。

因此，我可以继续删除它，复制这个实例，选择该列，然后选择第二列或任何列，并将其粘贴到那里。

确保它对齐合适。

我们知道你的选择视图是如何实现的。

我们现在有了我们的日期选择器视图或您的选择视图，只需再复制一次。

这是我们的专栏。

但是你实际上看不到它，也看不到我们当前是如何指定它的。

但是你得到了这个组件的概念，虽然它看起来是隐藏的。

所以我们可以做同样的事情，复制这个。

确保正确地捕捉。

现在，我已经构建好了这些列视图，我们可以开始了，我们有了年度选择视图。

因此，如果我真的想用一个选定的组件替换这个组件，根据我们的命名约定，我们可以在我们的实例下拉菜单中非常快速地进行。

这就是我们的年度选择视图。

使其成为主要组成部分。

去把它拖到那边。

现在我们必须创建一个指定的。

让我们继续处理一个更复杂的视图。

有了这个日历视图。

让我们继续复制这个命名约定。

在移动设备上，我们有日历视图，我们显然可以利用移动标题日历标题。

所以我们有了。

我们的移动日历添加器已经完成，我们将把它包装在一个框架中，它将被称为移动 slash 日历视图。

我们将高度指定为 512。

哎呦。

一旦设置为 512，我们将设置表面的填充颜色。

同样，今年的选择视图实际上有我很抱歉，实际上样式 24 dips，这个日历视图也是如此，让我们继续指定它，在这个元素中需要继续将这个 000 颜色样式更改为文本图标高度强调。

现在，一切都是附加的颜色风格是好的。

在这个日历上，你将把拐角半径指定为四个倾角。

现在我们有了这些，我们需要开始创造这些单独的细胞。

因此，在这种格式下，我们将在这里指定日期。

所以它使用 40×40 倾角的单元。

因此，使框架 40 乘 40，设置填充表面表面和复制这个纺织品不要粘贴在这里，并把它放在中心，只是唯一的区别是颜色。

因此，这使用了中等强调，颜色风格。

因此，我们需要指定设置中等强调，我甚至可以复制它，哎呀，可以复制它，我敢肯定，现在设置为一个数字。

那是居中的。

数字有一个颜色风格的高度重视。

我们需要给这些细胞命名。

所以我们有这是一个元素。

这是日期，细胞日选择。

德塞尔。

然后我们将继续复制，将它指定为元素日期。

天，所以现在日期，数字。

所以你可以诚实地命名，无论什么对你或你的团队或组织最有意义。

然后，当然，我们将需要一个 Ace 未选定的变体，这将是什么。

然后我们会有一个选择的变体。

如果我继续把它改成选中的。

对于所选的变体，我们需要一个 36×36 倾角的矩形集合。

当您将此颜色样式更改为“主要内容”时，使用主要颜色，然后在框架内创建一个矩形。

将它设置为 36 或 36 度倾斜和圆形，将它弄圆，然后居中，使用左括号命令将其向后推。

现在我们已经选择了这个状态，我们需要改变填充颜色为原色。

现在我们已经正确设置了文本样式。

因此，我们可能希望将它推过一个像素，因为它看起来不在光学中心。

但是现在我们把它推倒了。

有了这些，我们就可以继续把它作为一个主要的组成部分。

一切都应该相应地扩展。

我还会将约束适当地设置到选定的状态。

这种版式很适合将它作为主要组件，并将日期作为主要组件和日期数字元素组件。

我们应该准备好了。

我们需要做的就是开始相应地创作。

在这个变体中，你可以看到日期被显示出来，它们看起来就在日历标题的右下方。

因此，我将使该快照和左填充设置为 24。

我要做的就是重复这个，确保它向右倾斜四次。

然后我要再复制一次，一直复制。

因为我有指定的间距，我可以看到一个副本。

现在我已经有了这三天，六天，我需要在一周的七天里都这样做。

现在，我将继续仔细检查我的间距。

因此，左边的间距不应该设置为 24，应该设置为 12，因为单元格实际上选择了日期，我将其设置为 24。

所以我道歉。

现在，内容已正确居中，我们接下来要做的是选择我们的日期数字单元格，复制并在此处捕捉。

这是第一天。

然后我们将继续创建这一整行。

这将非常简单，我将继续与您一起创建一行。

所以我们要做的就是复制它，确保所有元素之间有四个像素的间距。

我会在整行中重复这个动作。

我要做同样的事情，选择所有这些元素，并确保它们都堆叠在一起。

再重复一遍，再重复一遍，我就有了我的，我的 556 行。

我们还需要一个，我们需要最下面的，也就是第 32 天。

去看看吧。

我们能做的是，因为我们已经正确命名了组件，我可以单击它并确保它处于选中状态。

我将暂停这个视频，继续添加这些按钮，并重命名这些天。

所以我马上回来。

现在我已经创建了这个移动日历视图。

我所做的只是从我们的年份选择视图中复制了按钮，因为在这个日历视图中，间距规格是相同的。

所以我把它粘贴进去，当我选择了父容器后就粘贴了。

这帮了我一个大忙，我申请了完全相同的职位。

现在，我们有了自己的日历视图，新的主要组件已经成熟。

我们已经揭开了这个问题的复杂性，唯一缺少的是当前的日期。

这是当前日期单元格。

我们要做的是复制选中的日期数字单元格，然后分离它，我要继续将它重命名为当前日期，因为它是一个特定的单元格。

然后，指定当前日期，我将选择我的矩形，并应用一个像素的中风。

也就是说，应该设置为让我们看看表面高度强调，我们将删除填充。

我们现在将继续选择我们的文本，并确保内容设置为活动或高度强调。

这是单元格的当前日期变量。

所以我能做的就是让这个主组件或主要组件使用我。

然后我们甚至可以在日历视图中选择该单元格。

现在我们有了一个当前日期变量。

所以现在我们有了。

对于我们当前的日期变量，我将实际上把那个像素向右推一个像素，哦，向左推一个像素，使它看起来更加光学对齐。

我们现在准备好了。

现在，我们可以在日历视图中将它放在一边。

我们有手机日历所需的合适的单元格。

我们还有一个移动范围选择器，我将把它放在一边，让你挑战自我，制造它。

本质上，有了这个覆盖，你要做的只是创建一个元素，你可以覆盖或放置在所有这些行下面，这些是相应地查看效果的单元格。

因此，我们实际上可以在日历组件中选择所有这些选项。

您可以根据需要将它们按列或行排列。

例如，无论你和你的团队发现什么最有效，我都可以指定为第三行。

在这里，你可以清楚地看到我们有第一排。

第一行，然后这刚好是第二行。

所以这很重要。

我刚刚意识到我弄乱了这个日历视图，我们需要这个日历视图来正确堆叠。

因此，如果我继续点击这个日历视图，它实际上没有正确定位。

所以我需要继续，我要做的是暂停，让所有这些行，我要确保我按下这个元素，我会马上回到我的元素正确对齐，我将它们分组到行中。

即使该行中只有一个元素，假设您要向前构建整行，您可以将其拉伸并对齐，您可以确保宽度对齐正确。

然后，如果您需要修改它，可以根据需要将插入捕捉元素复制到元素的末尾和中间。

这只是一种快速整理东西的方法。

另外，也要保持这些图层的整洁，这是强烈推荐的，就像你在这里看到的，还有那些被包裹在框架里的图层。

如果没有，当你在图层面板中展开它的时候，就会看到大量的单元格。

这就是为什么我们做了反视图。

我将要求你们构建另一个视图，移动范围选择器将处于选中状态。

对于所选的州，你也有两个月的时间。

所以你可以把日历编辑器拆下来，复制那些税属性之类的东西。

在 Yeah 中，你只需要创建一个选中的状态元素，把它变成组件，然后把它放到行下面。

你可以参考日历视图。

所以，现在我们要继续前进，接近桌面的一个变种。

桌面略有不同。

因为它在一个更大的设备上，你会注意到它非常密集。

在桌面上，这里，我们有一个非常密集的视图，其中桌面变体的单元格在这里设置为 32 乘 32 倾角。

每个单元格都有相同的状态，我们有当前日期，我们有选定的日期，然后这里有默认日期。

是的，我的意思是，我们还有一个更复杂的功能，日期范围选择器，您可以从中选择一个月到另一个月。

我们将继续构建这个日期范围选择器。

然后，我将挑战你自己构建以下日期范围选择器，因为这是一个非常密集的信息视频，不应该是你的好做法。

因此，我们将从识别低级元素开始。

也就是说，我们这里有 32 乘 32 的单元格。

然后我们有这些图标。

所以我要继续下去，在这里抓取这些细胞。

既然我们已经对这些单元施加了约束，那么我们所要做的就是分离它们，正确地重命名它们。

现在我意识到了，我们需要对这些细胞进行分类。

所以这些将会是移动版本。

因此，我们必须将所有这些都归类到移动设备下，因为我们也将有桌面设备单元。

所以我要进去，在这里重新命名某些元素。

只是应用移动。

所以只要快速复制粘贴就行了。

一旦你做到了，你就可以走了。

好消息是，由于我们将所有这些都重命名为 mobile，我们可以继续复制所有这些变体，我们可能一开始不需要这些，但我将分离这些实例。

我们可以继续进行更改，如果我们通过选择所有内容来批量重命名，然后点击命令 R，然后我键入 mobile，然后我可以转到这个 replace with，并在 desktop 中键入。

所以现在我有了我的桌面变种。

如果我们看看这里的层，我们有我们的桌面细胞。

由于我们的约束被正确地设置为居中，我们只需修改父容器，所有内容都会正确地调整大小，这非常有用。

我们要做的一件事是在这里创建这个选择的状态，在当前日期、选择的日期、开始日期和结束日期之间的这个范围。

所以我们现在要做的就是创建它。

因此，我们需要确保我们知道这一天的高度设置为多少，以便我们可以在另一个规范中引用它，让我们看看那里选定的日期。

在桌面上的高度，很可能设置为 28。

高度下降，放慢我的脚步。

对，对，调到 28。

现在说了，让我们继续开始，我要抓住图标，我需要这里，我的，我的箭头在这里，左右，我今年需要我的副本。

我们不需要下拉箭头。

所以我要删掉它。

我确实需要这个标题纺织品。

当然，这太强调了。

去改变它，确保它被设置为高强度。

知道这是系统化的，我们可以继续，我们有一个框架。

让我们继续，在这里抓住这个名字，桌面日期范围选择器。

现在我已经复制好了，选择这个父名称，父框架。

我们将继续设置拐角半径为 4。

我们得到了 512 倾角的宽度和 280 倾角的高度。

就这样，我们可以开始了。

我们可以做的一件事是关注这些组件的基础层。

对吗？那么基础层是什么呢？嗯，不会移动的东西会留在原地，它会随着月份而动态变化，因为每个月都有特定的日子。

所以这将会改变这些细胞。

所以会有不同，比如有多少行，每行有多少个单元格。

但是日子会留下来。

箭头和月份会留在这个分隔线上。

所以我们要继续添加这个分隔线，这个分隔线占据了这个日期范围选择器的整个高度。

因此，如果我们转到“assets”面板，选择拉入该分隔线，我将在“rotate left”中点击 Command Shift 选项类型，然后我将它设置为垂直。

它已经在父容器的父框架中了，这很好，我要把高度设为 280。

然后我会按住选项 v 选项 h。

所以现在它是水平对齐的。

李在两边都有这个日期范围选择器，如下图所示。

如果我们还没有，让我们继续把这个填充改为表面表面。

应用嘴唇，计划效果风格的八个下降，因为这是用在菜单和子菜单，像一个菜单或子菜单。

现在我们将继续添加我们的箭头。

也就是说，我们要在这里加上这个箭头，在那里加上第二个箭头。

从右上往下是 16 度倾斜。

所以，如果我按一下，从顶部和右侧向下倾斜 16 度，我们就可以到那里了。

现在我们需要复制这种纺织品。

这里的 9 月似乎是从基线下降了 32，从父容器的顶部下降了两位数。

而且间距好像是对齐的。

在此处指定的这些元素之间水平居中。

所以如果我们做 256 减去 32，我们得到 224。

所以如果我们创建一个 224 位数宽的框架。

如果我把它放在一个框架里，标记为第一行或者标题行标题，我只标记那个标题。

当我将它指定为 224 时，您会注意到这个距离使我向右移动了 16 位。

我之所以把它放在一个框架里，是因为当我把这段文字复制到这里，粘贴到这个标题里，所以我要把它粘贴到那里。

它在父框架中。

因此，我将垂直对齐和水平对齐它，以便它在日历侧是垂直的。

我甚至可以做的一件事是，如果我想，我，如果我不想重复，我可以删除这个图标。

我能做的是选择这里的图标，按住选项 D 并向右移动，然后按 Shift H，这样就能正确对齐了。

现在我已经适当地设置了这个间距。

这太棒了。

所以现在我们有九月和十月。

所以我可以重新命名。

我们要确保的一点是，它被设置为两个元素的文本居中对齐。

我们将继续指定这种版式在调整大小时水平居中。

所以这很重要。

继续，把它改成 2019 年 10 月，然后我们把它改成 2019 年 9 月。

我们应该准备好了。

太棒了。

有了这个说明，我们就剩下标题了。

然后我们有了标题，对吧。

这些都可以在左边分类，比如左边的月和右边的月。

因此，我们将从这几天开始讨论桌面视图。

这里，它们的高度是 32 乘 32 倾角，我们已经在这里指定了。

我们要做的是选择所有这些单元格。

现在我想实际上继续下去，将这些大小调整为 32。

请注意，所有内容都居中，然后被裁剪，对吧。

但是它被裁剪的原因是因为我们需要指定它的高度和宽度为 28。

然后，我将再次将它居中。

我将对当前日期单元格类型做同样的事情。

所以现在，然后只是中心，现在我们可以走了。

我们已经推过一个像素，所以它是中心，我们的元素在这里。

有了这个中心，我们就万事俱备了。

所以现在我要创建这些多个主要组件。

我可以复制这个桌面平台，也就是它现在的标签。

我会在左右两边都注明日期。

正如你在这里看到的，上面图标之间的间距和日期之间的间距没有指定，可以在这里指定，它被设置为 12。

如你所见，设置为 12。

我们可以在这里实现它，选择日期范围选择器，粘贴它，没有选项，一个快照，在 60 设置为 16 个左倾角，默认情况下间距已经是 12。

太棒了，我可以把这些日期复制下来。

现在我在这里指定了所有的七天。

我能做的就是从“获取”“我的图层”面板中选择所有这些图层，对它们进行分组，并对它们进行标注。

连续几天，我可以把这称为连续几天，或者任何你觉得最直观或者最有效的。

这就是我推荐的。

然后我要确保这里的间距设置得合适。

一旦设置妥当，我们就可以开始了。

在这里度过那些日子。

公路日，左边，推上去。

哎呦。

然后，我将在我的图层面板中相应地组织这些内容，右边是标题，右边是道路日。

接下来，我们将继续前进，开始实施我们在这里的每一天。

我要做的就是复制这个日期数字，粘贴到桌面上，这样我们现在就有一行了。

然后我们将在这里创建这些行，这些单元格之间没有间隔。

所以我可以继续敲 D 命令来复制它。

然后我可以把这个组合起来，标记为第三行，第三行向左，然后我可以标记这个，哎呀，我也可以把这个组合起来，向左的路。

然后我能做的就是继续向下复制，确保它们正确递增。

这是第四排。

然后我们看到第五排在工作，然后我们看到第六排。

因此，我们将继续验证这里需要多少行。

对于这一天，我们有，我们有五个，六个，总共，我们有 1234 个，我们需要创建一个行似乎她似乎不对，哎呀，我检查出错误的 1234566 个。

所以我们有 123456。

这一行，我们只有，哦，我们在最后一行只有一个指定的日期，在底部有一个八个倾斜，八个像素的填充。

因此，我需要继续验证，目前，它设置为 4，我不知道这是为什么。

然后将顶部的填充设置为 16。

底部设置为 8。

然后我把高度设置为 280。

但它仍然在底部提示我四维空间，我不明白。

所以我们能做的就是选择我们的父帧，并将其指定为 8。

目前设置为 7。

我把它再向下推一个像素。

现在我们有了，我们现在有了这个变体，我要继续，暂停一下，做另一边，你也应该这样做。

我会给所有东西起相应的名字。

积极的。

现在我在右边创建了这些行。

因此，我将允许您在此处添加所有日期和日子，以确保准确无误。

您可以将它与此处规格中指定的内容保持一致。

我要挑战你自己制作这个日期桌面日期选择器。

和 year selection 视图，我们已经准备好了元素，并为设计系统正确命名。

你只需要调整这些年度选择单元格的大小。

有一件事您可以做，我将和您一起讨论，但不是在试运行中构建，我只是指导您创建这个日期范围选择。

因此，你要做的是代表范围选择，我们要我复制这一点，只是删除了填充，并改变形状的颜色为主低强调。

我改变了约束，这样这个的约束就在这个元素上，我需要把它拆下来。

现在，我需要选择形状，并将约束设置为左和右。

这样，当我选择父容器时，我可以适当地展开它。

所以我可以得到我需要的范围。

在范围下，我将选择死亡，它将被命名为元素斜杠桌面斜杠范围选择。

现在我们已经有了，你可以继续，你知道，让它成为一个主要的组成部分，你可以复制它。

然后你将如何使用它，如果你把它粘贴到你的日期范围选择器，你会想把它放在所有你的下面的层，并对齐你的文本框的高度和大小。

然后你可以根据需要选择你的日期。

请注意，在这方面一切都很好。

有一种状态，只有一个细胞，这很棒。

这就是我今天要展示给你们的全部内容。

伙计们，我向你们发出挑战，让你们做出这个，这些其他桌面变体和这个移动变体，有了你们应该有的所有知识，你们就有了所有细胞和一切需要的东西。

来构建下面的。

非常感谢您的收看。

这是一个很长的问题，希望你学到了很多，以及如何解构这些图像，并把它变成一个系统。

我知道，这很有挑战性，但对你来说很有意义。

如果你能把这个进入你的肌肉记忆，作为一个设计师，你会成为一个非常有价值的设计系统设计师和设计师。

今天，我们将非常简单地构建一些分割线。

我们将介绍如何使用它们，然后只需创建它们，这将是超级快速和简单的。

最长的部分将只是描述用法。

所以分隔线是一条细线，它将列表和布局中的内容分组。

如你所见，这里，我们有一堆列表组件，它们被这个分隔线分成各自的部分，如你所见，这里，我们有那个分隔线元素。

它讲述了背后的原理。

所以它只是用来将内容分成清晰的组。

这里你可以看到它被大量用于信息传递。

而这个收件箱，他们用线条来划分每条收发的信息。

在这里，你可以使用分割线，你可以看到分割线，分割图片的描述和价格。

此外，它还拆分了上面描述性很强的内容，下面是一些操作，并拆分了这些内容。

这里我们有一个分隔线，用来分割这一页看起来像联系人。

这里有很多非常深入的内容，我强烈建议您通读，这将让您在设计前进时做出更有意图的决定。

作为设计师，我们会改进你的游戏。

这里有一些在某些材料设计主题中使用的分隔物的例子，例如标记为双周刊的主题和 owl 材料主题。

在这里，您可以看到分割线在主背景上的样子。

然后还欢呼只是规格，这是现在在 figma。

我们将继续创建它。

非常简单。

它是一个像素和高度。

我们将它设置为 Android 设备的宽度，即 30 到 60 度倾斜。

所以如果我在键盘上按 l，就会产生分隔线，所以我将宽度设置为 360，高度设置为一个像素。

所以现在我有，哦，我要把它倒过来。

所以宽度是 360。

现在我们有了这条线。

但是这条线的问题是我们需要把它弄平。

所以当我说展平时，我们实际上需要勾勒出笔画的轮廓，因为我们实际上有一个笔画，正如你在这里看到的。

当我们在这一行飞行时，你会注意到在属性面板中，它会更新，我们将转到轮廓笔画，它现在将成为菲尔。

为什么我们需要那堵墙，我会举两个例子，因为在测量元素的时候，它会帮助我们。

因为现在这个笔画是居中的。

这条蓝线是测量物体的中心。

你会注意到，这实际上并没有给我们一个合适的度量。

因此，如果我们想在这里分隔一组文本，我们指定了这个文本框，您会看到这是如何 60。

好的，如果我把这个排成一行，那么它们都是笔画，如果我继续，现在改变笔画，你会注意到距离是 60 度。

但是看，当我把它变成轮廓，笔画，我画轮廓，它会变成填充。

然后你会注意到现在距离被设置为 59 像素。

因此，我想说的是，这些分频器要尽可能精确，我们需要确保它们是平坦的。

扁平化只是一个术语，你可以看到这条线。

当我把它展平时，它实际上不再是中风了，所以它不在中风的中心。

所以它现在基本上有点像一个非常薄的一个像素的矩形，你可以把它想象成这样，测量将会更加精确。

这正是我们努力实现的目标。

与此相反，这个笔画单词的距离设置为 60，这个不再是笔画的扁平笔画设置为 59，这样实际上更准确。

这太好了。

我们有那个。

我们需要做的只是应用适当的颜色样式，您需要确保您的材料设计系统库在该文件中启用，一如既往，我们将继续选择我们的颜色样式，并转到内容，选择用于此处分隔器的表面。

同样，您可以看到该分隔线上指定的颜色设置为 00，即黑色，使用 12%的不透明度。

所以它很轻，很微妙。

需要注意的一些重要事情是，其他设计系统指定了几个分隔线，这实际上只是利用了不同的颜色风格。

有了这些不同的颜色风格，它只是在交流。

你可以传达不同程度的不透明。

例如，我分离它，假设这是设置为 12%的常规分频器。

但我们有另一个分割线，我们希望在颜色上更加突出，我们实际上可以提高不透明度。

也许我把它提高到了 80% 87%。

如果我们想要某种类型的，例如，静音的分隔器，哎呀，让我们看看这里，重复我们想要静音的分隔器，我们可以改变不透明度，再次表示分隔器中不同的突出程度，以及您希望它在您的设计中如何可见。

所以这是建造东西的一种方式。

但在材料设计中，我们只有一个分隔器，我将把它标记为常规，这只是为大家提供的一个示例，只是重新应用表面颜色样式上的内容表面覆盖，我们现在有了我们的分隔器，我将继续将其标记为分隔器空间斜线空间，然后分隔器。

这太好了。

实际上，我可以将它标记为 divider，并使它成为主组件主组件，对不起，我们可以开始了。

这就是我们所要做的，来创建一个分隔器，非常感谢您的观看。